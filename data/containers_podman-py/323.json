{
    "number": 323,
    "type": "issue",
    "title": "Incorrect documentation and incompatibility with docker-py in exec_run",
    "user": "claraphyll",
    "body": "exec_run uses `tty = True` by default, although the documentation comment below states that the default is `False`. Furthermore, `docker-py` uses false as the default value, so I think we should use `False` here as well, to minimize suprises when switching over.",
    "comments": []
}