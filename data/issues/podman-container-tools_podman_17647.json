{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 17647,
    "title": "Resource temporarily unavailable",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "acharbha",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nAlso, there is a running list of known issues in the [Podman Troubleshooting Guide](https://github.com/containers/podman/blob/main/troubleshooting.md),\nplease reference that page before opening a new issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\nGetting this error when tried to exec into the container -\n\n```\npodman exec -it mongo_db /bin/bash\nbash: fork: retry: Resource temporarily unavailable\nbash: fork: retry: Resource temporarily unavailable\nbash: fork: retry: Resource temporarily unavailable\n\n```\n\nFrom the mongod container log  -\n\n```\nFailed to create service entry worker thread: pthread_create failed\n```\n\nIs this is a known issue ?\n\n```\npodman --version\npodman version 4.3.1\n```\n\n",
    "comments": [
        {
            "user": "vrothberg",
            "body": "Thanks for reaching out, @acharbha!\n\nPlease fill out the entire issue template with the requested data (`podman info` etc.) next time.  That being said.  Can you share the output of `podman info` along with how the container has been created (i.e., `podman run ...`)?"
        },
        {
            "user": "giuseppe",
            "body": "you either reached the maximum number of pids imposed by ulimit, or the limit inside the cgroup.\n\nTry to raise the limit with `--pids-limit`"
        },
        {
            "user": "giuseppe",
            "body": "@acharbha does `--pids-limit` fix your issue?"
        },
        {
            "user": "acharbha",
            "body": "sorry we didn't try this instead client connections to mongodb container was reset and and started the connections and the issue got fixed."
        },
        {
            "user": "acharbha",
            "body": "Issue again appear back:\nI inspected container for PID limit and found this.\n\n```\npodman inspect <container ID> | grep PidsLimit\n               \"PidsLimit\": 2048,\n```\n\nthere's default limit set on PidsLimit on podman/docker containers and unless explicitly we specify using `--pids-limit` \n\nI've increased to 5000 and issue got resolved.\n\nActually issues arising from client connections which will addressed using connection pooling. "
        }
    ]
}