{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3579,
    "type": "pull_request",
    "title": "fix --dns and --network conflict",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "QiWang19",
    "body": "Close #3553\nThis PR makes --dns and --network flag mutually exclusive for podman build and create. Returns conflict error if both flags are set.\n\nSigned-off-by: Qi Wang <qiwan@redhat.com>",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Should also conflict with\n       --dns-option=option\n       --dns-search=domain\n\nMan pages should be updated to say this also.\n"
        },
        {
            "user": "QiWang19",
            "body": "@rhatdan sure,  but don't see docker return conflict error with --dns-option and\n--dns-search"
        },
        {
            "user": "mheon",
            "body": "I think we need to check if `--dns` works with `--net=host` now. If it does, we retain that behavior; if not, making it conflict seems fine."
        },
        {
            "user": "QiWang19",
            "body": "@mheon @giuseppe PTAL"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM assuming happy tests"
        },
        {
            "user": "giuseppe",
            "body": "> I think we need to check if `--dns` works with `--net=host` now. If it does, we retain that behavior; if not, making it conflict seems fine.\n\nit works with `--net host` and I think it is an useful combination.  You can use the host devices but use a different DNS configuration"
        },
        {
            "user": "mheon",
            "body": "/approve"
        },
        {
            "user": "mheon",
            "body": "Alright, given that this works with `--dns` and `--net=host` both set we should make sure that we don't regress there."
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/3579#issuecomment-511945623\" title=\"Approved\">mheon</a>*, *<a href=\"https://github.com/containers/libpod/pull/3579#\" title=\"Author self-approved\">QiWang19</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "Ok fix the docs to explain what is going on, and then we can merge."
        },
        {
            "user": "rhatdan",
            "body": "I still think we should fail on --net=none, since resolv.conf makes no sense in this situation.  You are basically informing the user that he/she probably made a mistake."
        },
        {
            "user": "QiWang19",
            "body": "`podman build` also has `--network` and `--dns` . Will this also have conflict?"
        },
        {
            "user": "QiWang19",
            "body": "The doc said build --network supports container, but I didn't see the code?\nhttps://github.com/containers/libpod/blob/1c02905ec7af9f63a35ee05e9e9ce594c45c4c58/cmd/podman/build.go#L96-L100"
        },
        {
            "user": "mheon",
            "body": "I view the build thing as a Buildah problem - @TomSweeneyRedHat Mind checking that out to see if things work properly there?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@mheon too many PR's and too many comments.  What do you think needs to be tested on the Buildah side with this?"
        },
        {
            "user": "mheon",
            "body": "@TomSweeneyRedHat The original issue, #3553 - the --dns flags conflicting with --net when net != host"
        },
        {
            "user": "QiWang19",
            "body": "@TomSweeneyRedHat https://github.com/containers/libpod/issues/3553#issuecomment-510588901\nDan let me check all the commands might have --dns* conflict with --net. I found `podman build` also have these flags but not sure if they will conflict. If `build` has this issue, maybe come from buildah?\nI let --dns* invalid with `--net=\"none\"` or `--net=\"container:<id>\"`, dose podman build or build bud support --net=\"container:<id>\"?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@mheon  I played with `buildah bud -t myimg --dns = \"<MYDNS>\" .` this afternoon.  If I went ahead and did 'buildah from myimg --name myctr` and then checked the containers `/etc/resolv.conf` it had the hosts value in there and not \"<MYDNS>\".  However if I added the `--dns` option to the from command ala `buildah from myimg --name mctr --dns \"<MYDNS>\"` \n\nI remember having a conversation with @nalind about similar behaviour for another option in the build that was ignored until the from/create container command set it.  I believe this is expected, but I'll let him correct me.\n\nSo at this point I think we should go forward with this PR once @QiWang19 finishes up her tests and if we need to circle back and touch up the build functionality we can.\n "
        },
        {
            "user": "rhatdan",
            "body": "/lgtm\nLets finish up buildah in a separate PR."
        },
        {
            "user": "QiWang19",
            "body": "there's no issue with --dns* flag, they take  effect during the build and do not change the /etc/resolv.conf in the final container. \nmy question is  using --dns and --network together in the command\n\n1. `podman build --network=\"container\"` doesn't work as described in the doc.  I opened an issue for this? I'm not sure it's a doc problem or a bug of the code\n2.  and will there be a conflict with `buildah bud --dns --network`?\n\n```\n$ podman build --dns 4.4.4.4 --network=\"container\" .\nError: unsupported configuration network=container\n```\n```\n$ buildah bud --dns 4.4.4.4 --network=\"container\" .\nSTEP 1: FROM alpine:latest\nSTEP 2: RUN cat /etc/resolv.conf\nsearch redhat.com usersys.redhat.com\nnameserver 4.4.4.4\nSTEP 3: COMMIT\nGetting image source signatures\nCopying blob 1bfeebd65323 skipped: already exists\n\n```"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@QiWang19 please do open a Buildah bug on this.  I'm not 100% sure it's a bug, but we can always close it if we find out it is not."
        }
    ],
    "cross_references": []
}