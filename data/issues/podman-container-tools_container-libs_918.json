{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 918,
    "type": "issue",
    "title": "Allow not failing on no-op copies to registries which reject writes to existing tags",
    "state": "open",
    "labels": [],
    "user": "banjoh",
    "body": "## Summary\n\nWhen copying a multi-arch image with `OptimizeDestinationImageAlreadyExists: true`, the library correctly skips every child manifest that already exists at the destination (thanks to https://github.com/containers/image/pull/1041) — but it still writes the parent manifest list unconditionally. Against registries that enforce tag immutability (e.g. Artifactory's \"Block Pushing Existing Tags\", Harbor's Tag Immutability rules, JFrog Container Registry, Quay), the second copy of an unchanged multi-arch image fails at the parent list write, even though every child was correctly skipped.\n\nThe option's doc comment promises that the library \"optimize the copy assuming that the destination image already exists.\" For multi-arch sources, that promise isn't kept end-to-end.\n\nObserved against `go.podman.io/image/v5 v5.38.0`.\n\n## Reproduction\n\nReproduced end-to-end against a local Harbor 2.x instance (Helm chart, NodePort) configured with a Tag Immutability rule that covers every tag in every repo, using `skopeo sync` as the consumer.\n\n### Set up the destination registry\n\nInstall Harbor:\n\n```bash\nhelm repo add harbor https://helm.goharbor.io\nhelm install harbor harbor/harbor \\\n  --namespace harbor --create-namespace \\\n  --version 1.19.1 \\\n  --set expose.type=nodePort \\\n  --set expose.tls.enabled=false \\\n  --set expose.nodePort.ports.http.nodePort=30002 \\\n  --set externalURL=http://localhost:30002 \\\n  --set harborAdminPassword=Harbor12345 \\\n  --wait\n```\n\nAdd a Tag Immutability rule to the auto-created `library` project covering every repository and every tag:\n\n1. Open `http://localhost:30002` in a browser and log in as `admin` / `Harbor12345`.\n2. **Projects** (left sidebar) → click the `library` project.\n3. Open the **Policy** tab (in the row of tabs along the top of the project page).\n4. Scroll to the **Tag Immutability** section.\n5. Click **ADD RULE**.\n6. In the dialog, set:\n   - **Repositories: matching** → `**`\n   - **Tags: matching** → `**`\n   - Leave the \"excluding\" fields blank.\n7. Click **ADD**.\n\nThe rule should appear in the list with its toggle in the enabled (blue) state and take effect immediately. To verify, you can try pushing the same image twice via `docker push` to `localhost:30002/library/<anything>:<tag>` — the second push should fail with `unsupported: The tag is immutable, please contact the project administrator to disable the immutability rule.`.\n\n### Trigger the bug\n\nSync any public multi-arch image into the immutable project. `docker.io/library/ubuntu:latest` is a manifest list with 12 platform entries at the time of writing — any multi-arch image works. The key to copying multi-arch images is using `--all` flag.\n\n```bash\nskopeo sync \\\n  --insecure-policy \\\n  --src docker --dest docker \\\n  --dest-creds admin:Harbor12345 \\\n  --dest-tls-verify=false \\\n  --all \\\n  docker.io/library/ubuntu:latest \\\n  localhost:30002/library\n```\n\nFirst run: copies all 12 child images and writes the parent manifest list. Succeeds.\n\nSecond run of the identical command: should be a no-op (`OptimizeDestinationImageAlreadyExists` is enabled by `skopeo sync`). Instead, fails at the parent list write — see actual output below.\n\n## Expected vs. actual\n\n**Expected:** The library skips the parent manifest-list write when the destination's existing list matches the source, mirroring the per-child behaviour. The second `skopeo sync` returns success with the children skipped and no PUT issued to the tag.\n\n**Actual (verbatim, second run):**\n\n```\nINFO[0000] Tag presence check                            imagename=\"docker.io/library/ubuntu:latest\" tagged=true\nINFO[0000] Copying image ref 1/1                         from=\"docker://ubuntu:latest\" to=\"docker://localhost:30002/library/ubuntu:latest\"\nGetting image list signatures\nCopying 12 images generated from 12 images in list\nCopying image sha256:d31acef2a964b6df1f2b7e20a1525c4f2378024e087a4f8a8a9a4247e6a79573 (1/12)\nGetting image source signatures\nSkipping: image already present at destination\n...  (children 2 through 12 each print \"Skipping: image already present at destination\")\nCopying image sha256:ec8d255b567b63cd955ec855ffbbfc4fe8ddf828b9349d3f6285d5923b632b90 (12/12)\nGetting image source signatures\nSkipping: image already present at destination\nWriting manifest list to image destination\nFATA[0009] Error copying ref \"docker://ubuntu:latest\": Uploading manifest list failed, attempted the following formats: application/vnd.oci.image.index.v1+json(uploading manifest latest to localhost:30002/library/ubuntu: unknown: Failed to process request due to 'ubuntu:latest' configured as immutable.), application/vnd.docker.distribution.manifest.list.v2+json(uploading manifest latest to localhost:30002/library/ubuntu: unknown: Failed to process request due to 'ubuntu:latest' configured as immutable.)\n```\n\nAll 12 child manifests correctly short-circuit via the optimization. The library then logs `Writing manifest list to image destination` and issues `PUT /v2/library/ubuntu/manifests/latest` with the parent list — once as OCI image index, retried as Docker manifest list v2. Harbor's tag-immutability rule rejects both with `unsupported: ... configured as immutable.`, and the sync fails.\n\nThe bytes being PUT are byte-identical to what is already at the destination (the same digest comparison the per-child path uses would prove this trivially at the list level), so the rewrite is purely cosmetic from the registry's point of view — but immutable-tag policies reject it on principle.\n",
    "comments": [
        {
            "user": "mtrmac",
            "body": "Thanks for reaching out.\n\nThe `Optimize…` field is a guide to _trade-offs_ (it causes an _extra_ round-trip that wouldn’t otherwise happen to perhaps avoid doing a HTTP round-trips for each layer); but it was never a _promise_ of no-op operation (e.g. the no-copy path does not engage for signed images at all).\n\nYou’re right that this is not implemented for manifest lists. Doing a similar trade-off at the manifest level is plausible _in principle_, but it would be harder to maintain — the more removed is the alternate route logic from the primary copy code, the easier it is for the two to get out of sync.\n\nRight now, using the ~existing logic at the manifest list level would not work: as mentioned above, the no-copy path does not engage for signed per-platform images, so the manifest list code would still need to read and work on each of the per-platform images to even _check_ whether they are signed. And that would mean that there is no optimization / trade-off at the manifest-list level: the current code, after iterating through the per-platform images, does exactly one operation to write the (updated or unmodified) manifest list to the destination; vs. a hypothetical no-copy path would first do one operation to read from the destination, and then might do another to write an updated list to the destination. In that situation, the hypothetical no-copy path is never better.\n\nSo actually achieving an optimization here would first require adding more complexity to the per-platform no-copy code path (and I didn’t try to think though whether we can actually optimize things for signatures and the like, maybe there is nothing to optimize there, at least for the GPG signature storage), and more risk. It doesn’t look _impossible_ but it is quite a lot of work — work you seem not to actually need.\n\n---\n\n“Never update a manifest if it already exists at the destination” is potentially interesting, but it would be a different feature from `Optimize…` (Luckily, one rather easier to implement.)\n\nBut then again, as https://github.com/replicatedhq/kots/pull/5940 shows, callers of c/image can do such a check themselves. A check inside the c/image/copy code would be more efficient (it would only authenticate to each registry once), so there is still _a bit_ of a benefit to having the feature in c/image."
        }
    ],
    "cross_references": [
        {
            "organization": "replicatedhq",
            "repository": "kots",
            "number": 5940
        }
    ]
}