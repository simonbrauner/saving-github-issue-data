{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29055,
    "type": "pull_request",
    "title": "pkg/domain: return an error instead of nil on remote event parse failure",
    "state": "closed",
    "labels": [],
    "user": "ROKUMATE",
    "body": "## What\nIn remote mode, `podman events --format json` could print the literal `null`\ninstead of an event object. `ConvertToLibpodEvent` returned `nil` on a parse\nfailure and the tunnel forwarded it as an event with no error set, so the nil\nevent was marshalled to `null`.\n\nThis returns an error from `ConvertToLibpodEvent` and sends it on the event\nchannel (which the CLI already handles) instead of a nil event.\n\n## Test\n`pkg/domain/entities/events_test.go`: a valid event converts correctly, and\nunknown status, unknown type, and an invalid `containerExitCode` each return an\nerror instead of nil.\n\n## Checklist\n- [x] Certify you wrote the patch ... (git commit -s)\n- [ ] Referenced issues (not completely fixes #28325)\n- [x] Tests have been added/updated\n- [x] Documentation ... (no docs needed)\n- [ ] All commits pass `make validatepr`\n- [x] Release note entered below\n\n#### Does this PR introduce a user-facing change?\nthe`podman events --format json` cmd no longer prints null when a server event can't be parsed ... it returns an error instead\n\n",
    "comments": [
        {
            "user": "ROKUMATE",
            "body": "@danishprakash i read it i added the checklist for now \nand hanceforth in next pr wont use it for writting the pr description and all 👍🏼 \nany point i missed in the fix u would like me to check on ?? \n"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "ROKUMATE",
            "body": "done with the resolving of all comments and i also squashed the commites into one commit with a descriptive commit message ... 👍🏼 "
        },
        {
            "user": "inknos",
            "body": "LGTM!"
        }
    ],
    "cross_references": []
}