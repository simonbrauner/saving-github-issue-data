{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 575,
    "type": "pull_request",
    "title": "seccomp: block AF_VSOCK sockets",
    "state": "closed",
    "labels": [
        "common"
    ],
    "user": "giuseppe",
    "body": "Block the socket() syscall with AF_VSOCK to prevent container escapes via VM sockets.\n\n<!--- Please read the [contributing guidelines](https://github.com/containers/container-libs/blob/main/CONTRIBUTING.md) before proceeding --->\n",
    "comments": [
        {
            "user": "podmanbot",
            "body": "✅ A new PR has been created in buildah to vendor these changes: **https://github.com/containers/buildah/pull/6623**"
        },
        {
            "user": "tylerfanelli",
            "body": "The krun runtime uses vsock for VM guest communication. Would this PR conflict with that in any way?"
        },
        {
            "user": "Luap99",
            "body": "> The krun runtime uses vsock for VM guest communication. Would this PR conflict with that in any way?\n\nyeah I was wondering about that too, I am not familiar with the krun details but taking the profile from this PR still works AFAICT\n```\npodman --runtime krun  run --rm --security-opt seccomp=/home/pholzing/go/src/github.com/containers/container-libs/common/pkg/seccomp/seccomp.json  quay.io/libpod/testimage:20241011 uname -a\nLinux afe6635da40a 6.12.34 #1 SMP PREEMPT_DYNAMIC Thu Jun 26 12:45:10 CEST 2025 x86_64 Linux\n```\nSo we should not break krun if that test is good enough? I also checked with a volume mount, is there anything specific to test the vsock communication?\n"
        },
        {
            "user": "tylerfanelli",
            "body": "I can test with other libkrun variants that use vsock more heavily (like libkrun-nitro for AWS Nitro Enclaves, where all outside communication is done through vsock). Would you mind marking this as draft until I'm able to do so?"
        },
        {
            "user": "Luap99",
            "body": "the seccomp profile is applied by crun, if it is applied for krun as well I guess we could try to special case it to allow vsock for only that. Or modify the seccomp generation on the podman side so we can create a custom rule per runtime? Of course regardless this would get even more complicated to the already complicated rule syntax."
        },
        {
            "user": "giuseppe",
            "body": "Could we open the socket before we apply the seccomp profile? I don't think that krun should depend on this to not be blocked, we need to address it in the runtime "
        },
        {
            "user": "giuseppe",
            "body": "can we merge?"
        },
        {
            "user": "mtrmac",
            "body": "@giuseppe what is the path forward for krun? I don’t think we need to _block_ on having that implemented but we should at least have _some_ idea of what we are getting into, and a issue filed _somewhere_ to check/follow up/update."
        },
        {
            "user": "giuseppe",
            "body": "The OCI runtime should be able to work with such seccomp setting.   We can apply the filter later, or not apply it (for the runtime itself).  It is not realistic to expect that users won't block this syscall because the runtime can't deal with it"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 6623
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 601
        }
    ]
}