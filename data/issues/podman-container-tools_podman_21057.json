{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 21057,
    "type": "issue",
    "title": "podman kube play should be able to read the bash variables",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-issue",
        "kube",
        "locked - please file new issue/PR"
    ],
    "user": "antverpp",
    "body": "### Feature request description\n\nFor example, I have a number of manifests, where I want to refer some values by an environment variable:\nspec:\n  containers:\n  - name: <myname>\n    image: ${REGISTRY}/<myname>:${TAG}\n\nwhere REGISTRY and TAG are defined as env variables, for example in the .env file.\n\n### Suggest potential solution\n\nA clear and concise description of what you want to happen.\n\n### Have you considered any alternatives?\n\nA clear and concise description of any alternative solutions or features you've considered.\n\n### Additional context\n\nAdd any other context or screenshots about the feature request here.",
    "comments": [
        {
            "user": "antverpp",
            "body": "A clear and concise description of what you want to happen:\nSo, I define variables in .env file.\nAnd I refer them in the manifest, as described above.\nLike, REGISTRY=myregistry.com, TAG=0.0.1\n\nAnd as a result, the podman play kube should render the manifest and pull the image myregistry.com/myname:0.0.1, \nbeing defined in the yaml manifest as \"image: ${REGISTRY}/myname:${TAG}\""
        },
        {
            "user": "rhatdan",
            "body": "Interested in opening a PR to make this happen?"
        },
        {
            "user": "giuseppe",
            "body": "Does Kubernetes support it?"
        },
        {
            "user": "roman-kiselenko",
            "body": "Kubernetes YAML doesn't support this kind of variable interpolation.\nYou can achieve the same thing with [envsubst](https://man7.org/linux/man-pages/man1/envsubst.1.html)\n\n```yaml\n# my_pod.yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: foobar\nspec:\n  containers:\n  - name: container-1\n    image: ${REGISTRY}/${REPO}:${TAG}\n```\n\n```shell\nREGISTRY=myregistry.com REPO=project TAG=0.0.1 envsubst < my_pod.yaml > pod.yaml\n```\n```yaml\n# pod.yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: foobar\nspec:\n  containers:\n  - name: container-1\n    image: myregistry.com/project:0.0.1\n```"
        },
        {
            "user": "antverpp",
            "body": "> Does Kubernetes support it?\n\nWell, at least the well-known RedHat product can do it pretty well: oc process --param-file=test.env -f test.yaml | oc create -f -"
        },
        {
            "user": "rgprajeen",
            "body": "So, can't this be achieved by using `sed`? Like using a format like `__VARNAME__` in the yaml file and later generating a new yaml using `sed -i \"s|__VARNAME__|${VARNAME}|g\"`"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "Since we have not heard back, closing. Reopen if you have more feedback."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 21098
        }
    ]
}