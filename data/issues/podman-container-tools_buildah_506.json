{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 506,
    "type": "pull_request",
    "title": "shallowCopy: avoid a second read of the container's layer",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "nalind",
    "body": "Avoid reading the container's layer a second time in `shallowCopy()` (after the first time it's read, in `NewImageSource()`).\n\nOur second copy of the diff isn't going to match the first one if it's of any serious size, because when `NaiveDiff()` is used to generate the diff, whiteout entries have their times set to \"now\", rather than a\nfixed value, which causes both the uncompressed and compressed digests to differ from what `NewImageSource()` records in the manifest.\n\nInstead, use the generic manifest-reading API that the image library now provides to read the layer info list and the config blob info, and use their values to `PutBlob()` them to the destination image.\n\nIn the medium-term, we can probably drop `shallowCopy()` completely, since the newer version of the image library has a more reliable `HasBlob()`, which should save us the time that `shallowCopy()` was originally meant to save, but we should probably consider the use of compression for this code path as part of that, too.\n\nFixes #502.",
    "comments": [
        {
            "user": "giuseppe",
            "body": "great, I've just tried and it solves the issue I've reported!"
        },
        {
            "user": "giuseppe",
            "body": "@rhatdan with this patch, I am able to fully use Buildah as non privileged user and the vfs storage both using bwrap-oci[1] and crun.\n\n[1] this change in bubblewrap is required: https://github.com/projectatomic/bubblewrap/pull/256"
        },
        {
            "user": "rhatdan",
            "body": "@rh-atomic-bot r+\nShould we cut a new release?  Anything else we want to get in?"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":pushpin: Commit 9ef0687 has been approved by `rhatdan`\n\n<!-- @rh-atomic-bot r=rhatdan 9ef0687f8a3b008a3819c60dd89b47f9348aa1b7 -->"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":zap: Test exempted: pull fully rebased and already tested."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 502
        }
    ]
}