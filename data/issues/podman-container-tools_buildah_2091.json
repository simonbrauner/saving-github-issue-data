{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 2091,
    "type": "issue",
    "title": "Copying executable to mounted filesystem doesn't work",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "georgek",
    "body": "<!--\nIf you are reporting a new issue, make sure that we do not have any duplicates\nalready open. You can ensure this by searching the issue list for this\nrepository. If there is a duplicate, please close your issue and add a comment\nto the existing issue instead.\n\nIf you suspect your issue is a bug, please edit your issue description to\ninclude the BUG REPORT INFORMATION shown below. If you fail to provide this\ninformation within 7 days, we cannot debug your issue and will close it. We\nwill, however, reopen it if you later provide the information.\n\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n-->\n\nIf I mount a new container and copy in an executable, it is not available in the subsequent image. It does, however, work when using `buildah copy` instead.\n\n<!--\nBriefly describe the problem you are having in a few paragraphs.\n-->\n\n**Steps to reproduce the issue:**\n1. `container=$(buildah from scratch)`\n2. `scratchmnt=$(buildah unshare buildah mount $container)`\n3. `cp some_static_executable $scratchmnt/some_static_executable`\n4. `buildah run $container ./some_static_executable` (works),\n5. `buildah config --cmd ./some_static_executable $container`\n6. `buildah commit $container image`\n7. `podman run --rm image`\n\n\n**Describe the results you received:**\n\n`./some_static_executable: no such file or directory`\n\n**Describe the results you expected:**\n\nThe static executable is found and run.\n\n**Output of `rpm -q buildah` or `apt list buildah`:**\n\n```\nbuildah/bionic,now 1.10.1-1~ubuntu18.04~ppa1 amd64 [installed]\n```\n\n**Output of `buildah version`:**\n\n```\nVersion:         1.10.1\nGo Version:      go1.10.4\nImage Spec:      1.0.1\nRuntime Spec:    1.0.1-dev\nCNI Spec:        0.4.0\nlibcni Version:  \nGit Commit:      \nBuilt:           Thu Aug  8 21:29:48 2019\nOS/Arch:         linux/amd64\n```\n\n**Output of `podman version` if reporting a `podman build` issue:**\n\n```\nVersion:            1.6.2\nRemoteAPI Version:  1\nGo Version:         go1.10.4\nOS/Arch:            linux/amd64\n```\n\n**Output of `cat /etc/*release`:**\n\n```\nDISTRIB_ID=Ubuntu\nDISTRIB_RELEASE=18.04\nDISTRIB_CODENAME=bionic\nDISTRIB_DESCRIPTION=\"Ubuntu 18.04.3 LTS\"\nNAME=\"Ubuntu\"\nVERSION=\"18.04.3 LTS (Bionic Beaver)\"\nID=ubuntu\nID_LIKE=debian\nPRETTY_NAME=\"Ubuntu 18.04.3 LTS\"\nVERSION_ID=\"18.04\"\nHOME_URL=\"https://www.ubuntu.com/\"\nSUPPORT_URL=\"https://help.ubuntu.com/\"\nBUG_REPORT_URL=\"https://bugs.launchpad.net/ubuntu/\"\nPRIVACY_POLICY_URL=\"https://www.ubuntu.com/legal/terms-and-policies/privacy-policy\"\nVERSION_CODENAME=bionic\nUBUNTU_CODENAME=bionic\n```\n\n**Output of `uname -a`:**\n\n```\nLinux local 4.15.0-72-generic #81-Ubuntu SMP Tue Nov 26 12:20:02 UTC 2019 x86_64 x86_64 x86_64 GNU/Linux\n```\n\n**Output of `cat /etc/containers/storage.conf`:**\n\n```\n# storage.conf is the configuration file for all tools\n# that share the containers/storage libraries\n# See man 5 containers-storage.conf for more information\n\n# The \"container storage\" table contains all of the server options.\n[storage]\n\n# Default Storage Driver\ndriver = \"overlay\"\n\n# Temporary storage location\nrunroot = \"/var/run/containers/storage\"\n\n# Primary read-write location of container storage\ngraphroot = \"/var/lib/containers/storage\"\n\n[storage.options]\n# AdditionalImageStores is used to pass paths to additional read-only image stores\n# Must be comma separated list.\nadditionalimagestores = [\n]\n\n# Size is used to set a maximum size of the container image.  Only supported by\n# certain container storage drivers (currently overlay, zfs, vfs, btrfs)\nsize = \"\"\n\n# OverrideKernelCheck tells the driver to ignore kernel checks based on kernel version\noverride_kernel_check = \"true\"\n```\n",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Did you run your script as root or non-root?"
        },
        {
            "user": "georgek",
            "body": "All non-root."
        },
        {
            "user": "rhatdan",
            "body": "Can you try to run your script under `buildah unshare`"
        },
        {
            "user": "georgek",
            "body": "It works if I run the whole script under `buildah unshare`. I thought it was only necessary when issuing the `buildah mount` command.\n\nIs it recommended to always run scripts under `buildah unshare` when building without root? Maybe this should be added to the README section about rootless operation? The name \"unshare\" doesn't seem very intuitive (why is it called that?)."
        },
        {
            "user": "rhatdan",
            "body": "Any use of the mounted file system, requires the `buildah unshare`.\nSo at least\n```\nscratchmnt=$(buildah unshare buildah mount $container)\ncp some_static_executable $scratchmnt/some_static_executable\n```\nHas to be done in the user namespace via `buildah unshare`"
        },
        {
            "user": "jamescassell",
            "body": "probably can solve this one by updating the docs to say that if your build process includes `buildah mount`, you must run the entire build process under a `buildah unshare` session for rootless."
        },
        {
            "user": "rhatdan",
            "body": "Please open a PR to change the docs the way you would like."
        },
        {
            "user": "rhatdan",
            "body": "Now if you run buildah mount $CID in rootless mode without the unshare it says\n\n$ buildah mount working-container\ncannot mount using driver overlay in rootless mode. You need to run it in a `buildah unshare` session\n\nMan page also documents this.\n\n```\nbuildah-mount(1)                                                                                         General Commands Manual                                                                                        buildah-mount(1)\n\nNAME\n       buildah-mount - Mount a working container's root filesystem.\n\nSYNOPSIS\n       buildah mount [container ...]\n\nDESCRIPTION\n       Mounts the specified container's root file system in a location which can be accessed from the host, and returns its location.\n\n       If the mount command is invoked without any arguments, the tool will list all of the currently mounted containers.\n\n       When  running  in  rootless mode, mount runs in a different namespace so that the mounted volume might not be accessible from the host when using a driver different than vfs.  To be able to access the file system mounted, you\n       might need to create the mount namespace separately as part of buildah unshare.  In the environment created with buildah unshare you can then use buildah mount and have access to the mounted file system.\n\n```"
        },
        {
            "user": "jamescassell",
            "body": "I'm curious whether it'd be possible to do a some sort of unprivileged fuse-mount for non-VFS drivers, but it probably should be its own feature..."
        },
        {
            "user": "georgek",
            "body": "Would this actually affect the commands in my bug report? Notice that I used: `scratchmnt=$(buildah unshare buildah mount $container)`."
        },
        {
            "user": "rhatdan",
            "body": "scratchmnt=$(buildah unshare buildah mount $container).\nAs soon as the buildah unshare process finishes, the mount namespace that you mounted the buildah container within will exit and unmount the file system.\nAll interaction with the mount point has to happen while in a buildah unshare shell.\n"
        },
        {
            "user": "georgek",
            "body": "Yeah, I understand that, just pointing out that the additional warning for `buildah-mount` won't help someone who makes the same mistake as me."
        },
        {
            "user": "rhatdan",
            "body": "Sure, do you have a suggestion, on how we could allow them not to make this mistake?"
        }
    ],
    "cross_references": []
}