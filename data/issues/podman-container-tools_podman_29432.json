{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29432,
    "type": "pull_request",
    "title": "libpod/events: use filepath.Dir for filesystem path",
    "state": "open",
    "labels": [],
    "user": "shrevid03",
    "body": "## Description\n\nSmall fix for one of the path vs path/filepath cases from #25165.\n\nIn libpod/events/logfile.go, the truncate() function used path.Dir on a filesystem path. The file is Linux/FreeBSD only per its build tag, so this isn't about Windows correctness — it's about internal consistency: the same file already uses filepath.Dir at line 32 for the same kind of path handling, and filepath is idiomatic for filesystem operations. Also drops the now-unused \"path\" import.\n\nTesting\n\ngo build ./libpod/events/ passes. No tests in the package to run.\n\nRelease note\n\n```release-note\nNONE\n```",
    "comments": [
        {
            "user": "baude",
            "body": "```\n//go:build linux || freebsd\n```\n\ndoes that not preclude this from being on windows?  I don't really object to the PR but your commit message at the very least seems misdirected."
        },
        {
            "user": "shrevid03",
            "body": "Good catch @baude , you're right, the build tag means Windows isn't the driver here. Amended the commit and updated the PR description to frame it as consistency with the existing filepath.Dir call at line 32. Thanks for the review."
        },
        {
            "user": "mheon",
            "body": "LGTM, though I don't know if this actually fixes the linked issue, even in part"
        },
        {
            "user": "mheon",
            "body": "I suppose we should be using it for consistency, even on Linux..."
        }
    ],
    "cross_references": []
}