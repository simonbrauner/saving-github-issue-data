{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 226,
    "type": "pull_request",
    "title": "vendor containers/image",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "runcom",
    "body": "specifically https://github.com/containers/image/pull/115 and also add an integration test\n\nSigned-off-by: Antonio Murdaca runcom@redhat.com\n",
    "comments": [
        {
            "user": "runcom",
            "body": "@mtrmac PTAL\n"
        },
        {
            "user": "runcom",
            "body": "I think it's time to setup the schema 1 registry in tests :) I'll do it\n"
        },
        {
            "user": "mtrmac",
            "body": "If it’s all the same to you, I’d prefer running the tests against a v2 registry with s2; s1 images’ signatures eventually _should_ break with fixed s1 upload (#88), so converting most of the tests from Docker Hub estesp (s1+s2+list) against OpenShift to s2 against v2 registry would be nicer.\n"
        },
        {
            "user": "runcom",
            "body": "> If it’s all the same to you, I’d prefer running the tests against a v2 registry with s2; s1 images’ signatures eventually should break with fixed s1 upload (#88), so converting most of the tests from Docker Hub estesp (s1+s2+list) against OpenShift to s2 against v2 registry would be nicer.\n\nwait, v2s2 broke something in openshift here, I'll revert some changes and you can see\n"
        },
        {
            "user": "runcom",
            "body": "@mtrmac do you know why the current test with `docker://busybox:latest` which is v2s2 will make atomic registry fails with 500 when uploading the image there?\n"
        },
        {
            "user": "mtrmac",
            "body": "Atomic Registry by default refuses to accept s2.  https://github.com/mtrmac/skopeo/tree/docker-push-to-tag enables that, but then we are running tests in a non-default configuration, not a great idea.\n\nThat’s why I was suggesting a docker/distribution registry + s2, instead of Atomic. (And why this is so much work.)\n"
        },
        {
            "user": "runcom",
            "body": "To summarize, you want that test to push the image to an in-test docker/distribution V2 registry, instead of the atomic registry, right? \n"
        },
        {
            "user": "mtrmac",
            "body": "At least `TestCopySignatures`, yes; perhaps others may be needed. There should still be _something_ testing against the Atomic Registry.\n"
        },
        {
            "user": "runcom",
            "body": "Right, we can find an s1 image to still test _something_ for the atomic registry, fine. I'll finish this tomorrow morning. The v2s2 registry is already setup in the test infra so it shouldn't be _that_ hard I hope. \n"
        },
        {
            "user": "mtrmac",
            "body": "Yes, at least `TestCopyDockerSigstore` is already using it.\n"
        },
        {
            "user": "runcom",
            "body": "@mtrmac tests fixed, PTAL\n"
        },
        {
            "user": "runcom",
            "body": "Should be good to go if tests agree @mtrmac \n"
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "image",
            "number": 113
        }
    ]
}