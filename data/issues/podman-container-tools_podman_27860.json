{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 27860,
    "type": "pull_request",
    "title": "Quadlet - do not link pod service units to quadlet templates",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "ygalblum",
    "body": "When a template container unit is linked to a pod do not add its service to the pod units dependency\n\nResolves: https://github.com/containers/podman/issues/27844\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nQuadlet - Do not link pod service units to template container service units\n```\n",
    "comments": [
        {
            "user": "baude",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "/lgtm"
        },
        {
            "user": "jankaluza",
            "body": "LGTM.\n\nI spent time thinking if we should search for `@` or `@.`. The thing is that strictly speaking, template is just `foo@.service`. If there is a `foo@bar.service`, it is an instance of that template afaik. The `isTemplateUnit` currently matches both templates and instances, but I think it is fine in this context.\n\nJust saying that in case someone with more experience with this code would find this an issue."
        },
        {
            "user": "ygalblum",
            "body": "@jankaluza good catch. I've fixed the code. Note that it required reverting part of the change since in the other cases, the check is in fact for \"template or an instance of a template\".\nI've updated the test as well. PTAL"
        },
        {
            "user": "ygalblum",
            "body": "BTW is there an issue with the bot? @TomSweeneyRedHat's LGTM did not trigger any labeling (and as a result the PR did not merge already)"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@ygalblum this is the second time this week the bot has ignored my merge request.  @bbaude do you know what's going on?"
        }
    ],
    "cross_references": []
}