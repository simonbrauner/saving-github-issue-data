{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 413,
    "type": "pull_request",
    "title": "Spruce up the README.md a bit",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "TomSweeneyRedHat",
    "body": "Signed-off-by: TomSweeneyRedHat <tsweeney@redhat.com>\n\nChanges up the verbiage a bit, especially the opening stanza.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Better add the ICON, and add some of this text from the skopeo man page.\n\n```\n\n       atomic:hostname/namespace/stream:tag\n         An image served by an OpenShift(Atomic) Registry server. The current OpenShift project and OpenShift Registry instance are by default read from $HOME/.kube/config, which is set e.g. using (oc\n       login).\n\n       containers-storage://docker-reference\n         An image located in a local containers/storage image store.  Location and image store specified in /etc/containers/storage.conf\n\n       dir:path\n         An existing local directory path storing the manifest, layer tarballs and signatures as individual files. This is a non-standardized format, primarily useful for debugging or noninvasive container\n       inspection.\n\n       docker://docker-reference\n         An image in a registry implementing the \"Docker Registry HTTP API V2\". By default, uses the authorization state in $HOME/.docker/config.json, which is set e.g. using (docker login).\n\n       docker-archive:path[:docker-reference]\n         An image is stored in the docker save formated file.  docker-reference is only used when creating such a file, and it must not contain a digest.\n\n       docker-daemon:docker-reference\n         An image docker-reference stored in the docker daemon internal storage.  docker-reference must contain either a tag or a digest.  Alternatively, when reading images, the format can also be\n       docker-daemon:algo:digest (an image ID).\n\n       oci:path:tag\n         An image tag in a directory compliant with \"Open Container Image Layout Specification\" at path.\n\n       ostree:image[@/absolute/repo/path]\n         An image in local OSTree repository.  /absolute/repo/path defaults to /ostree/repo.\n```"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@rhatdan ack verbiage.  But what/where is the icon?  If that's the graphic from @runcom's submit, I'd like to let that merge in that way."
        },
        {
            "user": "rhatdan",
            "body": "Yes that is what I am talking about."
        },
        {
            "user": "mtrmac",
            "body": "LGTM, please rebase."
        },
        {
            "user": "runcom",
            "body": "LGTM "
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Rebased.  That was real git strangeness. It flagged one line as having changed from head, but they looked to be exactly the same to my eye.  "
        },
        {
            "user": "rhatdan",
            "body": "LGTM, Lets get this merged, and get @runcom Logo in place."
        },
        {
            "user": "runcom",
            "body": "I'm waiting for a no-skopeo-text-in-logo modification from Mairin actually :("
        }
    ],
    "cross_references": []
}