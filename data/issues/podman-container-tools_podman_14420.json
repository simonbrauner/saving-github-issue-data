{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 14420,
    "type": "issue",
    "title": "podman cp can overwrite existing files/directories ",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "Luap99",
    "body": "\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind bug\n\n**Description**\n\nusing podman cp incorrect can lead to very easy data loss.\nCurrently it is possible to overwrite an existing file/directory on the host without any warnings.\n\nI noticed this while looking at https://github.com/containers/podman/pull/14393.\nSo my question is why is this an option, and why it is not the default? I cannot imagine that somebody actually would want this and if they do it is simple to just remove the file/directory beforehand.\n\n\n**Steps to reproduce the issue:**\n\n1.\n```\npodman run --name test alpine touch /test1\n$ bin/podman cp ^C\n$ mkdir test1\n$ bin/podman cp test:/test1 .\n$ ls -l test1 \n-rw-r--r--. 1 pholzing pholzing 0 May 30 17:36 test1\n```\n\n2.\n```\n$ podman run --name test alpine mkdir /test1\n$ touch test1\n$ bin/podman cp test:/test1 .\n$ ls -ld test1\ndrwxr-xr-x. 1 pholzing pholzing 0 May 30 17:39 test1\n```\n\n**Describe the results you received:**\nThe original file/directory is gone.\n\n**Describe the results you expected:**\nAn error like the one from `cp`:\n`cp: cannot overwrite non-directory './test1' with directory 'test/test1'`\n\n**Additional information you deem important (e.g. issue happens only occasionally):**\nLooks like we regressed on this one, I already reported this a long time ago: https://github.com/containers/podman/issues/7790\n\nAlso I verified that `docker cp` works as I would expect and errors correctly in both cases.\n\n",
    "comments": [
        {
            "user": "vrothberg",
            "body": "It screams for regression tests."
        },
        {
            "user": "vrothberg",
            "body": "I think we need to provide a flag for that on the CLI and let it default to the Docker behavior. There is a risk that we may break certain use cases; having a flag will allow for a quick work around.\n\n@rhatdan @baude WDYT?"
        },
        {
            "user": "Luap99",
            "body": "An option is fine for me but the default should be to not overwrite."
        },
        {
            "user": "rhatdan",
            "body": "SGTM"
        },
        {
            "user": "vrothberg",
            "body": "@nalind, buildah's copy options `NoOverwriteDirNonDir` blocks dirs from being replaced with files.  But we need the same the other way around.  Do you want a new option for that or can we extend `NoOverwriteDirNonDir`?"
        },
        {
            "user": "nalind",
            "body": "It needs to be a separate option.  This logic backends our handling of COPY and ADD instructions in Dockerfiles, and we definitely don't want to change how they behave."
        },
        {
            "user": "nalind",
            "body": "Correcting myself: we don't set the flag for ADD/COPY, so overloading it wouldn't break them.  It's still my preference."
        },
        {
            "user": "vrothberg",
            "body": "-> https://github.com/containers/buildah/pull/4043"
        },
        {
            "user": "vrothberg",
            "body": "-> https://github.com/containers/podman/pull/14526"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 14393
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 4043
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 14526
        }
    ]
}