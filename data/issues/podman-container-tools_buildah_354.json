{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 354,
    "type": "pull_request",
    "title": "include Debian install docs",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "anarcat",
    "body": "This does small changes I have found necessary while installing Buildah on Debian stretch.\n\nFirst, we splits install docs more clearly across different OSes - at first glance, you're tempted to copy-paste duplicate instructions, even across RHEL/Fedora. Then we explicitly run `make install` as root, showing how the build itself can be ran as non-root, and finally, include Debian-specific install docs, for which `apt-add-repository` unfortunately fails.",
    "comments": [
        {
            "user": "rh-atomic-bot",
            "body": "Can one of the admins verify this patch?\nI understand the following commands:\n- `bot, add author to whitelist`\n- `bot, test pull request`\n- `bot, test pull request once`"
        },
        {
            "user": "anarcat",
            "body": "incidentally, what's up with `make` here? why can't we just `go get github.com/projectatomic/buildah` anyways?"
        },
        {
            "user": "rhatdan",
            "body": "bot, add author to whitelist"
        },
        {
            "user": "rhatdan",
            "body": "@lsm5 Could you take a look at this. "
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "rhatdan",
            "body": "@umohnani8 PTAL"
        },
        {
            "user": "umohnani8",
            "body": "LGTM"
        },
        {
            "user": "anarcat",
            "body": "hold the presses"
        },
        {
            "user": "anarcat",
            "body": "okay, now it's good - sorry for the noise, but there was one last tricky bit about the golang package I hadn't noticed.... `apt install golang-1.8` doesn't actually install the `/usr/bin/go` binary, that's the job of the metapackage, which needs to be installed from backports.\n\nphew!"
        },
        {
            "user": "nalind",
            "body": "The Makefile sets some build tags for the storage library so that it can build with older versions of some of the C libraries that it uses, and sets the commit ID and build date that are output by `buildah version`.  \nThe build tags not being present, if they're needed, will cause the compile to fail, but 'go get github.com/projectatomic/buildah/cmd/buildah' can produce a binary when the tags aren't necessary.  The `buildah version` command will fail when the commit ID and build date aren't set, though, so I can't recommend it as an alternative to using the makefile, at least not yet."
        },
        {
            "user": "rhatdan",
            "body": "@rh-atomic-bot r+"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":pushpin: Commit e44fce4 has been approved by `rhatdan`\n\n<!-- @rh-atomic-bot r=rhatdan e44fce4d144c93a7d0e6be89b6fcf8d5a98a1d77 -->"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":zap: Test exempted: merge already tested."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 355
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 119
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 243
        }
    ]
}