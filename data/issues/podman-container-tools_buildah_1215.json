{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 1215,
    "type": "issue",
    "title": "Should CommitOptions.Compression exist?",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "mtrmac",
    "body": "`CommitOptions.Compression` is used, via `makeImageRef`, in `containerImageRef.NewImageSource`, when deciding whether to compress or not the temporary file which contains the newly added, or the squashed, layer (and to use the compressed layer digest in the created manifest).\n\nThe created layer is then copied in `Builder.Commit` to the destination of the commit: AFAICS, in almost all cases, this is c/storage:\n\n- `cmd/buildah/commit.go` could use any destination if the user uses the _transport_`:`… syntax, but falls back to `ResolveName`+`ParseStoreReference` (which is likely to be the common case); meanwhile, it defaults to `archive.Gzip` unless the user explicitly opts out.\n- `imagebuildah/Executor.Commit`’s destination is _always_ a `ParseStoreReference`; the only in-tree user, `cmd/buildah/bud.go`, hard-codes `archive.Gzip`\n\nc/storage, when used as a destination, always uncompresses the layer; so, AFAICS, compressing the layer is just undone.\n\nMeanwhile, `copy.Image` already has destination-specific logic to decide whether to compress data on push or not, so a `buildah commit docker://…` would have the layer compressed by `copy.Image` whether or not `containerImageRef` creates a compressed layer.\n\nSo, the compression at time of `NewImageSource` seems at best redundant, at worst a fairly costly waste.\n\nAm I missing some other why the compression exists?\n\nThe only other use I could find is `containerImageRef.computeLayerMIMEType`, which\n- seems like something `copy.Image` should handle automatically\n- can, strictly speaking, continue to exist whether or not `NewImageSource` actually creates a compressed blob or not.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@TomSweeneyRedHat @nalind @mtrmac Is this still and issue?"
        },
        {
            "user": "mtrmac",
            "body": "The option continues to exist; do you have any other reason to think the situation changed?"
        },
        {
            "user": "rhatdan",
            "body": "@mtrmac I am going through hundreds of issues, and ones that have not changed for several months, I want to verify if they are still a problem and try to get people to start working on them. Otherwise they are either not important, or forgotten."
        },
        {
            "user": "rhatdan",
            "body": "@ashley-cui Could you open a PR to remove the Compression flag."
        },
        {
            "user": "rhatdan",
            "body": "Looks to me like we always disablecompression by default in master.  Reopen if I am mistaken."
        },
        {
            "user": "mtrmac",
            "body": "https://github.com/containers/buildah/blob/cd72e45f3f13e283a6be427d3ea198a52284145d/cmd/buildah/commit.go#L120 is still the default."
        },
        {
            "user": "nalind",
            "body": "#1336 also set `--disable-compression` to `true` by default.  Is it getting overridden or turned off later?"
        },
        {
            "user": "mtrmac",
            "body": "@nalind Thanks, I missed that. Anyway, I think it’s up to you whether this should be kept open."
        },
        {
            "user": "rhatdan",
            "body": "Right, the very next line turns it off by default\n\tif iopts.disableCompression {\n\t\tcompress = imagebuildah.Uncompressed\n\t}\nAnd iopts.disableCompression is true by default for buildah bud and buildah commit, which is why I disabled this.\n$ podman build --help | grep disable-compression\n  -D, --disable-compression                        don't compress layers by default (default true)\n\n\n"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 1216
        }
    ]
}