{
    "organization": "containers",
    "repository": "podman-py",
    "number": 573,
    "title": "fixed reading status from self.attrs",
    "type": "pull_request",
    "state": "closed",
    "labels": [],
    "user": "sekyHC",
    "body": "This is a fix for the following error:\n```\n  File \"/usr/local/lib/python3.11/dist-packages/podman/domain/containers.py\", line 61, in status\n    return self.attrs[\"State\"][\"Status\"]\n           ~~~~~~~~~~~~~~~~~~~^^^^^^^^^^\nTypeError: string indices must be integers, not 'str'\n```",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **NOT APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman-py/pull/573#\" title=\"Author self-approved\">sekyHC</a>*\n**Once this PR has been reviewed and has the lgtm label**, please assign [mheon](https://github.com/mheon) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman-py).\n\n<details open>\nNeeds approval from an approver in each of these files:\n\n- **[OWNERS](https://github.com/containers/podman-py/blob/main/OWNERS)**\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[\"mheon\"]} -->"
        },
        {
            "user": "sekyHC",
            "body": "As far as I understood from the openshift-ci bot workflow I have to tag reviewers and approvers in a comment:\n@ashley-cui \n@baude \n@inknos \n"
        },
        {
            "user": "inknos",
            "body": "@sekyHC , thanks for the PR.\n\nA bunch of changes were made recently that improved testing a little bit. Would you mind rebasing your branch? That should filter out few test errors.\n\nThe PR is very simple and looks good by itself, but we need to be sure that nothing breaks"
        },
        {
            "user": "sekyHC",
            "body": "@inknos, only errors I can see in the logs of failed tests are:\n1. can't find module tomli\n2. changelog entry has to start with *"
        },
        {
            "user": "inknos",
            "body": "mmh, for every `testing-farm` looks like I am getting this\n\n```\n        with self.subTest(\"Run Container\"):\n            top_ctnr = self.client.containers.run(\n                self.alpine_image, \"/usr/bin/top\", name=\"TestRunPs\", detach=True\n            )\n>           self.assertEqual(top_ctnr.status, \"running\")\nE           AssertionError: {'OciVersion': '1.2.1', 'Status': 'runnin[425 chars]00Z'} != 'running'\n\npodman/tests/integration/test_containers.py:125: AssertionError\n```\n\nlet me investigate"
        },
        {
            "user": "inknos",
            "body": "I confirm there's a failing test. can you reproduce it locally with `tox -e py -- -k test_container_crud`?"
        },
        {
            "user": "inknos",
            "body": "yup, looks like `self.attrs[\"State\"][\"Status\"]` gets just the status, ie, `running`, and you are returning the whole `\"State\"` structure. so tests are right to fail\n\nIf you can give me a reproducer of how you got the error in the PR description I can help you with the investigation"
        },
        {
            "user": "sekyHC",
            "body": "```python\n    async def list_containers(self, request):\n        def sync_list():\n            with PodmanClient(base_url=\"unix:///tmp/podman.sock\") as client:\n                result = []\n                for c in client.containers.list(all=True):\n                    result.append({\n                        \"Id\": c.id,\n                        \"Name\": c.name,          # container name\n                        \"Image\": c.image.tags[0],\n                        \"Status\": c.status,   # podman-py gives status directly\n                    })\n                return result\n\n        containers = await asyncio.to_thread(sync_list)\n        return web.json_response(containers)\n```\n\nThis was giving me the error:\n\n```\n  File \"/usr/local/lib/python3.11/dist-packages/podman/domain/containers.py\", line 61, in status\n    return self.attrs[\"State\"][\"Status\"]\n           ~~~~~~~~~~~~~~~~~~~^^^^^^^^^^\nTypeError: string indices must be integers, not 'str'\n```\n\nin order to fix it and actually get status, I had to delete [\"Status\"] in the library, like I did in this PR"
        },
        {
            "user": "inknos",
            "body": "Aha! then I know!\n\n- here's the issue you are having:\n  https://github.com/containers/podman-py/issues/446\n\n- and here's the fix that will be released in the next versoin and merged just few mins ago:\n  https://github.com/containers/podman-py/pull/540\n  \nNow, you have 2 options:\n\n1. know the usage of list, and call reload manually as per the README. https://github.com/containers/podman-py/blob/main/README.md?plain=1#L55-L61\n2. use the upstream version (or the one I am planning to releaes today or tomorrow, 5.6.0) and use `list(sparse=False)` which will reload under the hood.\n\nto add a little bit of context, the state of containers is not retrieved automatically on each `list` call because the `podman inspect` call (which is what gets you the correct status) is expensive, so by default we don't do that.\n\nhope that helps :)"
        }
    ]
}