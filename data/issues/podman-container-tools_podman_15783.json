{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 15783,
    "type": "issue",
    "title": "cannot remove container: ... running or paused containers cannot be removed without force: container state improper",
    "state": "closed",
    "labels": [
        "flakes",
        "remote",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "New flake seen in [remote f36-aarch64 root](https://api.cirrus-ci.com/v1/artifact/task/6076189906305024/html/sys-remote-fedora-36-aarch64-root-host.log.html#t--00079):\n```\n# podman-remote --url unix:/tmp/podman_tmp_nLiX run -d --log-driver=k8s-file quay.io/libpod/testimage:20220615 sh -c echo test1; echo test2\nSHA\n# podman-remote --url unix:/tmp/podman_tmp_nLiX logs --tail 1 SHA\ntest2\n# podman-remote --url unix:/tmp/podman_tmp_nLiX restart SHA\nSHA\n# podman-remote --url unix:/tmp/podman_tmp_nLiX logs --tail 1 SHA\ntest2\n# podman-remote --url unix:/tmp/podman_tmp_nLiX rm SHA\nError: cannot remove container SHA as it is running - running or paused containers cannot be removed without force: container state improper\n[ rc=2 (** EXPECTED 0 **) ]\n```\nNot sure if this is a race in the test (missing `podman wait`?) or in podman-remote itself. Filing for later.\n\nMy flake logs shwo three instances, all remote, all aarch64.\n\n### [sys] 79 podman logs - tail test, k8s-file\n* fedora-36-aarch64 : sys remote fedora-36-aarch64 root host [remote]\n  * PR #15665\n    * [09-07 16:18](https://api.cirrus-ci.com/v1/artifact/task/5633451834474496/html/sys-remote-fedora-36-aarch64-root-host.log.html#t--00079)\n  * PR #15607\n    * [09-06 06:33](https://api.cirrus-ci.com/v1/artifact/task/6255462285836288/html/sys-remote-fedora-36-aarch64-root-host.log.html#t--00079)\n  * PR #15571\n    * [08-31 15:28](https://api.cirrus-ci.com/v1/artifact/task/6065614195261440/html/sys-remote-fedora-36-aarch64-root-host.log.html#t--00079)\n ",
    "comments": [
        {
            "user": "edsantiago",
            "body": "Different test, same symptom: [f36-aarch64 again](https://api.cirrus-ci.com/v1/artifact/task/6098254629699584/html/sys-podman-fedora-36-aarch64-root-host.log.html) but not remote:\n```\n# podman info --format {{.Host.CgroupManager}}\nsystemd\n# podman --cgroup-manager=cgroupfs run --name myc quay.io/libpod/testimage:20220615 true\n# podman container inspect --format {{.HostConfig.CgroupManager}} myc\ncgroupfs\n# podman start myc\nmyc\n# podman rm myc\nError: cannot remove container SHA as it is running - running or paused containers cannot be removed without force: container state improper\n[ rc=2 (** EXPECTED 0 **) ]\n```\nThis one does not look like a bug in the test, it actually looks like a bug-bug. `podman start container-that-only-runs-true`, followed by `podman rm that-container`, should never fail with \"container is running\"."
        },
        {
            "user": "rhatdan",
            "body": "@mheon This looks like we are waiting for podman container cleanup to change the state from running to complete?"
        },
        {
            "user": "vrothberg",
            "body": "Yes, that smells like a state-transition issue."
        },
        {
            "user": "Luap99",
            "body": "That sounds like a test problem to me. podman start will only start the container process, there is absolutely zero guarantee that it will be finished by time we call podman rm, even if it is just `true`. After the container process finishes conmon needs to launch the podman container cleanup process. \n\nThe timing depends on the scheduler, I think the test assumption that this should work is wrong.\n\nIf you need a guarantee you either have to use podman wait or start the process in the foreground."
        },
        {
            "user": "vrothberg",
            "body": "> If you need a guarantee you either have to use podman wait or start the process in the foreground.\n\nIsn't the test running the container in the foreground?  There's no `-d`."
        },
        {
            "user": "edsantiago",
            "body": "I think the  bug is missing `-a` in `podman start`. I'll open a PR to address that."
        },
        {
            "user": "Luap99",
            "body": "> ```\n> # podman start myc\n> myc\n> # podman rm myc\n> Error: cannot remove container SHA as it is running - running or paused containers cannot be removed without force: container > state improper\n> [ rc=2 (** EXPECTED 0 **) ]\n> ```\n> This one does not look like a bug in the test, it actually looks like a bug-bug. podman start container-that-only-runs-true, followed > by podman rm that-container, should never fail with \"container is running\".\n\n\nAnd also in the original report you see podman restart and run -d"
        },
        {
            "user": "vrothberg",
            "body": "Aaaah, nice catch, @Luap99!  I somehow thought it was a `run` `rm` sequence."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 15794
        }
    ]
}