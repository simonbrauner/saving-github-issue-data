{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 27988,
    "type": "pull_request",
    "title": "set up default IDMappings when none are set and userns=auto",
    "state": "open",
    "labels": [],
    "user": "vmsh0",
    "body": "This PR originates from [podman-py#499](https://github.com/containers/podman-py/pull/499).\n\nThe rationale is to make the behavior of userns=auto a bit more consistent, by setting up default IDMappings when none are provided. Before this patch, Podman would just ignore the userns parameter silently, resulting in a container with non-private userns. This is not ideal from the point of view of security, and it can also be confusing (it was to me at least).\n\nAfter this patch, e.g. API call `POST libpod/containers/create UserNS='{\"NSMode\":\"auto\"}'` results in a container with private userns, as imo one could reasonably expect.\n\n<!--\nThanks for sending a pull request!\n\nFor more detailed information, please review our contributing guidelines:\nhttps://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests\n-->\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [ ] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nlibpod API sets up default IDMappings during container creation when userns is set to 'auto'\n```\n",
    "comments": [
        {
            "user": "vmsh0",
            "body": "I have also submitted #27998, which will fail the test added to this PR, so if the plan is to accept the other PR I will update the test in this one "
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        },
        {
            "user": "Honny1",
            "body": "@vmsh0, can you please rebase on main?"
        },
        {
            "user": "jankaluza",
            "body": "LGTM if tests pass."
        },
        {
            "user": "vmsh0",
            "body": "The usual tests + some new ones are still failing for this and #27998, but the failures still look unrelated by these patches imho"
        },
        {
            "user": "Honny1",
            "body": "I re-run failed tests."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 27998
        }
    ]
}