{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6883,
    "type": "pull_request",
    "title": "fix: replace dead len > 2 guard with len != 2 in addSysctl",
    "state": "open",
    "labels": [
        "No New Tests"
    ],
    "user": "immanuwell",
    "body": "#### What type of PR is this?\n\n> /kind bug\n\n#### What this PR does / why we need it:\n\n`strings.SplitN(s, \"=\", 2)` returns at most 2 elements, so `len(splitn) > 2` in `addSysctl` is dead code that never fires. When a sysctl in `containers.conf` has no `=`, `splitn` has length 1 and `splitn[1]` panics instead of hitting the error return right below.\n\nChanging `> 2` to `!= 2` kills the dead code and catches the bad input.\n\n#### How to verify it\n\n```\n# ~/.config/containers/containers.conf\n[containers]\ndefault_sysctls = [\"net.core.rmem_max\"]\n```\n\nBefore: `buildah run` panics with `index out of range [1] with length 1`\nAfter: returns `sysctl \"net.core.rmem_max\" defined in containers.conf must be formatted name=value`\n\n#### Which issue(s) this PR fixes:\n\nNone\n\n#### Special notes for your reviewer:\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "/lgtm"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Ping @immanuwell, can you rebase please per @honny1"
        },
        {
            "user": "Honny1",
            "body": "@TomSweeneyRedHat Let's give @immanuwell a week to respond. After that, a maintainer (with permission to rebase contributors' PRs) should rebase this PR. Since @immanuwell hasn't responded in a month, it's okay to take over since it only needs a rebase."
        }
    ],
    "cross_references": []
}