{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 24277,
    "type": "pull_request",
    "title": "api: Move close function in condition body",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note-none",
        "kind/api-change",
        "locked - please file new issue/PR",
        "No New Tests",
        "5.3"
    ],
    "user": "mi4r",
    "body": "The close is moved in the body of the error condition.\n\nFound by Linux Verification Center (linuxtesting.org) with SVACE.\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\n   None\n```\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "baude",
            "body": "approved not having tests"
        },
        {
            "user": "baude",
            "body": "in looking at this PR, I saw the the error is not handled or logged.  Then looking at just this file more broadly, sometimes it is and sometimes not.  Some are deferred.  If we have a consensus on how these should handled in the whole file, I would prefer that.  I did not look close enough to verify that this is a possibility.\n\n@mheon @Luap99 wdyt"
        },
        {
            "user": "mheon",
            "body": "I don't think there's value in logging the error from close. Maybe do an explicit `_ = xyz.Close()` so it's clear we are actually ignoring it."
        },
        {
            "user": "Luap99",
            "body": "> I don't think there's value in logging the error from close. Maybe do an explicit `_ = xyz.Close()` so it's clear we are actually ignoring it.\n\nRead the close() man page, there absolute is a reason to check for errors (even when super unlikely)\n\n> A careful programmer will check the return value of close(), since it is quite possible that errors on a previous write(2) operation are reported only on the final close() that releases the open file description.  Failing  to\n       check the return value when closing a file may lead to silent loss of data.  This can especially be observed with NFS and with disk quota.\n\n\nI know we ignore this pretty much elsewhere but here we are actually writing something potential large that my increase the odds of hitting such cases."
        },
        {
            "user": "baude",
            "body": "This PR has been marked for 5.3 inclusion but it must be merged prior to Nov 5 for inclusion in 5.3 RC3. PRs not merged by that date are considered on a case by case basis for backporting."
        },
        {
            "user": "mi4r",
            "body": "Once again I would like to clarify what needs to be done. Do you want me to add if err = tmpfile.Close(); err != nill {log} or do you want me to fix this everywhere in the file, or would it be better to make this a separate pr and then accept this one without processing this error?"
        },
        {
            "user": "Luap99",
            "body": "I would want this\n```\ndiff --git a/pkg/api/handlers/libpod/images.go b/pkg/api/handlers/libpod/images.go\nindex 6b5e5abb3..3e35a0dbb 100644\n--- a/pkg/api/handlers/libpod/images.go\n+++ b/pkg/api/handlers/libpod/images.go\n@@ -410,7 +410,10 @@ func ImagesImport(w http.ResponseWriter, r *http.Request) {\n                        return\n                }\n \n-               tmpfile.Close()\n+               if err := tmpfile.Close(); err != nil {\n+                       utils.Error(w, http.StatusInternalServerError, fmt.Errorf(\"unable to close tempfile: %w\", err))\n+                       return\n+               }\n                source = tmpfile.Name()\n        }\n \n```\nignoring the close error on the copy error path is fine but on the good path we should make sure we closed the file successfully to avoid io errors"
        },
        {
            "user": "mheon",
            "body": "LGTM"
        },
        {
            "user": "rhatdan",
            "body": "/approve\n/lgtm"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/24277#\" title=\"Author self-approved\">mi4r</a>*, *<a href=\"https://github.com/containers/podman/pull/24277#issuecomment-2462213198\" title=\"LGTM\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "Thanks @mi4r "
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 24269
        }
    ]
}