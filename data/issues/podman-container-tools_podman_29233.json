{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29233,
    "type": "issue",
    "title": "Documentation unclear regarding Podman configuration files on Windows",
    "state": "open",
    "labels": [
        "documentation",
        "machine",
        "windows"
    ],
    "user": "hansmbakker",
    "body": "This issue was posted before (https://github.com/podman-container-tools/podman/issues/17644); however it was closed without a solution.\n\nThe suggestion to look in the man pages did not solve the issue because it is not documented there.\nSee https://github.com/podman-container-tools/container-libs/blob/main/common/docs/containers.conf.5.md\n\nThe question is still: how to properly configure podman using a configuration file on Windows?\n\nI added the configuration file below to `%APPDATA%\\Roaming\\containers\\containers.conf.d` and it does not seem to be picked up.\n\n1-userns-keep-id.conf\n```toml\n[containers]\nuserns = \"keep-id\"\n```\n\nI wanted to add this setting as a configuration file instead of as `runArgs` setting in my `devcontainer.json` files to prevent breaking their compatibility with docker.",
    "comments": [
        {
            "user": "Luap99",
            "body": "What podman version are you using? (AS a general rule it would be great to follow the bug template when reporting issues to providse the infos there)\n\nMost options must be set server side so inside the VM. That said since this is a common problem starting with podman 6 the machine will mount the hosts config dir  `%APPDATA%\\containers\\` into the VM at `/etc/containers` so at that point you can edit it there on the host.\n\nhttps://github.com/podman-container-tools/container-libs/blob/main/common/docs/containers-config.5.md\n\nNot sure where Roaming there comes from but that path was never used AFAICT.\n"
        },
        {
            "user": "hansmbakker",
            "body": "So there are 2 issues here:\n- the man-page I referenced is unclear for windows. This is version-independent and did not fit well in the bug template\n- the .conf functionality does not work as expected. I created https://github.com/podman-container-tools/podman/issues/29239 for that"
        }
    ],
    "cross_references": []
}