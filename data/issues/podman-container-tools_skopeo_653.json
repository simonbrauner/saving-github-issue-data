{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 653,
    "type": "pull_request",
    "title": "rootless: don't create a namespace unless for containers-storage",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "TristanCacqueray",
    "body": "This change fixes skopeo usage in restricted environment such as\nbubblewrap where it doesn't need extra capabilities or user namespace\nto perform its action.\n\nClose #649\n\nSigned-off-by: Tristan Cacqueray <tdecacqu@redhat.com>",
    "comments": [
        {
            "user": "vrothberg",
            "body": "This looks like a clever solution to me. @giuseppe PTAL"
        },
        {
            "user": "giuseppe",
            "body": "thanks for the patch!  Yes, I think it is a good fix, and should also address https://github.com/containers/skopeo/issues/652"
        },
        {
            "user": "TristanCacqueray",
            "body": "You're welcome, thank you for the prompt review. FWIW I tried using a cleaner method (such as alltransports.ParseImageName) but I couldn't find the right predicate to test for containers-storage type. The proposed solution assumes all the containers-storage image reference start with the `containers-storage:` prefix."
        },
        {
            "user": "giuseppe",
            "body": "@mtrmac is there any cleaner way to test that?  Are you fine with this approach?"
        },
        {
            "user": "vrothberg",
            "body": "One way could be to use `ref, err := ParseImageName(...)` and if `err == nil` to compare `ref.Transport().Name() == storage.Transport.Name()`.\n"
        },
        {
            "user": "TristanCacqueray",
            "body": "The issue with ParseImageName is that it may fail early with `chown /home/fedora/.local/share/containers/storage/vfs: operation not permitted`"
        },
        {
            "user": "vrothberg",
            "body": "Ouch, sorry for missing that. In this case, we can mimic what `ParseImageName(...)` effectively does:\n```golang\nparts := strings.SplitN(imgName, \":\", 2)\nif len(parts) != 2 {\n   continue\n}\nif parts[0] == storage.Transport.Name() { // we have storage reference (but can't validate it yet) }\n```\n"
        },
        {
            "user": "TristanCacqueray",
            "body": "Done, thank you for the suggestion."
        },
        {
            "user": "rhatdan",
            "body": "LGTM\nAlthough I still want @mtrmac to chime in."
        },
        {
            "user": "mtrmac",
            "body": "> The issue with ParseImageName is that it may fail early with `chown /home/fedora/.local/share/containers/storage/vfs: operation not permitted`\n\nOh, wow. That’s… a mess.\n\n@nalind AFAICS this is essentially because `storageTransport.ParseStoreReference` needs a working `store` so that it can support image ID prefixes:\n```go\nif img, err := store.Image(possibleID); err == nil && img != nil && len(possibleID) >= minimumTruncatedIDLength && strings.HasPrefix(img.ID, possibleID)\n```\nand the like.  Is there any chance we could drop that functionality? (I guess not, but it would make dealing with this in callers so much simpler.)\n\nAlternatively, would it be reasonably possible for `c/storage.GetStore` to return a typed error indicating that “this would work in a user namespace”?"
        },
        {
            "user": "fungi",
            "body": "Just for another data point, we're temporarily running with a16489e applied in production and it seems to have solved #649 for us. Thanks @TristanCacqueray!"
        },
        {
            "user": "TristanCacqueray",
            "body": "@mtrmac Thank you for the suggestions, I think the last change implements them along with https://github.com/containers/image/pull/631 for the TransportFromImageName procedure. Not sure what is the policy around vendoring thus I inlined the c/image change in that PR.\n\nAs @fungi mentioned, the opendev.org infrastructure is using a continuously built version of skopeo which triggered the issue of using reexec in unprivileged environment like bubblewrap."
        },
        {
            "user": "mtrmac",
            "body": "LGTM. Thanks!"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 657
        }
    ]
}