{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28898,
    "type": "pull_request",
    "title": "quadlet: only log to kmsg when running under systemd",
    "state": "open",
    "labels": [],
    "user": "mvanhorn",
    "body": "#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `gofmt` and `go vet` (`make validatepr` needs a containerized environment not available locally; CI runs the full validation)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nThe quadlet generator no longer writes warnings to /dev/kmsg when run manually from a terminal; kmsg logging is kept for systemd invocations.\n```\n\nFixes #28888\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "tmt tests failed for commit 2ce9b56fc53fc2f18b000e94529692d7e839a81b. @lsm5, @psss, @thrix please check."
        },
        {
            "user": "mvanhorn",
            "body": "/packit retest-failed"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        },
        {
            "user": "mvanhorn",
            "body": "Both right, and switched to the euid gate in 96593320. The tty check was really a proxy for \"can we actually write kmsg\", and the real answer is euid: /dev/kmsg is only writable by root, so under rootless the write was failing and the line was lost. quadlet now attempts kmsg only when euid == 0 and falls back to stderr otherwise (and on dry-run), so a rootless run's logs show up on stderr regardless of whether stderr is a tty or redirected to a file, while root under systemd keeps going to kmsg as before. That also drops the stderrIsTerminal helper entirely. @Luap99 to your point about just logging to both: gating on euid avoids the double lines under systemd while still never dropping a rootless log."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        }
    ],
    "cross_references": []
}