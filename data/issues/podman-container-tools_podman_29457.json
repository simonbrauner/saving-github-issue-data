{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29457,
    "type": "pull_request",
    "title": "pkg/machine: retry atomic config rename on Windows to fix access denied flake",
    "state": "open",
    "labels": [
        "machine"
    ],
    "user": "Srujansa",
    "body": "### Summary\nFixes a Windows CI flake where concurrent reads (e.g. `podman machine list`) lock the machine JSON configuration file, causing atomic renames via `os.Rename` to fail with `Access is denied`.\n\n### Solution\nAdded a retry loop with short backoff in `config_windows.go` to tolerate transient lock contention during atomic file updates on Windows.\n\nFixes #28873",
    "comments": [
        {
            "user": "lsm5",
            "body": "@Srujansa thanks for the PR. The `validate` job is failing. Please run `make validate-source` and check the issues with the changes. "
        },
        {
            "user": "lsm5",
            "body": "> @Srujansa thanks for the PR. The `validate` job is failing. Please run `make validate-source` and check the issues with the changes.\n\nAt least GHA is complaining about the second `import`. I suspect there could be other issues pointed in local runs, but that's the one we need to get past for CI."
        }
    ],
    "cross_references": []
}