{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 11481,
    "title": "suggest a --replace flag for play kube",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "chenzhiwei",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nAlso, there is a running list of known issues in the [Podman Troubleshooting Guide](https://github.com/containers/podman/blob/master/troubleshooting.md),\nplease reference that page before opening a new issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n\n\n/kind feature\n\n**Description**\n\n* podman kube create -f k8s-manifests.yaml\n* podman kube apply -f k8s-manifests.yaml\n* podman kube delete -f k8s-manifests.yaml",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Please further explain or open PRs to examine.\n@haircommander @mrunalp WDYT?"
        },
        {
            "user": "haircommander",
            "body": "the `create` and `apply` commands are done with `podman play kube`, and `delete` was just recently added with `podman play kube --down`\n\nare you just looking for an alias between the two sets of commands?"
        },
        {
            "user": "chenzhiwei",
            "body": "@haircommander something similar.\n\n* create just creates the manifests, return error if already exist\n* apply pulls the images in the manifests, if new images or containers detected, apply the new images to the manifests\n* delete means deleting the manifests"
        },
        {
            "user": "haircommander",
            "body": "Ah so you'd like podman behaving like api server/etcd, rather than kubelet/runtime which is closer to how it's behaving now. What use case are you looking to fill? I can't think of a use of  keeping track of the underlying manifest if the pod/containers is what podman typically thinks about"
        },
        {
            "user": "chenzhiwei",
            "body": "one of the container use `latest` tag image, can podman now support update the image to the real `latest` one?"
        },
        {
            "user": "flouthoc",
            "body": "@chenzhiwei I dont think that even `k8s` supports that, you can have a `image pull policy`  as `always` on deployment and `k8s` will only update image if pod is restarted. You can have similar pull policy on `podman pods` but afaik they need to be restarted as well."
        },
        {
            "user": "chenzhiwei",
            "body": "@flouthoc Thank you.\n\nWhat about introducing a new flag `--replace` to play kube?\n\nI think my core requirement is apply the new changes from both my manifest file and the images inside it.\n\nIf the pod already exists, delete and re-create it, this can trigger the image refresh if the pull policy is always or image tag is `latest`. If no pod found, create one."
        },
        {
            "user": "rhatdan",
            "body": "That makes some sense to me.  @baude @mheon WDYT?"
        },
        {
            "user": "mheon",
            "body": "A `--replace` flag SGTM"
        }
    ]
}