{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 19054,
    "type": "issue",
    "title": "Container volume write permission denied when not root uid inside container",
    "state": "closed",
    "labels": [],
    "user": "erharb",
    "body": "### Bug description\n\nComing from Docker Desktop and Rancher Desktop, I'm used to having scripts run the container as the same uid/gid as the OS user (`-u $(id -u):$(id -g)`) when there is a need to attach volumes for writing files to persist outside the container runtime, and for those files written out to be owned by the OS user uid/gid.\n\nHowever, with Podman Desktop and using the default podman machine it creates out of the box, running these same scripts fail with permission denied errors unless I run the container as the root uid/gid.\n\n### Operating system\n\nmacOS Ventura 13.4.1 (Arm)\n\n### Installation Method\n\nBrew (macOS)\n\n### Version\n\n1.1.0\n\n### Steps to reproduce\n\nInstalled Podman Desktop on macOS using brew: `brew install --cask podman-desktop`\n\nExample fails to create file `test` as my uid/gid in container:\n```bash\npodman run --rm -it -v $(pwd):/work -w /work -u $(id -u):$(id -g) alpine touch /work/test\ntouch: /work/test: Permission denied\n\nls -l test\nls: test: No such file or directory\n\nid -u\n501\nid -g\n20\n```\n\nExample creates file `test` as root uid/gid in container:\n```bash\npodman run --rm -it -v $(pwd):/work -w /work -u 0:0 alpine touch /work/test\n\nls -l test\n-rw-r--r--@ 1 myuser  mygroup  0 Jun 29 14:45 test\n```\n\n### Relevant log output\n\n_No response_\n\n### Additional context\n\n_No response_",
    "comments": [
        {
            "user": "benoitf",
            "body": "I'm transferring the issue to podman repository as podman machine, mounts, etc are managed in this repository."
        },
        {
            "user": "erharb",
            "body": "Here's the relevant podman version info too:\n\n```\npodman version\nClient:       Podman Engine\nVersion:      4.5.1\nAPI Version:  4.5.1\nGo Version:   go1.20.4\nGit Commit:   9eef30051c83f62816a1772a743e5f1271b196d7\nBuilt:        Fri May 26 11:10:12 2023\nOS/Arch:      darwin/arm64\n\nServer:       Podman Engine\nVersion:      4.5.1\nAPI Version:  4.5.1\nGo Version:   go1.20.4\nBuilt:        Fri May 26 13:58:19 2023\nOS/Arch:      linux/arm64\n```"
        },
        {
            "user": "rhatdan",
            "body": "podman run --rm -it -v $(pwd):/work -w /work -u $(id -u):$(id -g) alpine touch /work/test\n\nYou are running rootless containers, which will run as root inside of a user namespace. Docker does not do this since it runs as root.\n\nCould you try\n\npodman run --userns=keep-id --rm -it -v $(pwd):/work -w /work -u $(id -u):$(id -g) alpine touch /work/test"
        }
    ],
    "cross_references": []
}