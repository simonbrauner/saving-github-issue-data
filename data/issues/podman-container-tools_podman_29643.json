{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29643,
    "type": "pull_request",
    "title": "test/e2e: do not use os.Exit for error handling",
    "state": "closed",
    "labels": [],
    "user": "vtushar06",
    "body": "another item off #18540, \"do not use os.Exit() as error handling, let ginkgo handle errors so it can provide useful output\".\n\nthere were six of them. os.Exit(1) takes the process down before ginkgo can report anything, so what you get in CI is a suite that stops with no failure and no location. three are inside It blocks in top_test.go and trust_test.go, the other three are in the SynchronizedBeforeSuite and in GetPortLock, which also run under ginkgo, so Expect works in all six:\n\n```go\n-\t\tuser, err := user.Current()\n-\t\tif err != nil {\n-\t\t\tos.Exit(1)\n-\t\t}\n+\t\tuser, err := user.Current()\n+\t\tExpect(err).ToNot(HaveOccurred())\n```\n\nthe two in the SynchronizedBeforeSuite were already printing to GinkgoWriter before exiting, so those lose nothing, Expect prints the same error plus the line.\n\nthe os.Exit(m.Run()) in TestMain is left alone, that one is how a Go test binary is supposed to exit.\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @podman-container-tools/packit-jobs please check. Everyone else, feel free to ignore."
        },
        {
            "user": "vtushar06",
            "body": "hey @danishprakash can you look into one still failing test, what to do with that\n"
        },
        {
            "user": "danishprakash",
            "body": "farm tests are non-blocking but I wonder if there's a runner shortage for the two other unit test checks fail to get assigned a runner cc/ @Luap99 "
        },
        {
            "user": "Luap99",
            "body": "\n\n\n\n> farm tests are non-blocking but I wonder if there's a runner shortage for the two other unit test checks fail to get assigned a runner cc/ @Luap99\n\nThe github UI doesn't tell us anything useful, the only thing I see is that you cancelled it\nhttps://github.com/podman-container-tools/podman/actions/runs/32878039558/job/97906196311?pr=29643\n\nThe runners are not owned by us (except the macos ones), they are run by the CNCF staff. In the `#cncf-ci-infra` cncf slack channel there is a post about capacity issues so that was likely it."
        }
    ],
    "cross_references": []
}