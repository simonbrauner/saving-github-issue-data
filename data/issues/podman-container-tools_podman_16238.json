{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 16238,
    "type": "pull_request",
    "title": "use cached containers.conf",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "vrothberg",
    "body": "Use `Default()` instead of re-loading containers.conf.\n\n[NO NEW TESTS NEEDED] as it's not a functional change.\n\nFixes: containers/common/issues/1200\nSigned-off-by: Valentin Rothberg <vrothberg@redhat.com>\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\". That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\nFix a bug where the containers.conf files are reloaded redundantly.\n```\n@Luap99 @alexlarsson PTAL\nI did not measure performance impacts.",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/16238#\" title=\"Author self-approved\">vrothberg</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [vrothberg]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "vrothberg",
            "body": "@baude @rhatdan PTAL"
        },
        {
            "user": "vrothberg",
            "body": "Performance benefits on my machine are in ns but I assume that's because containers.conf is mostly commented out.  Parsing a custom containers.conf would take longer where the benefits would be more visible."
        },
        {
            "user": "vrothberg",
            "body": "An unexpected armada of errors."
        },
        {
            "user": "vrothberg",
            "body": "@Luap99 any suspicion what's causing the farts?"
        },
        {
            "user": "vrothberg",
            "body": "OK, I think I got it.  Must have been the volume path."
        },
        {
            "user": "vrothberg",
            "body": "@containers/podman-maintainers PTAL"
        },
        {
            "user": "baude",
            "body": "/hold\n/lgtm\n"
        },
        {
            "user": "mheon",
            "body": "Tests are very angry"
        },
        {
            "user": "vrothberg",
            "body": "> Tests are very angry\n\nDown to one failing test.  I'll tackle that tomorrow o/"
        },
        {
            "user": "rhatdan",
            "body": "I have an alternative to this.\nhttps://github.com/containers/podman/pull/16242 Might want to combine them."
        },
        {
            "user": "vrothberg",
            "body": "I really don't like how we are using the pointer to the default config and mess with it.  There's still _some_ issue when creating the reset runtime which is painful to debug."
        },
        {
            "user": "vrothberg",
            "body": "OK, I see what's going on.  The static dir (i.e., root) is  being joined with \"libpod\" later on which in turn changes the default config.  We really need to separate the concerns of \"displaying defaults on the CLI\", \"loading a default config\", \"storing and altering a config in the libpod runtime\".  At the moment, we conflate them into one and I think that's too risky to cause hard-to-debug issues in the future."
        },
        {
            "user": "vrothberg",
            "body": "Updated the PR to not use the default config for storing the CLI values.\n\nI will have a closer look at how we pass around the `config.Config` in the stack in another commit; I don't want to start yak shaving."
        },
        {
            "user": "vrothberg",
            "body": "OK, I started shaving the yak.  I thought this will be a quick fix."
        },
        {
            "user": "vrothberg",
            "body": "I reworked `PodmanConfig` a bit to make it less ambiguous which values come from the defaults loaded from containers.conf and which ones came from the CLI.  For that purpose, there are two fields now."
        },
        {
            "user": "vrothberg",
            "body": "Search flake is kicking in hard :( I opened https://github.com/containers/podman/issues/16248 but think I need to tackle that here as well."
        },
        {
            "user": "vrothberg",
            "body": "@baude @rhatdan can you take another look?  I restarted the flake but tests are green now."
        },
        {
            "user": "vrothberg",
            "body": "@giuseppe @Luap99 PTAL"
        },
        {
            "user": "rhatdan",
            "body": "LGTM\n/hold cancel\n@containers/podman-maintainers PTAL"
        },
        {
            "user": "vrothberg",
            "body": "> I am not sure why we would need two configs? This will increase memory usage, `unsafe.Sizeof(*config)` returns 1704 bytes right now, with this change we would double it.\n\nDid you read the [commit message](https://github.com/containers/podman/pull/16238/commits/4e29ce2baf1d287bc136776fa265cc5c10de3260)?  I personally prefer a couple of bytes over messing with the default config.  If so, please give actionable proposals.  \"I don't like XY\" is not something I can work with.\n\nThere's an inherent design flaw in `c/common/pkg/config` to expose the internal data (i.e., `config.Config`) which is used for parsing.  That's not possible with `registries.conf` because the package clearly separates the concerns of reading/parsing the config (in an internal package) from the runtime data that external users/callers are allowed to use.\n\nPodman ran exactly into this trap as it was abusing the default config for storing CLI values.  A clearer separation of concerns would be to not use a `config.Config` at all internally but I don't aim for perfection; one issue at a time.\n\n> Also I don't like that the original field is also renamed since this creates a unnecessary large diff IMO.\n\nPlease read the commit message.  I don't care much about the diff but care about error potentials."
        },
        {
            "user": "Luap99",
            "body": "I try to understand this but I think I missing something. I see that we overwrite the config fields with cli options and you fix this by using another empty Config for that. But what is the point of that when in [libpod.NewRuntime()](https://github.com/containers/podman/pull/16238/files#diff-0577c1c9b036a03143b3ce475228a67d592abbe0cfe5a07379c150c6f3d385f6R168) you use the default config, which is then modified in [newRuntimeFromConfig()](https://github.com/containers/podman/pull/16238/files#diff-0577c1c9b036a03143b3ce475228a67d592abbe0cfe5a07379c150c6f3d385f6R208) with the values from the empty config. Therefore the default is still changed so I would assume we need a copy there as well? Or maybe it is better to just make config.Default() return a copy so that callers cannot modify the internal data."
        },
        {
            "user": "vrothberg",
            "body": "> I try to understand this but I think I missing something. I see that we overwrite the config fields with cli options and you fix this by using another empty Config for that. But what is the point of that when in [libpod.NewRuntime()](https://github.com/containers/podman/pull/16238/files#diff-0577c1c9b036a03143b3ce475228a67d592abbe0cfe5a07379c150c6f3d385f6R168) you use the default config, which is then modified in [newRuntimeFromConfig()](https://github.com/containers/podman/pull/16238/files#diff-0577c1c9b036a03143b3ce475228a67d592abbe0cfe5a07379c150c6f3d385f6R208) with the values from the empty config. Therefore the default is still changed so I would assume we need a copy there as well? Or maybe it is better to just make config.Default() return a copy so that callers cannot modify the internal data.\n\nThere is definitely more to improve.  I really don't like how the `config.Config` is abused and think that Podman should only use the Getter/Accessor API instead of reading fields from `config.Confg`.  But that is beyond what I intended to do with this PR.  Looking at the commit message:\n\n>  we were conflating \"loading the defaults\" with \"storing values from the CLI\" with \"libpod may further change fields\"\n\nThis PR stops conflating \"loading the defaults\" with \"storing values from the CLI\".  It does not stop \"libpod may further change fields\".\n\nI can volunteer to have another look this week at the issue.  But I think we should merge the PR as the issue is now at least more obvious than before.  I was surprised by how many things fell apart after the initial change."
        },
        {
            "user": "Luap99",
            "body": "Ok fine for me, this is definitely an improvement but we should keep an eye on this because it is super fragile.\n/lgtm"
        },
        {
            "user": "vrothberg",
            "body": "> Ok fine for me, this is definitely an improvement but we should keep an eye on this because it is super fragile. /lgtm\n\nThanks!  I agree and will take another look this week."
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "storage",
            "number": 1404
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 16242
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 16277
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 16282
        }
    ]
}