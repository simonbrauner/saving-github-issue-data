{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2958,
    "type": "pull_request",
    "title": "RFC: Start using log/slog as the primary logging mechanism",
    "state": "open",
    "labels": [],
    "user": "mtrmac",
    "body": "RFC @podman-container-tools/core-maintainers , for doing something like this project-wide.\n\nDirect `logrus.*` calls to `log/slog.Default()`. logrus 1.10.0 includes that as a native feature, I think that’s the best hint to migrate to the standard logging mechanisms we are ever going to get.\n\nI’d suggest adding the logging redirect infrastructure to `main` of the various projects, _with no plan to immediately migrate_ — lot of churn we just don’t need; but for new code, we would ask for `log/slog` to be used.\n\nThis PR does migrate the few uses in Skopeo, basically to demonstrate what that looks like (both the `Sprintf`s at the very top level, and the more useful usage within `sync.go`).\n\n---\n\nThis changes formatting, from \n```\nFATA[0001] Error ...\n```\n(with the `FATA` red, and the `[0001]` part, very unintuitively, a timestamp in seconds) when interactive, and from\n```\ntime=\"2026-08-14T00:24:59+02:00\" level=fatal msg=\"Error...\"\n```\nwhen non-interactive, to\n```\nERROR Error ...\n```    \n    \nI think that is good enough, probably a bit of an improvement; we can tweak the formatting later if necessary.\n\n---\n\nThe more important aspect is that `log/slog` does not provide `Printf`-like helpers (although a manual `Sprintf` is always an option), strongly directing callers towards a key-value format. Given that _most_ logs are for debugging or unexpected situations, that seems acceptable (especially if it allows sharing some fields,\n```go\nlogger := slog.With(\"fields\", \"relevant\", \"to-the-whole\", \"function\")\n…\nlogger.Error(\"step A failed\", \"err\", err)\n…\nlogger.Error(\"step C failed\", \"err\", err)\n```\ncan be more consistent and less repetitive).",
    "comments": [
        {
            "user": "Luap99",
            "body": "I don't have time to closely at this but overall I am definitely +1 to slowly transition to log/slog.\nPersonally I quite liked the timestamps on non interactive logs, in podman but also elsewhere it gives you a good idea how long certain steps take. I know I also have used them to correlate things with other logs, i.e. journal."
        },
        {
            "user": "mheon",
            "body": "+1 on a transition as well. Should we start converting container-libs over after this?"
        },
        {
            "user": "Luap99",
            "body": "> Should we start converting container-libs over after this?\n\nI think the point the opposite there is no direct need to switch existing library users over. In fact that also impacts all the other external library users that we not ourselves (cri-o and so many other things) so I am not sure we would want to do it just like that there.\n\nThe goal here is to switch the main logrus init logic so podman/buildah to make it use log/slog and then we can start using log/slog for new log lines in these projects. "
        },
        {
            "user": "mtrmac",
            "body": "> > Should we start converting container-libs over after this?\n> \n> I think the point the opposite there is no direct need to switch existing library users over. In fact that also impacts all the other external library users that we not ourselves (cri-o and so many other things) so I am not sure we would want to do it just like that there.\n\nWell, consumers need to take action _either way_, the day we add the first `log/slog` call: their log level options need to affect `log/slog` (and they can continue also configuring `logrus`, or they can set up `logrus` to log everything, and filter at the `log/slog` path only — at some performance cost).\n\nBut I agree that I don’t think converting immediately (except perhaps a few inidivual subpackages as a small-scale experiment) is worth it: apart from the simple case of logging a static string, this needs a _bit_ of human judgement on the key/value pair naming, and code structure. That’s a lot of churn. I’m sure we could have that work _done_, by AI and/or by enthusiastic volunteers, but it would still need to be reviewed, and I don’t think we need that extra review load, for, really, no end-user benefit\n\n> The goal here is to switch the main logrus init logic so podman/buildah to make it use log/slog and then we can start using log/slog for new log lines in these projects.\n\nI’d prefer this."
        },
        {
            "user": "mtrmac",
            "body": "> Personally I quite liked the timestamps on non interactive logs, in podman but also elsewhere it gives you a good idea how long certain steps take. I know I also have used them to correlate things with other logs, i.e. journal.\n\nRe-enabled when stderr is not a TTY, example output `2026/08/17 21:58:51 ERROR  …`. (We can easily add microseconds or use time in UTC; doing other things would require writing a custom `slog.Handler`.)"
        },
        {
            "user": "mtrmac",
            "body": "Let’s do it, then. Please review+merge."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 7082
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2977
        }
    ]
}