{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 2172,
    "type": "pull_request",
    "title": "manifest push --format: force an image type, not a list type",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "nalind",
    "body": "The image library's image copying API expects the type of a single image in the options structure's `ForceManifestMIMEType` field, and it infers the corresponding list type when it's copying multiple images.\n\nPreviously, though, we were passing in a _list_ MIME type, which was also being used as the target type when converting single images, which would fail.  Switch to passing in the single image type instead, and infer the single-image type in our internal API if a consumer passes us a list type.\n\nThis makes it easier to notice that we force v2s1 images to be converted to v2s2 images when we push them as part of a list, even if the registry doesn't reject v2s1 images.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@TomSweeneyRedHat @vrothberg @giuseppe @QiWang19 PTAL"
        },
        {
            "user": "rhatdan",
            "body": "bors r+"
        },
        {
            "user": "bors[bot]",
            "body": "# Build failed\n  * cirrus-ci/success"
        },
        {
            "user": "rhatdan",
            "body": "bors retry"
        },
        {
            "user": "bors[bot]",
            "body": "# Build failed\n  * cirrus-ci/success"
        },
        {
            "user": "vrothberg",
            "body": "bors retry"
        },
        {
            "user": "bors[bot]",
            "body": "# Build failed\n  * cirrus-ci/success"
        },
        {
            "user": "rhatdan",
            "body": "@cevich Any chance we can get some info from bors telling why it is failing.\nForce merging."
        },
        {
            "user": "cevich",
            "body": "> Any chance we can get some info from bors telling why it is failing.\n\n@rhatdan yes, click the big-red-X on the right, just above the 'build failed' comment.  In this case it's the `cirrus-ci/only_prs/gate` task.  Looks like a false-positive git-validation finding."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 2107
        }
    ]
}