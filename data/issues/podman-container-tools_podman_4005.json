{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 4005,
    "title": "errors with varlink via systemd",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "cevich",
    "body": "/kind bug\n\n**Description**\n\nWhen executing in a tight loop, or multiple simultaneous podman-remote throws `EOF` errors.\n\n**Steps to reproduce the issue:**\n\n1. `make podman podman-remote`\n1. `make install PREFIX=/usr`\n2. `systemctl daemon-reload`\n3. `systemctl enable io.podman.socket`\n3. `systemctl enable io.podman.service`\n3. `systemctl start io.podman.socket`\n3. `while podman-remote run -it --rm alpine echo \"hi\"; do :; done`\n\n**Describe the results you received:**\n\nEither:\n\n```\nhi\n...cut several repeats...\nhi\nError: unexpected EOF\n...\n```\n\nor\n\n```\nhi\n...cut several repeats...\nhi\nError: Virtual Read failed, 0: EOF\n...\n```\n\n**Describe the results you expected:**\n\nA continuous stream of `hi` and no errors.\n\n**Additional information you deem important (e.g. issue happens only occasionally):**\n\nFound while discovering/debugging issue #3998\n\nAlso sometimes reproducible with:\n`podman-remote run --rm alpine echo \"hello\" & podman-remote run --rm alpine echo \"hello\"`\n\n**Output of `podman version`:**\n\n```\n(paste your output here)\n```\n\n**Output of `podman info --debug`:**\n\n```\n# podman-remote version\nClient:\nVersion:            1.5.2-dev\nRemoteAPI Version:  1\nGo Version:         go1.12.7\nOS/Arch:            linux/amd64\n\nService:\nVersion:            1.5.2-dev\nRemoteAPI Version:  1\nGo Version:         go1.12.7\nOS/Arch:            linux/amd64\n```\n\n**Package info (e.g. output of `rpm -q podman` or `apt list podman`):**\n\naf8fedcc78674d71d43ca3000438c42b7b6b6994\n\n**Additional environment details (AWS, VirtualBox, physical, etc.):**\n\nReproduced on a VM with 2 vCPUs (no threads) and 4 GiB memory.",
    "comments": [
        {
            "user": "vrothberg",
            "body": "I can reproduce immediately. Interestingly, I cannot reproduce when avoiding the socket activation and running `sudo podman varlink -t 0 unix:/run/podman/io.podman` directly."
        },
        {
            "user": "vrothberg",
            "body": "~I get a 100 percent error rate when doing a podman-remote run alpine echo \"hi\" > /dev/null.~\n...that was a red herring."
        },
        {
            "user": "vrothberg",
            "body": "It looks like the logic to disable sdnotify was wrong. Opened #4089 to address the issue. It fixes the issues locally but lets wait what the CI has to say."
        },
        {
            "user": "cevich",
            "body": "Oh this is fantastic! Thanks @vrothberg :smiley_cat: "
        }
    ]
}