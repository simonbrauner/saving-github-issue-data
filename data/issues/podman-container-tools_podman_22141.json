{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 22141,
    "type": "pull_request",
    "title": "Add a warning if tmpdir/rundir are not on tmpfs",
    "state": "closed",
    "labels": [
        "approved",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "Podman requires several directories to be on tmpfs so we can properly detect (and clean up after) reboots: the tmpdir (for Libpod files) and runroot (for c/storage files). We have not done any validation that they are actual on tmpfs filesystems in the past, which leads to no end of people wondering why Podman is broken on their system (where, perhaps, /tmp is not a tmpfs, which the FHS allows because sanity cannot exist in this world, and there is no systemd session so /run/user/$UID does not exist).\n\nThis adds an actual warning on launching Podman with such an unsupported configuration. There's an environment variable if the user is certain they want to run Podman like this (perhaps having used systemd-tmpfiles to automatically clean the relevant dir on reboot), but otherwise we warn every time a command is run with runroot/tmpdir on a non-tmpfs filesystem.\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\". That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\nPodman now warns if a temporary files directory is not placed on a tmpfs filesystem.\n```\n",
    "comments": [
        {
            "user": "afbjorklund",
            "body": "> perhaps, /tmp is not a tmpfs, which the FHS allows because sanity cannot exist in this world, and there is no systemd session so /run/user/$UID does not exist)\n\nI thought Podman still supported installations with a traditional `/tmp` filesystem and a traditional `init` system..."
        },
        {
            "user": "Luap99",
            "body": "> > perhaps, /tmp is not a tmpfs, which the FHS allows because sanity cannot exist in this world, and there is no systemd session so /run/user/$UID does not exist)\n> \n> I thought Podman still supported installations with a traditional `/tmp` filesystem and a traditional `init` system...\n\nWe do, but that doesn't change the fact that we depend on tmpfs or some other util to remove these paths on boot."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Cockpit tests failed for commit d4ec4737e083c84efecc531ce5bed4c088bbc03e. @martinpitt, @jelly, @mvollmer please check."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Cockpit tests failed for commit 811ecc38dc3dca830b4ff3d86c53418c9f103a25. @martinpitt, @jelly, @mvollmer please check."
        },
        {
            "user": "mheon",
            "body": "@edsantiago The system tests don't use put the test directory on tmpfs, right? If so we can test this on tmpfs"
        },
        {
            "user": "baude",
            "body": "LGTM, do any docs need updates or should have clarification?"
        },
        {
            "user": "edsantiago",
            "body": "> @edsantiago The system tests don't use put the test directory on tmpfs, right? If so we can test this on tmpfs\n\n@mheon I've been struggling to understand this. The best answer I can give is, (for the most part) system tests just invoke `podman` with no special magic args. There are a few very specific cases in which there are `--root --runroot --tmpdir`. Does that address your question?"
        },
        {
            "user": "giuseppe",
            "body": "could we instead write a file to the tmpdir/rundir and with the value of `/proc/sys/kernel/random/boot_id` and compare it each time?  If it has a different value, then we return an error instead of a warning."
        },
        {
            "user": "mheon",
            "body": "@giuseppe That'd work for generic reboot protection, but it wouldn't help clear files afterwards - this PR was motivated by the IPAM DB not being removed on reboot leading to us running out of IP addresses to allocate. We could theoretically build that entirely into Podman as well, have a list of files which must be removed if reboot is detected, but that seems overly complex when we can just make sure a tmpfs is in use?"
        },
        {
            "user": "mheon",
            "body": "@edsantiago Yeah that should be fine, thanks. Needed to make sure that we're using standard paths and not throwing everything on a tmpfs for performance or similar."
        },
        {
            "user": "giuseppe",
            "body": "> @giuseppe That'd work for generic reboot protection, but it wouldn't help clear files afterwards - this PR was motivated by the IPAM DB not being removed on reboot leading to us running out of IP addresses to allocate. We could theoretically build that entirely into Podman as well, have a list of files which must be removed if reboot is detected, but that seems overly complex when we can just make sure a tmpfs is in use?\n\nI was thinking about raising an error and terminating the Podman process if the content of `/proc/sys/kernel/random/boot_id` is different than `$RUNDIR/boot_id` with something like `the content of $FILE1 is different than $FILE2, please remove $RUNDIR and try again`.\n\nEDIT: would that be enough to protect from the IPAM DB not being cleaned up?"
        },
        {
            "user": "mheon",
            "body": "Hm. I like the idea, but I'd also like to backport this patch, and I don't know if introducing a new error in existing releases is a good idea. Maybe I should target this at 5.0 only, and we make it an error in main?"
        },
        {
            "user": "giuseppe",
            "body": "> Hm. I like the idea, but I'd also like to backport this patch, and I don't know if introducing a new error in existing releases is a good idea. Maybe I should target this at 5.0 only, and we make it an error in main?\n\nsure, that works for me"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/22141#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/v5.0/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "Retargeted at 5.0 branch. Will add a test this morning."
        },
        {
            "user": "mheon",
            "body": "Update: None of our VMs actually use tmpfs /tmp so this breaks absolutely everything."
        },
        {
            "user": "mheon",
            "body": "Passed the ignore-warning environment variable through to all Cirrus tasks. Good news is that it makes the test easier; just unset that environment variable and run Podman."
        },
        {
            "user": "edsantiago",
            "body": "https://github.com/containers/automation_images/pull/340 will make `/tmp` be tmpfs, but that won't help with this v5.0 PR."
        },
        {
            "user": "mheon",
            "body": "The PR against main will probably be completely different, and not need the environment variable at all.\nFortunately v5.0 is not going to be an LTS branch so we can stop caring about this little idiosyncrasy in a few months when 5.1 comes out. "
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "mheon",
            "body": "@edsantiago Does the test logic seem OK to you? It's acting like the environment variable is still defined."
        },
        {
            "user": "edsantiago",
            "body": "Maybe because `/run`?\n```\nrunRoot: /run/containers/storage\n```\nI don't see tmpdir, though. Maybe that's in XDG?\n\nI still think it's better to test with explicit `--runroot/--tmpdir` + `/dev/shm / /var/tmp`"
        },
        {
            "user": "Luap99",
            "body": "Why is this targeted against the 5.0 branch? I don't think it is a good idea to backport such a big warning in a patch release."
        },
        {
            "user": "mheon",
            "body": "We decided on an alternate implementation for Main - we'll be outright refusing to run if we detect an unhandled reboot in 5.1. This was lower-impact so I thought it would still be useful in 5.0.1. I'm OK with leaving it out until we discuss further when you get back, though."
        },
        {
            "user": "Luap99",
            "body": "Note I feel rather strongly about not adding such big warnings in a patch release, we lived long enough with the status quo so waiting few more weeks shouldn't be that big of a deal. And if we plan to fix this in a different way on main anyway then I think we should just do that and not introduce two different fixes in two different versions which will likely cause some differences in behavior and thus confusion. "
        },
        {
            "user": "mheon",
            "body": "I'll go ahead and close, then. We can fix properly in 5.1."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 22159
        },
        {
            "organization": "containers",
            "repository": "automation_images",
            "number": 340
        }
    ]
}