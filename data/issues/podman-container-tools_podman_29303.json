{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29303,
    "type": "pull_request",
    "title": "compat: report cgroup driver \"none\" when rootless with cgroupfs",
    "state": "open",
    "labels": [
        "stale-pr",
        "kind/api-change"
    ],
    "user": "virzak",
    "body": "Rootless Podman with the cgroupfs manager cannot create a cgroup parent outside its delegated subtree, and container creation already leaves it unset (`libpod/runtime_ctr.go:412`). Compat `/info` still reports `cgroupfs` though, so buildx pins its BuildKit container to `/docker/buildx` and crun fails:\n\n```\ncrun: create `/sys/fs/cgroup/docker`: Permission denied: OCI permission denied\n```\n\nRootless dockerd reports `none`, never `cgroupfs` (`cgroupDriver` in moby `daemon/daemon_unix.go`). This does the same.\n\nRepro without Docker involved:\n\n```\npodman --cgroup-manager=cgroupfs run --rm alpine true                                  # works\npodman --cgroup-manager=cgroupfs run --rm --cgroup-parent=/docker/buildx alpine true   # fails\n```\n\nWith the same buildx client against a rootless service, `HostConfig.CgroupParent` on the buildkit container goes from `/docker/buildx` to empty.\n\nFixes #27824\n",
    "comments": [
        {
            "user": "mheon",
            "body": "@giuseppe Does this look correct? I know we've had cgroupfs issues with rootless, but I thought those were ~mostly resolved with cgroupsv2"
        },
        {
            "user": "giuseppe",
            "body": "@mheon a rootless user still can't use arbitrary cgroups.  Moving processes around is limited by the kernel, as the user must own all the common ancestors (from the source cgroup to the new destination).\n\nSaid so, cgroupfs works fine if the user picks the correct cgroup.  Is it something to fix in buildx instead?  Shouldn't try to use a cgroup the user doesn't own"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": [
        {
            "organization": "ddev",
            "repository": "ddev",
            "number": 8634
        },
        {
            "organization": "ddev",
            "repository": "ddev",
            "number": 8633
        }
    ]
}