{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 16299,
    "type": "pull_request",
    "title": "quadlet: Warn in generator if using short names",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note-none",
        "locked - please file new issue/PR"
    ],
    "user": "alexlarsson",
    "body": "This was broken out of https://github.com/containers/podman/pull/16237 due to the discussions there. \n\nBasically, it isn't a fatal error to use a shortname as `Image=shortname` in a quadlet file, but it does have some non-obvious drawbacks:\n\n * Using a shortname will cause podman to load the shortname aliases file, which on rhel is 300k and takes > 70 msecs. This delay is non-obvious and unnecessary, but most sysadmins will not be aware of it, so logging a warning is helpfule\n * Shortnames do not uniquely define an image, so over time what is run by the service may change. For instance, if initially there was only `regitstry1.com/image`, but then someone installs `registry2.com/image`, then the service may switch to that.\n\nSo, the idea was to at least warn about this to ensure people are aware of the issue.\n\nThe second issue was that this PS uses a simple-minded re-implementation of isShortName(), even though there exist a `containers/image/pkg/shortnames.IsShortName()`. However, that one pulls in dependencies that make quadlet larger and slower, which is a problem because quadlet (being a systemd generator) is on the blocking path of system boot.\n\n```release-note\nNONE\n```\n",
    "comments": [
        {
            "user": "alexlarsson",
            "body": "So, new version with CI tests. I also renamed the method isFullyQualifiedName() as that is more in line with what it checks. I.e. the real IsShortName() will do a more thorough validation that it is actually a valid shortname which we don't care about for the warning."
        },
        {
            "user": "vrothberg",
            "body": "@mtrmac PTAL"
        },
        {
            "user": "alexlarsson",
            "body": "New version with unit tests and some renaming of the functions related to the ambigious names helpers."
        },
        {
            "user": "alexlarsson",
            "body": "Fixed e2e shortname test to match the new warning string."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Code LGTM.  Can we flip a coin on the naming and move this forward?"
        },
        {
            "user": "rhatdan",
            "body": "/approve\n/lgtm"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/16299#\" title=\"Author self-approved\">alexlarsson</a>*, *<a href=\"https://github.com/containers/podman/pull/16299#issuecomment-1320465320\" title=\"LGTM\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        }
    ],
    "cross_references": []
}