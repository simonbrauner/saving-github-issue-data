{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 14706,
    "type": "pull_request",
    "title": "Only allow Rootless runs of Podman Machine",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "ashley-cui",
    "body": "Podman Machine crashes if run as root, as we need the UID of the core user to match the UID of the user on the host. If the user on the host is root, the core UID and the Root UID collide, causing a the VM not to boot.\n\n[NO NEW TESTS NEEDED]\n\nSigned-off-by: Ashley Cui <acui@redhat.com>\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\". That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\nAllow `podman machine` to only be run rootless, as the VM refuses to boot when run as root. \n\n```\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/14706#\" title=\"Author self-approved\">ashley-cui</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [ashley-cui]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "ashley-cui",
            "body": "No tests because this just disables running podman machine as root. Also, wasn't sure if it was a better idea to do this with annotations or by just copying and pasting `if !rootless.IsRootless() || os.Getuid() == 0` into every machine file, annotations felt cleaner so I went with that. Let me know if this is the right way."
        },
        {
            "user": "Luap99",
            "body": "I think it would be much less code if you just add it to the PersitentPreRunE function for the machine command: https://github.com/containers/podman/blob/8e88abda85f7bf44b6857ad5d62c8ef58206fce5/cmd/podman/machine/machine.go#L37"
        },
        {
            "user": "giuseppe",
            "body": "> Podman Machine crashes if run as root, as we need the UID of the core user to match the UID of the user on the host. If the user on the host is root, the core UID and the Root UID collide, causing a the VM not to boot.\n\ndoes it mean it crashes with any UID != UID in the VM?  e.g. what happens if the UID on the local system is 1001?\n"
        },
        {
            "user": "ashley-cui",
            "body": "@giuseppe We create the root user and the core user in the machine by writing the UID to the ignition file. Podman machine checks your current UID on the host and sets the core user to that UID. If we were to use podman machine as root, we end up trying to  create the core user and the root user with UID 0, which causes the machine not to boot. So, the root UID != core UID in order for the machine to properly boot.\n\nIt shouldn't crash if user UID != core UID in the VM (I think..)\n"
        },
        {
            "user": "ashley-cui",
            "body": "Thanks @Luap99 ! Great idea, updated "
        },
        {
            "user": "giuseppe",
            "body": "> @giuseppe We create the root user and the core user in the machine by writing the UID to the ignition file. Podman machine checks your current UID on the host and sets the core user to that UID. If we were to use podman machine as root, we end up trying to create the core user and the root user with UID 0, which causes the machine not to boot. So, the root UID != core UID in order for the machine to properly boot.\n> \n> It shouldn't crash if user UID != core UID in the VM (I think..)\n\nit makes sense.  Thanks for the explanation"
        },
        {
            "user": "mheon",
            "body": "System tests are angry"
        },
        {
            "user": "ashley-cui",
            "body": "Hmm, okay. Looks like putting the rootless check in `PersistentPreRunE` for `podman machine` makes it so that running just `podman machine` without a subcommand fails with `podman-machine can only be run as root`, whereas the test expects the help output to be printed. I can't think of a way to get this working atm, other than putting the check back into every machine subcommand, @Luap99 Any thoughts? "
        },
        {
            "user": "Luap99",
            "body": "> Hmm, okay. Looks like putting the rootless check in `PersistentPreRunE` for `podman machine` makes it so that running just `podman machine` without a subcommand fails with `podman-machine can only be run as root`, whereas the test expects the help output to be printed. I can't think of a way to get this working atm, other than putting the check back into every machine subcommand, @Luap99 Any thoughts?\n\nMhh, I don't think there is a way to avoid that. Code LGTM but you need to update the man pages."
        },
        {
            "user": "ashley-cui",
            "body": "Updated, @containers/podman-maintainers  PTAL"
        },
        {
            "user": "ashley-cui",
            "body": "@cdoern Those currently don't run in CI at all :) \n"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        },
        {
            "user": "gbraad",
            "body": "Unfortunately this broke `podman machine` on Windows. I discussed this with @n1hility and he provided a fix. Will test #14794 \n"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 14762
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 14794
        }
    ]
}