{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6441,
    "type": "pull_request",
    "title": "Move define.TempDirForURL() to pkg/tmpdir.ForURL(), unify its and Add()'s http client configurations",
    "state": "open",
    "labels": [
        "kind/bug",
        "approved",
        "size:XL"
    ],
    "user": "nalind",
    "body": "#### What type of PR is this?\n\n/kind bug\n\n#### What this PR does / why we need it:\n\nThe HTTP client configurations used by Add() and the logic that we use for pulling down build contexts that are specified using URLs differ in their setup, and the latter doesn't implement the --tls-verify=false flag.\n\n#### How to verify it\n\nNew integration test!\n\n#### Which issue(s) this PR fixes:\n\nNone\n\n#### Special notes for your reviewer:\n\nThis is also a follow-up to #6274.\n\nThis moves the function that we use for retrieving things and putting them into temporary directories into its own package, but leaves behind an API-compatible wrapper function.\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nRetrieval of build contexts which are specified using HTTPS URLs now respect the value of the `--tls-verify` flag.\n```",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/buildah/pull/6441#\" title=\"Author self-approved\">nalind</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fbuildah).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/buildah/blob/main/OWNERS)~~ [nalind]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@nalind, did you intend to use the name `tmpdir` for the new `/pkg` directory that you're putting the tls code into?"
        },
        {
            "user": "nalind",
            "body": "Yeah, it's used by `tmpdir.ForURL()`, which is meant to replace `define.TempDirForURL()`."
        },
        {
            "user": "nalind",
            "body": "Moved those bits to a new `internal/httpclient` package."
        },
        {
            "user": "nalind",
            "body": "Tweaked a bit to expose the proxy setting directly for callers."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": []
}