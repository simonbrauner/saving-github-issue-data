{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 5393,
    "type": "issue",
    "title": "SBOM: merge strategies that deduplicate by name+version are suboptimal",
    "state": "open",
    "labels": [
        "stale-issue"
    ],
    "user": "chmeliik",
    "body": "**Description**\n\nThe default [merge strategies](https://www.mankier.com/1/buildah-commit#--sbom-merge-strategy) deduplicate SBOM components by name and version. This can result in losing components that are not duplicates.\n\nFor example:\n* A package with the same name and version can exist in many package ecosystems. For example, just about every popular language has a `requests` library.\n* CycloneDX components have a [group](https://cyclonedx.org/docs/1.5/json/#components_items_group) attribute, which should be considered part of the package identity for some package ecosystems. E.g. JavaScript, where `@types/react` is a very different package than `react`.\n\nFor the purpose of de-duplicating components, a more appropriate identifier is typically the [purl](https://cyclonedx.org/docs/1.5/json/#components_items_purl), which _should_ uniquely identify a package.\n\n_Note that merging SBOMs by simply de-duplicating components is never optimal, as it often results in losing data from one or both SBOMs (e.g. the [dependencies](https://cyclonedx.org/docs/1.5/json/#dependencies) data). But that's a much harder problem to solve._",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ],
    "cross_references": []
}