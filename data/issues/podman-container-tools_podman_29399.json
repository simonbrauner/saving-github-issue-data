{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29399,
    "type": "pull_request",
    "title": "fix: align volume event filter to match by name or ID prefix",
    "state": "closed",
    "labels": [],
    "user": "AftAb-25",
    "body": "\n\nThis PR fixes a bug in how volume events are filtered, making the behavior consistent with all other resource filters.\n\nCurrently, the `VOLUME` filter only performs a prefix match on the volume name (`strings.HasPrefix(e.Name, filterValue)`). This means that filtering by a volume ID (e.g. `podman events --filter volume=<id>`) silently fails and returns zero events because there's no fallback to check the ID.\n\nThis updates the `VOLUME` filter logic in `libpod/events/filters.go` to match the exact same pattern already used for `CONTAINER`, `IMAGE`, `POD`, and `ARTIFACT` filters:\n1. Check for an exact name match first.\n2. Fall back to an ID prefix match.\n\nFixes #29398\n\n**Release Note**\n\nFixed a bug where filtering events by volume ID (`podman events --filter volume=<id>`) would silently return no results. The volume filter now correctly matches by exact name or ID prefix, consistent with other resource filters.\n",
    "comments": [
        {
            "user": "baude",
            "body": "i would like to see an `e2e` volume test modified to test for this behavior too."
        },
        {
            "user": "AftAb-25",
            "body": "@baude Updated! I've extended the existing `podman events with a volume filter` test in `test/e2e/events_test.go` rather than adding a new test block.\n\nThe test now:\n1. Creates a second volume (`vname + \"-alt\"`) with an overlapping prefix to verify exact-name precision—ensuring an exact name filter returns only the intended volume's event and doesn't match other volumes sharing the prefix.\n2. Asserts that name-prefix filtering (`vname[:5]`) matches both volumes as expected.\n3. Asserts that filtering with a non-matching string returns zero events (no false positives).\n"
        }
    ],
    "cross_references": []
}