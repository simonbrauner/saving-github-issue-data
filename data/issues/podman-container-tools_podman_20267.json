{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 20267,
    "type": "issue",
    "title": "Add support for stdin-once",
    "state": "open",
    "labels": [
        "kind/feature",
        "stale-issue",
        "HTTP API"
    ],
    "user": "lefinal",
    "body": "### Feature request description\n\nThis feature request comes from working with the Docker Go SDK that allows setting `StdinOnce` when creating a container. This closes stdin for the container after the first attached client (to stdin) disconnects.\nPodman currently does not support this, so applications that read from an input stream (in this case stdin) will hang until stopped manually.\n\n### Suggest potential solution\n\nAdd an stdin-once flag for container creation. If set, stdin to the container is closed after the first attached client (to stdin) disconnects.\n\n### Have you considered any alternatives?\n\n_No response_\n\n### Additional context\n\nThe struct in Docker Go SDK:\nhttps://pkg.go.dev/github.com/docker/docker@v24.0.6+incompatible/api/types/container#Config\n\nAlready existing annotation for container inspect details in Podman Go bindings:\nhttps://github.com/containers/podman/blob/46ca057f6baab1525cc5b2f9b010a26ec4715d46/libpod/define/container_inspect.go#L35",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Interested in opening a PR?"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "@Luap99 PTAL"
        },
        {
            "user": "lefinal",
            "body": "Sorry for the late reply. I tried to look into it but I am having a hard time understanding the codebase. From what I got, it seems like conmon does not have support for this. I already opened a discussion at https://github.com/containers/conmon/discussions/459 but got no response, yet."
        },
        {
            "user": "Luap99",
            "body": "A command like `podman-remote run --rm -i alpine cat - <<<\"test\"` works so this should technically already work without such parameter. `cat -` wouldn't exit if the stdin is not closed."
        },
        {
            "user": "Luap99",
            "body": "This was the fix for it it: https://github.com/containers/podman/commit/fbce7584d786ed99354a4b33a9c127abd673c3bb\nSo as long a your client closes the writing side of the socket it should behave correctly.\n\nThat of course does not need to mean that we shouldn't support this API parameter."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ],
    "cross_references": []
}