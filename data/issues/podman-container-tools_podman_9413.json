{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 9413,
    "type": "issue",
    "title": "system reset doesn't work after user has been recreated",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "quantum77",
    "body": "\n/kind bug\n\n**Description**\nWhen creating a new user and deleting an old user podman no longer works.\n$ podman images \nError: error creating tmpdir: mkdir /run/user/1000: permission denied\n\n$ podman system reset\nError: error creating tmpdir: mkdir /run/user/1000: permission denied\n\nWhat does resolve the error:\n$ rm -rf ~/.local/share/containers\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Core of the issue seems to be that `system reset` requires Podman to successfully create the libpod Runtime before `system reset` will run. If you encounter an issue where the runtime cannot be created, e.g. a path issue like this, `podman system reset` cannot run, so the user can't reset their storage and fix the bug."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "This looks like XDG_RUNTIME_DIR was not set correctly.  How do you get into this condition?"
        },
        {
            "user": "mheon",
            "body": "@rhatdan The paths cached in the database no longer match system configuration. `XDG_RUNTIME_DIR` does not come into this.\n\nThe solution will require a significant rewrite to `system reset`, which will have to be rewritten to be part of runtime init so we can avoid errors of this type."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "vrothberg",
            "body": "> How do you get into this condition?\n\nI wonder as well."
        },
        {
            "user": "vrothberg",
            "body": "Closing in favor of https://github.com/containers/podman/issues/11377 which has some reproducer"
        }
    ],
    "cross_references": []
}