{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 777,
    "type": "pull_request",
    "title": "Makefile: Use ?= for shell variables (ISODATE, etc.)",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "wking",
    "body": "Builds on #776; review that first.\n\nPreviously, Make would execute these shell commands even if we didn't need the resulting variable.  With [`?=`'s recursive expansion][1], we only expand the variable when it's consumed.  For example, the `ISODATE` variable is only needed in the recipe for the `changelog` target, so most Make invocations won't need the value, and the computation is just making whatever Make actually is doing slower.\n\nI've shifted the `GIT_COMMIT` and `BUILD_INFO` values over to `LDFLAGS_PODMAN`, because the `test/*/*` targets don't care about those.  I've also moved the Go-specific `-ldflags` from the variables into the recipes themselves, because callers probably expect C semantics for `LDFLAGS` and not Go's wrapper.  That means that there's no longer a need for the `LDFLAGS`/`BASE_LDFLAGS` separation, so I'm just using `LDFLAGS` (and `LDFLAGS_PODMAN`) now.\n\n[1]: https://www.gnu.org/software/make/manual/html_node/Setting.html",
    "comments": [
        {
            "user": "mheon",
            "body": "LGTM once #776 merges"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":umbrella: The latest upstream changes (presumably 5b2627d) made this pull request unmergeable. Please resolve the merge conflicts."
        },
        {
            "user": "mheon",
            "body": "#776 is in, this should be good to merge after a rebase to fix conflicts"
        },
        {
            "user": "wking",
            "body": "Rebased onto master with 8e2a911 -> a425c8d, which also shifts the `LDFLAGS_PODMAN` declaration up closer to the `GIT_COMMIT` and `BUILD_INFO` declarations."
        },
        {
            "user": "rhatdan",
            "body": "@rh-atomic-bot r+"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":pushpin: Commit a425c8d has been approved by `rhatdan`\n\n<!-- @rh-atomic-bot r=rhatdan a425c8d419d031b20388f2c800e4ebf2eb53a096 -->"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":zap: Test exempted: pull fully rebased and already tested."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 813
        }
    ]
}