{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 18768,
    "title": "e2e: \"HTTP if one registry\" test looks like a NOP",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "The \"podman search doesn't attempt HTTP if one registry is not listed as insecure\" test looks like a NOP, it always spits out:\n```\npodman [options] search my-alpine\n           Error: 2 errors occurred:\n           \t* couldn't search registry \"localhost:33859\": pinging container registry localhost:33859: Get \"http://localhost:33859/v2/\": dial tcp [::1]:33859: connect: connection refused\n           \t* couldn't search registry \"localhost:6000\": pinging container registry localhost:6000: Get \"https://localhost:6000/v2/\": dial tcp [::1]:6000: connect: connection refused\n```\nThe first number, 33859, is a random port **but we never start a registry on that port**\n\nThe second number, 6000, is a constant, and ditto.\n\nThe test passes because it simply looks for the presence of the substring \"error\".  I suspect the intention was to check for something like \"server gave HTTP response to HTTPS client\", but this code has too many levels of indirection for me to be able to understand. Someone more familiar with this test file needs to figure it out.",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "edsantiago",
            "body": "Going once, going twice. Since nobody has stepped up to fix this test, I am removing it. #19495."
        }
    ]
}