{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2510,
    "title": "podman: tries to create directories in /var while being rootless ",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "tobwen",
    "body": "/kind bug\n\n**Description**\nWhen trying to run _podman_ rootless with `--log-level=debug` this message occures. \n\n```\nDEBU[0000] Error creating parent directories for blob-info-cache-v1.boltdb, using a memory-only cache: mkdir /var/lib/containers/cache: permission denied\n```\n\n**Expected**\n_podman_ shouldn't try to write to `/var/lib/containers/cache` when it's in rootless mode.\n\nMight be connected with https://github.com/containers/libpod/issues/2508",
    "comments": [
        {
            "user": "mheon",
            "body": "It seems like some paths might be set incorrectly - we should be using paths exclusively under `/home` with the default configuration."
        },
        {
            "user": "mheon",
            "body": "Possibly related to #2508 - both of them seem to be failing to detect that we're rootless and adjust paths to appropriate defaults"
        },
        {
            "user": "giuseppe",
            "body": "PR here: https://github.com/containers/libpod/pull/2537"
        },
        {
            "user": "tobwen",
            "body": "It's alive again on rootless.\n\n```\n$ podman --version\npodman version 1.3.0-dev\n\n$ podman ...\n[...]\nDEBU[0000] Error creating parent directories for blob-info-cache-v1.boltdb, using a memory-only cache: mkdir /var/lib/containers: permission denied\n[...]\n\n$ ls -al /var/lib/containers\nls: cannot access '/var/lib/containers': No such file or directory\n```\n\n\n"
        },
        {
            "user": "rhatdan",
            "body": "@giuseppe Anything to do with your rootless merge?"
        },
        {
            "user": "mheon",
            "body": "I would bet this was the big rootless single userns patch that just merged."
        },
        {
            "user": "giuseppe",
            "body": "this is related to a change in containers/image where we use `os.Geteuid()` inside of a user namespace"
        },
        {
            "user": "giuseppe",
            "body": "PR: https://github.com/containers/image/pull/612"
        },
        {
            "user": "TomasTomecek",
            "body": "also got bitten by this; will we get a new release soon?"
        }
    ]
}