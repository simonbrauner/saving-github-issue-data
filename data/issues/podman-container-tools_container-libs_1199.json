{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 1199,
    "type": "pull_request",
    "title": "libnetwork/etchosts: fix host IP resolution in WSL mirrored networking mode",
    "state": "open",
    "labels": [
        "common"
    ],
    "user": "gabrielima7",
    "body": "Fixes host.containers.internal resolution in WSL2 with mirrored networking mode (networkingMode=Mirrored).\n\n### Problem\nIn WSL2 with mirrored networking mode, Linux shares network interfaces and IPs with the Windows host.\nThe default route gateway (r.Gw) points to the external LAN router (e.g. 192.168.5.1), not the Windows host.\nResolving host.containers.internal to r.Gw sends container traffic to the external router interface rather than the Windows host, causing subsequent Windows bind() calls on the same port to fail with WSAEADDRINUSE / WinError 10013.\n\n### Solution\n1. Detect mirrored networking mode via wslinfo --networking-mode (checking PATH and fallback /usr/sbin/wslinfo).\n2. When mirrored mode is detected, ask the kernel routing table (netlink.RouteGet) for the preferred source IP (routes[0].Src) used to reach the default gateway, which corresponds to the Windows host's mirrored IP.\n3. Fall back to the first global unicast IPv4 address assigned to the default route's network interface.\n4. If no valid host IP can be resolved in mirrored mode, return empty string rather than the router gateway to avoid port conflicts.\n5. Preserve default gateway resolution in WSL NAT mode.",
    "comments": [
        {
            "user": "mtrmac",
            "body": "@l0rd PTAL"
        },
        {
            "user": "gabrielima7",
            "body": "Thanks @l0rd! I addressed the cleanup comments and tested the fix on the affected setup.\n\nI couldn't find official WSL documentation explicitly stating that RouteGet(defaultGateway).Src is the host IP, so this part is based on mirrored networking behavior and was validated experimentally.\n\nIn my environment:\nip route get 192.168.5.1\n192.168.5.1 dev eth0 src 192.168.5.101\n\n192.168.5.101 is the Windows host IP, while 192.168.5.1 is the upstream gateway.\n\nI also tested the real container > Windows path:\npodman run --rm --network podman alpine \\\n  wget -S -T 3 http://host.containers.internal:58123\n\nwhich resolved to 192.168.5.101 and returned HTTP/1.0 200 OK.\n\nI reproduced the #29778 ordering: container traffic first, then Windows bind() on the same port. The bind succeeded and the container received the HTTP response.\n\nUnit tests and go vet also pass. The full podman machine start lifecycle was blocked by an unrelated Docker named-pipe conflict on the host."
        }
    ],
    "cross_references": []
}