{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28562,
    "title": "Include annotations in filters",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-issue",
        "volunteers-wanted",
        "Contribfest"
    ],
    "user": "mildred",
    "body": "### Feature request description\n\nI'd like to have the `--filter` option to include annotations.\n\nThis could be used for #23207, the podman-restart.service could filter out containers with annotations that excludes the container from the restart:\n\n    /usr/bin/podman $LOGGING start --all --filter restart-policy=always --filter annotation!=io.container.no-restart\n\n### Suggest potential solution\n\nFilters by annotations should be made possible the same way filtering is done by labels. Both positive and negative filters.\n\n### Have you considered any alternatives?\n\nFiltering by label is an alternative but probably less suited for the problem\n\n### Additional context\n\n_No response_",
    "comments": [
        {
            "user": "Honny1",
            "body": "The filter idea sounds good, but regarding the restarting issue, are you using the latest version of Podman? It uses a different approach now, and `--filter restart-policy=always` is no longer used. Instead, it uses `--filter should-start-on-boot=true`."
        },
        {
            "user": "Honny1",
            "body": "Podman should now match Docker's behavior regarding restart policies."
        },
        {
            "user": "jiwahn",
            "body": "/assign"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ]
}