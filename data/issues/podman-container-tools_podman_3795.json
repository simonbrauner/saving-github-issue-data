{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3795,
    "title": "regression: podman run: exit status 0 after 'rm --force`",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "```\n# (podman run --name foo alpine sleep 30;echo $?)&\n[1] 13212\n# podman rm --force foo\n[pause 10 seconds]\n57c5063848af9f40b436d224ea2ef5808c5db591b6cd1aa0addb3be7e3eddf34\n# 0   <--- this is the exit status. It should be 137\n```\npodman-1.5.0-2.fc30.x86_64",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Why should it be 137?  Didn't `podman rm --force foo` work?"
        },
        {
            "user": "mheon",
            "body": "When trying to debug this, I found that events is acting properly strange.\n`podman events --since 1h --until 1s` on 1.4.4 exits instantly, shows me the events in question. In 1.5.0, it seems to hang."
        },
        {
            "user": "edsantiago",
            "body": "`podman rm` worked. The exit status pointed to by the arrow is that of the container itself, which was killed, therefore its exit status should be 137. Another way to say this:\n\nIn window 1:\n```\n# podman run --name foo alpine sleep 30\n```\nIn window 2:\n```\n# podman rm -f foo\n```\nThen, after the 10-second wait, go back to window 1:\n```\n# echo $?\n0\n```\nSorry for not being clear."
        },
        {
            "user": "edsantiago",
            "body": "Side note: `podman kill foo` and `podman stop foo` both result in a container exit status of 137, as expected. It's only `rm -f` that is weird."
        },
        {
            "user": "edsantiago",
            "body": "@mheon I cannot reproduce that behavior. I tried with a running container, without; also with running container and running `podman rm -f foo` during the 10-second wait. The `podman events` command in question always dumps a bunch of lines, then exits 0."
        },
        {
            "user": "mheon",
            "body": "It's journald. We're scanning every single journal entry on the system. It's taking 30 seconds to complete on my desktop."
        },
        {
            "user": "mheon",
            "body": "@baude We're probably going to need a revert of https://github.com/containers/libpod/pull/3745 - it nuked performance of events on systems with long journals."
        },
        {
            "user": "rhatdan",
            "body": "This seems like something we should fix before releasing to Fedora."
        },
        {
            "user": "mheon",
            "body": "I think I got the core issue here - problem was that we were testing with file backend and not journald. Might want to look into that later.\n\nThe events performance thing is separate, and probably does require a revert."
        },
        {
            "user": "edsantiago",
            "body": "Oh, *yuk*:\n```\n# podman run --rm alpine sh -c /bin/false;echo $?\n0    <--- should be 1\n```\nWithout `--rm`, works as expected (1). Without `sh -c`, also works as expected. \n\nThis may merit a separate issue but I've got a gut sense that they're related. LMK."
        },
        {
            "user": "mheon",
            "body": "Same issue. Fix in progress."
        },
        {
            "user": "mheon",
            "body": "#3797 to fix both issues"
        }
    ]
}