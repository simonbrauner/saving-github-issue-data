{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 760,
    "type": "pull_request",
    "title": "Support running tests from c/image",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "mtrmac",
    "body": "See https://github.com/containers/skopeo/pull/760#issuecomment-557286929 below.",
    "comments": [
        {
            "user": "mtrmac",
            "body": "For this PR to work, _both_ the tests in here and https://github.com/containers/image/pull/744 need to succeed, which is not currently the case."
        },
        {
            "user": "mtrmac",
            "body": "Yay, success!\n\nThis primarily modifies the test suite so that it can again succeed when run from c/image’s `make test-skopeo`. See individual commit messages for details.\n\nThe two major elements are:\n- Use `-mod=vendor` everywhere, so that a vendored-in copy of c/image under test is visible to Go 1.13.\n- Remove `--net=host` from the container running `make test-system`; it nowadays breaks localhost accesses (e.g. `ping 127.0.0.1` fails). I have no idea _why_ it breaks networking, but arguably `--net=host` was never necessary and having separate namespaces is cleaner anyway, and we don’t quite have the luxury to stop c/image development just to diagnose this right now.\n\nhttps://github.com/containers/image/pull/744 , with\n> ```\n> SKOPEO_REPO = mtrmac/skopeo\n> SKOPEO_BRANCH = test-failures\n> ```\n(which will be momentarily removed) succeeds.\n\n@vrothberg PTAL."
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "image",
            "number": 744
        }
    ]
}