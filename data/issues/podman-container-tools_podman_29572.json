{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29572,
    "type": "pull_request",
    "title": "compat/secrets: do not expose secret data on inspect endpoint",
    "state": "closed",
    "labels": [
        "kind/api-change"
    ],
    "user": "dexp007",
    "body": "The Docker-compat secret inspect endpoint was incorrectly honoring the\nShowSecret query parameter, which could expose plaintext secret data\nto clients using the Docker-compatible API.\n\nThis fix restricts ShowSecret to libpod-only requests. Docker-compat\nclients always receive redacted secret data, matching Docker's own behavior.\n\nFixes: #29570\n",
    "comments": [
        {
            "user": "dexp007",
            "body": "@Honny1 @Luap99 I don’t have permission to re-run CI for this PR, so could one of you please re-run:\nci / sys local rootless fedora-prior / lima\nthe only failing test seems to be 012-manifest.bats:\npodman images - bare manifest list\nthat path is unrelated to the compat/secrets change. everything else in the job passed, and the APIv2 checks are green, so this looks like a flaky failure on the CI side.\nThanks!"
        },
        {
            "user": "Honny1",
            "body": "Sure, rerun the failed test."
        },
        {
            "user": "dexp007",
            "body": "Thanks for the quick reviews and the re-run @Honny1 @Luap99 🙏 Glad we got the compat/secrets fix in"
        }
    ],
    "cross_references": []
}