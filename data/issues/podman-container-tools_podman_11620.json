{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 11620,
    "type": "pull_request",
    "title": "[CI:DOCS] markdown cleanup",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "* podman-inspect: make references be live links, not a static\n  list. Also, remove container- and image-inspect, because\n  those are NOPs.\n\n* podman-pull: add a missing right-paren\n\n* podman-search, podman: remove unwanted indentation from\n  some file descriptions. Markdown indentation renders as\n  one very very long line, requiring the user to use a\n  horizontal scroll bar to read the text. I searched\n  using grep '^    ' and eyeball-looking for text that\n  doesn't look like one-line code examples, and see\n  no more, but eyeball checks are fragile.\n\nOne bug remains: MyST renders mailto: links uglily. I can find\nno way to fix this other than patching the source code.\n\nSigned-off-by: Ed Santiago <santiago@redhat.com>\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/11620#\" title=\"Author self-approved\">edsantiago</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [edsantiago]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "Luap99",
            "body": "I think `podman container inspect` and `podman image inspect` should get their own man page. They have different options:\n```\npodman image inspect --help\nDisplay the configuration of an image\n\nDescription:\n  Displays the low-level information of an image identified by name or ID.\n\nUsage:\n  podman image inspect [options] IMAGE [IMAGE...]\n\nExamples:\n  podman inspect alpine\n  podman inspect --format \"imageId: {{.Id}} size: {{.Size}}\" alpine\n  podman inspect --format \"image: {{.ImageName}} driver: {{.Driver}}\" myctr\n\nOptions:\n  -f, --format string   Format the output to a Go template or json (default \"json\")\n```\n```\npodman container inspect --help\nDisplay the configuration of a container\n\nDescription:\n  Displays the low-level information on a container identified by name or ID.\n\nUsage:\n  podman container inspect [options] CONTAINER [CONTAINER...]\n\nExamples:\n  podman container inspect myCtr\n  podman container inspect -l --format '{{.Id}} {{.Config.Labels}}'\n\nOptions:\n  -f, --format string   Format the output to a Go template or json (default \"json\")\n  -l, --latest          Act on the latest container podman is aware of\n                        Not supported with the \"--remote\" flag\n  -s, --size            Display total file size\n```\nBut this doesn't need to happen in this PR."
        },
        {
            "user": "Luap99",
            "body": "I also looked at the \"mailto\" thing, but I cannot find a reference to it in the docs, neither in myst-parser nor in sphinx."
        },
        {
            "user": "rhatdan",
            "body": "I agree on the inspect man pages we need to create new ones.\n/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 11660
        }
    ]
}