{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 16430,
    "type": "pull_request",
    "title": "[skip ci] Formalize our compare-against-docker mechanism",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note-none",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "Long-term followup to #14917. This adds a new one-off script, to be run periodically, which runs our man-page crossref against docker, highlighting commands and options that docker lists in its --help but we don't list in our man pages.\n\nSigned-off-by: Ed Santiago <santiago@redhat.com>\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/16430#\" title=\"Author self-approved\">edsantiago</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [edsantiago]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "edsantiago",
            "body": "Suggestion: rather than keep updating 14917, let's just close it. Once this PR merges (with a complete list of hidden options), create a new issue."
        },
        {
            "user": "edsantiago",
            "body": "Marking as draft until someone gives me the exact list of hidden options"
        },
        {
            "user": "edsantiago",
            "body": "Okay, I added `podman manifest push --insecure` and all `--runtime` options that leaves this list, which still seems a little excessive?\n\n- [ ] docker --tls\n- [ ] docker --tlscacert\n- [ ] docker --tlscert\n- [ ] docker --tlskey\n- [ ] docker --tlsverify\n- [ ] docker -l\n- [ ] docker builder\n- [ ] docker config\n- [ ] docker container cp --follow-link\n- [ ] docker container cp -L\n- [ ] docker container create --domainname\n- [ ] docker container create --isolation\n- [ ] docker container create --kernel-memory\n- [ ] docker container create --link\n- [ ] docker container create --link-local-ip\n- [ ] docker container create --storage-opt\n- [ ] docker container create --volume-driver\n- [ ] docker container logs --details\n- [ ] docker container ls\n- [ ] docker container rm --link\n- [ ] docker container run --domainname\n- [ ] docker container run --gpus\n- [ ] docker container run --isolation\n- [ ] docker container run --kernel-memory\n- [ ] docker container run --link\n- [ ] docker container run --link-local-ip\n- [ ] docker container run --storage-opt\n- [ ] docker container run --volume-driver\n- [ ] docker container update --kernel-memory\n- [ ] docker container update --pids-limit\n- [ ] docker container update --restart\n- [ ] docker context\n- [ ] docker cp --follow-link\n- [ ] docker cp -L\n- [ ] docker create --domainname\n- [ ] docker create --isolation\n- [ ] docker create --kernel-memory\n- [ ] docker create --link\n- [ ] docker create --link-local-ip\n- [ ] docker create --storage-opt\n- [ ] docker create --volume-driver\n- [ ] docker image import --platform\n- [ ] docker image ls\n- [ ] docker image push --all-tags\n- [ ] docker image push -a\n- [ ] docker import --platform\n- [ ] docker logs --details\n- [ ] docker manifest inspect --insecure\n- [ ] docker manifest inspect --verbose\n- [ ] docker manifest inspect -v\n- [ ] docker manifest push --purge\n- [ ] docker manifest push -p\n- [ ] docker network connect --driver-opt\n- [ ] docker network connect --link\n- [ ] docker network connect --link-local-ip\n- [ ] docker network create --attachable\n- [ ] docker network create --aux-address\n- [ ] docker network create --config-from\n- [ ] docker network create --config-only\n- [ ] docker network create --ingress\n- [ ] docker network create --ipam-opt\n- [ ] docker network create --scope\n- [ ] docker network inspect --verbose\n- [ ] docker network inspect -v\n- [ ] docker push --all-tags\n- [ ] docker push -a\n- [ ] docker rm --link\n- [ ] docker run --domainname\n- [ ] docker run --isolation\n- [ ] docker run --link\n- [ ] docker run --link-local-ip\n- [ ] docker run --storage-opt\n- [ ] docker run --volume-driver\n- [ ] docker secret create --template-driver\n- [ ] docker service\n- [ ] docker stack\n- [ ] docker system events\n- [ ] docker trust\n- [ ] docker update --kernel-memory\n- [ ] docker update --pids-limit\n- [ ] docker update --restart\n- [ ] docker version --kubeconfig\n- [ ] docker volume create -d\n"
        },
        {
            "user": "mheon",
            "body": "```\ndocker container run --gpus\ndocker container run --isolation\ndocker container run --kernel-memory\ndocker container run --link\n```\nAll of these (plus their `container create` counterparts) are known. Isolation is for Windows containers only, kernel-memory is gone from CGroups v2 and as such is hidden in Podman, `gpus` and `link` are not implemented and not likely to be (we don't require Docker's OCI runtime hacks for GPUs, and `--link` is deprecated in Docker and we have no intention of duplicating)."
        },
        {
            "user": "mheon",
            "body": "` docker context` is available in Podman but hidden."
        },
        {
            "user": "mheon",
            "body": "```\ndocker service\ndocker stack\n```\n\nThese are both swarm commands, no intention of adding"
        },
        {
            "user": "edsantiago",
            "body": "Thanks. Re-pushed with your additions, and some fixes (anchors, so `--link` doesn't hide `--link-local-ip`), refactoring, comments. New output is:\n\n- [ ] docker --tls\n- [ ] docker --tlscacert\n- [ ] docker --tlscert\n- [ ] docker --tlskey\n- [ ] docker --tlsverify\n- [ ] docker -l # Can't do, since cobra would have a hard time with this overriding `podman stop -l` type commands\n- [x] docker builder #Hidden\n- [x] docker config #Hidden\n- [ ] docker container cp --follow-link https://github.com/containers/podman/pull/15730\n- [ ] docker container cp -L https://github.com/containers/podman/pull/15730\n- [ ] docker container create --domainname https://github.com/containers/podman/pull/15200\n- [ ] docker container create --link-local-ip\n- [x] docker container create --storage-opt # this is a legal command, just we document under podman command.\n- [ ] docker container create --volume-driver\n- [x] docker container logs --details #hidden\n- [ ] docker container run --domainname https://github.com/containers/podman/pull/15200\n- [ ] docker container run --link-local-ip\n- [x] docker container run --storage-opt # this is a legal command, just we document under podman command.\n- [ ] docker container run --volume-driver\n- [ ] docker container update --pids-limit\n- [ ] docker container update --restart\n- [ ] docker cp --follow-link https://github.com/containers/podman/pull/15730\n- [ ] docker cp -L https://github.com/containers/podman/pull/15730\n- [ ] docker create --domainname https://github.com/containers/podman/pull/15200\n- [ ] docker create --link-local-ip\n- [x] docker create --storage-opt  # this is a legal command, just we document under podman command.\n- [ ] docker create --volume-driver\n- [ ] docker image import --platform\n- [x] docker image ls # this alias exists, you can do `podman image ls`\n- [ ] docker image push --all-tags\n- [ ] docker image push -a\n- [ ] docker import --platform\n- [x] docker logs --details #hidden\n- [x] docker manifest inspect --insecure #hidden\n- [x] docker manifest inspect --verbose #hidden\n- [ ] docker manifest inspect -v https://github.com/containers/podman/pull/16462\n- [x] docker manifest push --purge #hidden\n- [x] docker manifest push -p https://github.com/containers/podman/pull/16466\n- [ ] docker network connect --driver-opt\n- [ ] docker network connect --link-local-ip\n- [ ] docker network create --attachable\n- [ ] docker network create --aux-address\n- [ ] docker network create --config-from\n- [ ] docker network create --config-only\n- [ ] docker network create --ingress\n- [ ] docker network create --ipam-opt\n- [ ] docker network create --scope\n- [ ] docker network inspect --verbose\n- [ ] docker network inspect -v\n- [ ] docker push --all-tags\n- [ ] docker push -a\n- [ ] docker run --domainname https://github.com/containers/podman/pull/15200\n- [ ] docker run --link-local-ip \n- [x] docker run --storage-opt # this is a legal command, just we document under podman command.\n- [ ] docker run --volume-driver\n- [ ] docker secret create --template-driver\n- [ ] docker system events https://github.com/containers/podman/pull/16464\n- [ ] docker trust https://github.com/containers/podman/pull/16463\n- [ ] docker update --pids-limit\n- [ ] docker update --restart\n- [ ] docker version --kubeconfig\n- [x] docker volume create -d https://github.com/containers/podman/pull/16467\n\n...and I've removed Draft status. Once this merges I will want to close #14917 and open a new PR, instead of just updating comment-0 in 14917. LMK if that's not OK."
        },
        {
            "user": "rhatdan",
            "body": "@cdoern Is this possible ` docker container update --pids-limit` with current design of update?"
        },
        {
            "user": "rhatdan",
            "body": "@umohnani8 ` docker version --kubeconfig` does this make any sense for us?  IE Do we have a default kubeconfig?"
        },
        {
            "user": "edsantiago",
            "body": "> docker config #Hidden\n\n@rhatdan are you sure about this?\n```console\n$ bin/podman config\nError: unrecognized command `podman config`\nTry 'podman --help' for more information\n```\nI've applied your other deltas and re-pushed. Rather than post a new list, here are the subtractions:\n- [x] docker container create --storage-opt\n- [x] docker container logs --details\n- [x] docker container run --storage-opt\n- [x] docker create --storage-opt\n- [x] docker image ls\n- [x] docker logs --details\n- [x] docker manifest inspect --insecure\n- [x] docker manifest inspect --verbose\n- [x] docker manifest push --purge\n- [x] docker run --storage-opt"
        },
        {
            "user": "mheon",
            "body": "I think we have `podman --config` but no `podman config` yet. Not seen that command before in the wild."
        },
        {
            "user": "rhatdan",
            "body": "Sadly, I think I did the work on podman config, and have lost it, I thought it was merged.  "
        },
        {
            "user": "rhatdan",
            "body": "@edsantiago I think we should keep an up2date list at the top of this issue. Along with my comments to see where we are, only showing the ones that are missing along with the links to any PR that is working to solve them."
        },
        {
            "user": "rhatdan",
            "body": "Actually open an issue that is with the outstanding potential work items.  If we don't plan on implementing items we should update the transfer.md doc with the ones we don't plan to update."
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 16543
        }
    ]
}