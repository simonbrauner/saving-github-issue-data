{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7537,
    "type": "issue",
    "title": "support \"--security-opt systempaths=unconfined\"",
    "state": "closed",
    "labels": [
        "Good First Issue",
        "kind/feature",
        "In Progress",
        "locked - please file new issue/PR"
    ],
    "user": "cyphar",
    "body": "/kind feature\n\n**Description**\n\nRight now you can't disable the `/proc` overmounts. While these are a security feature in general, due to kernel restrictions, this means that you cannot run a subcontainer or other sandbox that tries to mount `procfs` (even if it uses user namespaces). Having a way to optionally disable this would be neat and would also bring more feature parity with Docker (which supports `--security-opt systempaths=unconfined`).\n\nIn [newer kernels](https://lkml.kernel.org/lkml/cover.1597931457.git.gladkov.alexey@gmail.com/) you will be able to work around this with `mount -t procfs -o subset=pids procfs /proc` but right now the only other workaround is to mount *some* `/proc` handle into the container. The safest way is with something like:\n\n```\n% mkdir /tmp/empty-procfs\n% sudo unshare -pf mount -t proc proc /tmp/empty-procfs\n% podman run -v /tmp/empty-procfs:/.stashed-proc # ...\n```\n\nBut most people probably end up doing `/proc:/.stashed-proc` which is quite dangerous. This is also probably needed to completely support `AllowedProcMountTypes = Unmasked`.\n",
    "comments": [
        {
            "user": "mheon",
            "body": "We already support passing options to the `/proc` mount via `--security-opt proc-opts` (added in #7227). It looks like Docker went ahead and added it at some point at well, using a completely incompatible syntax, so now we'll have to support that as well..."
        },
        {
            "user": "cyphar",
            "body": "This is separate to procfs mount options. This is about disabling the `/proc/scsi` et al overmounts (`MaskedPaths`)."
        },
        {
            "user": "mheon",
            "body": "Ah - I see, you're entirely right. This is definitely worth adding."
        },
        {
            "user": "vrothberg",
            "body": "@cyphar, do you have cycles to open a PR?"
        },
        {
            "user": "cyphar",
            "body": "I'll write something up this week."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "@umohnani8 PR to handle --security-opt=mask/unmask should fix this Issue.\n\n@umohnani8 We might want to add a unmask=all flag to remove all masking."
        },
        {
            "user": "umohnani8",
            "body": "Yup, we have `unmask=ALL` as an option as well. The PR is https://github.com/containers/podman/pull/8408"
        },
        {
            "user": "cyphar",
            "body": "Uh this isn't quite fixed because you don't have flag-parity with Docker but I can send a PR to fix that up."
        },
        {
            "user": "rhatdan",
            "body": "SGTM"
        },
        {
            "user": "umohnani8",
            "body": "Opened https://github.com/containers/podman/pull/8630 for the flag-parity"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 8408
        }
    ]
}