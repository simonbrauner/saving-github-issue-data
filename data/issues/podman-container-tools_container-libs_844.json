{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 844,
    "type": "pull_request",
    "title": "seccomp: block AF_ALG sockets",
    "state": "closed",
    "labels": [
        "common"
    ],
    "user": "mtclinton",
    "body": "- Block socket(AF_ALG) in the default profile, mirroring the existing AF_VSOCK deny in eaec878beb\n- AF_ALG is the kernel crypto API interface; rarely needed inside containers; recurring CVE family (most recent CVE-2026-31431)\n- JSON was regenerated via cmd/seccomp/generate.go and go test ./pkg/seccomp/... passes\n- Following @Luap99's reply to a private report on [security@lists.podman.io](mailto:security@lists.podman.io), filed publicly here.",
    "comments": [
        {
            "user": "Luap99",
            "body": "To be clear I asked for an issue to discuss but sure we can discus on PR just as well.\n\nI think blocking this is one socket family would be reasonable but overall I am not sure if this is what we want to do in general. The generic seccomp profile is not there to work around kernel CVEs. The copy fail CVE is fundamentally a bad kernel bug, blocking that for containers sounds useful first but then it does not actual protect the system outside of such container.\n\nThe only way to protect the system would be to patch the kernel, or block AF_ALG system wide not just for containers and then fixing this on our profile seem not so well suited. And then there is the practical concern of us patching + releasing podman will always be slower than a kernel update. Because this is just a json file users who like this can always deploy it locally themselves. \n\nThe seccomp profile denials here should be for what we consider design issues, i.e. AF_VSOCK is not namespaced so it escapes the normal container network namespace.\n\ncc @containers/podman-maintainers "
        },
        {
            "user": "mheon",
            "body": "If we're going to do this, now is the time. This is arguably a breaking change, and our window for doing so - Podman 6 - is closing fast.\n\nI am personally in favor because we're now up to 3 exploits, all closely related, using this vector, so it seems like a pretty significant attack surface with little benefit to retaining"
        },
        {
            "user": "Luap99",
            "body": "\n\n\n\n> If we're going to do this, now is the time. This is arguably a breaking change, and our window for doing so - Podman 6 - is closing fast.\n> \n> I am personally in favor because we're now up to 3 exploits, all closely related, using this vector, so it seems like a pretty significant attack surface with little benefit to retaining\n\nNot sure I follow that thinking, if you say this needs podman 6 bc it is breaking then there is no need to patch this at all IMO. Podman 6 will not be out for most users until many weeks/months/years and by then hopefully all people patched their kernels."
        },
        {
            "user": "giuseppe",
            "body": "Copy Fail is bad, but once the kernel bug is fixed, I don't think `socket(AF_ALG, ...)` is intrinsically unsafe that we need to block it in the default seccomp profile.  It has real benefits, as user space can use the kernel crypto API.\n\n"
        },
        {
            "user": "mtrmac",
            "body": "> The only way to protect the system would be to patch the kernel, or block AF_ALG system wide not just for containers and then fixing this on our profile seem not so well suited.\n\nThere might be systems that have trusted users running untrusted containers, where this does help — but I agree with the larger point that any organization that decides they want an immediate mitigation without updating the kernel will need a mitigation _not limited to containers_.\n\n> And then there is the practical concern of us patching + releasing podman will always be slower than a kernel update. \n\nI think this is the decisive point. Users can be persuaded to update a kernel urgently given known vulnerabilities, but they are not going to rush updating `containers-common` or whatever it is for a non-security update; so, basically, for mitigating _known_ vulnerabilities, there is really no benefit to shipping this seccomp change in some future release.\n\nIt would make sense to do this if the kernel community’s consensus ended up being “this whole feature set is not ready to be exposed to untrusted users” (and I think we did have some subsystems like that), but so far my impression has been that we’re not at that point (yet?)."
        },
        {
            "user": "mtclinton",
            "body": "Closing this...\n\nThanks for the thoughtful review."
        }
    ],
    "cross_references": []
}