{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 4508,
    "title": "configuration option for dns server to use",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-issue",
        "locked - please file new issue/PR"
    ],
    "user": "langdon",
    "body": "/kind feature\nalso kind of a bug\n\n**Description**\nIf you are using 127.0.0.1 in resolv.conf, podman appears to switch to using 8.8.8.8 in the containers it runs. In some networks that dns server may be blocked. You can work around this issue by passing `--dns=<my-dns-server>` but it would be nice to be able to set this system/user -wide. \n\nI propose an entry in to the libpod.conf file(s).  However, a similar issue is fixed via resolv.conf (if I am reading it correctly) here: https://github.com/containers/libpod/issues/3277\n\nDocker/Moby had a similar problem: https://github.com/moby/moby/issues/23910\n\n**Output of `podman version`:**\n\n```\nVersion:            1.6.2\nRemoteAPI Version:  1\nGo Version:         go1.13.1\nOS/Arch:            linux/amd64\n```\n\n**Package info (e.g. output of `rpm -q podman` or `apt list podman`):**\n\n```\npodman-1.6.2-2.fc31.x86_64\n```\n\n",
    "comments": [
        {
            "user": "mheon",
            "body": "@rhatdan @QiWang19 Looks like a good candidate for containers.conf"
        },
        {
            "user": "larsks",
            "body": "Note that this is already possible in Docker, which lets you set the default resolver via `/etc/docker/daemon.json` (or as a command line argument to `dockerd`). I've hit the same problem as langdon and I agree that always having to add `--dns a.b.c.d` to my `podman run` invocations is annoying (particularly when I forget!)."
        },
        {
            "user": "baude",
            "body": "@rhatdan containers.conf ?"
        },
        {
            "user": "rhatdan",
            "body": "Yes @QiWang19 could easily add this option."
        },
        {
            "user": "rhatdan",
            "body": "Begin adding support https://github.com/containers/common/pull/29"
        },
        {
            "user": "ensc",
            "body": "Is this available in 1.7.0?  `containers.conf` seems to be ignored with podman-1.7.0-0.5.rc2.fc31.x86_64"
        },
        {
            "user": "mheon",
            "body": "I don't believe it is - we're expecting initial support to land in 1.8.0. We'll begin gradually deprecating libpod.conf at that time as well."
        },
        {
            "user": "larsks",
            "body": "@gbryant200 you seem to be in the wrong place: this is for bug reports about [podman](https://podman.io/), which is an alternative to Docker. It's not for questions about Docker."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "@whitel The latest containers.conf in github.com/containers/common, has support for the dns server issue.  The PR to get containers.conf support into Podman is very close. One more issue to get it merged."
        },
        {
            "user": "rhatdan",
            "body": "containers.conf support is now in."
        },
        {
            "user": "langdon",
            "body": "\\o/"
        }
    ]
}