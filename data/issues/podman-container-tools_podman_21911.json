{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 21911,
    "title": "Provide instructions for linting on Windows",
    "state": "closed",
    "labels": [
        "kind/feature",
        "windows",
        "locked - please file new issue/PR"
    ],
    "user": "l0rd",
    "body": "### Feature request description\n\n`make lint` or `winmake lint` are both failing on windows currently so contributors don't know how to run the linter against the local changes before pushing a commit.\n\n### Suggest potential solution\n\nIt's possible to install golangci-lint on Windows (`winget add -e  GolangCI.golangci-lint `) but running it complains because [`diff` is not found](https://github.com/golangci/golangci-lint/issues/3408#issuecomment-1439908798). To get `diff` we can then include Git for windows bin folder to `PATH` (`$Env:Path += \";C:\\Program Files\\Git\\usr\\bin\"`) but then some new errors are thrown...it will eventually work but it needs some more time.\n\n### Have you considered any alternatives?\n\nWe could provide instructions (or a new make target) to execute `make lint` in a container using `podman run`.\n\n### Additional context\n\nAdd any other context or screenshots about the feature request here.",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "l0rd",
            "body": "This issue has been addressed by #22913"
        }
    ]
}