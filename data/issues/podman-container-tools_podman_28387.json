{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28387,
    "type": "pull_request",
    "title": "feat: allow dns flags with --network=container",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "tobwen",
    "body": "## Summary\n\nAllow `--dns`/`--dns-search`/`--dns-opt` with `--network=container` by using a per-container `resolv.conf` when joining another container's netns. Update docs and e2e coverage to match.\n\nFixes: #25386\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nAllow --dns/--dns-search/--dns-opt with --network=container by creating a per-container resolv.conf.\n```\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "tobwen",
            "body": "@Luap99 WDYT?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "I'm not sure where, but the tests aren't looking real happy.  I don't think it's a flake."
        },
        {
            "user": "tobwen",
            "body": "> I'm not sure where, but the tests aren't looking real happy. I don't think it's a flake.\n\nActually, I can see one failing check only: https://github.com/containers/podman/pull/28387/checks?check_run_id=68897893783\n\n### Analysis\nWhat I've found: `contrib/packit-tmt/dnf-repo-setup.sh` expects `testing-farm-tag-repository` on the remote-root guest, but that repo doesn’t exist there, so `dnf --disable-repo=testing-farm-tag-repository` fails.\n\n### Relevant logs\n- results.xml: https://artifacts.dev.testing-farm.io/883f7f80-cefb-4032-b10f-336f16e4de68/results.xml\n- Plan log (short): https://artifacts.dev.testing-farm.io/883f7f80-cefb-4032-b10f-336f16e4de68/work-remote-root_s1h78pm/tmt-run.log\n- Verbose log: https://artifacts.dev.testing-farm.io/883f7f80-cefb-4032-b10f-336f16e4de68/work-remote-root_s1h78pm/log.txt\n"
        },
        {
            "user": "tobwen",
            "body": "I added a note to `docs/source/markdown/options/dns.md`:\nWhen `--network=container:<id>` is used together with `--dns`, `--dns-search`, or `--dns-option`, Podman creates a dedicated per-container `/etc/resolv.conf`; without DNS flags it reuses the dependency container's `/etc/resolv.conf`.\n"
        },
        {
            "user": "tobwen",
            "body": "Okay, closed - as always."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 25386
        }
    ]
}