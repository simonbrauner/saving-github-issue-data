{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 3259,
    "type": "issue",
    "title": "`buildah` fails when `REGISTRY_AUTH_FILE` doesn't exist",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "rcny",
    "body": "Hello!\n\nSetting `REGISTRY_AUTH_FILE` to a non-existent file will result in `buildah` failing to operate with public registries:\n\n```\n[root@dd836c415cc3 /]# env REGISTRY_AUTH_FILE=/tmp/non_existent_creds.json buildah pull docker.io/library/hello-world\nerror checking authfile path /tmp/non_existent_creds.json: stat /tmp/non_existent_creds.json: no such file or directory\n```\n\nIs this a feature or a bug?\n\nCurrently I resolve it with e.g. `env REGISTRY_AUTH_FILE= buildah pull docker.io/library/hello-world` for specific commands. Can't remove `REGISTRY_AUTH_FILE` at all because of #3216.\n\nRunning `1.20.1` from current `quay.io/buildah/stable` image.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "I am thinking this is a bug.\n\nWe should only check for the existence of the authfile if the user specifies it."
        },
        {
            "user": "vrothberg",
            "body": "I consider setting the env var as user-specified input (i.e., the counterpart of `--authfile`)."
        },
        {
            "user": "rhatdan",
            "body": "The thing is we do not need to fail until the user actually attempts to use the file, in that case.\n"
        },
        {
            "user": "rhatdan",
            "body": "For example the use case that this user was hitting was having it set within a container image, and only used when needed.  We would fail all the time."
        },
        {
            "user": "TriplEight",
            "body": "I'm having the same issue with `buildah logout`, even when it wasn't logged in anywhere:\n```\nbuildah logout --all\nerror checking authfile path /tmp/registry_auth.json: stat /tmp/registry_auth.json: no such file or directory\n```"
        },
        {
            "user": "TriplEight",
            "body": "and with the workaround I'm getting the other misleading output:\n```\nenv REGISTRY_AUTH_FILE= buildah logout docker.io\nNot logged into docker.io\n```"
        },
        {
            "user": "rhatdan",
            "body": "Why is this misleading?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "I think the logout error is misleading as it doesn't indicate that the file doesn't exist.  From reading the message, I'd think the authfile existed, I just didn't have an entry in the authfile."
        },
        {
            "user": "rhatdan",
            "body": "We treat REGISTRY_AUTH_FILE=\"\" the same as we do not being set at all."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 3260
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 3274
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 3278
        }
    ]
}