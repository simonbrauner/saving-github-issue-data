{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3328,
    "type": "pull_request",
    "title": "When creating exit command, pass storage options on",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "We made changes earlier that empty storage options when setting storage driver explicitly. Unfortunately, this breaks rootless cleanup commands, as they lose the fuse-overlayfs mount program path.\n\nFix this by passing along the storage options to the cleanup process.\n\nAlso, fix --syslog, which was broken a while ago (probably when we broke up main to add main_remote).\n\nFixes #3326",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/3328#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "@edsantiago PTAL. Can't promise this does anything about #3325 but I confirmed it fixes 3326."
        },
        {
            "user": "edsantiago",
            "body": "Good news / bad news. You've fixed the problems when running as nonroot; but those problems (both 3325 and 3326) are now present when run as root. (via sudo, at least)"
        },
        {
            "user": "edsantiago",
            "body": "More news: the (root) hang on `podman logs -f` happens only when `mountopt = \"nodev,metacopy=on\"`. Without `metacopy`, both the container and the logs process exit fine. Have a nice day :-("
        },
        {
            "user": "mheon",
            "body": "So... metacopy is breaking cleanup processes, somehow. That's rather confusing."
        },
        {
            "user": "mheon",
            "body": "```\nbin/podman[29233]: time=\"2019-06-13T17:38:35-04:00\" level=error msg=\"could not get runtime: overlay: Unknown option metacopy\"\n```"
        },
        {
            "user": "mheon",
            "body": "This is rather confusing because I'm pretty sure metacopy is overlay-specific?"
        },
        {
            "user": "edsantiago",
            "body": "I can't reproduce that error message. I've tried using all manner of combinations of `driver` and `mountopt` in `storage.conf`.\n\nTomorrow is another day, and maybe the bits will behave better then."
        },
        {
            "user": "mheon",
            "body": "@edsantiago `podman run -d -t --log-level=debug --syslog fedora sleep 60; podman stop --timeout=0 --latest` and check syslog - you'll see that error out of the podman cleanup process"
        },
        {
            "user": "giuseppe",
            "body": "LGTM"
        },
        {
            "user": "giuseppe",
            "body": "I confirm this fixes https://github.com/containers/libpod/issues/3325"
        },
        {
            "user": "edsantiago",
            "body": "@giuseppe please try it as root, with `driver = \"overlay\"` and `mountopt = \"nodev,metacopy=on\"`"
        },
        {
            "user": "giuseppe",
            "body": "`nodev,metacopy=on` should not be converted to `--storage-opt nodev --storage-opt metacopy=on` but to `--storage-opt $DRIVER.mountopt=nodev,metacopy=on`.  The issue now is that cobra splits the option in `$DRIVER.mountopt=nodev` and `metacopy=on` which is also not correct"
        },
        {
            "user": "giuseppe",
            "body": "with the following patch it seems to work for me:\n\n```\ndiff --git a/cmd/podman/main_local.go b/cmd/podman/main_local.go\nindex b4f21bd0..132f35ab 100644\n--- a/cmd/podman/main_local.go\n+++ b/cmd/podman/main_local.go\n@@ -48,7 +48,7 @@ func init() {\n        rootCmd.PersistentFlags().StringVar(&MainGlobalOpts.Runtime, \"runtime\", \"\", \"Path to the OCI-compatible binary used to run containers, default is /usr/bin/runc\")\n        // -s is depracated due to conflict with -s on subcommands\n        rootCmd.PersistentFlags().StringVar(&MainGlobalOpts.StorageDriver, \"storage-driver\", \"\", \"Select which storage driver is used to manage storage of images and containers (default is overlay)\")\n-       rootCmd.PersistentFlags().StringSliceVar(&MainGlobalOpts.StorageOpts, \"storage-opt\", []string{}, \"Used to pass an option to the storage driver\")\n+       rootCmd.PersistentFlags().StringArrayVar(&MainGlobalOpts.StorageOpts, \"storage-opt\", []string{}, \"Used to pass an option to the storage driver\")\n        rootCmd.PersistentFlags().BoolVar(&MainGlobalOpts.Syslog, \"syslog\", false, \"Output logging information to syslog as well as the console\")\n \n        rootCmd.PersistentFlags().StringVar(&MainGlobalOpts.TmpDir, \"tmpdir\", \"\", \"Path to the tmp directory\")\n```"
        },
        {
            "user": "mheon",
            "body": "Repushed with your patch @giuseppe - thanks"
        },
        {
            "user": "mheon",
            "body": "podman-in-podman seems like it might be failing consistently on the runlabel global options test...\n\nGoing to try again. If we get another failure I'll fix it."
        },
        {
            "user": "mheon",
            "body": "Alright, I got a theory. Pushed a commit. Let's see..."
        },
        {
            "user": "mheon",
            "body": "Alright, that wasn't it..."
        },
        {
            "user": "mheon",
            "body": "Alright, I have no idea what's going on with the test failure here. No clues in the logs and I can't reproduce...\n\nProbably going to just skip (for podman in podman only, if possible)"
        },
        {
            "user": "mheon",
            "body": "@haircommander @rhatdan @jwhonce Can I get a `/lgtm` and `/hold` on this one, so we can merge and start the release?"
        },
        {
            "user": "jwhonce",
            "body": "/hold\n/lgtm\n"
        },
        {
            "user": "mheon",
            "body": "It's ready. Finally.\n/hold cancel"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 3325
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 3338
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 3278
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 2947
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12436
        }
    ]
}