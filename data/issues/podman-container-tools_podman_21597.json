{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 21597,
    "type": "pull_request",
    "title": "Complete WSL implementation in Podman 5",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "machine",
        "release-note-none",
        "locked - please file new issue/PR"
    ],
    "user": "n1hility",
    "body": " Complete WSL implementation, refactor a few areas\n    \n Also addresses a number of issues:\n    - StopHostNetworking isn't plumbed, win-sshproxy leaks on hyperv\n    - Wait api and print output doesn't work properly on Windows\n    - API forwarding doesn't work on WSL\n    - Terminal corruption with after start/stop on Windows\n    - Gvproxy is forcefully killed vs gracefully quit\n    - Switching rootful/rootless does not update /var/run/docker.sock on the guest\n    - File already closed error on init\n    - HyperV backend is publishing Unix sockets when it should be named pipes\n    - User-mode networking doesn't always work\n    - Stop state outside of lock boundaries\n    - WSL blocks parallel machined (should be supported)\n    \n```release-note\nnone\n```\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/21597#\" title=\"Author self-approved\">n1hility</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [n1hility]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "n1hility",
            "body": "/hold"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "baude",
            "body": "the stop failure is legit problem ..."
        },
        {
            "user": "baude",
            "body": "@n1hility this patch resolves the test problem.  My only pause is I am not dead sure if I am treating a symptom instead of fixing the real problem.  But on that front, I do not see something like a missed .Close() ... maybe take a quick peek before applying my patch.  The patch also explains whats going on where the cause/effect? is ... maybe someone else will see it too.\n\n```\ndiff --git a/pkg/machine/qemu/machine.go b/pkg/machine/qemu/machine.go\nindex 1bcb46ce46..65040ed457 100644\n--- a/pkg/machine/qemu/machine.go\n+++ b/pkg/machine/qemu/machine.go\n@@ -6,6 +6,7 @@ import (\n        \"encoding/json\"\n        \"errors\"\n        \"fmt\"\n+       \"io\"\n        \"io/fs\"\n        \"os\"\n        \"os/exec\"\n@@ -266,6 +267,21 @@ func (q *QEMUStubber) State(mc *vmconfigs.MachineConfig, bypass bool) (define.St\n                return \"\", err\n        }\n        if err := monitor.Connect(); err != nil {\n+               // There is a case where if we stop the same vm (from running) two\n+               // consecutive times we can get an econnreset when trying to get the\n+               // state\n+               if errors.Is(err, syscall.ECONNRESET) {\n+                       // try again\n+                       logrus.Debug(\"received ECCONNRESET from QEMU monitor; trying again\")\n+                       secondTry := monitor.Connect()\n+                       if errors.Is(secondTry, io.EOF) {\n+                               return define.Stopped, nil\n+                       }\n+                       if secondTry != nil {\n+                               logrus.Debugf(\"second attempt to connect to QEMU monitor failed\")\n+                               return \"\", secondTry\n+                       }\n+               }\n                return \"\", err\n        }\n        defer func() {\n```"
        },
        {
            "user": "n1hility",
            "body": "@baude thanks! I  applied your patch and rebased again. i mentioned in chat but just in case you miss it that i noticed some underlying raciness in qemu start/stop that we can fixup later (e.g. the logic has the netdev sock setup overwriting the qmp monitor sock which confuse client connection code)"
        },
        {
            "user": "baude",
            "body": "LGTM"
        },
        {
            "user": "arixmkii",
            "body": "@n1hility regarding this comment https://github.com/containers/podman/pull/21597#issuecomment-1937839956 Why don't just eliminate the root cause and don't reuse QMP for VLAN in QEMU. It is what I proposed in https://github.com/containers/podman/pull/21594 with a bigger change. But this could be also achieved w/o removing this FD. Just use dedicated socket for the purpose."
        },
        {
            "user": "ashley-cui",
            "body": "LGTM other than the prev reviews. "
        },
        {
            "user": "n1hility",
            "body": "> @n1hility regarding this comment [#21597 (comment)](https://github.com/containers/podman/pull/21597#issuecomment-1937839956) Why don't just eliminate the root cause and don't reuse QMP for VLAN in QEMU. It is what I proposed in #21594 with a bigger change. But this could be also achieved w/o removing this FD. Just use dedicated socket for the purpose.\n\nI totally agree we need to fix this, and soon, but I also don't want this PR to grow beyond what it is (already pretty huge) and its holding up WSL working in 5. That and other stuff can come in other commits."
        },
        {
            "user": "n1hility",
            "body": "I added the small comment review charge in a separate commit to address the review, yet still show the record of CI passing on the earlier commits."
        },
        {
            "user": "baude",
            "body": "i think there is consensus to get this PR in after @n1hility addressed the first round of comments ... \n\n/hold\n/lgtm\n"
        },
        {
            "user": "baude",
            "body": "/hold cancel"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 21562
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 21573
        }
    ]
}