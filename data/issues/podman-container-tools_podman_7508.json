{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7508,
    "title": "broken dependencies on debian 10",
    "state": "closed",
    "labels": [
        "kind/bug",
        "Packaging",
        "locked - please file new issue/PR"
    ],
    "user": "x70b1",
    "body": "/kind bug\n\n**Description**\n\nAt the moment the dependencies for podman 2.0.5 on debian are broken.\n\n\n**Steps to reproduce the issue:**\n\n1. use sources from the [docs](https://podman.io/getting-started/installation.html)\n\n2. `sudo apt-get -qq -y install podman`\n\n3. Error: `E: Unable to correct problems, you have held broken packages.`\n\n\n**Describe the results you received:**\n\nThe core problem seems to be `libseccomp2`.\n\n`podman` depends on `cri-o-runc` package which wants `libseccomp2`.\n\n```\nThe following packages have unmet dependencies:\n cri-o-runc : Depends: libseccomp2 (>= 2.4.1) but 2.3.3-4 is to be installed\n```\n\n\n**Describe the results you expected:**\n\nI am should be able to install podman.\n",
    "comments": [
        {
            "user": "stephankoelle",
            "body": "Same issue here, with update to dated debian 10.\nI have tried to exclude cri-o-runc, I think it is not needed, podman uses crun by default?\n(it's a new problem from the latest release of the package)\n"
        },
        {
            "user": "mheon",
            "body": "@lsm5 Poke, PTAL"
        },
        {
            "user": "lsm5",
            "body": "so @mheon mentioned that we're defaulting to runc (provided by cri-o-runc) so I'll check whatsup with the seccomp dependency."
        },
        {
            "user": "stephankoelle",
            "body": "Work arround:\nget an old cri-o-runc packge from some where an older poman 2 is installed:\n\n/var/cache/apt/archives/cri-o-runc_1.0.0-3~dev2_amd64.deb\n\ninstall that with dpk -i \ncontinue to install pomman with apt-get ... (should install without complain)\n"
        },
        {
            "user": "lsm5",
            "body": "@mheon @baude @cevich I sent a `2.0.5~3` to OBS just now which should unblock debian 10 users (should be ready in less than an hour). It goes back to preferring runc over cri-o-runc in the dependencies. However, if there's a cri-o-runc installed already, it will prefer using that instead as `/usr/lib/cri-o-runc/sbin/runc` is listed before `/usr/sbin/runc` in containers.conf. So, as long as we have the CI image builder explicitly install cri-o-runc, we should be good to go."
        },
        {
            "user": "lsm5",
            "body": "2.0.5~3 is available in the OBS stable repo now. Closing this. Please reopen if you still see this issue."
        },
        {
            "user": "ssbarnea",
            "body": "Apparently installing podman from debian repos got broken one or two days ago for a similar kind of conflict: https://github.com/ansible-community/toolset/pull/40/checks?check_run_id=1528906129\n\nCode that installs it can be seen at https://github.com/ansible-community/toolset/blob/main/Dockerfile#L28-L29"
        },
        {
            "user": "mheon",
            "body": "Libseccomp - this is https://github.com/containers/podman/issues/8665 again"
        },
        {
            "user": "stephankoelle",
            "body": "I think the issue is back - debian 10\n\n```\n16-Dec-2020 13:43:46 | podman : Depends: libseccomp2 (>= 2.4.3-1) but 2.3.3-4 is to be installed\n-- | --\n16-Dec-2020 13:43:46 | Recommends: crun but it is not going to be installed\n16-Dec-2020 13:43:46 | Recommends: varlink but it is not going to be installed\n```\n\n"
        },
        {
            "user": "stephankoelle",
            "body": "Workarround from #8665 did work\napt-get -t buster-backports install libseccomp-dev "
        },
        {
            "user": "zhangguanzhang",
            "body": "@lsm5 PTAL"
        },
        {
            "user": "dmlond",
            "body": "I tried to install podman on a Raspberry Pi 4 using the Raspberry Pi OS. The cpu is ARMv7 Processor rev 3 (v7l).  I am not able to follow the instructions to install buster-backports because I am not able to install the gpg key for it. apt-get -t buster-backports install libseccomp-dev gives the following error:\nE: The value 'buster-backports' is invalid for APT::Default-Release as such a release is not available in the sources.\n\nI also get the above error when I try to apt-get install podman\napt-get -qq -y install podman\nE: Unable to correct problems, you have held broken packages.\n"
        },
        {
            "user": "Andreyooo",
            "body": "I also tried installing Podman following the instructions on a Raspberry Pi 3 (Raspbian GNU/Linux 10 (buster) & ARMv7 Processor rev 4 (v7l)) and get:\n\n```\napt-get -qq -y install podman\nE: Unable to correct problems, you have held broken packages.\n```\n"
        },
        {
            "user": "justinschuldt",
            "body": "@dmlond @Andreyooo if you remove the `-qq` from the `apt-get` call it will tell you what is going on. The tl;dr is that there is not a compatible version of `libseccomp2` for Debian10/Raspberry Pi OS 10, so you have to manually install it from the `buster-backports` source before installing podman.\n\n- First make sure you have the keys for the debian archive: `sudo apt-get install debian-archive-keyring`\n- Source the repo with the unstable version that will satisfy podman's dependencies: `echo 'deb http://deb.debian.org/debian buster-backports main' >> /etc/apt/sources.list`\n- Then update `sudo apt-get update`\n- Manually install the `libseccomp2: v2.4.4-1` package to make podman happy `sudo apt-get -y -t buster-backports install libseccomp2`\n\nGood luck!"
        },
        {
            "user": "Andreyooo",
            "body": "Thx a lot @justinschuldt. Podman is now successfully installed on my Raspberry Pi :)"
        },
        {
            "user": "rhatdan",
            "body": "Should this issue be closed then?"
        },
        {
            "user": "x70b1",
            "body": "@rhatdan Yes, can be closed again.\nUsing `libseccomp2` from debian-backports solves the latest issue.\n"
        },
        {
            "user": "dmlond",
            "body": "I was not able to install the gpg keys for debian-backport because most of the older public key servers are not available."
        },
        {
            "user": "yacineMTB",
            "body": "@dmlond Might be your location, I was just able to do so"
        }
    ]
}