{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 1965,
    "type": "pull_request",
    "title": "Fix error handling of signature.NewEphemeralGPGSigningMechanism",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "mtrmac",
    "body": "`signature.NewEphemeralGPGSigningMechanism is called` in an `if` branch where the previous `err :=` introduces a \"new\" `err` variable, which means the failure isn't visible after the `if`.\n\nSo, do the dumb thing and just check on both branches explicitly. (We still need to worry about correctly setting `mech` and `publicKeyfingerprints` to persist after the `if`.)\n\nHow I hate Go sometimes. And this shows we really should update the linter.",
    "comments": [
        {
            "user": "mtrmac",
            "body": "@vrothberg @TomSweeneyRedHat PTAL, I’d like to fix this before a release."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\nlooks like the Skopeo tests are being bit by the sha1 issue too."
        },
        {
            "user": "mtrmac",
            "body": "The RPM tests are (so far?) intentionally advisory-only, so that’s not an immediate major concern."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 1967
        }
    ]
}