{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 339,
    "type": "pull_request",
    "title": "Update CI image",
    "state": "closed",
    "labels": [
        "storage"
    ],
    "user": "mtrmac",
    "body": "https://github.com/containers/skopeo/pull/2645 has updated the expected outputs of Skopeo to match podman-sequoia-0.2.0, so we need to be running with an updated image that includes that version, not 0.1.0.\n\nOtherwise, all tests in this repo are failing.\n\n@containers/container-libs-maintainers PTAL, this is blocking all other work. Compare the test failure in https://api.cirrus-ci.com/v1/task/6345581385220096/logs/integration.log .",
    "comments": [
        {
            "user": "Luap99",
            "body": "Looks like storage debian tests are now failing. Note I have downgraded the debian kernel to 6.1 so I assume we likely miss a skip if not new kernel feature in the tests now because of that"
        },
        {
            "user": "mtrmac",
            "body": "Yes: `/tmp` is a `tmpfs`, and that supports extended attributes only since kernel 6.6."
        },
        {
            "user": "mtrmac",
            "body": "Updated to use `/var/tmp` as `TMPDIR` for Debian. That costs 20-30 extra seconds on these checks, and seems to work.\n\n(I can’t see anything in the commit history suggesting we ever did anything different in the past — did we start relying on xattrs only recently? If not, there might be some smarter way to deal with this that I’m missing.)"
        },
        {
            "user": "Luap99",
            "body": "> (I can’t see anything in the commit history suggesting we ever did anything different in the past — did we start relying on xattrs only recently? If not, there might be some smarter way to deal with this that I’m missing.)\n\n/tmp wasn't a tmpfs in the past, I switched over the Ci VMs to a tmpfs like a year ago. So I guess this was never intentional one way or another."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 338
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 891
        }
    ]
}