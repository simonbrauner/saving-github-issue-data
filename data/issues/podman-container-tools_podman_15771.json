{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 15771,
    "type": "pull_request",
    "title": "[systemd] Ensure that podCreateArgs appear last in ExecStartPre=",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "dcermak",
    "body": "When creating a new pod without the `--name` flag, e.g.: `podman pod create foobar`\nit will get the name `foobar` implicitly and this will be recorded as the in the `podCreateArgs`. Unfortunately, the implicit name only works if it appears as the **last** argument of the startup command.\nWith 6e2e3a78ed1d05ee5f23f65b814e8135021961dd we started appending the pod security policy to the `startCommand`, resulting in the following `ExecStartPre=` line:\n```\n/usr/bin/podman pod create --infra-conmon-pidfile %t/pod-foobar.pid --pod-id-file %t/pod-foobar.pod-id foobar --exit-policy=stop\n```\nThis fails to launch, as the `pod create` command expects only a single non-flag parameter, but it assumes that `exit-policy=stop` is a second and terminates immediately instead.\n\n#### Does this PR introduce a user-facing change?\n\n\n```release-note\n- Fix systemd unit generation for pods created without `--name $podName`\n```\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Code LGTM, minus the comment about a test from @Luap99 "
        },
        {
            "user": "mheon",
            "body": "/approve"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/15771#\" title=\"Author self-approved\">dcermak</a>*, *<a href=\"https://github.com/containers/podman/pull/15771#issuecomment-1245405717\" title=\"Approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "dcermak",
            "body": "@mheon @Luap99 I've added a test explicitly for this case, adjusted the existing tests and added a release notes entry."
        },
        {
            "user": "dcermak",
            "body": "The CI passed now after a temporary fluke with quay.io yesterday. Is there anything missing now? tide complains about a missing `lgtm` label."
        }
    ],
    "cross_references": []
}