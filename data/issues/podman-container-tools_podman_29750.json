{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29750,
    "type": "issue",
    "title": "RFC: drop --cpu-rt-period and --cpu-rt-runtime flags?",
    "state": "open",
    "labels": [
        "Good First Issue",
        "triaged",
        "7.0"
    ],
    "user": "Luap99",
    "body": "With podman 6 we dropped cgroupv2 support but we never removed the --cpu-rt-period and --cpu-rt-runtime cli options. As documented they only work on a cgroupsv1 system.\n\nIt does not make sense to have options that cannot work anymore since running on cgroupv1 returns a hard error. However currently the options only print a warning not a hard error, so technically removing them would break whoever set them since they only saw a warning before.\n\nhttps://github.com/podman-container-tools/podman/blob/f1b80e51215f8edb6f8e01929685339e94a8ae72/pkg/specgen/generate/validate_linux.go#L55-L65\n\nWhile I would not think anyone would set them on a v2 system I guess it is best to avoid this breaking change right now and defer full removal for podman 7.\n\nThat said I guess at minimum right now we should remove the docs and hide the flags",
    "comments": [
        {
            "user": "TanayJalan",
            "body": "@Luap99 Makes sense to hide the flags and docs now. One suggestion: since the flags currently only warn about cgroups v2, it might be worth adding a deprecation warning too something like \"this option will be removed in a future version\" so users aren't surprised when it disappears in v7."
        },
        {
            "user": "mheon",
            "body": "I tagged 7.0 for the full removal, but I completely agree that these should be hidden and undocumented in 6.2. If we have consensus this seems like it can be tagged good first issue"
        },
        {
            "user": "Luap99",
            "body": "So in short we accept a PR that removes the documentation, changing the warning to state the flags are a NOP and no longer supported and will be removed in the next major.\n"
        },
        {
            "user": "harrisleesh",
            "body": "I'd like to take this — implementing the 6.2 scope Luap99 outlined (docs removal + NOP warning). PR shortly."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29784
        }
    ]
}