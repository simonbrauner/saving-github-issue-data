{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29644,
    "type": "pull_request",
    "title": "fix: specgen: do not inherit image USER with --userns=keep-id",
    "state": "closed",
    "labels": [],
    "user": "sundeep8967",
    "body": "## Problem\nWhen running an image that defines a `USER`, `podman run --userns=keep-id` incorrectly keeps using the image-defined user instead of running the container process as the invoking host user.\n\n## Solution\nModified `pkg/specgen/generate/container.go` to skip populating `s.User` from the image configuration when `s.UserNS.NSMode == specgen.KeepID`. This preserves the fact that no explicit `--user` was passed, allowing `namespaceOptions()` to accurately apply the `keep-id` UID mapping.\n\nFixes: #29600\n\n## Testing\n- Added an e2e test `podman --userns=keep-id overrides image USER` in `test/e2e/run_userns_test.go`.\n- All tests passed cleanly.",
    "comments": [
        {
            "user": "Honny1",
            "body": "We have been getting a lot of low quality PRs that do not follow our [contribution guidelines](https://github.com/podman-container-tools/podman/blob/main/CONTRIBUTING.md) and [LLM policy](https://github.com/podman-container-tools/community/blob/main/LLM_POLICY.md). So please follow them; i.e. this PR is addressing the same thing as https://github.com/podman-container-tools/podman/pull/29634. So I will close this PR in favor of https://github.com/podman-container-tools/podman/pull/29634"
        }
    ],
    "cross_references": []
}