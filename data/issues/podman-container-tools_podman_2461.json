{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2461,
    "type": "pull_request",
    "title": "Skip checkpoint/restore tests on Fedora for now",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "adrianreber",
    "body": "There is currently still one SELinux related checkpoint/restore problem:\nhttps://github.com/containers/libpod/issues/2334\n\nTo avoid unnecessary CI failures the checkpoint/restore tests are\ntemporarily disabled on Fedora.\n\nIt is not necessary to disable the tests on Ubuntu as it is running\nwithout SELinux and it is also not necessary to disable the RHEL 7 tests\nas RHEL's CRIU is too old to run the checkpoint/restore tests at all.\n\nSigned-off-by: Adrian Reber <areber@redhat.com>",
    "comments": [
        {
            "user": "adrianreber",
            "body": "This tries to help with #2435 \n\nCC: @cevich "
        },
        {
            "user": "vrothberg",
            "body": "/approve"
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/2461#\" title=\"Author self-approved\">adrianreber</a>*, *<a href=\"https://github.com/containers/libpod/pull/2461#issuecomment-467774387\" title=\"Approved\">vrothberg</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [vrothberg]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "giuseppe",
            "body": "I've been playing with the tests suite and the checkpoint/restore tests.  It would be nice to keep a whitelist of OCI runtimes that support the feature.  It is not part of the OCI specs, although runc supports it."
        },
        {
            "user": "mheon",
            "body": "@giuseppe Maybe this is something we can add to libpod.conf - whether a given runtime includes support for non-standard features like checkpoint/restore?"
        },
        {
            "user": "adrianreber",
            "body": "@giuseppe good point. Not sure how it could look like. Something like `if basename(r.path) == \"runc\"` does not sound like the best idea. Especially as a checkpoint is done by directly calling runc and a restore is done through conmon. So maybe something globally which tells Podman that special features (like checkpoint/restore) are not part of the current OCI runtime."
        },
        {
            "user": "cevich",
            "body": "(this should also help with #1936 )"
        },
        {
            "user": "cevich",
            "body": "That's an odd test to fail on only Ubuntu `podman run n user created network namespace with resolv.conf`\n\nOutput shows:\n\n```\nLoaded image(s): docker.io/library/busybox:latest\nRunning: /var/tmp/go/src/github.com/containers/libpod/bin/podman --root /tmp/podman_test455853544/crio --runroot /tmp/podman_test455853544/crio-run --runtime /usr/bin/runc --conmon /usr/libexec/podman/conmon --cni-config-dir /etc/cni/net.d --cgroup-manager cgroupfs --storage-driver vfs run --net ns:/run/netns/xxx docker.io/library/alpine:latest cat /etc/resolv.conf\nbash: /etc/netns/xxx/resolv.conf: No such file or directory\nsearch c.SECRET.internal google.internal\nnameserver 127.0.0.53\noptions edns0\n```\n\nLooking at test code..."
        },
        {
            "user": "cevich",
            "body": "(code ``test/e2e/run_networking_test.go:193``)\n\nI really can't understand how that could fail, the test before it passed fine.  Unless (somehow) one of those `SystemExec` broke but didn't report.  Re-running the test on ubuntu, hoping it's a flake."
        },
        {
            "user": "giuseppe",
            "body": "> @giuseppe Maybe this is something we can add to libpod.conf - whether a given runtime includes support for non-standard features like checkpoint/restore?\n\nI think for the tests we could simply do something like:\n\n`$OCI_RUNTIME checkpoint --help > /dev/null 2>&1`\n\nif it fails then the OCI runtime doesn't support checkpoint/restore"
        },
        {
            "user": "mheon",
            "body": "Also works fine - I'd be fine doing that probe to check runtime capabilities"
        },
        {
            "user": "cevich",
            "body": "@adrianreber okay, looks like the test-failure is a race condition.  I learned that others are seeing it as well.  Pretty sure #2481 (or similar) should address it.  Until that merges, we can just smash the re-test button until it passes :confused: "
        },
        {
            "user": "cevich",
            "body": "There...tests all green now."
        },
        {
            "user": "mheon",
            "body": "Code LGTM"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}