{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28539,
    "type": "pull_request",
    "title": "[v4.2.0-rhel] Fix system test: podman logs restarted journald",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "cevich",
    "body": "When run with `podman-remote`, the `--events-backend` option is unavailable.  However, the `podman logs restarted journald` test blindly sets `events_backend` without checking if the remote client is being used.  This causes the test to fail.  Fix this by skipping the test when the remote client is being used.\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "cevich",
            "body": "@TomSweeneyRedHat somehow I missed this during my testing.  I can't recall if I saw it on other `-rhel` branches or not, so BOLO."
        },
        {
            "user": "Luap99",
            "body": "Can you please add the branch name in the PR tiles so unrelated people do not click on these PRs thinking this is something for main"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "/lgtm"
        },
        {
            "user": "cevich",
            "body": "> Can you please add the branch name in the PR tiles\n\nSorry Paul, I completely forgot about that.  I'll make more of an effort to adhere to that standard in the future."
        }
    ],
    "cross_references": []
}