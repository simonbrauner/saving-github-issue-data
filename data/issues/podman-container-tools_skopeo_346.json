{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 346,
    "type": "pull_request",
    "title": "Always combine RUN apt-get update with apt-get install in the same RUN statement.",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "jingqiuELE",
    "body": "From the [docs](https://docs.docker.com/engine/userguide/eng-image/dockerfile_best-practices/#build-cache) in March 2017:\n\nAlways combine `RUN apt-get update` with `apt-get install` in the same  RUN statement, for example\n\n`RUN apt-get update && apt-get install -y package-bar`\n\nUsing `apt-get update` alone in a RUN statement causes caching issues and subsequent` apt-get install `instructions fail.\n\nSigned-off-by: Jing Qiu <aqiu0720@gmail.com>",
    "comments": [
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "runcom",
            "body": "LGTM "
        }
    ],
    "cross_references": []
}