{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29654,
    "type": "pull_request",
    "title": "fix(libpod): keep '=' in a volume subpath",
    "state": "closed",
    "labels": [],
    "user": "Atishyy27",
    "body": "The subpath mount option is parsed with `strings.Split(o, \"=\")[1]`, so it keeps only the text between the first and second `=`. A subpath that contains `=` gets cut short and the container mounts a different directory than the one that was asked for.\n\n```\n--mount type=volume,src=v,dst=/mnt,subpath=/opt/a=b/c   mounts /opt/a\n```\n\nThe existing subpath e2e test now uses a path with an `=` in it.\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @podman-container-tools/packit-jobs please check. Everyone else, feel free to ignore."
        },
        {
            "user": "Atishyy27",
            "body": "updated"
        },
        {
            "user": "Luap99",
            "body": "@Honny1 PTAL again, should be good to merge"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29678
        }
    ]
}