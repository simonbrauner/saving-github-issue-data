{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 52,
    "type": "issue",
    "title": "Login with non-declaritive registry credential is overwriting `auth.json` of the previous credential when using `podman-docker`",
    "state": "open",
    "labels": [
        "common"
    ],
    "user": "lslavkov",
    "body": "Pasting this issue from the project https://github.com/containers/podman/issues/22400.\n\nInitially when you have already login from `registry.fedoraproject.org` using `podman login` then using `docker login` to the default registry.\n\n### Steps to reproduce\n\n1. Install podman-docker\n2. Follow docker.io's instructions to login\n   a. Create access token\n   b. run `docker login -u $USER`\n   c. enter token\n\n### Describe the results you received\n```\n$ podman login -u $USER\nPassword:\nLogin Succeeded!\n$ cat /run/user/1000/containers/auth.json\n{\n    \"auths\": {\n\t    \"registry.fedoraproject.org\": {\n\t\t    \"auth\": \"cGF0cmljazpkY2tyX3BhdF84X2xvbHRoaXNhaW50cmVhbAo=\"\n       }\n    }\n}\n$ docker push $USER/$IMAGE\n[...]\nError: writing blob: initiating layer upload to /v2/$USER/$IMAGE/blobs/uploads/ in registry-1.docker.io: requested access to the resource is denied\n```\n\n### Describe the results you expected\n```\n$ docker login -u $USER\nPassword: \nLogin Succeeded!\n$ cat /run/user/1000/containers/auth.json\n{\n    \"auths\": {\n\t    \"registry-1.docker.io\": {\n\t\t    \"auth\": \"cGF0cmljazpkY2tyX3BhdF84X2xvbHRoaXNhaW50cmVhbAo=\"\n\t    }\n    }\n}⏎\n$ docker push $USER/$IMAGE\nWriting manifest to image destination\n\n^ exit success\n\nor\n\nFailure for the login command\n```",
    "comments": [
        {
            "user": "mtrmac",
            "body": "(A drive-by comment:) Note that there is _much more_ discussion in the Podman issue. It’s not at all clear that the consensus is the “expected behavior” above."
        },
        {
            "user": "lslavkov",
            "body": "Good point on that but I think is a good idea to keep aware that there is such thread talking about this issue."
        }
    ],
    "cross_references": []
}