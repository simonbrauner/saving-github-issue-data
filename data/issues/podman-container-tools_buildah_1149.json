{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 1149,
    "type": "pull_request",
    "title": "Add optional blob caching for very specific use cases",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "nalind",
    "body": "Add a blob cache reference type which wraps other reference types.  When used to create source and destination images for copying, it returns wrappers for source and destination images created by the wrapped references.\n\nThe cache reference can be created with a specified directory, or it will create a temporary directory of its own.  When told to clear its cache, if a temporary directory is being used, the reference will also remove the temporary directory.\n\nBlobs that are written to a cache image destination are stored as-is on disk.  When reading a blob from a cache image source, if a blob with the matching digest (and if specified, a matching size) can be found on disk, read it directly instead of reading it from the real image source.\n\nAdd API hooks for designating locations to be used as blob caches when pulling and pushing images.  When we commit read-only copies of container layers for use in images, if we're using blob caching, store a copy of the layer in the blob cache directory so that it can be found.",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "Luv me Red Hat CI, looks like it's not happy today, I don't believe due to this PR."
        },
        {
            "user": "rhatdan",
            "body": "bot, retest this please"
        },
        {
            "user": "rhatdan",
            "body": "@vrothberg @mtrmac PTAL"
        },
        {
            "user": "rhatdan",
            "body": "@nalind Shouldn't we remove the blob-cache when we remove the image?"
        },
        {
            "user": "rhatdan",
            "body": "@mtrmac PTAL"
        },
        {
            "user": "nalind",
            "body": "Updated so that we don't change the manifest that `Commit()` commits, and we do substitutions in `LayerInfosForCopy()` based on whether we prefer compressed or uncompressed blobs, though that part's largely guesswork."
        },
        {
            "user": "nalind",
            "body": "Hmm, the bits that decide when we should offer a substitution could use some tweaking - when I try adding this to openshift/builder using @runcom's branch as a guide, that logic isn't being triggered unless the `PushOptions` specify a `Compression` value of `imagebuildah.Gzip`.  Otherwise I'm seeing a consistent speedup."
        },
        {
            "user": "rh-atomic-bot",
            "body": ":umbrella: The latest upstream changes (presumably c9cb148) made this pull request unmergeable. Please resolve the merge conflicts."
        },
        {
            "user": "rhatdan",
            "body": "@nalind  needs a rebase."
        },
        {
            "user": "nalind",
            "body": "Before we push an image, we _could_ open the destination ourselves and ask its `DesiredLayerCompression()` method what the destination would prefer, and use that to guide whether or not we make adjustments to what we return from the source `LayerInfosForCopy()`, but that might be assuming too much about how `copy.Image()` will continue to do things."
        },
        {
            "user": "mtrmac",
            "body": "> Before we push an image, we _could_ open the destination ourselves and ask its `DesiredLayerCompression()` method what the destination would prefer, and use that to guide whether or not we make adjustments to what we return from the source `LayerInfosForCopy()`, but that might be assuming too much about how `copy.Image()` will continue to do things.\n\nYeah; `NewImageDestination` can in principle be fairly expensive (so far the worst is probably `docker-daemon:`, which is not negligible but not a disaster either), and in `copy.Image` there’s also the `canModify{Manifest,Blob}` logic involved…\n\nAt a first glance, it would be much more convenient to move the decision from the source (and `LayerInfosForCopy`) to the destination — after https://github.com/mtrmac/image/tree/blob-info-caching merges `HasBlob` with `ReapplyBlob` to `TryReusingBlob`, which is (unlike the current interface) allowed to replace the source-provided blob with a different semantically equivalent one (changing the digest).\n\nOTOH I can’t remember the full flow of the data in this PR, maybe that is unworkable; also, moving the cache references from `GetBlob` to `TryReusingBlob` would hide copy operation from `copy.Image`, e.g. losing progress reporting."
        },
        {
            "user": "rhatdan",
            "body": "@nalind @mtrmac Is this ready to merge and handle the other conversation in a different PR?"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":umbrella: The latest upstream changes (presumably e5e4588) made this pull request unmergeable. Please resolve the merge conflicts."
        },
        {
            "user": "rhatdan",
            "body": "@nalind needs a rebase."
        },
        {
            "user": "mrunalp",
            "body": "Can we get this in?"
        },
        {
            "user": "nalind",
            "body": "Rebased."
        },
        {
            "user": "rhatdan",
            "body": "@rh-atomic-bot r+"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":pushpin: Commit cceb225 has been approved by `rhatdan`\n\n<!-- @rh-atomic-bot r=rhatdan cceb22528d64260545afaffb14d51d3576fb8676 -->"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":zap: Test exempted: pull fully rebased and already tested."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 1193
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 1218
        }
    ]
}