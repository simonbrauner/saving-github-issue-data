{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 977,
    "type": "issue",
    "title": "Docker schema2 manifests with OCI layer media types are rejected",
    "state": "open",
    "labels": [],
    "user": "lumincui",
    "body": "### Description\n\n`go.podman.io/image/v5/manifest` rejects Docker schema 2 manifests that contain OCI config or layer media types, even though these mixed manifests exist in registries and can be copied by container tooling after accepting the descriptor media types.\n\nThe failure is raised by `manifest.SupportedSchema2MediaType` while parsing a Docker v2 schema 2 manifest:\n\n```text\nunsupported docker v2s2 media type: \"application/vnd.oci.image.layer.v1.tar+gzip\"\n```\n\n### Why this matters\n\nWe hit this while using `skopeo copy --all` to mirror images between private registries. The source registry returned a Docker schema 2 manifest whose layer descriptors used OCI layer media types such as:\n\n- `application/vnd.oci.image.layer.v1.tar+gzip`\n- `application/vnd.oci.image.layer.v1.tar`\n\nBecause `skopeo` vendors `go.podman.io/image/v5`, the copy failed before it could push the image to the destination registry.\n\n### Expected behavior\n\nDocker schema 2 manifest parsing should accept OCI image config/layer descriptor media types when they appear inside an otherwise Docker schema 2 manifest, matching the compatibility behavior already used by registries and other tools in the wild.\n\n### Actual behavior\n\n`Schema2FromManifest` rejects the manifest via `SupportedSchema2MediaType`.\n\n### Proposed fix\n\nAllow these OCI media types in `SupportedSchema2MediaType`:\n\n- `imgspecv1.MediaTypeImageConfig`\n- `imgspecv1.MediaTypeImageLayer`\n- `imgspecv1.MediaTypeImageLayerGzip`\n- `imgspecv1.MediaTypeImageLayerNonDistributable`\n- `imgspecv1.MediaTypeImageLayerNonDistributableGzip`\n\nThis was originally filed in the archived/migrated repo as containers/image#2972.\n",
    "comments": [
        {
            "user": "mtrmac",
            "body": "Thanks for your report.\n\nImages with mixed MIME types are invalid, and have always been invalid. This code has been rejecting them since 2019, and with every passing month the case for accepting them grows weaker.\n\nSo the first recommendation must be to fix the producer, and relaxing these checks would, by now, need an _exceptionally strong_ argument that these images are 1) widespread in the ecosystem and 2) unlikely to be fixed."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 978
        },
        {
            "organization": "containers",
            "repository": "image",
            "number": 2972
        }
    ]
}