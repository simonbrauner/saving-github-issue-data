{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28980,
    "type": "pull_request",
    "title": "docs: fix --image-volume default and accepted values",
    "state": "open",
    "labels": [
        "No New Tests"
    ],
    "user": "stefan8",
    "body": "`--image-volume` is documented and auto-completed as `bind`, but the canonical values are `anonymous | tmpfs | ignore` (default `anonymous`); `bind` is only a deprecated alias. This shows `anonymous` in the docs, `--help` and completion, and notes that `bind` still works. No behavior change.\n\n#### Checklist\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits (`git commit -s`).\n- [x] Referenced issues using `Fixes: #27674` in commit message (if applicable)\n- [x] Tests have been added/updated (or no tests are needed)\n- [x] Documentation has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] Release note entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "mheon",
            "body": "One nit on docs wording, and I'd like to see the notice suggested by @inknos - otherwise LGTM"
        }
    ],
    "cross_references": []
}