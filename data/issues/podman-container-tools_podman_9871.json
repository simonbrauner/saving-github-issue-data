{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 9871,
    "title": "podman-remote build: -f gets confused",
    "state": "closed",
    "labels": [
        "kind/bug",
        "remote",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "The `-f` flag is looking for ~love~ Dockerfiles in all the wrong places:\n```console\n$ bin/podman system service --timeout=0\n```\nIn window 2:\n```console\n$ mkdir ccc\n$ echo \"FROM scratch\" >ccc/Dockerfile2\n$ ./bin/podman-remote build -t foo -f Dockerfile2 ccc\nERRO[0000] 1 error occurred:\n        * lstat /path/to/my/current/directory/Dockerfile2: no such file or directory\n\nError: Post: \"http:...long stuff probably related to the previous failure so I'm not going to bother filing an issue for it\"\n```\nNon-remote `podman build` works fine.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@vrothberg @nalind is there an easy way to add a single file to the copier of the context directory?"
        },
        {
            "user": "rhatdan",
            "body": "@edsantiago is finding all of the little warts in podman-remote build. :^). \n\nI have been trying to kill them off as quickly as possible."
        },
        {
            "user": "vrothberg",
            "body": "> @vrothberg @nalind is there an easy way to add a single file to the copier of the context directory?\n\nYes, that's possible (see https://github.com/containers/podman/blob/master/cmd/podman/containers/cp.go#L362).  Looking that the bindings for `build`, I think that [nTar(...)](https://github.com/containers/podman/blob/master/pkg/bindings/images/build.go#L332) should be replaced with using the copier package. It also supports `Excludes` among many other things."
        },
        {
            "user": "nalind",
            "body": "The logic there is already checking for exclusions, and I think that slipping additional files into the tar stream (if we need to emulate something like the multiple-Dockerfiles feature) is going to be marginally easier with the current logic."
        },
        {
            "user": "edsantiago",
            "body": "As an FYI: there are 52 tests in `bud.bats` that I'm currently skipping because they don't work with podman-remote due to this bug; all of them of the form `buildah bud -f path`. It seems like a common pattern."
        },
        {
            "user": "vrothberg",
            "body": "I had a look at the code and it seems that the logic in `cmd/podman/images/build.go` needs some polishing.  IMHO the data passed to the bindings should be fully evaluated since we _must_ tar the files up.  Using @edsantiago's reproducer above, we `Dockerfile2` as the Dockerfile and  `/home/vrothberg/ccc` as the context directory.  I think the Dockerfile should have already been evaluated or at least be relative."
        },
        {
            "user": "vrothberg",
            "body": "Ah, I have another idea."
        },
        {
            "user": "vrothberg",
            "body": "Something for bug week.  The longer I read the code, the more questions I have."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "edsantiago",
            "body": "Still present. This is the biggest problem facing buildah-bud tests under podman-remote: there are 53 buildah-bud tests that need to be skipped because of this bug."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "edsantiago",
            "body": "Still present: https://storage.googleapis.com/cirrus-ci-6707778565701632-fcae48/artifacts/containers/podman/6152338764201984/html/bud-remote-fedora-34-root-host.log.html#t-00205"
        }
    ]
}