{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2011,
    "type": "pull_request",
    "title": "[CI:BUILD] Packit: add jobs for downstream Fedora package builds",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "lsm5",
    "body": "Get rid of `skopeo.spec.rpkg` in favour of\n`rpm/skopeo.spec` which gets synced with fedora dist-git on every upstream release. The version in the new spec file is set to `0` by default and gets updated by packit automatically on every packit task.\n\nPackit will automatically create a PR on fedora dist-git on every new upstream release. A sample PR will look like:\nhttps://src.fedoraproject.org/rpms/container-selinux/pull-request/10#\n\nA dry run for this can be triggered using:\n`$ packit propose-downstream --local-content`\n\nTo run this command locally, you would need to have your packit user-configuration-file set.\nRef: https://packit.dev/docs/configuration/#user-configuration-file\n\nalong with a fedora api key created at:\nhttps://src.fedoraproject.org/settings#nav-api-tab with sufficient ACLs.\n\nThis patch incurs zero additional cost to upstream as the downstream\ntasks occur only after upstream release and do not block anything\nupstream.\n\n\n[NO NEW TESTS NEEDED]",
    "comments": [
        {
            "user": "lsm5",
            "body": "@mtrmac @vrothberg @rhatdan @edsantiago @cevich PTAL"
        },
        {
            "user": "lsm5",
            "body": "Successful copr build at: https://copr.fedorainfracloud.org/coprs/rhcontainerbot/packit-builds/build/6058022/"
        },
        {
            "user": "lsm5",
            "body": "I'm gonna try disabling `.packit.sh` and see how things go. Please hold off further reviews."
        },
        {
            "user": "lsm5",
            "body": "alright, I think I have addressed all comments. Added some additional comments in the spec file and also got rid of `.packit.sh`. Packit's defaults are good enough.\n\nPTAL."
        },
        {
            "user": "lsm5",
            "body": "> alright, I think I have addressed all comments. Added some additional comments in the spec file and also got rid of `.packit.sh`. Packit's defaults are good enough.\n> \n> PTAL.\n\nerr, once again I spoke too soon. https://copr.fedorainfracloud.org/coprs/rhcontainerbot/packit-builds/package/skopeo/ something seems wrong with version fetching."
        },
        {
            "user": "lsm5",
            "body": "> err, once again I spoke too soon. https://copr.fedorainfracloud.org/coprs/rhcontainerbot/packit-builds/package/skopeo/ something seems wrong with version fetching.\n\nSo, using packit's defaults means the Version string is fetched using: `git describe --tags --abbrev=0` and not using the string in `version/version.go`. So the current build has version `1.12.0` and not `1.12.1~dev`.  If that's acceptable, then we should be ok. I will upgrade the `Epoch:102` so existing copr users are not broken.\n\nI guess upstream doesn't need to concern itself too much with all this, more of a copr usability and breakage thing."
        },
        {
            "user": "lsm5",
            "body": "> I will upgrade the `Epoch:102` so existing copr users are not broken.\n\nset `Epoch: 102` to preserve upgrade path for copr users. With that I think we should be good to go.\n"
        },
        {
            "user": "lsm5",
            "body": "@mtrmac all comments should be addressed now. Marking ready for review. PTAL."
        }
    ],
    "cross_references": []
}