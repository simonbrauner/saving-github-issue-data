{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 3650,
    "type": "issue",
    "title": "Question: How to debug buildah bud with \"unshare.MaybeReexecUsingUserNamespace(false)\" ?",
    "state": "closed",
    "labels": [
        "stale-issue"
    ],
    "user": "cmoulliard",
    "body": "## Question\n\nIs there a way to avoid that when we remote debug using dlv, the following line of code, which launches under the hood a sub-command will cause an exit of the debugger ?\n\n```\nfunc main() {\n\tif _, ok := os.LookupEnv(\"DEBUG\"); ok && (len(os.Args) <= 1 || os.Args[1] != \"from-debugger\") {\n\t\targs := []string {\n\t\t\t\"--listen=:2345\",\n\t\t\t\"--headless=true\",\n\t\t\t\"--api-version=2\",\n\t\t\t\"--accept-multiclient\",\n\t\t\t\"exec\",\n\t\t\t\"/buildah-app\", \"from-debugger\",\n\t\t}\n\t\terr := syscall.Exec(\"/usr/local/bin/dlv\", append([]string{\"/usr/local/bin/dlv\"}, args...), os.Environ())\n\t\tif err != nil {\n\t\t\tfmt.Fprintf(os.Stderr, \"error: %v\\n\", err)\n\t\t\tos.Exit(1)\n\t\t}\n\t}\n\n\tctx := context.TODO()\n\n\tif buildah.InitReexec() {\n\t\treturn\n\t}\n\n\t// TODO: Check how we could continue to use the debugger as the following code exec a sub-command and by consequence it exits\n\tunshare.MaybeReexecUsingUserNamespace(false)\n```",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ],
    "cross_references": []
}