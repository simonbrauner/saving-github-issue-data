{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 1018,
    "type": "pull_request",
    "title": "cmd/podman/utils.go: Cancel-able resize writes",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "wking",
    "body": "On Thu, Jun 28, 2018 at 03:48:26AM -0700, Marco Vedovati [wrote][1]:\n> The root cause is a deadlock between two channel writes made by two different goroutines:\n>\n> 1. `resizeTty() : go func(){} : sendUpdate()` is sending a resize  message thru `resize` right at the beginning, but the channel is never read if some startup error occurs.\n>\n> 2. Upon program termination, `startAttachCtr() : defer func(){} ` is telling the goroutine in \"1.\" to stop via the `resizeTerminate` channel. But that guy is still waiting for the write to `resize` to complete so the the termination message is never read.\n>\n> I think the go deadlock detection does not kick in because not all goroutines are seen as asleep. E.g. `os/signal Notify()` is enough to have the deadlock not detected.\n\n333ab8c2 (#1010) addressed this with a deferred drain.  This commit adjusts that approach to use a single select tocover \"have we been canceled?\", \"has there been a resize signal?\", and (when we have one) \"can we write the most recent resize event to the resize channel?\".\n\nA side benefit to this approach is that if we have a slow `resize` consumer and several resize signals, the `resizeTty` function will keep updating its local `resizeEvent`.  Once the `resize` channel is able to accept, only the most-recent event will be written.  Previously we'd have written one resize event for every received signal, even if the `resize` consumer was falling behind.\n\nCC @marcov.\n\n[1]: https://github.com/projectatomic/libpod/pull/1010#issuecomment-400994436",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@baude @mheon @giuseppe PTAL"
        },
        {
            "user": "mheon",
            "body": "LGTM"
        },
        {
            "user": "rhatdan",
            "body": "@rh-atomic-bot r+"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":pushpin: Commit 50b2d2d has been approved by `rhatdan`\n\n<!-- @rh-atomic-bot r=rhatdan 50b2d2dc3320ca480041541ad58b977780a44091 -->"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":zap: Test exempted: pull fully rebased and already tested."
        },
        {
            "user": "giuseppe",
            "body": "Please take a look at: https://github.com/projectatomic/libpod/pull/1020"
        }
    ],
    "cross_references": []
}