{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 598,
    "type": "issue",
    "title": "`buildah bud --rm -t webserver .` fails",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "TomSweeneyRedHat",
    "body": "**Description**\n\ndocker has this command: `docker build --rm -t webserver .` which if you replace docker with 'buildah' your receive the error:\n\nIncorrect Usage: flag provided but not defined: -rm\n\nIf we're not going to support this, we should at least provide an error message which tells the user how to make it so.\n\nReported/Requested by @dustymabe\n\n**Output of `buildah version`:**\n\nBuildah v0.16\n",
    "comments": [
        {
            "user": "rhatdan",
            "body": "We should support it."
        },
        {
            "user": "pixdrift",
            "body": "I was looking at rm when compiling the list for #599. The behaviour of `--rm` in docker is to remove intermediate containers after successful build (defaults to true). Is `buildah bud` currently generating/storing intermediate containers?\n\nI suspect the expected behaviour of `--rm` is what buildah is already doing (not generating/storing intermediate containers after build).. and `--rm=false` is where the work would be matching docker behaviour."
        },
        {
            "user": "pixdrift",
            "body": "@rhatdan, if this does match default buildah behaviour, would there be benefit in having a noop for the `--rm` for the time being, and then implementing `--rm=false` when the layer caching is implemented in #601?. This would improve compatibility in the short term while the caching capability is developed."
        },
        {
            "user": "rhatdan",
            "body": "Sure Open a PR..."
        },
        {
            "user": "pixdrift",
            "body": "noop for `--rm` implemented in PR #639 from @rhatdan \n"
        },
        {
            "user": "pixdrift",
            "body": "@TomSweeneyRedHat, the original use case should work now without the `-rm` error."
        },
        {
            "user": "rhatdan",
            "body": "I'm closing this since I believe we cover this in other issues."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 599
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 630
        }
    ]
}