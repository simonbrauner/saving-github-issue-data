{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2882,
    "type": "issue",
    "title": "skopeo copy --multi-arch with specific platforms fails with \"blob unknown to registry\" when manifest list contains other platforms",
    "state": "closed",
    "labels": [],
    "user": "viccch",
    "body": "Description\n\nWhen using skopeo copy with --multi-arch \"linux/amd64,linux/arm64\" to copy only specific platforms from a multi-arch image, the command fails with blob unknown to registry error. The issue appears to be that the manifest list still references other platforms (e.g., linux/386, linux/arm/v7) that were not uploaded to the destination registry.\n\nSteps to Reproduce\n\nSource image is a multi-arch manifest list containing multiple platforms:\n\nbash\nskopeo inspect docker://registry.k8s.io/pause:3.9 --raw | jq '.manifests[].platform'\nOutput shows platforms: linux/amd64, linux/arm64, linux/386, linux/arm/v7, etc.\n\nAttempt to copy only linux/amd64 and linux/arm64 platforms:\n\nbash\nskopeo copy \\\n--multi-arch \"linux/amd64,linux/arm64\" \\\n--src-tls-verify=false \\\n--dest-tls-verify=false \\\ndocker://registry.k8s.io/pause:3.9 \\\ndocker://myregistry.com/registry.k8s.io/pause:3.9\nError occurs:\n\ntext\nFATA[0001] Uploading manifest list failed, attempted the following formats:\napplication/vnd.docker.distribution.manifest.list.v2+json(uploading manifest 3.9 to myregistry.com/registry.k8s.io/pause: blob unknown to registry),\napplication/vnd.oci.image.index.v1+json(uploading manifest 3.9 to myregistry.com/registry.k8s.io/pause: blob unknown to registry)\nExpected Behavior\n\nWhen specifying --multi-arch \"linux/amd64,linux/arm64\", skopeo should:\n\nDownload only the specified platform images\n\nCreate a new manifest list containing ONLY these platforms\n\nSuccessfully push the filtered manifest list to the destination registry\n\nActual Behavior\n\nskopeo attempts to push the original manifest list (containing all platforms) to the destination registry, but the blobs for unspecified platforms (e.g., linux/386, linux/arm/v7) don't exist in the destination, causing blob unknown errors.\n\nRoot Cause Analysis\n\nBased on code inspection (parseMultiArch function in cmd/skopeo/copy.go), when --multi-arch \"linux/amd64,linux/arm64\" is provided:\n\ngo\ncase \"linux/amd64,linux/arm64\":\nreturn copy.CopySpecificImages, platforms, nil\nThe function correctly returns CopySpecificImages with platform filters. However, when building the manifest list for upload, skopeo appears to preserve the original manifest list structure instead of creating a filtered one that excludes unspecified platforms.\n\n\nskopeo version: 1.23.0",
    "comments": [
        {
            "user": "mtrmac",
            "body": "Thanks for your report.\n\nYes. We also need to expose `StripSparseManifestList`. That is coming in the future. Cc: @aguidirh "
        },
        {
            "user": "aguidirh",
            "body": "Thanks for your report @viccch,\n\nThe feature to keep the manifest list only with the archs requested is going to be implemented in PR #2892 so please keep watching this one."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "aguidirh",
            "body": "This issue is not a `stale-issue`, could anyone remove this label please? "
        },
        {
            "user": "foi",
            "body": "i've checked main and it also didn't work\n```\nskopeo-latest copy --multi-arch='linux/amd64,linux/arm64' -q  \"docker://golangci/golangci-lint:v2.12.2\" \"docker://someregistry/images/mirror/golangci-test:v2.12.2\"\n\nFATA[0006] Uploading manifest list failed, attempted the following formats: application/vnd.oci.image.index.v1+json(uploading manifest v2.12.2 to someregistry/images/mirror/golangci-test: manifest blob unknown), application/vnd.docker.distribution.manifest.list.v2+json(uploading manifest v2.12.2 to someregistry/images/mirror/golangci-test: manifest blob unknown)\n```"
        },
        {
            "user": "aguidirh",
            "body": "@foi does the target registry support sparse manifest? "
        },
        {
            "user": "foi",
            "body": "> [@foi](https://github.com/foi) does the target registry support sparse manifest?\n\nthe target registry is gitea 1.27.0\ni don't know what is \"sparse manifest\"\nIn gitea logs I see this\n```\n....\nor 10.255.0.13:60884, 200 OK in 17.6ms @ container/container.go:517(container.HeadBlob)\n2026/07/27 17:13:04 HTTPRequest [I] router: completed HEAD /v2/images/mirror/golangci-test/blobs/sha256:f1d933871212c967306d5c4943724e38eced4bc8bc814f01c280d0b014939278 for 10.255.0.13:60896, 200 OK in 27.3ms @ container/container.go:517(container.HeadBlob)\n2026/07/27 17:13:04 HTTPRequest [I] router: completed HEAD /v2/images/mirror/golangci-test/blobs/sha256:a985874912da61511dcddc0ffc9f4ee63fc6acd0d8a9ae9195af7b1f7c6ae315 for 10.255.0.13:60910, 200 OK in 6.8ms @ container/container.go:517(container.HeadBlob)\n2026/07/27 17:13:04 HTTPRequest [I] router: completed PUT /v2/images/mirror/golangci-test/manifests/sha256:d752ab437f65208cefc8970886c6a430ce21b1561b352f029411797913f511bd for 10.255.0.13:60920, 201 Created in 83.7ms @ container/container.go:569(container.PutManifest)\n2026/07/27 17:13:04 HTTPRequest [I] router: completed PUT /v2/images/mirror/golangci-test/manifests/v2.12.2 for 10.255.0.13:60936, 404 Not Found in 13.7ms @ container/container.go:569(container.PutManifest)\n2026/07/27 17:13:04 HTTPRequest [I] router: completed PUT /v2/images/mirror/golangci-test/manifests/v2.12.2 for 10.255.0.13:60950, 404 Not Found in 12.8ms @ container/container.go:569(container.PutManifest)\n```"
        },
        {
            "user": "aguidirh",
            "body": "Hello @foi  \n\nThe behavior you're seeing is by design — it's called a **sparse manifest**.\n\nWhen you specify `--multi-arch=linux/amd64,linux/arm64`, skopeo downloads only those platform blobs but intentionally pushes the **original full manifest list** (with all platforms) to the destination. The idea is to keep the manifest list intact while storing only selected blobs — useful for disconnected/air-gapped registries configured to accept incomplete manifest lists.\n\nThe problem is that **standard registries validate that every platform referenced in the manifest list has its blobs present**. Since the blobs for `linux/386`, `linux/arm/v7`, etc. were never uploaded, your registry rejects the push with `blob unknown`.\n\n> ⚠️ **Note:** Gitea's container registry does not support sparse manifest lists — there is no equivalent to the distribution registry's `validation.manifests.indexes.platforms: none` configuration, so this cannot be worked around on the Gitea side.\n\n**Solution 1 — use `--strip-sparse-manifest-list`** (added in [PR #2892](https://github.com/podman-container-tools/skopeo/pull/2892)):\n\nThis flag strips the non-copied platforms from the manifest list, producing a filtered manifest containing **only** the platforms you requested. It requires either `--remove-signatures` or `--remove-list-signatures` because stripping instances invalidates the existing manifest list signature:\n\n```bash\nskopeo copy \\\n  --multi-arch=linux/amd64,linux/arm64 \\\n  --strip-sparse-manifest-list \\\n  --remove-list-signatures \\\n  docker://registry.k8s.io/pause:3.9 \\\n  docker://your-private-registry/pause:3.9\n```\n\nThe trade-off is that the resulting manifest digest differs from the source since the manifest list is modified, but it works with any standard registry including Gitea.\n\n**Solution 2 — use a container registry that supports sparse manifest feature.** Requires configuration on the registry with `validation.manifests.indexes.platforms: none` (supported by the [distribution registry](https://github.com/distribution/distribution)).\n"
        },
        {
            "user": "foi",
            "body": "> Hello [@foi](https://github.com/foi)\n> \n> The behavior you're seeing is by design — it's called a **sparse manifest**.\n> \n> When you specify `--multi-arch=linux/amd64,linux/arm64`, skopeo downloads only those platform blobs but intentionally pushes the **original full manifest list** (with all platforms) to the destination. The idea is to keep the manifest list intact while storing only selected blobs — useful for disconnected/air-gapped registries configured to accept incomplete manifest lists.\n> \n> The problem is that **standard registries validate that every platform referenced in the manifest list has its blobs present**. Since the blobs for `linux/386`, `linux/arm/v7`, etc. were never uploaded, your registry rejects the push with `blob unknown`.\n> \n> > ⚠️ **Note:** Gitea's container registry does not support sparse manifest lists — there is no equivalent to the distribution registry's `validation.manifests.indexes.platforms: none` configuration, so this cannot be worked around on the Gitea side.\n> \n> **Solution 1 — use `--strip-sparse-manifest-list`** (added in [PR #2892](https://github.com/podman-container-tools/skopeo/pull/2892)):\n> \n> This flag strips the non-copied platforms from the manifest list, producing a filtered manifest containing **only** the platforms you requested. It requires either `--remove-signatures` or `--remove-list-signatures` because stripping instances invalidates the existing manifest list signature:\n> \n> skopeo copy \\\n>   --multi-arch=linux/amd64,linux/arm64 \\\n>   --strip-sparse-manifest-list \\\n>   --remove-list-signatures \\\n>   docker://registry.k8s.io/pause:3.9 \\\n>   docker://your-private-registry/pause:3.9\n> \n> The trade-off is that the resulting manifest digest differs from the source since the manifest list is modified, but it works with any standard registry including Gitea.\n> \n> **Solution 2 — use a container registry that supports sparse manifest feature.** Requires configuration on the registry with `validation.manifests.indexes.platforms: none` (supported by the [distribution registry](https://github.com/distribution/distribution)).\n\nI use latest skopeo \n```\nskopeo-latest --version\nskopeo version 1.24.0-dev commit: 8d3d28a3150fcf17e86f1b9e7691458d55db490c\n```\n`--strip-sparse-manifest-list` is missing\n```\nskopeo-latest copy --multi-arch='linux/amd64,linux/arm64'  --strip-sparse-manifest-list --remove-list-signatures   \"docker://golangci/golangci-lint:v2.12.2\" \"docker://someregistry/images/mirror/golangci-test:v2.12.2\"\nFATA[0000] unknown flag: --strip-sparse-manifest-list\n```\nbut I noticed that in your pull request `--strip-removed-platforms --remove-list-signatures`, i've tried it and it helped to push image to our registry! Thank you!"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2892
        }
    ]
}