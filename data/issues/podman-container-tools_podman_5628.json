{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 5628,
    "title": "podman running kata containers is not cleanup up correctly.",
    "state": "closed",
    "labels": [
        "stale-issue",
        "locked - please file new issue/PR"
    ],
    "user": "rhatdan",
    "body": "```\n# podman ps\n# podman run -ti --runtime=/usr/bin/kata-runtime --rm  fedora sh\n# ^d\n# \n# podman ps \nERRO[0000] OCI Runtime kata is in use by a container, but is not available (not in configuration file or not installed) \nCONTAINER ID  IMAGE  COMMAND  CREATED  STATUS  PORTS  NAMES\n```\n",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "just curious, does `buildah containers` show anything or `podman ps -a`?"
        },
        {
            "user": "mheon",
            "body": "Why are you specifying full path to `--runtime`? Why not `--runtime=kata`? I think our default configuration file includes it now...\n\nIMO, NOTABUG, `--runtime=FULL-PATH` is reserved for CI only."
        },
        {
            "user": "rhatdan",
            "body": "podman run -ti --runtime=kata --rm  fedora echo hello\nError: error creating libpod runtime: default OCI runtime \"kata\" not found: invalid argument\n"
        },
        {
            "user": "rhatdan",
            "body": "Anyways this error went away, since I think something was screwed up. "
        },
        {
            "user": "mheon",
            "body": "The root `libpod.conf` we ship by default includes the runtimes:\n\n```\n# Kata Containers with the default configured VMM\nkata-runtime = [\n    \"/usr/bin/kata-runtime\",\n]\n\n# Kata Containers with the QEMU VMM\nkata-qemu = [\n    \"/usr/bin/kata-qemu\",\n]\n\n# Kata Containers with the Firecracker VMM\nkata-fc = [\n    \"/usr/bin/kata-fc\",\n]\n```\n\nContainers.conf includes only one of these (`kata`) - https://github.com/containers/common/blob/master/pkg/config/containers.conf#L372-L379"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ]
}