{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 12867,
    "type": "pull_request",
    "title": "switch podman image scp from depending on machinectl to just os/exec",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "cdoern",
    "body": "switch podman image scp from depending on machinectl to just os/exec\n\nmachinectl does not propogate error messages and adds extra lines in the output, exec.Cmd is able to clear the env besides PATH and TERM, and use the given UID and GID to execute the command properly.\n\nmachinectl is still used to create a user session. Ubuntu support is limited by this.\n\nSigned-off-by: cdoern <cdoern@redhat.com>\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\".  That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name.  Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n",
    "comments": [
        {
            "user": "cdoern",
            "body": "@Luap99 think I got all of the necessary changes in there, just made a utils function to start the machinectl sleep."
        },
        {
            "user": "eriksjolund",
            "body": "> This does not work because systemd-run does not create the required login session.\n> \n> I think you have to use both, first run `machinectl ... sleep inf` to make sure that the login session is created (just start the process), then run your systemd-run command, when that is done kill the machinectl process.\n\nI wonder if  `machinectl ... sleep inf`  is really needed.\nIt seems the command\n`systemd-run --machine=test11@ --quiet --user --collect --pipe --wait ...`\nstarts a login session.\n\n```\n[root@laptop ~]# useradd test11\n[root@laptop ~]# systemd-run --machine=test11@ --quiet --user --collect --pipe --wait loginctl\nSESSION  UID USER     SEAT  TTY\n      2 1000 esjolund seat0 tty2\n     32 1019 test11         \n\n2 sessions listed.\n[root@laptop ~]# systemd-run --machine=test11@ --quiet --user --collect --pipe --wait systemctl --user status\n● laptop\n    State: running\n     Jobs: 0 queued\n   Failed: 0 units\n    Since: Sat 2022-01-15 08:10:23 CET; 131ms ago\n   CGroup: /user.slice/user-1019.slice/user@1019.service\n           ├─session.slice \n           │ └─dbus-broker.service \n           │   ├─6371 /usr/bin/dbus-broker-launch --scope user\n           │   └─6374 dbus-broker --log 4 --controller 9 --machine-id c341780e88a04800982a41eea47639d3 --max-bytes 100000000000000 --max-fds 25000000000000 --max-matches 5000000000\n           ├─app.slice \n           │ └─run-u0.service \n           │   ├─6376 systemctl --user status\n           │   └─6378 less\n           └─init.scope \n             ├─6346 /usr/lib/systemd/systemd --user\n             └─6350 (sd-pam)\n[root@laptop ~]# cat /etc/fedora-release \nFedora release 35 (Thirty Five)\n[root@laptop ~]# \n```\n\n"
        },
        {
            "user": "cdoern",
            "body": "> > This does not work because systemd-run does not create the required login session.\n> > I think you have to use both, first run `machinectl ... sleep inf` to make sure that the login session is created (just start the process), then run your systemd-run command, when that is done kill the machinectl process.\n> \n> I wonder if `machinectl ... sleep inf` is really needed. It seems the command `systemd-run --machine=test11@ --quiet --user --collect --pipe --wait ...` starts a login session.\n> \n> ```\n> [root@laptop ~]# useradd test11\n> [root@laptop ~]# systemd-run --machine=test11@ --quiet --user --collect --pipe --wait loginctl\n> SESSION  UID USER     SEAT  TTY\n>       2 1000 esjolund seat0 tty2\n>      32 1019 test11         \n> \n> 2 sessions listed.\n> [root@laptop ~]# systemd-run --machine=test11@ --quiet --user --collect --pipe --wait systemctl --user status\n> ● laptop\n>     State: running\n>      Jobs: 0 queued\n>    Failed: 0 units\n>     Since: Sat 2022-01-15 08:10:23 CET; 131ms ago\n>    CGroup: /user.slice/user-1019.slice/user@1019.service\n>            ├─session.slice \n>            │ └─dbus-broker.service \n>            │   ├─6371 /usr/bin/dbus-broker-launch --scope user\n>            │   └─6374 dbus-broker --log 4 --controller 9 --machine-id c341780e88a04800982a41eea47639d3 --max-bytes 100000000000000 --max-fds 25000000000000 --max-matches 5000000000\n>            ├─app.slice \n>            │ └─run-u0.service \n>            │   ├─6376 systemctl --user status\n>            │   └─6378 less\n>            └─init.scope \n>              ├─6346 /usr/lib/systemd/systemd --user\n>              └─6350 (sd-pam)\n> [root@laptop ~]# cat /etc/fedora-release \n> Fedora release 35 (Thirty Five)\n> [root@laptop ~]# \n> ```\n\nnot sure if executing on a container is what we are looking for here, this functionality is similar to that of machinectl and I am unsure if this would propagate errors the way we want to. \n\n--machine operates much differently than --uid if I am not mistaken, @Luap99 @edsantiago WDYT?"
        },
        {
            "user": "edsantiago",
            "body": "I'm old and crotchety, and I really, _really_ do not want to learn about systemd-run and all this newfangled crap. Can someone ELI5 why `sudo` or `su -l -c \"%s\" USER` aren't enough? (preferably documenting this in the code itself, so noone else has to ask again)"
        },
        {
            "user": "Luap99",
            "body": "I think --machine is what we want because it seems to create the login session. I just was never able to make it work on my test vm. You should still get the correct exit code according to the man page."
        },
        {
            "user": "Luap99",
            "body": "> I'm old and crotchety, and I really, _really_ do not want to learn about systemd-run and all this newfangled crap. Can someone ELI5 why `sudo` or `su -l -c \"%s\" USER` aren't enough? (preferably documenting this in the code itself, so noone else has to ask again)\n\nBecause both sudo and su do not create a login session they just change the uid/gid. Podman needs the systemd session to function properly. The only way su/sudo will work when you set enable-linger for that user set."
        },
        {
            "user": "edsantiago",
            "body": ">The only way su/sudo will work when you set enable-linger for that user set.\n\nBut podman is completely unusable without `linger`, so in what possible situation would anyone use `podman image scp` on a user that doesn't have the magic `linger`?"
        },
        {
            "user": "Luap99",
            "body": "You do not need linger!!!! If you login in you systemd as a user systemd will create the user session for you and you can use podman just fine. If you logout the session is destroyed so all containers are stopped. \nLinger is only needed when you want to run containers at boot or after logout."
        },
        {
            "user": "cdoern",
            "body": "also @Luap99 I am having the same issue as you, `--machine` does not work on my machine and reports: `Failed to get machine PTY: No machine 'charliedoern@'` same fails with `charliedoern@.host`\n\neven when using `--collect --pipe`:\n\n```\nFailed to connect to bus: $DBUS_SESSION_BUS_ADDRESS and $XDG_RUNTIME_DIR not defined (consider using --machine=<user>@.host --user to connect to bus of other user)\n```\n\nthose env variables are in fact set too..."
        },
        {
            "user": "Luap99",
            "body": "@cdoern On which os did you test this? I tested on f34 and it failed but I just tried with f35 and @eriksjolund commands worked there. "
        },
        {
            "user": "cdoern",
            "body": "@Luap99 ubuntu and f34"
        },
        {
            "user": "eriksjolund",
            "body": "I tested a few examples on Fedora 35 with a brand new user. (linger is off). It seems to work.\n```\n[root@laptop ~]# cat /etc/fedora-release \nFedora release 35 (Thirty Five)\n[root@laptop ~]# useradd test26\n[root@laptop ~]# systemd-run --machine=test26@ --quiet --user --collect --pipe --wait podman run --rm docker.io/library/alpine echo hello\nTrying to pull docker.io/library/alpine:latest...\nGetting image source signatures\nCopying blob 59bf1c3509f3 done  \nCopying config c059bfaa84 done  \nWriting manifest to image destination\nStoring signatures\nhello\n[root@laptop ~]# systemd-run --machine=test26@ --quiet --user --collect --pipe --wait /bin/false ; echo $?\n1\n[root@laptop ~]# systemd-run --machine=test26@ --quiet --user --collect --pipe --wait /bin/true ; echo $?\n0\n[root@laptop ~]# systemd-run --machine=test26@ --quiet --user --collect --pipe --wait ls -l /non-existent ; echo $?\nls: cannot access '/non-existent': No such file or directory\n2\n[root@laptop ~]# systemd-run --machine=test26@ --quiet --user --collect --pipe --wait /bin/bash -c \"exit 34\" ; echo $?\n34\n[root@laptop ~]# \n```\nRelevant PR \nhttps://github.com/systemd/systemd/pull/17967\nhence __systemd v248__"
        },
        {
            "user": "cdoern",
            "body": "@eriksjolund I think we are going to stick with the current approach as a fix for the moment, but I am going to look into if there is a better way to start the login session. Your approach is preferable but does not work on f34 or ubuntu."
        },
        {
            "user": "edsantiago",
            "body": "It does not seem to be working when run as root (on my f35 laptop):\n```console\n$ sudo bin/podman image scp foo.bar/nonesuch/c_zr5sx1le5n:mytag esm@localhost::\nRunning as unit: run-re976e3d02b7c4edea1205642220f85b8.service\nRunning as unit: run-r04f0570cc1cd4e778ed6368f137c8032.service\n$ bin/podman images\n[ does not show foo.bar/nonesuch/anything]\n```\nNote also that the *expected* output of `image scp` is 'Copying blob...Writing manifest...Storing signatures...' but none of that appears, only `Running as unit`. Could you test this as root?"
        },
        {
            "user": "cdoern",
            "body": "sorry @edsantiago left an extra argument in `execSystemd` works now as expected."
        },
        {
            "user": "edsantiago",
            "body": "Now I get:\n```console\n$ sudo bin/podman image scp foo.bar/nonesuch/c_zr5sx1le5n:mytag esm@localhost::\nError: exit status 208\n```\nAdding `--log-level=debug` yields:\n```\nDEBU[0000] Executing via systemd: \"/usr/bin/sudo /usr/bin/systemd-run --pty -q --wait --uid 0 /home/esm/src/atomic/2018-02.podman/libpod/bin/podman --log-level=debug save --output /var/tmp/podman997723794 foo.bar/nonesuch/c_zr5sx1le5n:mytag\"\n```"
        },
        {
            "user": "cdoern",
            "body": "@edsantiago this seems to be an selinux issue, https://bugzilla.redhat.com/show_bug.cgi?id=1559409 not sure how to get around this one..."
        },
        {
            "user": "Luap99",
            "body": "`/usr/bin/sudo /usr/bin/systemd-run --pty -q --wait --uid 0 /home/esm/src/atomic/2018-02.podman/libpod/bin/podman --log-level=debug save --output /var/tmp/podman997723794 foo.bar/nonesuch/c_zr5sx1le5n:mytag` The command looks wrong should't it be `--uid <uid from esm>` instead of `--uid 0` which is pointless since it should load the image as user."
        },
        {
            "user": "Luap99",
            "body": "Also why do you use sudo when you are already root"
        },
        {
            "user": "cdoern",
            "body": "@Luap99 in the example @edsantiago  is giving the save is `--uid 0` and the load is `--uid <edsantiago>`\n\nissue here is `exit code 208` from systemd-run this works on ubuntu meaning the syntax is correct, the incorrect part is the selinux permssions on f35"
        },
        {
            "user": "cdoern",
            "body": "--collect --pipe works on fedora rather than --pty, let me try that\n\nSeLinux blocks the executing of the podman binary....... via exit code 203"
        },
        {
            "user": "edsantiago",
            "body": "/hold\n\nPlease wait until #12797 merges, then rebase on it. This PR cannot merge without at least some basic minimal testing. I'm doing it manually so far, but that's costing me all day."
        },
        {
            "user": "cdoern",
            "body": "sgtm @edsantiago the issues here are between selinux and systemd-run so I am trying to find a way to get this to work on fedora, works well on ubuntu."
        },
        {
            "user": "cdoern",
            "body": "@Luap99 @edsantiago systemd-run seems to be completely non functional when using --pty or --machine or --user on f35 due to selinux permissions as well as systemctl --user quirks. Going to investigate other options here because utilizing systemd-run is making this much more complicated than it has to be\n\ncc @rhatdan in case theres an easy way to get around --pty being prohibited with selinux"
        },
        {
            "user": "edsantiago",
            "body": "@cdoern please rebase on main in order to pick up tests. Then remove the NO TESTS NEEDED, and apply the following diffs\n```diff\ndiff --git a/test/system/120-load.bats b/test/system/120-load.bats\nindex f5ba93d8a..8bf785081 100644\n--- a/test/system/120-load.bats\n+++ b/test/system/120-load.bats\n@@ -104,10 +104,6 @@ verify_iid_and_name() {\n     # If we can't sudo, we can't test.\n     _sudo true || skip \"cannot sudo to $notme\"\n\n-    # FIXME FIXME FIXME: it'd be reeeeeeally nice if we could pass --root\n-    #                    to the non-self user, hence avoid vandalizing\n-    #                    their storage.\n-\n     # Preserve digest of original image; we will compare against it later\n     run_podman image inspect --format '{{.Digest}}' $IMAGE\n     src_digest=$output\n@@ -117,12 +113,8 @@ verify_iid_and_name() {\n     run_podman tag $IMAGE $newname\n\n     # Copy it there.\n-    # FIXME: the first '.*' in the expect string below is unfortunate; it's\n-    #        a workaround for Ubuntu which gripes:\n-    #        \"warning.*defaulting to su since machinectl is not available\"\n-    #        Reexamine this once #12829 is fixed\n     run_podman image scp $newname ${notme}@localhost::\n-    is \"$output\" \".*Copying blob .*Copying config.*Writing manifest.*Storing signatures\"\n+    is \"$output\" \"Copying blob .*Copying config.*Writing manifest.*Storing signatures\"\n\n     # confirm that image was copied. FIXME: also try $PODMAN image inspect?\n     _sudo $PODMAN image exists $newname\n@@ -132,13 +124,6 @@ verify_iid_and_name() {\n     run_podman image scp -q ${notme}@localhost::$newname\n\n     expect=\"Loaded image(s): $newname\"\n-    # FIXME FIXME FIXME: ubuntu has no machinectl, emits useless warning message instead\n-    if ! is_rootless; then\n-        # FIXME: root on fedora uses machinectl, which emits useless \\n and \\r (#12829)\n-        NL=$'\\n'\n-        CR=$'\\r'\n-        expect=\"$NL$expect$CR\"\n-    fi\n     is \"$output\" \"$expect\" \"-q silences output\"\n\n     # Confirm that we have it, and that its digest matches our original\n@@ -150,17 +135,14 @@ verify_iid_and_name() {\n     run_podman untag $IMAGE $newname\n\n     # Negative test for nonexistent image.\n-    # FIXME FIXME: cannot test on root, because it uses machinectl (#12829)\n-    if is_rootless; then\n-        # FIXME: error message is 2 lines, the 2nd being \"exit status 125\".\n-        # FIXME: is that fixable, or do we have to live with it?\n-        nope=\"nope.nope/nonesuch:notag\"\n-        run_podman 125 image scp ${notme}@localhost::$nope\n-        is \"$output\" \"Error: $nope: image not known.*\" \"Pulling nonexistent image\"\n-\n-        run_podman 125 image scp $nope ${notme}@localhost::\n-        is \"$output\" \"Error: $nope: image not known.*\" \"Pushing nonexistent image\"\n-    fi\n+    # FIXME: error message is 2 lines, the 2nd being \"exit status 125\".\n+    # FIXME: is that fixable, or do we have to live with it?\n+    nope=\"nope.nope/nonesuch:notag\"\n+    run_podman 125 image scp ${notme}@localhost::$nope\n+    is \"$output\" \"Error: $nope: image not known.*\" \"Pulling nonexistent image\"\n+\n+    run_podman 125 image scp $nope ${notme}@localhost::\n+    is \"$output\" \"Error: $nope: image not known.*\" \"Pushing nonexistent image\"\n\n     # Negative test for copying to a different name\n     run_podman 125 image scp $IMAGE ${notme}@localhost::newname:newtag\n```\nYou may want to evaluate the other FIXMEs. Whether or not you remove the `ubuntu skip` is up to you. "
        },
        {
            "user": "Luap99",
            "body": "@cdoern It still does not make sense to use systemd-run and sudo when you are already root. You can just exec the podman command normally in this case since you are already the correct user. The systemd-run part is only needed when you are root and want exec podman rootless. If I start scp rootless then only sudo is needed to start the other command as root."
        },
        {
            "user": "cdoern",
            "body": "@Luap99 I am going to circle back to this later today, i see what you're saying about systemd-run and I only use it in one place (unnecessarily I think) I will eliminate the parts I can, apply @edsantiago tests/diffs and repush this afternoon"
        },
        {
            "user": "cdoern",
            "body": "@edsantiago system test failures here :/ can't figure out if it is an scp issue or a test issue, any ideas?\n\nyeah, this exact test case works on both my f34 and 35 setups. could we have to chown containers.conf in the system tests?"
        },
        {
            "user": "edsantiago",
            "body": "The error in both cases (f34/f35 root) is:\n```\n...level=error msg=\"reading system config \\\"/tmp/podman-bats-1JwO7Hc.containers.conf\\\": [...]: permission denied\"\n```\nThat definition of containers.conf comes from https://github.com/containers/podman/blob/094665ac0cccc04c7797881ff2a27ff330e9a72e/test/system/helpers.bash#L39-L54\n\nHypothesis: this new systemd-foo scheme is (incorrectly) initializing the environment of the su'ed podman.\n\nConfirmation:\n```console\n# touch /root/containers-foo.conf\n# chmod 0 !$\n# CONTAINERS_CONF=!$ /path/to/podman image scp myimage esm@localhost::\n...\nERRO[0000] finding config on system: CONTAINERS_CONF file: stat /root/containers-foo.conf: permission denied\nError: exit status 1\n```\nSolution: tell systemd not to transfer root environment to user?"
        },
        {
            "user": "cdoern",
            "body": "issue with that @edsantiago is systemd-run is out of the picture and we are using runuser which says that it resets all of the environmental variables to the user that is being logged into, which is what should be happening, right? https://man7.org/linux/man-pages/man1/runuser.1.html"
        },
        {
            "user": "edsantiago",
            "body": ">we are using runuser which says that it resets all of the environmental variables\n\nMy reading of `runuser(1)` is the opposite: it *does not* reset environment, unless you feed it `-l`, which (apparently) you cannot do in combination with `-u`."
        },
        {
            "user": "rhatdan",
            "body": "BTW runuser == su, without going through PAM stack.  I wrote it in another life time."
        },
        {
            "user": "cdoern",
            "body": "think I got it, `sudo runuser -l charliedoern -c \"podman info\"` works, I will do that. @rhatdan yeah I am really just looking for alternatives that work on ubuntu (by default) and fedora, systemd-run didn't want to cooperate."
        },
        {
            "user": "cdoern",
            "body": "I am unsure if `-l` creates a persistent login session because `sudo runuser -l testingUser -c loginctl` only shows charliedoern and root. I will leave the machinectl login in here until that is figured out, @Luap99 do you have any idea if runuser does the login as we need it to?"
        },
        {
            "user": "cdoern",
            "body": "@edsantiago @Luap99 thoughts on this? I am not sure if runuser creates a login session so I left the machinectl login as is."
        },
        {
            "user": "edsantiago",
            "body": "I'm sorry, I can't help. \"User session\" is not a UNIX concept, it's a systemd concept, and one that I've never been able to understand. @giuseppe might know."
        },
        {
            "user": "rhatdan",
            "body": "runuser will NOT create a user session it is just setting the UID and perhaps running the bash init scripts.\nUser Sessions require pam_systemd to run, and runuser specifically does not execute the PAM stack."
        },
        {
            "user": "cdoern",
            "body": "Ok, that makes sense @rhatdan I think this is all set then @containers/podman-maintainers PTAL"
        },
        {
            "user": "cdoern",
            "body": "I think it might be possible to consolidate `transferRootless` and `transferRootful` now but on my first try it failed due to the uid/gid aspect of transferRootful, I think after this merges I'll open a separate PR to work on that."
        },
        {
            "user": "cdoern",
            "body": "@giuseppe any idea why this is failing? This error has happened to me before in my initial iterations of scp, do I need to UID/GID map as well in the SysProcAttr?\n\nthis works on f34 and f35 for me as is when using runc as the runtime"
        },
        {
            "user": "giuseppe",
            "body": "> @giuseppe any idea why this is failing? This error has happened to me before in my initial iterations of scp, do I need to UID/GID map as well in the SysProcAttr?\n\nno we don't neet to specify the map, it has simply to run with the specified UID/GID"
        },
        {
            "user": "giuseppe",
            "body": "the tests are failing because it doesn't find the `newuidmap` executable.  Please propagate also `PATH` in addition to `TERM`"
        },
        {
            "user": "cdoern",
            "body": "I initially tried to do this with PATH as well and got the same error, could be because I was not doing `PATH=...` I will try that"
        },
        {
            "user": "rhatdan",
            "body": "/approve\n/lgtm\n\nI am going to merge, since I believe this is an improvement over what we currently have, and we should get it into podman 4.0.\n@cdoern continue working on improving it. and fixing other bugs in podman image scp."
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/12867#\" title=\"Author self-approved\">cdoern</a>*, *<a href=\"https://github.com/containers/podman/pull/12867#issuecomment-1023172026\" title=\"LGTM\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12797
        }
    ]
}