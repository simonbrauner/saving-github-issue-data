{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2660,
    "type": "pull_request",
    "title": "fix(sync): fix dry-run failing consecutive runs by creating directories in target dir",
    "state": "open",
    "labels": [
        "kind/bug",
        "stale-pr"
    ],
    "user": "frq-asgard-josi",
    "body": "`skopeo sync --dry-run --dest dir` currently fails consecutive executions since the `func destinationReference` attempts to create directories even when running in dry-run mode and fails if these already exist.",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Tests failed. @containers/packit-build please check."
        },
        {
            "user": "frq-asgard-josi",
            "body": "> Thanks!\n> \n> > attempts to create directories even when running in dry-run mode\n> \n> That’s clearly undesirable.\n> \n> > and fails if these already exist.\n> \n> I can’t see how? `MkdirAll` is not supposed to fail in that situation. What is failing exactly?\n\nSince the destination exists, `os.Stat(destination)` doesn't return any err and we return `Refusing to overwrite destination directory` (Line 179)"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": []
}