{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7278,
    "title": "Cannot run any podman commands",
    "state": "closed",
    "labels": [
        "kind/bug",
        "stale-issue",
        "locked - please file new issue/PR"
    ],
    "user": "j00bar",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind bug\n\n**Description**\n\nCannot run any podman commands.\n\n**Steps to reproduce the issue:**\n\n1. Run `podman` with or without subcommands, with or without arguments.\n\n**Describe the results you received:**\n\n```\n[jag@bad-chicken ~]$ podman\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n[jag@bad-chicken ~]$ podman ps\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n[jag@bad-chicken ~]$ podman --help\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n[jag@bad-chicken ~]$ podman -v\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n```\n\n**Describe the results you expected:**\nAnything at all.\n\n**Output of `podman version`:**\n\n```\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n```\n\n**Output of `podman info --debug`:**\n\n```\nERRO[0000] error validating engine configs: static directory must be an absolute path - instead got \"test/libpod\" \n```\n\n**Package info (e.g. output of `rpm -q podman` or `apt list podman`):**\n\n```\npodman-2.0.4-1.fc32.x86_64\n```\n\n**Additional environment details (AWS, VirtualBox, physical, etc.):**\nPhysical.",
    "comments": [
        {
            "user": "mheon",
            "body": "Appears that you're running rootless. Can you check if you have a `~/.config/containers/libpod.conf` or `~/.config.containers/containers.conf` and provide the contents of each if you do?"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "Since we have had no response @j00bar I figure this issue no longer exists, closing.  Reopen if I am mistaken"
        }
    ]
}