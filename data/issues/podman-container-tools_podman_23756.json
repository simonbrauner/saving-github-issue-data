{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 23756,
    "type": "issue",
    "title": "rootFsSize changing on a created (not running) container?",
    "state": "closed",
    "labels": [
        "flakes",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "```\nnot ok 162 |090| events - container inspect data - journald in 6871ms\n....\n   SizeRootFs: 11762614    ->   11761590\n```\nIn parallel tests. Only one instance, [f39 root](https://api.cirrus-ci.com/v1/artifact/task/5793292044992512/html/sys-podman-fedora-39-root-host-boltdb.log.html#t--00162p). Feel free to verify for yourself that `SizeRootFs` is the only diff.\n\nI'm having trouble understanding how this can happen. `rootFsSize()`[1] is image size + container size. A created container should not change size. Can the image? `podman tag existingimage newname` does not change the size of `existingimage`. Is there some other action that could? Note that load/save tests are not parallelizable.\n\n [1] https://github.com/containers/podman/blob/d04f0d2802832dfbe47a29f592fc023e7df70607/libpod/container_internal.go#L70\n\nAlmost certainly this needs to be addressed in the test, not in podman, but I'd like to understand it first.",
    "comments": [
        {
            "user": "Luap99",
            "body": "```\n// ImageSize computes the size of the image's layers and ancillary data.\nImageSize(id string) (int64, error)\n\n...and the in ImageSize()\n\t// Count big data items.\n\tnames, err := imageStore.BigDataNames(id)\n\tif err != nil {\n\t\treturn -1, fmt.Errorf(\"reading list of big data items for image %q: %w\", id, err)\n\t}\n\tfor _, name := range names {\n\t\tn, err := imageStore.BigDataSize(id, name)\n\t\tif err != nil {\n\t\t\treturn -1, fmt.Errorf(\"reading size of big data item %q for image %q: %w\", name, id, err)\n\t\t}\n\t\tsize += n\n\t}\n\n```\n\nThe word ancillary seems to suggest it counts more than just the layers, at least \"BigData\" is counted as well but I am not sure what exactly it stores and if other processes modify it after the image/container is created."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "edsantiago",
            "body": "Still [flaking](https://api.cirrus-ci.com/v1/artifact/task/4617516838813696/html/sys-podman-rawhide-rootless-host-sqlite.log.html#t--00150p)"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 24221
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 24264
        }
    ]
}