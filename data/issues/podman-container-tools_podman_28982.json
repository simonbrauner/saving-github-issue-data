{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28982,
    "type": "pull_request",
    "title": "fix: clearer error publishing a privileged port to a specific IP on macOS",
    "state": "closed",
    "labels": [],
    "user": "stefan8",
    "body": "Publishing a privileged port to a specific IP with `podman machine` on macOS failed with an opaque error. gvproxy binds the port on the host and runs unprivileged, and macOS rejects binding a `< 1024` port to a specific IP. This returns a clearer error for that case and adds unit tests.\n\n#### Checklist\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits (`git commit -s`).\n- [x] Referenced issues using `Fixes: #28009` in commit message (if applicable)\n- [x] Tests have been added/updated (or no tests are needed)\n- [x] Documentation has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] Release note entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nPublishing a privileged port (< 1024) to a specific IP address with `podman machine` on macOS now returns a clearer error explaining that gvproxy binds the port on the host.\n```\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        }
    ],
    "cross_references": []
}