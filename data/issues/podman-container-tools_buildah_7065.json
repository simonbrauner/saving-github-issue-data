{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 7065,
    "type": "pull_request",
    "title": "test(bud): cover symbolic COPY/ADD --chmod",
    "state": "open",
    "labels": [],
    "user": "paoloantinori",
    "body": "Upstream main already vendors imagebuilder v1.2.22 (#7068), the release tag containing openshift/imagebuilder#324, so the original vendor bump is no longer needed. What remains, and what this PR now adds, is bud coverage so the regression cannot come back silently:\n\n## What\n- `tests/bud/copy-chmod-symbolic`: a Containerfile exercising symbolic `COPY --chmod` forms (`u+x,go-rwx` on a 0644 file -> 0700; `a+rX,go-w` on a 0755 script -> 0755, capital `X` keeping the exec bit) plus a `Dockerfile.bad` asserting the still-invalid bare `rwxrwxrwx` form fails with `Error parsing chmod`.\n- Two `bud.bats` cases wiring those fixtures.\n\n## Why\n`checkChmodConversion` rejected every symbolic value at dispatch time before openshift/imagebuilder#324 (root cause of #6938); the executor was already symbolic-aware. With the fix vendored via v1.2.22, these tests pin both the accepted clause forms and the rejected ones at the layer that used to regress.\n\nFixes #6938 (test coverage; the functional fix is v1.2.22 vendored in #7068).",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "@paoloantinori this LGTM in general, however you need to resolve a number off conflicts.  While you're at it, can you revendor with the latest release rather then the latest from upstream?"
        },
        {
            "user": "paoloantinori",
            "body": "@TomSweeneyRedHat rebased and resolved. The conflicts fell out cleanly: #7068 already vendored imagebuilder v1.2.22, which is the release tag containing the symbolic-chmod fix (openshift/imagebuilder#324), so I took main's side for go.mod/go.sum and the vendored files.\n\nThe PR is now tests-only: bud coverage for symbolic COPY/ADD --chmod. Valid clause forms are asserted by resulting mode (u+x,go-rwx on a 0644 source -> 0700; a+rX,go-w on a 0755 script -> 0755; a+rX alone on a non-executable source stays 0644, covering the conditional capital X), plus the still-rejected bare rwxrwxrwx form failing at dispatch. DCO sign-off is also in place now."
        },
        {
            "user": "paoloantinori",
            "body": "thanks. i've updated the pr"
        }
    ],
    "cross_references": []
}