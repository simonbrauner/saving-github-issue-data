{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 1913,
    "type": "pull_request",
    "title": "Fix copy/add behaviour with .dockerignore",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "JamesWrigley",
    "body": "When a directory is being copied/added, the whole directory was being copied in unless there was an exclusion (e.g., `!include-this-file`) in the `.dockerignore`.\n\nSo for this `Dockerfile`:\n```Dockerfile\nFROM alpine\nCOPY . /tmp/foo\nRUN ls /tmp/foo\n```\n\nWith this `.dockerignore`:\n```bash\n# !foo\nquux\n```\n\nA file named `quux` in the build context would be copied in, unless the first line was uncommented. This is fixed by checking if there are any patterns at all, instead of for an exclusion.\n\nNote: the `run_buildah run myctr2 ls -l matched` test fails on my machine with: `printf: write error: Resource temporarily unavailable`. I'm not sure why this happens, as far as I can tell it's correct.",
    "comments": [
        {
            "user": "rh-atomic-bot",
            "body": "Can one of the admins verify this patch?\nI understand the following commands:\n- `bot, add author to whitelist`\n- `bot, test pull request`\n- `bot, test pull request once`"
        },
        {
            "user": "JamesWrigley",
            "body": "Hmm, while I believe the behaviour is now correct, this change would also trigger #1714 more often :\\"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@JamesWrigley  and @saschagrunert thanks for the dockerignore fixes, but are they fixing the same issue?  #1913 and #1914?  I've not taken a deep dive."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@giuseppe PTAL"
        },
        {
            "user": "saschagrunert",
            "body": "> @JamesWrigley and @saschagrunert thanks for the dockerignore fixes, but are they fixing the same issue? #1913 and #1914? I've not taken a deep dive.\n\nAh yes, sorry I did not see that. Feel free to pick the solution you prefer. :)"
        },
        {
            "user": "JamesWrigley",
            "body": "They do fix the same issue, but I think @saschagrunert's fix is better :)"
        },
        {
            "user": "rhatdan",
            "body": "Let's get @saschagrunert fix in."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 1914
        }
    ]
}