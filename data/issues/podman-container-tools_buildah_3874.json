{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 3874,
    "type": "pull_request",
    "title": "imagebuildah: if user sets `--jobs=0` they expect no parallelism",
    "state": "closed",
    "labels": [
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "flouthoc",
    "body": "Lot of users and tests set --jobs=0 they are expecting no parallelism\nbetween stages but we still end up running builds in parallel for\nmulti-arch builds which indirectly executes stages in parallel since builds are parallel.\n\nExample tests like https://github.com/containers/buildah/blob/main/tests/bud.bats#L3609 set `--jobs=0` but they still execute build for similar name but different arch in parallel.\n\n**[Note:]** This PR does not fix more that one race condition which is seen when multi-arch builds are performed without `--jobs=0` as documented here: https://github.com/containers/buildah/issues/3710#issuecomment-1081613407 but it should fix flakes in CI as reported here: https://github.com/containers/buildah/issues/3710\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/buildah/pull/3874#\" title=\"Author self-approved\">flouthoc</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fbuildah).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/buildah/blob/main/OWNERS)~~ [flouthoc]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "flouthoc",
            "body": "This should fix the flake as tests in CI use `--jobs=0` but this does not means that actual race is fixed which can be seen again without `--jobs=0` for `multi-arch` builds."
        },
        {
            "user": "nalind",
            "body": "> Lot of users and tests set --jobs=0 they are expecting no parallelism between stages but we still end up running builds in parallel for multi-arch builds which indirectly executes stages in parallel since builds are parallel.\n> \n> Example tests like https://github.com/containers/buildah/blob/main/tests/bud.bats#L3609 set `--jobs=0` but they still execute build for similar name but different arch in parallel.\n\nThat's why that test specifies `--jobs=0`.  The default is to behave as though `--jobs=1` was specified."
        },
        {
            "user": "flouthoc",
            "body": "@nalind If I inferred it correctly `--jobs=0` should mean no parallelism at all but we still do builds for each platform in parallel manner."
        },
        {
            "user": "nalind",
            "body": "The intent of that test is definitely maximum parallelization."
        },
        {
            "user": "flouthoc",
            "body": "@nalind Oh i see but then I think users have no way to serialize `mult-arch` builds. "
        },
        {
            "user": "cevich",
            "body": "> users have no way to serialize mult-arch builds.\n\nI think it's incorrect to equate serialized builds with serialized pulls, one doesn't necessarily imply the other.  Though I appreciate it vastly complicates the fix.  As a user doing an operation requiring a multi-arch pull, I expect that pull to happen as fast as my bandwidth allows - meaning in parallel when possible."
        },
        {
            "user": "nalind",
            "body": "> @nalind Oh i see but then I think users have no way to serialize `mult-arch` builds.\n\nThat's the default, and what happens with `--jobs=1`."
        },
        {
            "user": "flouthoc",
            "body": "> > users have no way to serialize mult-arch builds.\n> \n> I think it's incorrect to equate serialized builds with serialized pulls, one doesn't necessarily imply the other. Though I appreciate it vastly complicates the fix. As a user doing an operation requiring a multi-arch pull, I expect that pull to happen as fast as my bandwidth allows - meaning in parallel when possible.\n\n@cevich Pulls are not causing the issue but i think builds are causing the issue, its easily reproduce-able  when you run `bud-multiple-platform-no-run` a few times.\n\nI'm going to close this PR since it is not doing what i expected it to do."
        }
    ],
    "cross_references": []
}