{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6662,
    "type": "issue",
    "title": "Probably ineffective code in internal/mkcw/archive_test.go:TestArchive",
    "state": "closed",
    "labels": [
        "kind/bug",
        "stale-issue"
    ],
    "user": "mtrmac",
    "body": "- (Original motivation) The linter reports `tautological condition: nil == nil` after the “bail now if we got an error we didn't expect” code, the `if err == nil` is always true\n- It turns out `ignoreChainRetrievalErrors` is only consulted within the error handling, it is not an input to anything\n- If `IgnoreAttestationErrors`, the errors are turned into warnings, so they should never be _reported_ to the caller; the test sees them, that’s a problem, regardless of the `ignore…` values\n- For any kind of completely unexpected error type, the test just `return`s and does not fail. This _might_ be intentional but I see no evidence for it.\n\nCc: @nalind ",
    "comments": [
        {
            "user": "nalind",
            "body": "How did you get the linter to warn about this?  I don't see it when I use `make lint`, even when swapping in the linter configurations used by podman and skopeo, so I don't think our CI would be able to tell if it was resolved."
        },
        {
            "user": "mtrmac",
            "body": "This was in my IDE; the backend is probably some smarter version of `gopls check $(find internal/mkcw -name '*.go')`"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 6664
        }
    ]
}