{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 15444,
    "type": "pull_request",
    "title": "Rootless Docker API socket alias can be exposed with user mode systemd-tmpfiles",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "foriequal0",
    "body": "Rootless Docker daemon exposes its API socket on\n`$XDG_RUNTIME_DIR/docker.sock`. On tmpfiles.d, `%t` is same as\n`$XDG_RUNTIME_DIR` in `--user` mode, and `/run` otherwise.\nWe can reuse the same config file for both mode with this change.\n\n`systemd-tmpfiles` reads \"user\" configurations in\n`/usr/share/user-tmpfiles.d` when `--user` mode is set.\nUser unit `systemd-tmpfiles-setup.service` can be enabled to alias\nrootless socket through systemd-tmpfiles.\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nRootless Docker API socket alias can be exposed with user mode systemd-tmpfiles\n```\n",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Thanks @foriequal0 \nLGTM\nYes but I don't think we support podman-docker rpm in the upstream. We will need to fix this in the release spec.\nAm I right @lsm5 \n/approve\n/lgtm\n\n"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/15444#\" title=\"Author self-approved\">foriequal0</a>*, *<a href=\"https://github.com/containers/podman/pull/15444#issuecomment-1225680353\" title=\"LGTM\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "lsm5",
            "body": "We do have `podman-docker` subpackage upstream. I'll update the spec files to account for podman-docker.conf as well. It's affecting #15453 packit runs."
        },
        {
            "user": "lsm5",
            "body": "I got my streams mixed up earlier. I meant we do have the podman-docker package in Fedora and the package on `rhcontainerbot/podman-next` copr also has podman-docker. So, it would be nice for the conf files to live upstream itself, than be maintained separately downstream. "
        },
        {
            "user": "rhatdan",
            "body": "SGTM"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 15453
        }
    ]
}