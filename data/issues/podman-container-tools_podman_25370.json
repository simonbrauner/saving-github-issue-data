{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 25370,
    "type": "issue",
    "title": "Support compose.yml lifecycle hooks",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "davebenvenuti",
    "body": "### Feature request description\n\ndocker-compose supports `post_start` and `pre_down` hooks [as of Docker compose 2.30.0](https://docs.docker.com/compose/how-tos/lifecycle/).  If podman-compose aims to have feature parity these would be nice to have there as well.\n\n### Suggest potential solution\n\n- Given a `post_start` entry which conforms to the [Docker Compose spec](https://docs.docker.com/compose/how-tos/lifecycle/), `exec` this command in the container after it `start`s.\n- Given a `pre_down` entry which conforms to the [Docker Compose spec](https://docs.docker.com/compose/how-tos/lifecycle/), `exec` this command in the container before it goes `down`.\n\n### Have you considered any alternatives?\n\nThis can be worked around with a custom script but given it's a feature defined in the Docker Compose spec, it would be nice to have native support in `podman-compose`.\n\n### Additional context\n\n_No response_",
    "comments": [
        {
            "user": "Luap99",
            "body": "You can use docker-compose against the podman service, podman-compose is not maintained or directly supported by the podman developers. so you need to ask podman-compose to implement this.\nhttps://github.com/containers/podman-compose/"
        }
    ],
    "cross_references": []
}