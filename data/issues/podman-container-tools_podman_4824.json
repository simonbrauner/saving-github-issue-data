{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 4824,
    "type": "pull_request",
    "title": "more BATS tests",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "- run: --name (includes 'podman container exists' tests)\n- run: --pull (always, never, missing)\n- build: new test for ADD URL (#4420)\n- exec: new test for issue #4785 (pipe getting lost)\n- diff: new test\n- selinux (mostly copied from docker-autotest)\n\nPlus a bug fix: the wait_for_output() helper would continue\nchecking, eventually timing out, even if the container had\nalready exited (probably because of an error). Fix: as\npart of the loop, run 'podman inspect' and bail out if\ncontainer is not running. Include exit code and logs.\n\nSigned-off-by: Ed Santiago <santiago@redhat.com>",
    "comments": [
        {
            "user": "edsantiago",
            "body": "[Test failure was because podman-remote does not emit \"Trying to pull ...\" messages; so there's no way to distinguish I-pulled from image-was-already-here. Decided to just `skip_if_remote`.]"
        },
        {
            "user": "edsantiago",
            "body": "```console\n$ ./bin/podman run --rm --privileged --userns=host quay.io/libpod/alpine_labels:latest cat -v /proc/self/attr/current\nWARNING: The same type, major and minor should not be used for multiple devices.\n```\nAny ideas?"
        },
        {
            "user": "edsantiago",
            "body": "It's not a flake: rerunning fails the same way. There's something weird about the f30 test image, and I suspect it's a real problem. Unfortunately it will be almost impossible to debug. I've submitted https://github.com/opencontainers/runtime-tools/pull/701 as a long-term future way of getting a slight clue about what's wrong, but even if that's accepted (which is unlikely because its CI is failing and I have no idea why) it would take time to vendor this into podman. "
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Maybe I'm missing something, but I'm not seeing the warning when I run it \"by hand\" on my F30 vm with upstream Podman.  However the output is missing a carriage return, it couldn't be that could it?\n\n```\n$ podman run --rm --privileged --userns=host quay.io/libpod/alpine_labels:latest cat -v /proc/self/attr/current\nunconfined_u:system_r:container_runtime_t:s0^@[tom@localhost ~]$\n```\n\n"
        },
        {
            "user": "edsantiago",
            "body": "No, it works fine (no warning message) on all the f30 systems I've tried. The problem is something specific to the CI one. (And no, it's not the NUL-instead-of-NL terminator. That's just the way `/proc/self/attr/current` works everywhere). Thanks for checking!"
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/4824#\" title=\"Author self-approved\">edsantiago</a>*, *<a href=\"https://github.com/containers/libpod/pull/4824#pullrequestreview-341317215\" title=\"Approved\">jwhonce</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [jwhonce]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "sudo podman run --rm --privileged quay.io/libpod/alpine_labels:latest cat -v /proc/self/attr/current\nWARNING: The same type, major and minor should not be used for multiple devices.\n\nSomething is wacky with --privileged.  Looks like a bug."
        },
        {
            "user": "rhatdan",
            "body": "Well the good thing, is this bug exists with older podman.\n\npodman run --privileged alpine echo hello\nWARNING: The same type, major and minor should not be used for multiple devices.\nhello\n\nrpm -q podman\npodman-1.6.2-2.fc31.x86_64\n"
        },
        {
            "user": "rhatdan",
            "body": "Added a little extra ouput\n\nWARNING: The same type, major and minor should not be used for multiple devices. /dev/null\n\nLooks like we have two `/dev/null` added."
        },
        {
            "user": "rhatdan",
            "body": "Looks like the way my host was configured.\n\n```\nsh-5.0# mknod /dev/foo c 1 3 \nsh-5.0# podman run --privileged alpine echo hello\nWARNING: The same type, major and minor should not be used for multiple devices.\nsh-5.0# rm -f /dev/foo \nsh-5.0# podman run --privileged alpine echo hello\nhello\n```\n"
        },
        {
            "user": "edsantiago",
            "body": "https://github.com/opencontainers/runtime-tools/pull/701 is merged, so some day we'll get greater visibility into what the problem is, but that could take years (new runtime-tools release, then a podman revendor). ITM I've simply special-cased the warning and ignored it. The warning [will still appear in test logs](https://api.cirrus-ci.com/v1/task/5088121775718400/logs/system_test.log), in case anyone wants to fix it, but the selinux test will not fail because of it."
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 4772
        }
    ]
}