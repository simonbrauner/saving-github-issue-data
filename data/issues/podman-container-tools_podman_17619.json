{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 17619,
    "title": "Add custom subcommand for podman (or podman-remote) CLI",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-issue",
        "podman-desktop"
    ],
    "user": "benoitf",
    "body": "### Feature request description\n\nThere are existing set of commands for podman.\n\nI would like to be able to add my own sub-command\n\nfor example I could add my own `podman foo` command\n\n\n\n### Suggest potential solution\n\nIt could be implemented like for common CLI. Example `git` cli\n\nif I have in my path executables starting by `podman-` (in the case of git it's `git-`) then it adds the command\n\nexample with git:\n\nif I add a new file \n```\n/usr/local/bin/git-foo\n```\n\nwith content\n```\necho \"executing the custom foo\"\n```\n\nI can then invoke `git foo` command\n\n\nI expect to have the same way using podman, that all the `podman-****` commands could be invoked using `podman ****`\n\n### Have you considered any alternatives?\n\ncould be some fancy plug-ins systems but using something very simple and commonly used looks easier.\n\n### Additional context\n\nAdd any other context or screenshots about the feature request here.",
    "comments": [
        {
            "user": "vrothberg",
            "body": "The last time we've discussed a plug-in system (Podman f2f), there was consensus on not doing one.  Counter arguments I recall:\n* Unclear requirements.  A simple system does not meet complex requirements such as hooking into existing commands.\n* Docker already has a plugin system that is being deprecated.\n* Plugins can conflict with future Podman commands.\n* Simple plugins only executing other binaries don't require Podman to call them.\n\nI personally think that this should be use-case driven.  A certain use case (e.g., Compose) may very well get an exception that doesn't require a generic solution."
        },
        {
            "user": "slemeur",
            "body": "cc @vyasgun "
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ]
}