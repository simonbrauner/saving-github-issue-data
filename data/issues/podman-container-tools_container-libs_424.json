{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 424,
    "type": "pull_request",
    "title": "seccomp: allow perf_event_open if CAP_PERFMON",
    "state": "closed",
    "labels": [
        "common"
    ],
    "user": "blue42u",
    "body": "Currently `perf_event_open` is only allowed if both `CAP_SYS_ADMIN` and `CAP_PERFMON` are enabled. `CAP_SYS_ADMIN` is a very overloaded capability and is best avoided. This PR enables `perf_event_open` if either (or both) capabilities are enabled. In particular, this enables a container to profile itself by only enabling `CAP_PERFMON`.\n\nThis change does not deny anything new, nor does it enable `perf_event_open` by default. In summary:\n\n| Capabilities | `perf_event_open` return (before) | `perf_event_open` return (after) |\n|---|---|---|\n| `CAP_PERFMON` + `CAP_SYS_ADMIN` | No error | No error |\n| `CAP_PERFMON` | `EPERM` | No error |\n| `CAP_SYS_ADMIN` | `ENOSYS` | No error |\n| (none of the above) | `EPERM` | `EPERM` |",
    "comments": [
        {
            "user": "podmanbot",
            "body": "✅ A new PR has been created in buildah to vendor these changes: **https://github.com/containers/buildah/pull/6461**"
        },
        {
            "user": "mtrmac",
            "body": "@giuseppe PTAL. Historically I can see https://github.com/containers/common/pull/2040/commits/daa81f1363ffc2d7e550a69c8dedf9d52ccb7ffe was _already_ supposed to enable this, which makes me worried about this PR."
        },
        {
            "user": "Luap99",
            "body": "cc @martinetd "
        },
        {
            "user": "giuseppe",
            "body": "> @giuseppe PTAL. Historically I can see [containers/common@daa81f1](https://github.com/containers/common/commit/daa81f1363ffc2d7e550a69c8dedf9d52ccb7ffe) was _already_ supposed to enable this, which makes me worried about this PR.\n\nthis new PR enables it with CAP_PERFMON which seems to me like a good idea"
        },
        {
            "user": "mtrmac",
            "body": "Thanks @blue42u and @martinetd !"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 6461
        }
    ]
}