{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28979,
    "type": "pull_request",
    "title": "docs: clarify save/load vs export/import distinction",
    "state": "closed",
    "labels": [],
    "user": "stefan8",
    "body": "The `save`/`load` and `export`/`import` man pages cross-reference each other but never say how the two workflows differ. This adds a short note to each, pointing at the complementary command (images with their layers/history vs a flattened container filesystem).\n\n#### Checklist\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits (`git commit -s`).\n- [x] Referenced issues using `Fixes: #22155` in commit message (if applicable)\n- [x] Tests have been added/updated (or no tests are needed)\n- [x] Documentation has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] Release note entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "inknos",
            "body": "LGTM! thanks a lot"
        },
        {
            "user": "ashley-cui",
            "body": "LGTM"
        }
    ],
    "cross_references": []
}