{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28574,
    "type": "pull_request",
    "title": "Allow installing systemd units without systemd build tag",
    "state": "open",
    "labels": [
        "stale-pr"
    ],
    "user": "Gelbpunkt",
    "body": "<!--\nThanks for sending a pull request!\n\nFor more detailed information, please review our contributing guidelines:\nhttps://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests\n-->\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nThe install.systemd target now installs sytemd units even when built without the systemd build tag\n```\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "tmt tests failed for commit b5bd5d5b268533fcb7beb051dbcbf7c5911011a8. @lsm5, @psss, @thrix please check."
        },
        {
            "user": "Gelbpunkt",
            "body": "Hmm. Looking at the FreeBSD failure, perhaps I should just gate this on `GOOS = linux`?"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "Luap99",
            "body": "likely need to make `podman-release-%.tar.gz` just call make install there then and not each target which seems unnecessary regardless in order to make the freebsd build work"
        },
        {
            "user": "lsm5",
            "body": "> Would it make sense to always allow explicit calls to install.systemd but gate on the build tag as part of the install target then I do not think we would break anyone?\n\nUnless I read this wrong, gating install target on the systemd build tag should _also_ work for linux distros that have no systemd dependency at all. \n\nBut then postmarketos would need to use systemd as a build tag even though it's not actually building with systemd (assuming they're cool with it).\n\n"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": []
}