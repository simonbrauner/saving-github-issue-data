{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 1014,
    "type": "pull_request",
    "title": "ci: do not use custom ssh user for image tests and update skopeo clone url",
    "state": "open",
    "labels": [
        "CI"
    ],
    "user": "Luap99",
    "body": "ci: do not use custom ssh user for image tests\n\nWith lima we already run as rootless user so just run the tests as that\nuser.\n\n---\n\nci: update skopeo clone url to the new org\n\nThe link is redirected so it is fine but it is better to just use the\nnew repo URL to avoid the redirect.",
    "comments": [],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 1010
        }
    ]
}