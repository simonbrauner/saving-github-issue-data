{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 9109,
    "type": "pull_request",
    "title": "Fix #6618",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "Obsoletes #9092 \n\nEnsure that the networks list is populated, even when only the default network is in use.",
    "comments": [
        {
            "user": "mheon",
            "body": "E2E tests are timing out. It looks like the tests we added aren't the issue, too - they're running fine and not marked as slow.\n\nIt looks like the timeout is 30 minutes here, not 90? @cevich PTAL"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\nassuming happy tests"
        },
        {
            "user": "cevich",
            "body": "> It looks like the timeout is 30 minutes here, not 90\n\nFWIW: There are multiple timeouts in-play:\n\n* Ginkgo has one internally, for communication between multiple nodes.  I don't know how to see or set this.\n* There's an overall ginkgo timeout set by the `GINKGOTIMEOUT` Makefile variable.\n* Cirrus-CI has a hard-coded/unchangeable 2-hour timeout per task\n* We set several Cirrus-CI timeouts for tasks via the `timeout_in` option in `cirrus.yml`.\n\nFor the last item, I don't see any of them set lower than 60min."
        },
        {
            "user": "mheon",
            "body": "Failures are, unfortunately, consistent. Damn."
        },
        {
            "user": "mheon",
            "body": "Ah, it was a legitimate issue with the PR, that was for some reason hanging the tests instead of causing them to fail?"
        },
        {
            "user": "cevich",
            "body": "Sorry, my eyeballs aren't nearly as well-trained as yours when it comes to the tests and podman internals.  I only remember hitting this node-hang before in different contexts, a long while ago.  In every case I remember, it was the tests actually hanging because of a legitimate (i.e. bug) reason.  Though arguably, a test-bug could be caused by some CI-environmental goof, so I wouldn't completely rule that out, it just seems less-likely since master is passing (I assume).  It's also possible the bug was always there, but timing-dependent, and the PR's changes simply tickle it in the wrong way."
        },
        {
            "user": "mheon",
            "body": "OK, green now. @containers/podman-maintainers PTAL"
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/9109#\" title=\"Author self-approved\">mheon</a>*, *<a href=\"https://github.com/containers/podman/pull/9109#pullrequestreview-577069090\" title=\"Approved\">saschagrunert</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/master/OWNERS)~~ [mheon,saschagrunert]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 9092
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 5580
        },
        {
            "organization": "traefik",
            "repository": "traefik",
            "number": 5730
        }
    ]
}