{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 4500,
    "type": "issue",
    "title": "cannot set a static IP if joining additional CNI networks ",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "matihost",
    "body": "```\nsudo podman network create --subnet '172.25.0.0/24' mynetwork\nsudo podman run -d --rm --name instance0 --network mynetwork --ip 172.25.0.200 image:version\nError: error running container create option: cannot set a static IP if joining additional CNI networks: invalid argument\n```\n\nWhen '--ip 172.25.0.200' is removed, it assigns next available IP from the subnet.\n\n\n\npodman version\nVersion:            1.6.1\n\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Hmmm. This ought to be possible, but we'll need to talk to the CNI folks to make sure it's supported now."
        },
        {
            "user": "mheon",
            "body": "I'll take this."
        },
        {
            "user": "mheon",
            "body": "#4542 to fix"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 4542
        }
    ]
}