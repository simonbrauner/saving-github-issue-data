{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29083,
    "type": "pull_request",
    "title": "podman log-level debug must produce the same oci runtime errors",
    "state": "closed",
    "labels": [],
    "user": "Luap99",
    "body": "I do not understand this condition in the code, not passing the log file to the runtime when run with log level debug makes no sense. It means we do not get the proper error message from the runtime so debug logging shows a much worse error:\n\"container create failed (no logs from conmon)...\"\n\nWhen the actual error is the command is not in $PATH for example. So to fix this just remove the log level check. Also not the support json flag condition is done inside execOCILog().\n\n\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nProperly display oci runtime errors when running with `--log-level debug` \n```\n",
    "comments": [
        {
            "user": "inknos",
            "body": "LGTM"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29072
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29135
        }
    ]
}