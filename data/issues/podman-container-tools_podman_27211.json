{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 27211,
    "type": "issue",
    "title": "Ambiguous Podman Multi-Arch Build Output: Silent Single-Arch Image Creation",
    "state": "open",
    "labels": [
        "kind/feature",
        "macos",
        "triaged"
    ],
    "user": "kaovilai",
    "body": "### Feature request description\n\nWhen migrating from Docker to Podman, the multi-arch build workflow has several differences that create friction for users familiar with Docker's approach. While these are not functional blockers, they significantly impact ease of use and require rewriting existing build scripts.\n\n### Current Docker workflow:\n```bash\n# Single command builds all platforms into a multi-arch image\ndocker buildx build --platform linux/amd64,linux/arm64 -t myimage:tag .\n\n# Push works as expected\ndocker push myimage:tag\n```\n\n### Current Podman workflow:\n```bash\n# Comma-separated platforms work, but require --manifest flag to create multi-arch image\n# Using -t with multiple platforms only produces single arch image\npodman build --platform linux/amd64,linux/arm64 --manifest myimage .\n\n# Must use 'manifest push' subcommand instead of regular push\n# Regular 'podman push' only pushes single arch\npodman manifest push myimage docker://registry/myimage:tag\n```\n\n### Issues:\n\n1. **Must use `--manifest` flag for multi-arch**: While comma-separated `--platform` values work, using `-t/--tag` with multiple platforms only produces a single architecture image. You must use `--manifest` flag to create a multi-arch manifest.\n2. **Different push command**: Must use `podman manifest push` instead of `podman push`. Regular `podman push` only pushes single arch even if the local image is a manifest.\n\n### Impact:\n\n- Existing Dockerfiles and CI/CD scripts need modification\n- Mental model shift required for users familiar with Docker\n- Cannot use same `-t/--tag` pattern for both single-arch and multi-arch builds\n- Documentation and training materials need updates\n- Harder to switch between Docker and Podman in mixed environments\n\n### Suggest potential solution\n\nConsider adding Docker CLI compatibility mode for multi-arch builds:\n\n1. When `-t/--tag` is used with comma-separated `--platform` values, automatically create a manifest (instead of single arch)\n2. Make `podman push` detect if the image is a manifest and automatically use manifest push logic\n\nExample improved workflow:\n```bash\n# Works like Docker - automatically creates manifest when multiple platforms specified\npodman build --platform linux/amd64,linux/arm64 -t myimage:tag .\n\n# Push automatically detects manifest and pushes multi-arch\npodman push myimage:tag\n```\n\nThis would maintain backward compatibility with the current `--manifest` workflow while providing a more familiar interface for Docker users.\n\n### Have you considered any alternatives?\n\nAlternative approaches:\n1. Keep current workflow but improve documentation highlighting the differences and providing migration guides\n2. Add a compatibility flag like `--docker-compat` that enables Docker-like behavior\n3. Make `-t` an alias for `--manifest` when multiple platforms are specified\n\nHowever, these don't fully address the friction point of having to remember different commands for single-arch vs multi-arch workflows.\n\n### Additional context\n\nEnvironment:\n```\nClient:        Podman Engine\nVersion:       5.6.0\nAPI Version:   5.6.0\nGo Version:    go1.25.0\nGit Commit:    da671ef6cfa3fc9ac6225c18f1dd0a70a951e43f\nBuilt:         Fri Aug 15 11:42:23 2025\nBuild Origin:  pkginstaller\nOS/Arch:       darwin/arm64\n\nServer:       Podman Engine\nVersion:      5.5.2\nAPI Version:  5.5.2\nGo Version:   go1.23.10\nGit Commit:   e7d8226745ba07a64b7176a7f128e4ef53225a0e\nBuilt:        Mon Jun 23 20:00:00 2025\nOS/Arch:      linux/arm64\n```\n\nRelated issues:\n- #13676 - Building multi-arch with podman\n- #12680 - Accept --arch based on uname -m names\n- #21294 - Podman manifest create format concerns\n\nThis is primarily an **ease-of-use and migration concern**, not a functional limitation. The functionality exists, but the UX differs from Docker in ways that create unnecessary friction for users migrating their workflows.",
    "comments": [
        {
            "user": "Honny1",
            "body": "This seems like a good candidate for Podman 6.0. WDYT? @baude "
        },
        {
            "user": "baude",
            "body": "I think others more specialized in building images will need to speak up here.  I hate the notion of chasing Docker CLI for these kinds of things because it is an endless chase.  And that is the fundamental problem I have with this kind of request; it's not make ease of use better, it is make it more like Docker.  Sometimes that's valid.  Let's see what the gang says.\n\nI do agree the manifests/images thing is kind of confusing and can confuse those coming from docker.  on the other hand, many of our users are quite comfy with the approach.  hopefully some compromise can be driven."
        },
        {
            "user": "kaovilai",
            "body": "> kinds of things because it is an endless chase.\n\nagree.\n\nWhat I don't get is why `podman build --platform linux/amd64,linux/arm64 -t myimage:tag .` doesn't fail, and produces undesired single arch.\n\nProducing misleading results with no errors silently is what I am against."
        },
        {
            "user": "kaovilai",
            "body": "Either join em, make it work as expected, or nudge another command."
        },
        {
            "user": "kaovilai",
            "body": "There is zero scenario where\n`podman build --platform linux/amd64,linux/arm64 -t myimage:tag .`\n\nshould produce a resulting tag as a single arch image.\n"
        },
        {
            "user": "kaovilai",
            "body": "```\npodman build --platform linux/amd64,linux/arm64 -t myimage:tag . && podman push myimage:tag\npodman build --platform linux/amd64,linux/arm64 --manifest myimage:tag . && podman push myimage:tag\n```\n\nshould result in multiarch image being pushed, not single arch. That seems to not be the case last I reproduce."
        },
        {
            "user": "kaovilai",
            "body": "> I hate the notion of chasing Docker CLI for these kinds of things\n\nbecause podman is often advertised as a drop in.\n\nand for the compatibility layer where you could use the actual docker cli, we still have remaining open issues.\nhttps://github.com/containers/podman/issues/24000\n\nI have opened a PR in moby/buildkit on this btw to no avail."
        },
        {
            "user": "kaovilai",
            "body": "Retitled to get at the meat of the issue, which is the confusing results the podman cli currently produces, which if can be solved in a way that make it work like docker cli, great.. being different for sake of different is also ok if it make sense.\n\nCurrently it does not make sense."
        },
        {
            "user": "0xCA",
            "body": "> There is zero scenario where `podman build --platform linux/amd64,linux/arm64 -t myimage:tag .`\n> \n> should produce a resulting tag as a single arch image.\n\nExactly! Had this issue yesterday. Added a platform, forgot to change -t to --manifest. As a result, was highly confused why I can't run any binary.\nThis should either work seamlessly as with docker, or fail immediately as an unsupported params combination."
        },
        {
            "user": "baude",
            "body": "@flouthoc do you see your way through this and perhaps something we can improve?  Maybe even for Podman 6?"
        },
        {
            "user": "flouthoc",
            "body": "We have had conversations in past for adding bake file support that can provide declarative support for building multi-arch image.\n\nIrrespective of that I think the proposed solution can be implemented as-is without any issues, this looks like a good feature to be added into buildah. Thanks for creating the issue @kaovilai "
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "kaovilai",
            "body": "u no stale"
        },
        {
            "user": "daniel-pfeiffer",
            "body": "I’ve had a nightmare week, where I fell into this push trap and many others around this:\n\n- Various `<none>` images, on some of which not even the digest matches members of a multi-arch manifest. An intransparent mess!\n- `images` gives no clue as to which arch each has, not even optionally, nor where it came from.\n- `run <local-ID>` sometimes misinterprets it as a name and tries to download it from docker.io. In those cases I could not even run `arch; cat /etc/*-release` inside to find out what it is.\n- `run --arch=arm64` gives an easy to oversee *\"WARNING: image platform (linux/amd64) does not match the expected platform (linux/arm64)\"* while continuing to run with wrong results. Apparently, if another arch is available locally, it uses that, instead of downloading the requested one.\n- inconsistent name:tag handling: it kind of simultaneously refers to the multi-arch manifest and to one arch. I definitely caught a `run` reassigning the tag from the manifest to an inner single-arch image – WTF! I’m fairly sure `build` did the same thing on `FROM` images.\n\n**Please:**\n- never mess with name:tag except when explicitly requested to do so!\n- make `images` at least optionally clear as to what is a multi-arch manifest and which images are part of it for which platform.\n- give a way to unambiguously name manifest vs. arch, e.g. repo/name:tag@all, repo/name:tag@amd64, repo/name:tag@linux/arm64/v8"
        },
        {
            "user": "adarshaketh",
            "body": "would like to have this behave same like how docker does"
        }
    ],
    "cross_references": [
        {
            "organization": "velero-io",
            "repository": "velero",
            "number": 9363
        }
    ]
}