{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2381,
    "title": "rootless `podman export ... -o` fails",
    "state": "closed",
    "labels": [
        "rootless",
        "locked - please file new issue/PR"
    ],
    "user": "vrothberg",
    "body": "```\n[valentin@nebuchadnezzar libpod]$ podman export 254 -o foo.tar\nError: error creating file \"foo.tar\": open foo.tar: permission denied\n```\n\n... while redirecting `... > foo.tar` works. I assume that's namespace related but we might need to document that in the manpage (and the help message).",
    "comments": [
        {
            "user": "giuseppe",
            "body": "do you get any error if you try `buildah unshare touch foo.tar` from the same directory?"
        },
        {
            "user": "vrothberg",
            "body": "```\n$ buildah unshare\n$ podman export ... -o foo.tar\n```\nworks."
        },
        {
            "user": "vrothberg",
            "body": "If I do a `buildah unshare touch foo.tar` and try to export after, outside the namespace, it doesn't work."
        },
        {
            "user": "rhatdan",
            "body": "ls -l foo.tar\n\n"
        }
    ]
}