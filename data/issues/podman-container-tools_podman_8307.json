{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 8307,
    "type": "pull_request",
    "title": "Ensure we do not double-lock the same volume in create",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "When making containers, we want to lock all named volumes we are adding the container to, to ensure they aren't removed from under us while we are working. Unfortunately, this code did not account for a container having the same volume mounted in multiple places so it could deadlock. Add a map to ensure that we don't lock the same name more than once to resolve this.\n\nFixes #8221\n\n",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/8307#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "Adding a test now..."
        },
        {
            "user": "mheon",
            "body": "Test added"
        },
        {
            "user": "edsantiago",
            "body": "Probably not worth a respin of CI, but if you need to resubmit for some reason, could I request:\n```diff\ndiff --git a/test/system/160-volumes.bats b/test/system/160-volumes.bats\nindex c19e61669..0b7aab2fb 100644\n--- a/test/system/160-volumes.bats\n+++ b/test/system/160-volumes.bats\n@@ -162,7 +162,8 @@ EOF\n     myvol=myvol$(random_string)\n     rand=$(random_string)\n\n-    run_podman run --rm -v $myvol:/myvol:z $IMAGE \\\n+    # Duplicate \"-v\" confirms #8307, fix for double-lock on same volume\n+    run_podman run --rm -v $myvol:/myvol:z -v $myvol:/myvol2:z $IMAGE \\\n                sh -c \"echo $rand >/myvol/myfile\"\n     run_podman volume ls -q\n     is \"$output\" \"$myvol\" \"autocreated named container persists\"\n```\nI've confirmed that this fails (timeout) on master, succeeds with your PR. No worries if you can't add it, I'll add it later in a separate PR."
        },
        {
            "user": "mheon",
            "body": "@containers/podman-maintainers PTAL and merge"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 8325
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 9026
        }
    ]
}