{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 8238,
    "type": "pull_request",
    "title": "Use regex for \"pod ps\" name filter to match \"ps\" behavior",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "joelsmith",
    "body": "`podman ps --filter name=containername` supports specifying a regular expression as the container name. This provides flexibility to match exact names using `^` and `$` to anchor the string. For example, a filter of `name=hello` would match containers `hello` and `othello`, but a filter of `name=^hello$` will only match a container named `hello`. This is a good and useful feature.\n\nBy contrast, `podman pod ps --filter name=podname` does not use regular expressions. Instead, it matches any pod name which contains the filter string.  As a result, there is no way to filter on `hello` without also matching `othello`.\n\nThis patch makes the behavior consistent across `podman ps` and `podman pod ps`.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "Thanks @joelsmith \nLGTM\n/approve\n@containers/podman-maintainers PTAL"
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/8238#\" title=\"Author self-approved\">joelsmith</a>*, *<a href=\"https://github.com/containers/podman/pull/8238#issuecomment-721401244\" title=\"Approved\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/master/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "To confirm - `podman ps` also uses this regex-based matching for names? Wasn't aware we'd added that. Huh."
        },
        {
            "user": "joelsmith",
            "body": "> To confirm - `podman ps` also uses this regex-based matching for names? Wasn't aware we'd added that. Huh.\n\nYes. This patch basically copies the code at https://github.com/containers/podman/blob/master/libpod/filters/containers.go#L41"
        },
        {
            "user": "mheon",
            "body": "/lgtm\n/hold"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\nassuming happy tests"
        },
        {
            "user": "umohnani8",
            "body": "changes LGTM"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        },
        {
            "user": "rhatdan",
            "body": "/hold cancel"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 8264
        }
    ]
}