{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2939,
    "type": "pull_request",
    "title": "ci: remove leftover Cirrus config",
    "state": "open",
    "labels": [],
    "user": "vtushar06",
    "body": "Closes #2914.\n\nNothing outside these files points at them - no workflow, no Makefile target, no script under hack.\n\nThe one I was careful about is `contrib/cirrus/ostree_ext.dockerfile`, because there is a live `ostree_rs_ext` job in ci.yml and the names look related. That job runs in `quay.io/coreos-assembler/fcos-buildroot:testing-devel` and clones ostree-rs-ext at runtime, so it never uses the dockerfile.\n\nI also checked Cirrus is not still reporting somewhere I was not looking. It posts nothing on recent PRs and nothing on the last 8 commits on main either, every check-run there comes from the github-actions app.\n\n`.github/workflows/check_cirrus_cron.yml` is in the issue but is already gone. On coordinating with #2725, that one is already mergeable=false and has not been touched since February, so I do not think this changes much for it.\n",
    "comments": [
        {
            "user": "vtushar06",
            "body": "@timcoding1988 this is for #2914. I checked the ostree job separately since the dockerfile lives under contrib/cirrus but the GHA job does not use it, so it was safe to go."
        },
        {
            "user": "vtushar06",
            "body": "thanks Lokesh, should I also update this branch with rebase?"
        },
        {
            "user": "timcoding1988",
            "body": "LGTM"
        },
        {
            "user": "lsm5",
            "body": "> thanks Lokesh, should I also update this branch with rebase?\n\nusually we don't bother unless there are merge conflicts. I don't think our CI is under stress currently, so either way."
        }
    ],
    "cross_references": []
}