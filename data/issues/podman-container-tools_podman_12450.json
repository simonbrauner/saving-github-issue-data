{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 12450,
    "type": "pull_request",
    "title": "podman, push: expose --compression-format",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "giuseppe",
    "body": "support overriding the compression format at push time.\n\nSigned-off-by: Giuseppe Scrivano <gscrivan@redhat.com>\n",
    "comments": [
        {
            "user": "flouthoc",
            "body": "LGTM restarted flakes. Just a discussion point should this also include `no-compression` or `none` which could be `gzip` with `no-compression` some relevant discussion here https://github.com/moby/moby/issues/1266\n\n"
        },
        {
            "user": "giuseppe",
            "body": "do we need to add `--compression-level`?"
        },
        {
            "user": "flouthoc",
            "body": "> do we need to add `--compression-level`?\n\nI think yes `compression-level=<n>` which  could be `1 to 9` for `gzip` would be really nice. I guess scale would be relative to compression algorithm though like `zstd` supports upto `22` i guess."
        },
        {
            "user": "vrothberg",
            "body": "> do we need to add `--compression-level`?\n\nI would leave that to `skopeo` which already supports that.  I cannot imagine a Podman user to know which level to even chose."
        },
        {
            "user": "flouthoc",
            "body": "> > do we need to add `--compression-level`?\n> \n> I would leave that to `skopeo` which already supports that. I cannot imagine a Podman user to know which level to even chose.\n\n@vrothberg Fair point. "
        },
        {
            "user": "rhatdan",
            "body": "/approve\n/lgtm\n@giuseppe could you open a PR to change the defaults based on containers.conf entry.  Hopefully at some point zstd will become the default."
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/12450#\" title=\"Author self-approved\">giuseppe</a>*, *<a href=\"https://github.com/containers/podman/pull/12450#issuecomment-984006566\" title=\"LGTM\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [giuseppe,rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12486
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 9597
        }
    ]
}