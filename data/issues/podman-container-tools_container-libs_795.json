{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 795,
    "type": "pull_request",
    "title": "rpm: update config files for drop ins",
    "state": "closed",
    "labels": [
        "common",
        "podman 6"
    ],
    "user": "Luap99",
    "body": "Ship the fedora overwrites as proper drop ins. This allows us to remove the fragile script to patch the files but also makes it much clearer that we can ship the real upstream default as main file and then only have small drop in files for the distro customization.\n\n<!--- Please read the [contributing guidelines](https://github.com/containers/container-libs/blob/main/CONTRIBUTING.md) before proceeding --->\n",
    "comments": [
        {
            "user": "Luap99",
            "body": "cc @lsm5 @jnovy \n\nuntested at the moment, for upgrades I need to make sure we remove the old files from /etc/ but only if they were not modified by the user. If they were they should be kept."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Packit jobs failed. @containers/packit-build please check."
        },
        {
            "user": "Luap99",
            "body": "@lsm5 Thank you, this did the trick when I tested on a local VM. The unmodified policy.json is gone, while the modified registries.conf is kept.\n\nRPM reports\n\n> [RPM] /etc/containers/registries.conf saved as /etc/containers/registries.conf.rpmsave\n\nAnd then the posttrans moved it back successfully. I guess this means the theoretically there is a small window where podman might get run and the registries.conf.rpmsave is not yet moved back so it runs without the proper config. Given the non atomic nature that is unavoidable and I doubt it would be encountered by anyone."
        },
        {
            "user": "Luap99",
            "body": "For the record it would likely be good to have https://github.com/containers/container-libs/pull/753 merged and vendored into podman first as otherwise the code will not yet be ready to read the files there which could cause troubles on podman-next so I moving this back to draft but otherwise this should be good to go pending review."
        },
        {
            "user": "Luap99",
            "body": "> Also, we have talked about preventing the use of the updated c-common with old Podman/Buildah/Skopeo.\n> \n> If that will happen, where should that happen? I think `Conflicts: … < …` in this package is a natural place.\n\nYes I think that is fair, have we decided on what the next skopeo/buildah versions are?\n\nLooking at podman-next repo If at least the buildah and skopeo versions there are wrong (older than even the latest release)\nhttps://copr.fedorainfracloud.org/coprs/rhcontainerbot/podman-next/packages/\n\nSo I assume if I would add a conflict right now here that would break podman-next packages?\n\n```\nConflicts: podman < 6\nConflicts: buildah < 1.44\nConflicts: skopeo < 1.23\n```\nwould this be correct?\n"
        },
        {
            "user": "Luap99",
            "body": "looking at the current spec file it included the epoch, is that required for Conflicts to work? the podman-next repo contains a much higher epoch so would that always be newer than?"
        },
        {
            "user": "lsm5",
            "body": "\n> Looking at podman-next repo If at least the buildah and skopeo versions there are wrong (older than even the latest release) https://copr.fedorainfracloud.org/coprs/rhcontainerbot/podman-next/packages/\n> \n> So I assume if I would add a conflict right now here that would break podman-next packages?\n> \n> ```\n> Conflicts: podman < 6\n> Conflicts: buildah < 1.44\n> Conflicts: skopeo < 1.23\n> ```\n> \n> would this be correct?\n\nWe should include the Epoch as well and have it conflict on the `Epoch:Version` as it exists on official Fedora. The podman-next Epoch is 100+ something so those won't be affected."
        },
        {
            "user": "lsm5",
            "body": "cc @inknos @jankaluza "
        },
        {
            "user": "lsm5",
            "body": "Hmm, the `rpm Conflicts` would mean we can't ship `skopeo 1.23` and `buildah 1.44` on Fedora 44 and older.\nWould anyone be _not ok_ with that? \n\nI can update the packit configs likewise to not create unnecessary Fedora PRs for all related packages then.\n"
        },
        {
            "user": "Luap99",
            "body": "> Hmm, the rpm Conflicts would mean we can't ship skopeo 1.23 and buildah 1.44 on Fedora 44 and older.\nWould anyone be not ok with that?\n\nYes I think this is is a given, we cannot ship two different binaries using totally different config file parsing code of the same config files. This is a breaking change and must be treated as such, all of podman, buildah, skopeo, containers-common rpms must be updated in one go to avoid any mismatch. \n\n> I can update the packit configs likewise to not create unnecessary Fedora PRs for all related packages then.\n\nSounds good, the same applies to nv/av v2 btw"
        },
        {
            "user": "travier",
            "body": "Looking at the diff here, I'm wondering if we should add a `ghost` line for the now moved `policy.json` config file:\n\n```\n%ghost %{_sysconfdir}/containers/policy.json\n```"
        },
        {
            "user": "danishprakash",
            "body": "> Looking at the diff here, I'm wondering if we should add a `ghost` line for the now moved `policy.json` config file:\n> \n> ```\n> %ghost %{_sysconfdir}/containers/policy.json\n> ```\n\nBut then `rpm -qf /etc/containers/policy.json` would indicate ownership incorrectly, which is somewhat misleading. What is the problem you're trying to fix?"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28601
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 807
        },
        {
            "organization": "cockpit-project",
            "repository": "cockpit-podman",
            "number": 2525
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 808
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 811
        },
        {
            "organization": "podman-container-tools",
            "repository": "container-libs",
            "number": 902
        },
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 6880
        },
        {
            "organization": "coreos",
            "repository": "fedora-coreos-config",
            "number": 4220
        },
        {
            "organization": "bootc-dev",
            "repository": "bootc",
            "number": 2258
        }
    ]
}