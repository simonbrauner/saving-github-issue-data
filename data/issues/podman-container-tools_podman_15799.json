{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 15799,
    "type": "pull_request",
    "title": "Ensure that a broken OCI spec does not break inspect",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note-none",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "The process of saving the OCI spec is not particularly reboot-safe. Normally, this doesn't matter, because we recreate the spec every time a container starts, but if one was to reboot (or SIGKILL, or otherwise fatally interrupt) Podman in the middle of writing the spec to disk, we can end up with a malformed spec that sticks around until the container is next started. Some Podman commands want to read the latest version of the spec off disk (to get information only populated after a container is started), and will break in the case that a partially populated spec is present. Swap to just ignoring these errors (with a logged warning, to let folks know something went wrong) so we don't break important commands like `podman inspect` in these cases.\n\n[NO NEW TESTS NEEDED] Provided reproducer involves repeatedly rebooting the system\n\n\n```release-note\nNONE\n```\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Should go green now, @containers/podman-maintainers PTAL"
        },
        {
            "user": "Luap99",
            "body": "Will this cause panics when the spec is nil?"
        },
        {
            "user": "mheon",
            "body": "We're returning `c.config.Spec` which is still a valid spec - just not the invalid one from disk."
        },
        {
            "user": "mheon",
            "body": "Hm. We'd have to check if the container is running first, I imagine? Removing an invalid OCI spec while the container does not exist seems perfectly valid; removing an invalid OCI spec that has somehow been used to create a valid, running container seems like a bad idea."
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/15799#pullrequestreview-1108120793\" title=\"LGTM\">giuseppe</a>*, *<a href=\"https://github.com/containers/podman/pull/15799#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [giuseppe,mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "/cherry-pick v4.2.0-rhel"
        },
        {
            "user": "mheon",
            "body": "Bot is broklen, doing it manually"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 15915
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 15982
        }
    ]
}