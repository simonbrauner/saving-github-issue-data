{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 6977,
    "type": "pull_request",
    "title": "Preserve passwd on container restart",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "We added code to create a `/etc/passwd` file that we bind-mount into the container in some cases (most notably, `--userns=keep-id` containers). This, unfortunately, was not persistent, so user-added users would be dropped on container restart. Changing where we store the file should fix this.\n\nFurther, we want to ensure that lookups of users in the container use the right /etc/passwd if we replaced it. There was already logic to do this, but it only worked for user-added mounts; it's easy enough to alter it to use our mounts as well.\n\nUnfortunately, this is not enough for a full fix. You still cannot run `useradd` within the container because it wants to rename the `/etc/passwd` file (impossible with bind mounts).\n\nFixes #6953\n",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/6977#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "mheon",
            "body": "Toolbox also considers the lack of a working `useradd` a breaking change, so this isn't going to be enough.\n\nWon't hurt to merge, though."
        },
        {
            "user": "rhatdan",
            "body": "Needs a rebase."
        },
        {
            "user": "mheon",
            "body": "Done"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\nassuming happy tests"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm\n/hold"
        },
        {
            "user": "rhatdan",
            "body": "/hold cancel"
        }
    ],
    "cross_references": []
}