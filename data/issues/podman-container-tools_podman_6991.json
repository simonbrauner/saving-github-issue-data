{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 6991,
    "type": "pull_request",
    "title": "Make changes to /etc/passwd on disk for non-read only",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "Bind-mounting /etc/passwd into the container is problematic becuase of how system utilities like `useradd` work. They want to make a copy and then rename to try to prevent breakage; this is, unfortunately, impossible when the file they want to rename is a bind mount. The current behavior is fine for read-only containers, though, because we expect useradd to fail in those cases.\n\nInstead of bind-mounting, we can edit /etc/passwd in the container's rootfs. This is kind of gross, because the change will show up in `podman diff` and similar tools, and will be included in images made by `podman commit`. However, it's a lot better than breaking important system tools.\n\nFixes #6953",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/6991#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "Nice work @mheon, I was thinking of doing this myself.  Glad you did it."
        },
        {
            "user": "rhatdan",
            "body": "Does this work for both use cases.\n\nif I specify a --user 1234, we add an entry for 1234 to /etc/passwd.\n\nIf you use --userns=keepid we add your entry to /etc/passwd."
        },
        {
            "user": "mheon",
            "body": "Should work for both - this happens after we've already generated both entries for `/etc/passwd` and the only difference is where we write them to (a new file or the existing passwd)"
        },
        {
            "user": "rhatdan",
            "body": "@owtaylor FYI"
        },
        {
            "user": "mheon",
            "body": "Added a further test and un-broke the existing passwd tests (used read-only mode to force us back to the old bind mount behavior)."
        },
        {
            "user": "mheon",
            "body": "Finally green"
        },
        {
            "user": "mheon",
            "body": "@rhatdan @vrothberg @TomSweeneyRedHat @jwhonce PTAL and merge"
        },
        {
            "user": "rhatdan",
            "body": "LGTM\nI think we should think of some corner cases like @mtrmac Talks about, but we have those already."
        },
        {
            "user": "rhatdan",
            "body": "@vrothberg @giuseppe @baude @jwhonce @TomSweeneyRedHat @ashley-cui @QiWang19 PTAL"
        },
        {
            "user": "edsantiago",
            "body": "LGTM"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 7151
        }
    ]
}