{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 259,
    "type": "pull_request",
    "title": "[wip] integration: do not pull from Docker Hub",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "runcom",
    "body": "- Fix https://github.com/projectatomic/skopeo/pull/132\n- Requires https://github.com/containers/image/pull/148\n- Cleanup the `Dockerfile` by removing registry V1 since we don't support it at all\n\nWhen https://github.com/containers/image/pull/148 is merged - we'll load images on the test registries when the test suites come up.\n\n@mtrmac PTAL\n\nSigned-off-by: Antonio Murdaca <runcom@redhat.com>",
    "comments": [
        {
            "user": "mtrmac",
            "body": "This doesn’t actually remove any Hub references from the tests yet, does it?"
        },
        {
            "user": "runcom",
            "body": "> This doesn’t actually remove any Hub references from the tests yet, does it?\n\nnot yet, waiting on the `docker-archive` functionality at least"
        },
        {
            "user": "mtrmac",
            "body": "If I understand correctly, the idea is to use that shell script → `docker-archive:` → `docker load` → `docker push` to recreate the needed images in our registry; is that right?\n\nIf so, why not just do `docker pull && docker push`, perhaps both in the Dockerfile?"
        },
        {
            "user": "runcom",
            "body": "@mtrmac we don't have `docker` in the docker container - and I wouldn't have it also.\nThe idea is to download the images in the `Dockerfile`, and at test suite setup `skopeo copy ...` from tarballs directly:\n```sh\nskopeo copy docker-archive:/localimage.tar docker://testregistry.com:5000/busybox:latest\n```"
        },
        {
            "user": "mtrmac",
            "body": "Oops, that was stupid of me.\n\nIn that case, we could also use `dir:` instead of `docker-archive:`, that involves fewer manifest conversions. OTOH the conversions, both in the new script and in `docker-archive:`, are already implemented, so reworking the script to use the `dir:` format would be quite unnecessary work."
        },
        {
            "user": "cyphar",
            "body": ":+1: However we have to make sure that that `docker://` source is properly tested with the live Docker Hub. Maybe make it a separate suite or something -- but we still need to verify that the upstream Docker Hub still works with us."
        },
        {
            "user": "runcom",
            "body": "> However we have to make sure that that docker:// source is properly tested with the live Docker Hub. Maybe make it a separate suite or something -- but we still need to verify that the upstream Docker Hub still works with us.\n\nof course, yes"
        },
        {
            "user": "rhatdan",
            "body": "Is this pull request still something we are considering?"
        },
        {
            "user": "rhatdan",
            "body": "@runcom Either update this or close it."
        }
    ],
    "cross_references": []
}