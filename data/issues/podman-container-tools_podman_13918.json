{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 13918,
    "type": "pull_request",
    "title": "use etchosts package from c/common",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "Luap99",
    "body": "Use the new logic from c/common to create the hosts file. This will help\nto better align the hosts files between buildah and podman.\n\nAlso this fixes several bugs:\n- remove host entries when container is stopped and has a netNsCtr\n- add entries for containers in a pod\n- do not duplicate entries in the hosts file\n- use the correct slirp ip when an userns is used\n\nFeatures:\n- configure host.containers.internal entry in containers.conf\n- configure base hosts file in containers.conf\n\nFixes https://github.com/containers/podman/issues/12003\nFixes https://github.com/containers/podman/issues/13224\n\nlibpod: fix c.Hostname() to respect the utsNsCtr\n\nWhen we lookup the hostname for a given container we have to check if\nthe container is joined to another utsns and use this hostname then\ninstead.\nThis fixes a problem where the `hostname` command would use the correct\nname but /etc/hostname would contain a different name.\n\nnetwork dis-/connect: update /etc/hosts\n\nWhen we connect or disconnect from a network we also have to update\n/etc/hosts to ensure we only have valid entries in there.\nThis also fixes problems with docker-compose since this makes use of\nnetwork connect/disconnect.\n\nFixes https://github.com/containers/podman/issues/12533",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/13918#\" title=\"Author self-approved\">Luap99</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [Luap99]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@Luap99 all kinds of test unhappiness"
        },
        {
            "user": "Luap99",
            "body": "With my new logic you can no longer use `--add-host` on a container with a shared netns (e.g. pod).\nThe problem with the current logic is that if you use --add-host on the container it will add it to all containers which are joined to this netns. IMO all host entries should be set on the pod/dependency container and we should error if someone wants to add them for the container which is joined to another netns. However this could break some people.\n@containers/podman-maintainers WDYT?"
        },
        {
            "user": "rhatdan",
            "body": "I agree we should block the use of --add-hosts on containers in Pods as well as shared network namespace."
        },
        {
            "user": "mheon",
            "body": "I'm a little iffy on the change as it could be considered breaking. Can we check if Docker allows `--add-host` with `--net container:`?"
        },
        {
            "user": "Luap99",
            "body": "> I'm a little iffy on the change as it could be considered breaking. Can we check if Docker allows `--add-host` with `--net container:`?\n\nI just tried it and get an error\n```\n# docker run --add-host name:1.1.1.1 --network container:test alpine cat /etc/hosts\ndocker: Error response from daemon: conflicting options: custom host-to-IP mapping and the network mode.\n```"
        },
        {
            "user": "mheon",
            "body": "OK, I think we can justify this as docker-compat, then. I don't really see an issue with allowing it (it does add to all containers using the netns, but that isn't necessarily a bad thing) but if we can justify the removal I don't really mind."
        },
        {
            "user": "Luap99",
            "body": "The problem with keeping this is that we also have to remove them when the container stops. The current logic just completely ignores this case so if you restart the container it will duplicate the entries every time as long as the infra container stays up."
        },
        {
            "user": "rhatdan",
            "body": "Yes lets remove the complication.  Users have a workaround to add the hosts to the original pod or container."
        },
        {
            "user": "rhatdan",
            "body": "LGTM\nBut needs a rebase."
        },
        {
            "user": "rhatdan",
            "body": "/hold cancel"
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "common",
            "number": 998
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 11596
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 13808
        }
    ]
}