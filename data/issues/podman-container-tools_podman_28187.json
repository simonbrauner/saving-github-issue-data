{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28187,
    "type": "issue",
    "title": "journald log driver: support --log-opt labels to promote container labels as structured journal fields",
    "state": "open",
    "labels": [
        "Good First Issue",
        "kind/feature",
        "volunteers-wanted",
        "triaged",
        "Contribfest"
    ],
    "user": "ItsGlassPlus1",
    "body": "### Feature request description\n\nPodman's journald log driver does not support `--log-opt labels=` (or the quadlet equivalent `LogOpt=labels=`). Docker's journald driver supports this option, causing it to promote the specified container label key/value pairs as structured fields directly into each journal entry. In Podman, passing labels via LogOpt= is silently ignored.\n\n### Suggest potential solution\n\nWhen a container is started with `--log-opt labels=<key1>` (or the equivalent in a quadlet), the journald log driver should read the specified label keys from the container's label set and write their values as structured fields into each journal message sent to the journald socket, matching Docker's behavior.\n\n### Have you considered any alternatives?\n\n- `LogOpt=tag=` can write a custom value into SYSLOG_IDENTIFIER, but this overwrites the container's display name and only accepts a single value, making it unusable for grouping containers that belong to multiple logical groups simultaneously (see Additional context).\n- `LogExtraFields=` in the quadlet's [Service] block doesn't work in my case because the fields are only attached for system units (see systemd/systemd#29956).\n\n### Additional context\n\nThe motivating use case is grouping loosely coupled containers into observable \"log groups\" for unified filtering via journalctl, without tight runtime using Podman pods or systemd slices. A shared service may belong to multiple such groups simultaneously, requiring it to carry multiple label keys which is impossible with the single-value tag approach but trivially handled by label promotion since each label key becomes its own independent journal field.",
    "comments": [
        {
            "user": "Luap99",
            "body": "https://github.com/containers/podman/pull/26203 is about to add `--log-level label=` which is work different from the docker docs for labels= https://docs.docker.com/engine/logging/drivers/journald/\n\nI guess we could add labels= later.\n\ncc @p12tic \n"
        },
        {
            "user": "p12tic",
            "body": "@Luap99 Do you think it would be useful to expose log labels as `--log-opt labels=KEY=VALUE,KEY2=VALUE2` from the start to have consistency with docker? That is, I propose to not implement `label` opt at all."
        },
        {
            "user": "Luap99",
            "body": "@p12tic If that works for your use case I am certainly happy with less features that we need to expose.\nBut note the docker state\n\n> Comma-separated list of keys of labels\n\nSo it is only `--log-opt labels=KEY,KEY2` and then the value is used of the actual label from the container I guess"
        },
        {
            "user": "p12tic",
            "body": "Indeed this wouldn't work. I got confused by their example:\n\n```\ndocker run \\\n    --log-driver=journald \\\n    --log-opt labels=location \\\n    --log-opt env=TEST \\\n    --env \"TEST=false\" \\\n    --label location=west \\\n    your/application\n```\n\nand assumed that labels follows the same convention. \n\nAlso Docker sanitizes keys to replace characters that are not allowed by journald. If podman implemented `--labels` with the same semantics, this would be necessary as well. #26203 can get away without modifying keys because by being able to use go templating in values, users can implement their own arbitrary mapping. Without that, it would be impossible to send labels with names unsupported by journald."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "jude-ruben",
            "body": "/assign @jude-ruben"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29173
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29176
        }
    ]
}