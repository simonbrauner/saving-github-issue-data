{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3084,
    "type": "pull_request",
    "title": "rootless: use a pause process to keep namespaces alive",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "giuseppe",
    "body": "use a pause process to keep the user and mount namespace alive.\n\nThe pause process is created immediately on reload, and all successive Podman processes will refer to it for joining the user&mount namespace.\n\nThis solves all the race conditions we had on joining the correct namespaces using the conmon processes.\n\nAs a fallback if the join fails for any reason (e.g. the pause process was killed), then we try to join the running containers as we were doing before.\n\nThis PR also adds an alternative way of joining the namespaces.  We can now attempt to join them as soon as Podman starts, before the Go runtime kicks in, so that we don't need to re-exec and use just one process :tada: \n\nThis version doesn't try to recover and re-create a pause process if for any reason it terminated, but prints an error.  This is not trivial to recover from, as there can be more podman processes already running and will require some sort of locking.",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/3084#\" title=\"Author self-approved\">giuseppe</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [giuseppe]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "giuseppe",
            "body": "tests are passing here, @mheon if there are no objections, I'd like to get this into the next Podman release so that we have some time to play with it before 8.1"
        },
        {
            "user": "mheon",
            "body": "I'm reluctant to get this into 1.3.1, which is supposed to be a small bugfix release... Maybe we cut a 1.4 very soon (before end of month) to get this out quickly?"
        },
        {
            "user": "rh-atomic-bot",
            "body": ":umbrella: The latest upstream changes (presumably #3104) made this pull request unmergeable. Please resolve the merge conflicts."
        },
        {
            "user": "giuseppe",
            "body": "@mheon @rhatdan any other comment on this PR or could we merge it?"
        },
        {
            "user": "mheon",
            "body": "One question, but overall I think we're pretty good to merge. LGTM"
        },
        {
            "user": "baude",
            "body": "/lgtm"
        },
        {
            "user": "rufoa",
            "body": "@giuseppe \nI'm running into a problem which I believe is related to this change.\n\nIt appears that `pause.pid` is not being deleted after use, so next time I run podman I get errors like this:\n\n```\ncannot join pause process pid 18740.  You may need to remove /run/user/111/libpod/pause.pid and stop all containers\nyou can use `system migrate` to recreate the pause process\nopen /proc/18740/ns/user: no such file or directory\n```\nnote:\n- process 18740 had indeed exited (cleanly, I believe), but the pid file was left behind\n- I'm running podman rootlessly as uid 111\n- Deleting `pause.pid` resolves the problem temporarily, but then the same problem reoccurs next run\n\nI'm still trying to figure out the root cause. It occurs reliably every time a particular CI job of mine runs, which executes various podman commands in quick succession (build, run, top, inspect, rm).\n\nDo you have a hunch what might be the cause of this, so I can investigate further? Where in podman is the call which is supposed to delete `pause.pid`? Many thanks."
        },
        {
            "user": "mheon",
            "body": "The pause process does not appear to be pausing - which is our problem. If it's exiting cleanly, it's not doing its job of living as long as a user's session to preserve namespaces"
        },
        {
            "user": "giuseppe",
            "body": "the pause process should be left alive after the first execution.  It is implemented to avoid some race conditions trying to join the existing namespaces.\n\nDoes it exist immediately after you run podman? What do you see if you run something like the following?\n\n```\n$ podman images\n$ pgrep -fa pause\n```\n\nIf you exit the current user session, the pause process might be killed by systemd.  There is a fix for this issue in: https://github.com/containers/libpod/pull/3188\n\nIn particular this patch: https://github.com/containers/libpod/pull/3188/commits/30ef6ba125536077923f19ed079b321f264ede99\n\nYou can try it by yourself with `loginctl enable-linger $UID` and see if it makes any difference"
        },
        {
            "user": "rufoa",
            "body": "Thanks for the info! It appears to work correctly now.\n\nI had to manually enable-linger for my user as root, because [Debian appears not to install policykit by default](https://bugs.debian.org/cgi-bin/bugreport.cgi?bug=827756#10), so non-root users cannot enable-linger for themselves."
        },
        {
            "user": "rufoa",
            "body": "Perhaps the deb package should recommend/suggest policykit-1 as a dependency"
        },
        {
            "user": "giuseppe",
            "body": "@lsm5 can we add that dependency?"
        }
    ],
    "cross_references": []
}