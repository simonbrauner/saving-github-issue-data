{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 9670,
    "title": "API: Copy/Archive API Implementation",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "pzl",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nAlso, there is a running list of known issues in the [Podman Troubleshooting Guide](https://github.com/containers/podman/blob/master/troubleshooting.md),\nplease reference that page before opening a new issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind feature\n\n\n**Description**\n\nI noticed that the compat API has [Archive PUT](https://github.com/containers/podman/blob/master/pkg/api/handlers/compat/containers_archive.go#L23) (aka copy?) implemented.  but libpod API [does not](https://github.com/containers/podman/blob/master/pkg/api/handlers/libpod/copy.go#L10).\n\nIs there anything this is waiting for? Can it use a similar call (`ContainerCopyFromArchive`) or `AddArtifact` or anything similar?\n\nI was trying to use `github.com/containers/libpod/v2/pkg/bindings` to put a file into a container (where mounting was not preferable) and saw that I was not able to via the libpod API (of which `pkg/bindings` seems to be a client) but I seem to able able to via the compat API.\n\nIf this is a simple fix of mostly copy-over implementation, happy to submit a PR for that. But I didn't know what the thinking was surrounding `libpod/copy.go` here",
    "comments": [
        {
            "user": "vrothberg",
            "body": "Thanks for reaching out.  The libpod API implements the _same_ archive endpoints as the compat API.\n\nThe code you pointed to is dead since the libpod and the compat API share the same code in pkg/api/handlers/compat/containers_archive.go."
        },
        {
            "user": "vrothberg",
            "body": "I opened https://github.com/containers/podman/pull/9671 to clean up the dead code."
        }
    ]
}