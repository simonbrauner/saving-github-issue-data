{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 23811,
    "title": "CI: pods and container names in /etc/hosts: cleanup race?",
    "state": "closed",
    "labels": [
        "flakes",
        "stale-issue",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "Almost certainly a test bug: https://github.com/containers/podman/blob/f3db6b19d7d311782a2a7716c69ebf995e89f14b/test/system/500-networking.bats#L160-L178\n\n(I can't link to my parallel version).\n\nTest is failing in parallel mode, only on my laptop. Haven't seen it fail in CI. Failure is in the last two lines shown above, basically, container1 is still showing up in `/etc/hosts`. \n\nIs this expected? I'm going to try adding `--rm` to the first `podman run` and see if the failure vanishes, but I'm not sure if that's the right thing to do.",
    "comments": [
        {
            "user": "Luap99",
            "body": "Adding `--rm` will fix it because the run will remove the container at the end which means cleanup will be done before, \n\nWithout it podman run ... waits for the container to exit, it does not wait for the container to be fully cleaned up. The actual cleanup happens via podman container cleanup process in the background which is causing the race here. In practise it is a bit more complicated.\nFor a long running process it will always work because we wait for conmon to exit and conmon waits for the podman container cleanup process to finish first so cleanup is done in most of the cases since https://github.com/containers/podman/pull/23601. However a short running process such as cat might have exited before we call into WaitForExit() so we no longer wait for conmon and exit earlier there which https://github.com/containers/podman/pull/23646 was about.\n\nWe could try doing an explicit cleanup call at the end but this wouldn't really work via remote API so I rather not do it for only local podman. The --rm fix should work.\n"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "Luap99",
            "body": "I don't think I have ever seen this in CI so I am going to close it"
        }
    ]
}