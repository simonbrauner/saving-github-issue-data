{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 10910,
    "type": "pull_request",
    "title": "Add support for checkpoint/restore into and out of pods",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "adrianreber",
    "body": "This adds support to checkpoint containers out of pods and restore container into pods.\n\nIt is only possible to restore a container into a pod if it has been checkpointed out of pod. It is also not possible to restore a non pod container into a pod.\n\nThe main reason this does not work is the PID namespace. If a non pod container is being restored in a pod with a shared PID namespace, at least one process in the restored container uses PID 1 which is already in use by the infrastructure container. If someone tries to restore a container from a pod with a shared PID namespace without a shared PID namespace it will also fail because the resulting PID namespace will not have a PID 1.\n",
    "comments": [
        {
            "user": "mheon",
            "body": "I'm somewhat doubtful that checkpointing a container and restoring it into a different pod will work well - there's the potential for configuration differences that the container itself can't really detect but would result in major issues (a missing shared namespace being most notable)"
        },
        {
            "user": "adrianreber",
            "body": "@mheon Thanks for your review. I added a check to only restore containers into a pod that has the same namespaces shared as the original pod out of which the container was checkpointed. I also updated all container metadata to no longer reference the original pod, but updated it with the new pod."
        },
        {
            "user": "rhatdan",
            "body": "Should we have a podman pod checkpoint and podman pod restore?"
        },
        {
            "user": "adrianreber",
            "body": "> Should we have a podman pod checkpoint and podman pod restore?\n\nYes, that was my goal. This is just the first step towards that."
        },
        {
            "user": "vrothberg",
            "body": "> Recommended form is `Expect(session).To(Exit(0))`\n\nThat's still open, after that we can merge."
        },
        {
            "user": "adrianreber",
            "body": "> > Recommended form is `Expect(session).To(Exit(0))`\n> \n> That's still open, after that we can merge.\n\nOops, I thought I changed it. I will take a look later today."
        },
        {
            "user": "adrianreber",
            "body": "> > > Recommended form is `Expect(session).To(Exit(0))`\n> > \n> > \n> > That's still open, after that we can merge.\n> \n> Oops, I thought I changed it. I will take a look later today.\n\nI pushed but did not commit previously. But now I committed and pushed."
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/10910#\" title=\"Author self-approved\">adrianreber</a>*, *<a href=\"https://github.com/containers/podman/pull/10910#pullrequestreview-715977894\" title=\"Approved\">edsantiago</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [edsantiago]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}