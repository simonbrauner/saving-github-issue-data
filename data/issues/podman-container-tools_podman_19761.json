{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 19761,
    "title": "Enable ipv6 network by default",
    "state": "open",
    "labels": [
        "kind/feature",
        "network"
    ],
    "user": "maflcko",
    "body": "### Feature request description\n\nCurrently ipv6 is not supported by default out of the box:\n\n```\n# dnf install podman -y  &&  podman run -it --rm ubuntu:22.04 apt-get update \nLast metadata expiration check: 0:39:00 ago on Sun 27 Aug 2023 09:53:11 AM UTC.\nPackage podman-5:4.6.1-1.fc38.aarch64 is already installed.\nDependencies resolved.\nNothing to do.\nComplete!\n0% [Connecting to ports.ubuntu.com]\n...\n```\n\nIt is required to specify `--network`. For example `host` (insecure), or one created by `podman network create --ipv6 network_name`.\n\n### Suggest potential solution\n\nSet `--ipv6` by default out of the box on the default network (`podman network inspect podman | grep ipv6_enabled`)\n\n### Have you considered any alternatives?\n\nno\n\n### Additional context\n\n_No response_",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "su-ex",
            "body": "Meep, still relevant!"
        },
        {
            "user": "rhatdan",
            "body": "@Luap99 Is this something we can do for podman 5?"
        },
        {
            "user": "Luap99",
            "body": "Maybe? Problem is our ipv6 support is still in a somewhat akward state. We would need to use ipv6 NAT which is a bit controversial I guess.\n\ncc @mheon "
        },
        {
            "user": "mheon",
            "body": "Our v6 support does have a number of issues - for example, we need to get port forwarding off localhost working. No easy way to do that now (though it'd be interesting to wire that into conmon-rs when we migrate over there). IMO, enabling by default is a low priority for Podman 5 (mainly because we have so much else we need to do)"
        },
        {
            "user": "rhatdan",
            "body": "I am wondering if the importance of this might change from Red Hat's point of view."
        },
        {
            "user": "WhyNotHugo",
            "body": "This breaks trying to use podman as a drop-in replacement for docker. Even for the most basic commands like `docker run --rm --publish 8001:8001 mycontainer`."
        }
    ]
}