{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29336,
    "type": "pull_request",
    "title": "pkg/machine: remove unused InstalledProviders function",
    "state": "open",
    "labels": [
        "kind/cleanup",
        "machine"
    ],
    "user": "Aryanbhargava18",
    "body": "#### Checklist\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits.\n- [ ] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] Tests have been added/updated (or no tests are needed)\n- [ ] Documentation has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] Release note entered in the section below\n- [x] PR description, commit message, and GitHub comments are human-written, per [LLM Policy](https://github.com/podman-container-tools/community/blob/main/LLM_POLICY.md)\n\n#### What this PR does / why we need it:\nThe `InstalledProviders` function in `pkg/machine/provider/platform.go` is unused everywhere in the codebase except within its own test cases. This PR removes the dead code entirely to clean up the API surface.\n\n#### Does this PR introduce a user-facing change?\n```release-note\nNone\n```",
    "comments": [
        {
            "user": "Aryanbhargava18",
            "body": "Good catch @danishprakash. I audited the tree and `InstalledProviders()` isn't consumed anywhere outside of `platform_test.go`. I originally just patched the test to resolve the immediate CI flake on developer environments, but if it's dead code, deleting it entirely is definitely the cleaner fix.\n\nI'm happy to push a commit to this PR dropping the function and its tests entirely if you and @ashley-cui agree."
        },
        {
            "user": "ashley-cui",
            "body": "Looks like dead code to me, I'd be okay with removing it. "
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "Aryanbhargava18",
            "body": "I've dropped InstalledProviders() and its associated tests entirely in the latest commit as discussed. Ready for another look whenever you have time @danishprakash @ashley-cui."
        },
        {
            "user": "Aryanbhargava18",
            "body": "> There seems to be some issues, can you please run `make validatepr` and fix them?\n\nDeleting the function left behind an unused import (define) in platform.go. I've dropped the unused import and ran gofmt in the latest push. It should be fine now."
        },
        {
            "user": "Aryanbhargava18",
            "body": "Done. I've updated the commit message to accurately reflect the removal of `SupportedProviders` as well."
        },
        {
            "user": "mheon",
            "body": "Kind of surprised we don't use this, but dead code is dead code. LGTM"
        }
    ],
    "cross_references": []
}