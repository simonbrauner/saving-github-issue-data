{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 451,
    "type": "issue",
    "title": "Buildah bud ignores Dockerfile non-keywords by default ",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "ipbabble",
    "body": "\n**Description**\n\nA Dockerfile with lines containing syntax errors - lines starting with non-keywords - does not cause an error and halt the build. Instead it skips the offending line.\n\nAlso I think a simple change of setting ignoreUnrecognizedInstructions to false is not the fix. \n\ni.e. that would have the same effect as commenting out:\n//      if !b.ignoreUnrecognizedInstructions {\n//              logrus.Debugf(\"+(UNIMPLEMENTED?) %#v\", step)\n//              return nil\n//      }\n\nBut this means that the error output is too verbose because there is no check for log-level.  We probably need to have a if !debug type of check and just output a simple \"Buildah Error: unknown instructions: <non-keyword>\"\n\n\n**Steps to reproduce the issue:**\n1.  Create a Dockerfile that has commands that need to be run without the RUN keyword at the start. Or a file to be copied without the COPY keyword. \n2.  buildah bud .\n3.\n\n\n**Describe the results you received:**\nBuild finishes fast. No instructions are executed. All skipped.\nI received empty images because dnf commands were not getting run.\n\n**Describe the results you expected:**\nBuild error: Unknown instruction: DNF\n\n**Output of `rpm -q buildah` or `apt list buildah`:**\n\n```\nBuilt from HEAD\n```\n\n**Output of `buildah version`:**\n\n```\nbuildah version 0.8 (image-spec 1.0.0, runtime-spec 1.0.0)\n\n```\n\n**Output of `cat /etc/*release`:**\n\n```\nFedora release 27 (Twenty Seven)\n\n```\n\n**Output of `uname -a`:**\n\n```\nLinux localhost.localdomain 4.13.9-300.fc27.x86_64 #1 SMP Mon Oct 23 13:41:58 UTC 2017 x86_64 x86_64 x86_64 GNU/Linux\n```\n\n**Output of `cat /etc/containers/storage.conf`:**\n\n```\n# storage.conf is the configuration file for all tools\n# that share the containers/storage libraries\n# See man 5 containers-storage.conf for more information\n\n# The \"container storage\" table contains all of the server options.\n[storage]\n\n# Default Storage Driver\ndriver = \"overlay\"\n\n# Temporary storage location\nrunroot = \"/var/run/containers/storage\"\n\n# Primary Read/Write location of container storage\ngraphroot = \"/var/lib/containers/storage\"\n\n[storage.options]\n# AdditionalImageStores is used to pass paths to additional Read/Only image stores\n# Must be comma separated list.\nadditionalimagestores = [\n]\n\n# Size is used to set a maximum size of the container image.  Only supported by\n# certain container storage drivers.\nsize = \"\"\n\n# OverrideKernelCheck tells the driver to ignore kernel checks based on kernel version\noverride_kernel_check = \"true\"\n```\n",
    "comments": [
        {
            "user": "ipbabble",
            "body": "I don't mind trying to fix this one. I just need to understand or be shown how to effectively handle log-level checking in some other part of the code. I'm going to sniff around and see if I can see how it's done elsewhere. "
        },
        {
            "user": "ipbabble",
            "body": "May want to check the log-level and change the output for simple error.\n\nSomething like 👍 func (b *Executor) UnrecognizedInstruction(step *imagebuilder.Step) error {\n        if b.ignoreUnrecognizedInstructions {\n                logrus.Debugf(\"+(UNIMPLEMENTED?) %#v\", step)\n                return nil\n        }\n        if logrus.GetLevel() == logrus.ErrorLevel {\n                return errors.Errorf(\"Unknown instruction: %v\", step.Command)\n        } else if logrus.GetLevel() == logrus.DebugLevel {\n                logrus.Debugf(\"Unknown instruction: %#v\", step)\n                return errors.Errorf(\"Unknown instruction: %#v\", step)\n        } else {\n                return errors.Errorf(\"Unknown instruction: %#v\", step)\n        }\n\n\n????\n}"
        },
        {
            "user": "rhatdan",
            "body": "@ipbabble Open a separate PR"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 452
        }
    ]
}