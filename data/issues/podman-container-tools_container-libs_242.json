{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 242,
    "type": "issue",
    "title": "pkg/docker/config.GetAllCredentials should add more context to errors",
    "state": "open",
    "labels": [
        "image"
    ],
    "user": "mtrmac",
    "body": "Per https://github.com/containers/podman/issues/11636 , any errors reading specific credentials should be accompanied with an identification of that credential.\n\n(Also check if we need to do that for _any_ credential helper errors; `credentials.NewErrCredentialsNotFound` does not include any context.)",
    "comments": [
        {
            "user": "vrothberg",
            "body": "@mtrmac, I think that we can close this issue since https://github.com/containers/image/pull/1427 has merged.  But maybe I am missing the point."
        },
        {
            "user": "mtrmac",
            "body": "containers/image#1427 adds special handling for a specific error; the proposal here not to change _handling_ of specific errors, but to add _context_ (which helper / file, which credential key) — especially for the unexpected errors we don’t handle specially.\n\nE.g. if `getAuthFromCredHelper` fails with an unknown error, AFAICS we just return that all the way up to the caller, without saying which helper and which entry has failed in the returned error. (Some of that can be figured out from the debug log, but that’s only useful if the failure is reproducible.) Ideally this should be a subpackage-wide audit, not just affecting that one call."
        },
        {
            "user": "mtrmac",
            "body": "E.g. in the Podman bug `Error: 1 error occurred: … * credentials not found in native keychain` is basically unactionable."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 11636
        }
    ]
}