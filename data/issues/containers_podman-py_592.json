{
    "organization": "containers",
    "repository": "podman-py",
    "number": 592,
    "type": "issue",
    "title": "Add direct support to `--manifest` for building multi-architecture container images.",
    "user": "MisterOwlPT",
    "body": "Hi,\n\nI am using `podman‑py` to automate the creation of multi‑architecture container images with `podman`.\n\nMy existing CLI workflow looks like this:\n\n```bash\npodman manifest create example:v1.2.3\npodman build --manifest example:v1.2.3 --platform linux/amd64,linux/arm .\n```\n\nWhen translating these commands into calls to `podman-py`, I've noticed that the [`--manifest` argument](https://docs.podman.io/en/latest/markdown/podman-build.1.html#manifest-manifest) is **not** supported by the [`client.images.build` function](https://github.com/containers/podman-py/blob/main/podman/domain/images_build.py#L171).\n\nI am aware that `podman-py` already supports creating manifests and adding images to them.\nHowever, I am left wondering if supporting the `--manifest` would not give developers an experience closer to the existing `podman` CLI and thus simplify the creation of scripts, as the creation of manifests and addition of images could all be done in a single call.\n\nMy proposal, for the same example CLI workflow above, is to allow something like this:\n\n```python\n# ...\nwith PodmanClient(base_url=uri) as client:\n  image, _ = client.images.build(\n    path=\".\",\n    dockerfile=\"Containerfile\",\n    manifest=\"example:v1.2.3\",  # <-- here's the missing field - the manifest is created if it does not exist\n    platform=[\"linux/amd64\", \"linux/arm64\"],\n  )\n```\n\nHere, the new optional `manifest` _kwarg_ would accept the name of the manifest and automatically add the newly built image(s) to it.\n\nWhat are your opinions on this feature?\n\nThanks a lot for your attention and for the project!",
    "comments": [
        {
            "user": "inknos",
            "body": "Thanks @MisterOwlPT for the issue request. On a first glance, I believe this should be implemented and that the lack of `manifest` in our `/libpod/build` api docs is a bug."
        },
        {
            "user": "inknos",
            "body": "@MisterOwlPT I confirm it should be documented on podman's side and included in podman-py. would you like to open a PR for it? I can help with review and testing"
        },
        {
            "user": "MisterOwlPT",
            "body": "Hi @inknos,\n\n> would you like to open a PR for it? I can help with review and testing\n\nAbsolutely! \nI'll start working on it as soon as possible.\n\nThanks for the feedback!"
        }
    ]
}