{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2593,
    "type": "pull_request",
    "title": "Ensure that tmpfs mounts do not have symlinks",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "When mounting a tmpfs, runc attempts to make the directory it will be mounted at. Unfortunately, Golang's os.MkdirAll deals very poorly with symlinks being part of the path. I looked into fixing this in runc, but it's honestly much easier to just ensure we don't trigger the issue on our end.\n\nFixes BZ #1686610",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/2593#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "One nit for consideration, otherwise LGTM if you can tame the tests.  The issues there don't appear related."
        },
        {
            "user": "mheon",
            "body": "Bind mounts don't appear to use the same directory creation logic as tmpfs\nmounts, at least in runc, so they don't seem to have issues. I could add\nthem easily enough, but I was a bit concerned with performance b\nimplications - we usually only have 3 or so tmpfs mounts, but a lot more\nbind mounts\n\nOn Sat, Mar 9, 2019, 06:31 Daniel J Walsh <notifications@github.com> wrote:\n\n> *@rhatdan* commented on this pull request.\n> ------------------------------\n>\n> In libpod/container_internal_linux.go\n> <https://github.com/containers/libpod/pull/2593#discussion_r263929014>:\n>\n> > @@ -366,6 +367,18 @@ func (c *Container) generateSpec(ctx context.Context) (*spec.Spec, error) {\n>  \t// For private volumes any root propagation value should work.\n>  \trootPropagation := \"\"\n>  \tfor _, m := range mounts {\n> +\t\t// We need to remove all symlinks from tmpfs mounts.\n> +\t\t// Runc and other runtimes may choke on them.\n> +\t\t// Easy solution: use securejoin to do a scoped evaluation of\n> +\t\t// the links, then trim off the mount prefix.\n> +\t\tif m.Type == \"tmpfs\" {\n>\n> Why only on a tmpfs? Would this also happen on a bind mount?\n>\n> —\n> You are receiving this because you authored the thread.\n> Reply to this email directly, view it on GitHub\n> <https://github.com/containers/libpod/pull/2593#pullrequestreview-212478411>,\n> or mute the thread\n> <https://github.com/notifications/unsubscribe-auth/AHYHCIhP0MkarH65w8PA0njgRgBcY6Kzks5vU5upgaJpZM4bmFi4>\n> .\n>\n"
        },
        {
            "user": "rhatdan",
            "body": "@mheon Ok fix @TomSweeneyRedHat Comment and then we can merge."
        },
        {
            "user": "mheon",
            "body": "Tests green, should be good to merge"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}