{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 2706,
    "type": "issue",
    "title": "forward proxy",
    "state": "closed",
    "labels": [
        "kind/documentation",
        "locked - please file new issue/PR"
    ],
    "user": "kitty-catt",
    "body": "Can a tutorial be made that describes how to configure buildah to run behind a forward proxy?",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "@kitty-catt seems like a good thought.  I've not messed with it myself, but perhaps someone with more experience in that arena can hop on."
        },
        {
            "user": "rhatdan",
            "body": "@fatherlinux @mtrmac any ideas on to do this?"
        },
        {
            "user": "mtrmac",
            "body": "Without looking into the details, I’d expect some variant of the variables read by https://golang.org/pkg/net/http/#ProxyFromEnvironment to work, by default."
        },
        {
            "user": "rhatdan",
            "body": "@kitty-catt Are you looking for us to document how to setup a proxy or just want to define the PROXY Environment to use?\n\nPodman run man page documents the following.\n\n```\n       --http-proxy=true|false\n\n       By  default  proxy environment variables are passed into the container\n       if set for the Podman process.  This can be disabled  by  setting  the\n       value   to   false.   The  environment  variables  passed  in  include\n       http_proxy, https_proxy, ftp_proxy, no_proxy, and also the upper  case\n       versions  of  those.   This option is only needed when the host system\n       must use a proxy but the container should not use  any  proxy.   Proxy\n       environment  variables  specified  for  the container in any other way\n       will override the values that would have been passed through from  the\n       host.   (Other  ways  to  specify  the proxy for the container include\n       passing the values with the --env flag, or hard coding the proxy envi‐\n       ronment at container build time.) (Not available for remote commands)\n```"
        },
        {
            "user": "rhatdan",
            "body": "```\nman buildah bud\n...\n       --http-proxy\n\n       By default proxy environment variables are passed into  the  container\n       if  set  for the buildah process.  This can be disabled by setting the\n       --http-proxy option to false.  The environment variables passed in in‐\n       clude http_proxy, https_proxy, ftp_proxy, no_proxy, and also the upper\n       case versions of those.\n```"
        },
        {
            "user": "rhatdan",
            "body": "Since this is documented in the man pages, I don't think we need a blog.  Reopen if you disagree."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 2707
        },
        {
            "organization": "opendevstack",
            "repository": "ods-pipeline",
            "number": 192
        }
    ]
}