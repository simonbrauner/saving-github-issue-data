{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 5088,
    "type": "pull_request",
    "title": "Begin exec rework",
    "state": "closed",
    "labels": [
        "size/XXL",
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "As part of the rework of exec sessions, we need to address them independently of containers. In the new API, we need to be able to fetch them by their ID, regardless of what container they are associated with. Unfortunately, our existing exec sessions are tied to individual containers; there's no way to tell what container a session belongs to and retrieve it without getting every exec session for every container.\n\nI originally debated just adding a registry to the database to point individual exec sessions at each container, but if we're going so far as to completely restructure exec sessions, it makes sense to store them separately as well. Existing exec sessions will be retained as-is for legacy purposes, while the new-style exec sessions will be stored separately from containers and accessed via their own API endpoints.\n\nThere's some potential performance impact to be discussed here in that we'll now need one DB call to get each exec session, and then one call per exec session to update its state, whenever we need to do an operation that needs to check if there are running exec sessions. I'm also concerned as to the circumstances of them remaining in the database; I need to verify on the Docker side as to when they are cleaned (only on reboot? on container restart as well? when the exec session finishes?).\n    \nThis is very early; I'm only about halfway through DB changes, which will then require a refactor of existing exec code to use the new fields in the DB, and associated changes to the lifecycle of exec sessions.\n\nTODO:\n\n- [x] Finish updating database for new storage scheme of exec sessions\n- [x] Update Exec() API in container_exec.go to use new exec session storage\n- [x] Create function for checking if any exec sessions are running, and use this to replace all existing checks of that type\n- [x] Update container removal to deal with new exec sessions\n- [ ] Investigate when exec sessions are removed from the database and update our code to follow that lifecycle\n- [ ] Investigate cleanup processes for exec sessions so that we can support detached exec",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/5088#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "Update: looking more into exec sessions in Docker, it looks like they are removed immediately after termination - probably just the exit code is retrieved via Inspect. We will need the ability to autoremove exec sessions for detached containers. I still see some potential for leaking, but we can probably counter that by clearing them on container restart."
        },
        {
            "user": "mheon",
            "body": "DB changes should be complete. Still doesn't compile, but all functions are there. Going to convince everything to use the new exec sessions, then write unit tests for the DB."
        },
        {
            "user": "mheon",
            "body": "Exec API has been rewritten to use the new structure.\nNext steps: rewrite remainder of Libpod and cmd/podman to use the new APIs, write unit tests for DB work, implement missing APIs (Inspect, Resize)."
        },
        {
            "user": "mheon",
            "body": "Libpod work mostly done. It compiles, even.\n\nNext steps: need to rewrite resize code, write unit tests for DB code."
        },
        {
            "user": "mheon",
            "body": "Added resizing. Going to chase down test failures, add some unit tests, then we can merge."
        },
        {
            "user": "mheon",
            "body": "Rebased, healthcheck errors should be fixed. Stripping WIP, should be ready for merge."
        },
        {
            "user": "rhatdan",
            "body": "@mheon Needs rebase."
        },
        {
            "user": "mheon",
            "body": "Should be ready after #5429 merges"
        },
        {
            "user": "mheon",
            "body": "Rebased, repushed. @rhatdan @baude @vrothberg @giuseppe @haircommander PTAL"
        },
        {
            "user": "baude",
            "body": "lgtm, couple of things to think about"
        },
        {
            "user": "mheon",
            "body": "Rebased on top of fixed Conmon. Fingers are crossed."
        },
        {
            "user": "rhatdan",
            "body": "@mheon is this the same issue you were seeing or a different one?"
        },
        {
            "user": "mheon",
            "body": "Tests are (amazingly) passing now, save a network flake.\n\n@TomSweeneyRedHat @rhatdan @vrothberg PTAL, let's see about getting this landed"
        },
        {
            "user": "mheon",
            "body": "@rhatdan Different issue, looks like a flake in the APIv2 tests"
        },
        {
            "user": "rhatdan",
            "body": "@mheon Do we want to get this into podman 1.8.2 or wait until we ship it?"
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM and happy green tests buttons.\n"
        },
        {
            "user": "rhatdan",
            "body": "/hold\n/lgtm\nI want @mheon to cancel the hold if he wants it in the 1.8.2 release or afterwards."
        },
        {
            "user": "mheon",
            "body": "/hold cancel"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 5434
        }
    ]
}