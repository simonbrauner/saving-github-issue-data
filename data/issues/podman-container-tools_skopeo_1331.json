{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 1331,
    "type": "pull_request",
    "title": "Reintroduce the GNU semantics of DESTDIR",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "mtrmac",
    "body": "This partially reverts commit a81cd747341e04783e7e3a94a76c4444c3bd5cd8 so that most path variables like `PREFIX` and `BINDIR` refer to paths on the installed system, and `DESTDIR` is used only in `make install`, following the philosophy of the GNU coding standards for path variables. (But not precisely the variable names, which are lowercase in the standard, nor the principle that even `SYSCONFDIR` should be under `$PREFIX`.)\n\nKeep the use of `?=` instead of `=` because it somewhat better expresses the idea that the values can be overridden.\n\nUse `${DESTDIR}${BINDIR}` instead of `${DESTDIR}/${BINDIR}` etc, so that a plain `make install` does not use paths like `//usr/bin/...` ; strictly speaking they are IIRC reserved by POSIX, and more importantly it just looks untidy :)\n\nCc: @cevich This originally appeared in #1252, and I might be missing something that expects the other semantics.",
    "comments": [
        {
            "user": "Foxboron",
            "body": "Thank you for doing the revert! It was sadly a bit low on my todo list :disappointed: "
        },
        {
            "user": "rhatdan",
            "body": "LGTM\n@vrothberg PTAL"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM, but a test isn't happy and I too would like a head nod from @ceveich"
        },
        {
            "user": "cevich",
            "body": "> I want a final head nod from @cevich\n\nWell now...looks like we got a head-nod from Cirrus, so it must be okay :grin: "
        },
        {
            "user": "cevich",
            "body": "Just to be transparent: I've designed nearly all our automation (everywhere) to ignore details inside Makefiles.  We strive to use unadulterated `make` commands when convenient. Otherwise the scripts simply call the needed shell commands directly (bypassing make).  That's why \"tests passing\" is good enough for me."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 1252
        }
    ]
}