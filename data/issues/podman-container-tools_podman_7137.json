{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7137,
    "title": "podman-remote: build: does not handle errors",
    "state": "closed",
    "labels": [
        "kind/bug",
        "remote",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "`podman-remote build`, if there are errors, will happily exit 0 and with no output.\n\nIn window 1:\n```console\n$ ./bin/podman system service --timeout=0\n```\nIn window 2:\n```console\n$ printf \"FROM alpine\\nRUN false\\n\" > Containerfile\n$ ./bin/podman-remote build -t foo -f Containerfile .\n$ echo $?\n0\n```\nExpected:\n```console\n$ ./bin/podman build -t foo -f Containerfile .\nSTEP 1: FROM alpine\nSTEP 2: RUN false\nError: error building at STEP \"RUN false\": error while running runtime: exit status 1\n$ echo $?\n125\n```\n(bonus: there is no output in the `system server` window, either)",
    "comments": [
        {
            "user": "rhatdan",
            "body": "This continues to be worked on."
        }
    ]
}