{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29113,
    "type": "pull_request",
    "title": "quadlet: expand %N specifier in the Pod key",
    "state": "open",
    "labels": [],
    "user": "larrasket",
    "body": "The Pod key of a .container unit accepts systemd specifiers, `handlePod` looked the referenced pod up in the units map using the raw value, a `\"Pod=%N.pod\"` failed with `\"quadlet pod unit %N.pod does not exist\"` instead of resolving to the pod named after the container.\n\nthis expand %N to the container's service name before the lookup, matching the existing handling in`GetContainerResourceName`.\n\nFixes: #29108\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [ ] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nFixed the quadlet `Pod=` key not expanding the `%N` specifier (e.g. `Pod=%N.pod`).\n```\n",
    "comments": [
        {
            "user": "ygalblum",
            "body": "This fix works only if the `.pod` and `.container` have the same name. Otherwise, Quadlet will still not find the pod the container belongs to. Also, as a result, it will work only for one `.container`. I'm not sure what's the benefit of single container pods in Podman (I might be wrong here)\n\nSo while it does give some benefit, I'm not sure how much."
        },
        {
            "user": "larrasket",
            "body": "My aim was narrower than that case,`%N` is expanded for a container's own resource name i.e.  it's inconsistent that the same specifier silently fails in `Pod=` with a misleading `pod unit %N.pod does not exist`. If you'd not resolve specifiers in `Pod=` at all (which would also fit the man-page note about specifiers that reference the generated service unit breaking resource linking) then replacing the confusing error with a clear message plus a doc line would be a reasonable alternative imo\n"
        },
        {
            "user": "ygalblum",
            "body": "Sorry, not sure I understand your comment. First, Quadlet fails processing the `.container` file and logs the failure, so WDYM by `silently fails`.\nSecond, my concern is that it tries to fix a very narrow case in which the names are the same while actual usage will have different names making this fix irrelevant.\nThird, in order to test it, you should add e2e test cases in: https://github.com/podman-container-tools/podman/blob/d5213284f44643cc95cd2494300d402ce9b1bb62/test/e2e/quadlet_test.go . When testing, add tests that use different names, that set the container's systemd service name (via `ServiceName`). If it works in different combinations, I might be wrong."
        },
        {
            "user": "larrasket",
            "body": "the problem is that it reports `quadlet pod unit %N.pod does not exist`, which points at a literal unit name, rather than indicating specifiers aren't resolved here. And no I don't think it's a narrow-case,  `%N` expands to the container's service name (same as `systemd's %N`), not its file name. So with `ServiceName= set`, the container and pod files can have different names (like `web.container` with `ServiceName=frontend` and `Pod=%N.pod` resolves `frontend.pod`) \nI've added the e2e cases in `test/e2e/quadlet_test.go`."
        },
        {
            "user": "ygalblum",
            "body": "@larrasket you have an error in the man page. Not sure what it is"
        },
        {
            "user": "larrasket",
            "body": "the Pod= note contradicted the RESOURCE NAMING. I reworded it."
        },
        {
            "user": "ygalblum",
            "body": "@larrasket the doc build is still failing"
        },
        {
            "user": "djarbz",
            "body": "It looks like this change only effects `Pod=%N`, however, I think it should have a broader scope.\nI just ran into this issue trying to use `%N` for Image and Volumes.\n\n```ini\nImage=%N.image\nVolume=volume@%N-config.volume:/config\nVolume=volume@%N-data.volume:/data\n```"
        },
        {
            "user": "larrasket",
            "body": "@djarbz good point. I'll follow up with a PR that applies this across the other cross-reference keys, if this gets merged. @ygalblum PTAL"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29257
        }
    ]
}