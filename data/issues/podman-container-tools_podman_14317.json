{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 14317,
    "type": "issue",
    "title": "Make these directories adjustable through podman conf files",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "jahamilton12121",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nAlso, there is a running list of known issues in the [Podman Troubleshooting Guide](https://github.com/containers/podman/blob/main/troubleshooting.md),\nplease reference that page before opening a new issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind feature\n\n**Description**\n\n/tmp/containers-user-$UID\n/tmp/podman-run-$UID\n/tmp/run-$UID\n\nlooks like only way is to change env var $TMPDIR\nhere is the source:\n\n```\nfunc getRootlessRuntimeDir(rootlessUID int) (string, error) {\n\treturn getRootlessRuntimeDirIsolated(\n\t\trootlessRuntimeDirEnvironmentImplementation{\n\t\t\t\"/proc/1/comm\",\n\t\t\tfmt.Sprintf(\"/run/user/%d\", rootlessUID),\n\t\t\tfmt.Sprintf(\"%s/containers-user-%d\", os.TempDir(), rootlessUID),\n\t\t},\n\t)\n}\n```\n**Steps to reproduce the issue:**\n\n1. run rootless podman commands\n\n**Describe the results you received:**\nit creates unwanted files in /tmp\n\n**Describe the results you expected:**\nI want to change it to /var/tmp\n\n**Additional information you deem important (e.g. issue happens only occasionally):**\n\n**Output of `podman version`:**\n\n```\n(paste your output here)\n```\n\n**Output of `podman info --debug`:**\n\n```\n(paste your output here)\n```\n\n**Package info (e.g. output of `rpm -q podman` or `apt list podman`):**\n\n```\n(paste your output here)\n```\n\n**Have you tested with the latest version of Podman and have you checked the Podman Troubleshooting Guide? (https://github.com/containers/podman/blob/main/troubleshooting.md)**\n\n\nYes/No\n\n**Additional environment details (AWS, VirtualBox, physical, etc.):**\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Can you clarify more what the ask here is? It's not really clear."
        },
        {
            "user": "Luap99",
            "body": "also by default these directories should already be under XDG_RUNTIME_DIR?"
        },
        {
            "user": "jahamilton12121",
            "body": "Running rootless podman commands results in the creation of these three directories which are non-configurable\n```\n/tmp/containers-user-$UID\n/tmp/podman-run-$UID\n/tmp/run-$UID\n```\nI assumed by the code snippet yesterday that they could still be changed through $TMPDIR but it doesn't seem to be the case as changing that variable has no effect. Setting $XDG_RUNTIME_DIR has no effect either. \nI want to change them to be \n```\n/var/tmp/containers-user-$UID\netc\n```"
        },
        {
            "user": "jahamilton12121",
            "body": "Also under my user XDG_RUNTIME_DIR is set to /var/tmp.\n```\nsu -s /bin/bash - ubuntu_w_nc -c \"echo $XDG_RUNTIME_DIR\"\n/var/tmp\n```"
        },
        {
            "user": "Luap99",
            "body": "First you have to run podman system reset because it caches the paths. You cannot change them on the fly because this will break about everything. \n\nSecond it is important that the path is on tmpfs because podman will check for a given file to know if there was reboot. If you put this on a persistent file system this will no longer work and result in many bugs. You could work around that by deleting the directories on each boot."
        },
        {
            "user": "jahamilton12121",
            "body": "The /tmp on our system is not on tmpfs\n\nAlso I don't think I need to do a reset because I am working with a program that dynamically builds and destroys rootless containers. I test the env variable changes by having my scripts fully remove the user and container and then making a new user and loading a new container inside of it. Running a reset inside the user won't seem to do anything, running outside the user will mess with rootful containers I have running. "
        },
        {
            "user": "jahamilton12121",
            "body": "```\n:~# df -h /tmp\nFilesystem      Size  Used Avail Use% Mounted on\n/dev/sda3        14G  9.5G  3.6G  73% /\n\n```"
        },
        {
            "user": "jahamilton12121",
            "body": "okay I was able to fix it by modifying the .profile of the user before running any podman commands. \njust added this `export TMPDIR=\"/run/user/myuser\"`"
        },
        {
            "user": "jahamilton12121",
            "body": "changing the env variable causes docker-tar files to go into /run/user as well which is not wanted. Is there anyway to get the directories I mentioned into /run/user, and docker-tar files into /var/tmp?"
        },
        {
            "user": "jahamilton12121",
            "body": "closing again sorry. Using XDG_RUNTIME_DIR instead of TMPDIR worked for above comment"
        }
    ],
    "cross_references": []
}