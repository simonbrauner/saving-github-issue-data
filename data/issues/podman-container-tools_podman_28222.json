{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28222,
    "type": "issue",
    "title": "Use `podman-version` without starting the podman machine",
    "state": "closed",
    "labels": [
        "kind/feature",
        "volunteers-wanted",
        "locked - please file new issue/PR",
        "triaged"
    ],
    "user": "arthurbarr",
    "body": "### Feature request description\n\nRunning `podman version` fails on macOS is the Podman machine is not started.  Many other similar CLIs (e.g. `kubectl`) will at least print the client version.  It's annoying to need to start a VM to find out which version of the client I have.\n\n### Suggest potential solution\n\nRunning `podman version` should just print the client version if it can't connect.\n\n### Have you considered any alternatives?\n\nStarting the machine is a solution, but is frustrating.\n\n### Additional context\n\nExample of current output:\n\n```\n# podman version\nCannot connect to Podman. Please verify your connection to the Linux system using `podman system connection list`, or try `podman machine init` and `podman machine start` to manage a new Linux VM\nError: unable to connect to Podman socket: failed to connect: dial tcp 127.0.0.1:62099: connect: connection refused\n```\n\nExample from `kubectl`:\n\n```\n# kubectl version\nClient Version: v1.34.1\nKustomize Version: v5.7.1\nThe connection to the server 127.0.0.1:46227 was refused - did you specify the right host or port?\n```\n",
    "comments": [
        {
            "user": "afbjorklund",
            "body": "Podman Desktop uses `podman --version`, the only problem is that it doesn't say anything about the server version...\n\n* https://github.com/podman-desktop/podman-desktop/issues/4215"
        },
        {
            "user": "mheon",
            "body": "I don't know if we can say much about server version without the machine being started. Maybe we could store metadata about the machine in labels, but that's not a guarantee it hasn't bee updated since?"
        },
        {
            "user": "arthurbarr",
            "body": "Personally, I'm OK with not having the server version, if the server isn't currently available.  The problem is not displaying the client version, which the client does have, but won't reveal for some reason if it can't first talk to the server."
        },
        {
            "user": "danishprakash",
            "body": "> Maybe we could store metadata about the machine in labels, but that's not a guarantee it hasn't bee updated since?\n\nWouldn't recommend that, though I agree that we should display the client version irrespective of whether we can display the server version or not. FWIW, `docker version` behaves in this manner (showing only client version) if the daemon is not running.\n"
        },
        {
            "user": "mheon",
            "body": "I'm LGTM on showing only client version if the server is down."
        },
        {
            "user": "kyounghoonJang",
            "body": "/assign"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28265
        },
        {
            "organization": "Homebrew",
            "repository": "homebrew-core",
            "number": 289657
        }
    ]
}