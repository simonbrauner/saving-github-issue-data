{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2914,
    "type": "issue",
    "title": "ci: remove leftover Cirrus config after GHA migration",
    "state": "open",
    "labels": [],
    "user": "timcoding1988",
    "body": "## Problem\n\nGHA is now the merge-blocking CI (`Total Success`, `validate`, etc.). Cirrus is no longer a required check.\n\nThese Cirrus leftovers remain on `main`:\n\n- `.cirrus.yml`\n- `.github/workflows/check_cirrus_cron.yml` (scheduled cron monitor, still calls podman's Cirrus cron workflow)\n- `contrib/cirrus/` (`mac_cleanup.sh`, `ostree_ext.dockerfile`, `runner.sh`)\n\n## Proposed fix\n\nRemove the files above and any remaining references.\n\n## Notes\n\n- Coordinate with open PR #2725 (`Cirrus: use test-integration target`), which still touches `.cirrus.yml`.\n- `hack/get_ci_vm.sh` and `hack/get_fqin.sh` were already removed in the GHA migration.\n- Broader Cirrus → TMT work is tracked in #2633; this issue is specifically for deleting dead config now that GHA is blocking.",
    "comments": [
        {
            "user": "vtushar06",
            "body": "hi @timcoding1988, I was going through the CI cleanup issues and had a go at this one. The note about coordinating with #2725 stopped me though like I went and looked at it and it seems to have gone quiet, last update was back in February and github shows it as unmergeable right now.\n\nSo I wasn't sure how to handle that. Is it alright to just delete `.cirrus.yml` and `contrib/cirrus/ `and let that one go, or would you rather it got rebased first? I will be happy to do the deletion either way, I just didn't want to pull the file out from under an open PR."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2919
        }
    ]
}