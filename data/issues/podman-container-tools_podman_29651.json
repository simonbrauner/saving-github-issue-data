{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29651,
    "type": "pull_request",
    "title": "test/utils: fix BeValidJSON and remove IsJSONOutputValid",
    "state": "closed",
    "labels": [],
    "user": "vtushar06",
    "body": "IsJSONOutputValid is the last of the bool returning helpers on the #18540 list. I left it out of #29640 because swapping it for BeValidJSON() broke the negative rows of the info and version table tests, and it turned out the matcher is the reason.\n\nMatch() returned the unmarshal error for input that is simply not json. gomega treats a non nil error as a failure to evaluate, so the assertion fails whichever way you write it:\n\n```\nExpect(\"{not json\").ToNot(BeValidJSON())\n--> invalid character 'n' looking for beginning of object key string\n```\n\na non match is (false, nil). the error return is there for input the matcher cannot evaluate, which is the wrong type case just above it.\n\nfirst commit fixes that, second removes the helper and moves its five call sites over. the self test in podmansession_test.go now covers both directions, and with the matcher fix reverted it fails at that line, so it does catch the regression.\n\nran the test/utils suite, 23 of 23.\n",
    "comments": [
        {
            "user": "baude",
            "body": "if this is part of a cleanup and change in how we want things, then id like to see something proposed or done to enforce the proper use of this stuff ?  Or perhaps there is a func that needs to be written that's called and handles all this.  up to you"
        },
        {
            "user": "vtushar06",
            "body": "@baude right, though for this one the helper is deleted so there is nothing left to call wrongly, and the matcher fix is what makes the correct form actually work and  for the wider pattern ginkgolinter is already on but with no settings block, I can look at whether it has a rule that covers it if you want that as a follow up."
        },
        {
            "user": "vtushar06",
            "body": "@Luap99 done, the matcher keeps the unmarshal error now and FailureMessage prints it:\n\n```\nto be valid JSON: invalid character 'n' looking for beginning of object key string\n```\n\nnolint:nilerr on the return as you said, golangci-lint is clean on test/utils."
        }
    ],
    "cross_references": []
}