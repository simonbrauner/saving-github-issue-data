{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 18724,
    "title": "Environment files with new line support",
    "state": "open",
    "labels": [
        "kind/feature"
    ],
    "user": "SiNONiMiTY",
    "body": "### Feature request description\n\nSupport loading of environment variable files with new line via --env-file, just like how DotEnv does it.\n\n[DotEnv Multiline Feature](https://github.com/motdotla/dotenv#multiline-values)\n\nCurrently, loading environment files with values that span multiple lines don't work.\nThis is a very useful feature that I would like to be implemented.",
    "comments": [
        {
            "user": "Luap99",
            "body": "Adding support for this sounds good to me as long as it is backwards compatible with the existing parsing logic."
        },
        {
            "user": "hedayat",
            "body": "Is there any workarounds for this?"
        },
        {
            "user": "Luap99",
            "body": "pass the env on the cli with `--env`?"
        },
        {
            "user": "hedayat",
            "body": "Not sure if it is practical enough for me. Actually I'm using `podman-compose`, and with multiple big env files, and it doesn't work currently because of this missing feature. Currently, I'm forced to go back to docker/docker-compose for this use case, which is not what I preferred. "
        },
        {
            "user": "SiNONiMiTY",
            "body": "> Not sure if it is practical enough for me. Actually I'm using `podman-compose`, and with multiple big env files, and it doesn't work currently because of this missing feature. Currently, I'm forced to go back to docker/docker-compose for this use case, which is not what I preferred.\n\nSame here, I am more inclined to use the CLI rather than the podman/docker compose, and --env-file switch not parsing multiline environment variables is such a bummer."
        },
        {
            "user": "Luap99",
            "body": "This is an open source project we always welcome PRs from the community."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "Luap99",
            "body": "The change got reverted as it broke backwards compat."
        },
        {
            "user": "hedayat",
            "body": "Oops, now it breaks docker-compose compatibility... :("
        },
        {
            "user": "hedayat",
            "body": "If you want to keep backward compatibility to that extent, what about adding a new flag for using multi-line environment files?"
        },
        {
            "user": "Luap99",
            "body": "Note that the current behaviour is actually docker compatible. docker != docker-compose, docker run --env-file parses the file in the same format as we do. And you can just use docker-compose against the podman socket so there should not be an issue there as the file is always parsed on the client.\nAs for podman-compose you would need to ask there, podman-compose is not maintained by us.\n \nAdding a new flag with a new format is certainly a possibility"
        },
        {
            "user": "hedayat",
            "body": "Yeah, you are correct.\n\n> Adding a new flag with a new format is certainly a possibility\n\nCan we proceed with this option instead?\n"
        }
    ]
}