{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 1833,
    "type": "pull_request",
    "title": "install.md: add Windows static build example",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "christian-korneck",
    "body": "This adds an example to `install.md` how to build a static `skopeo.exe` on Windows. Remote registry functionality seems to be working fine on Windows.\n\nExamples where this might be useful:\n- There aren't many options to run Windows containers, one of the more popular ones is to use Docker Desktop which can either be in Linux or Windows mode. In Windows mode a Skopeo Linux container can't be used.\n- when only using a Windows _Docker_ or _containerd_ build (which can only run Windows containers, so again no Skopeo Linux container).",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "Looks OK to me, but I don't have a Windows box that I can easily test this on."
        },
        {
            "user": "christian-korneck",
            "body": "@mtrmac Thanks for the feedback.\n\n> Most of https://github.com/containers/skopeo/issues/715 likely still applies — the default paths and the like are probably not a good fit for Windows. It seems to me that looking into the functionality and integration should happen before widely publicizing a way to install the binary.\n\nThanks for mentioning this, I wasn't aware of #715. I'm closing this PR as there's already Windows work happening.\n\n> I don’t think it makes much sense to me to provide a for /f … script in the middle of installation instructions, _only_ in the non-recommended static case.\n\nThis isn't important going forward, just for completeness if someone ends up reading this thread: This was _only_ about making a static build. The code block just repeats the Linux static build instructions from above in a win `cmd` shell (essentially just a `go build ...` line and some some env vars). Intended as contrib info, not install instructions.\n\n\n\n"
        }
    ],
    "cross_references": []
}