{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 254,
    "type": "pull_request",
    "title": "use user/pass flags",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "runcom",
    "body": "We already use global flags for docker specific stuff. This patch enables `--username` and `--password` to be passed down to `containers/image` to setup docker's registries auth.\n\nFixes #253 \n\n@mtrmac @cyphar PTAL\n\nSigned-off-by: Antonio Murdaca <runcom@redhat.com>",
    "comments": [
        {
            "user": "mtrmac",
            "body": "I am not really thrilled with the generic option names: if `--username` and `--password` never worked, nothing stops us from naming them `--docker-username` / `--docker-password`. \n\nI am worried that we really shouldn’t send the same password to several different servers. OTOH the distinction really isn’t between transport types, but between individual hostnames, and that would be a horrible UI, so a generic `--username` and fervently hoping that the users know what they are doing might be the best we can do.\n\nThis should also restore `TestCanAuthToPrivateRegistryV2WithoutDockerCfg` and perhaps some of the neighboring tests, though.\n\nAfter that, I guess, LGTM."
        },
        {
            "user": "mtrmac",
            "body": "(As for “we already do use generic option names for Docker-specific options”, that's partly legacy. And partly using `--cert-path` for an unwanted service is not a vulnerability, but sending a password to an unwanted service is. But, really, I am bad at designing UIs,)"
        },
        {
            "user": "runcom",
            "body": "> This should also restore TestCanAuthToPrivateRegistryV2WithoutDockerCfg and perhaps some of the neighboring tests, though\n\nSort of.. I have to mock an empty Docker cfg first. I'll add that though, thx. \n\n> I am not really thrilled with the generic option names: if --username and --password never worked, nothing stops us from naming them --docker-username / --docker-password.\n\nWe used to have user/pass flag in the very first skopeo versions :/"
        },
        {
            "user": "mtrmac",
            "body": "Yeah; I am not _thrilled_ with `--password` but I can live with that."
        },
        {
            "user": "runcom",
            "body": "@mtrmac what if we add `--src-username` and `--src-password` and the same for `dest`. That's not horrible to me and IIUC should solve your concern around wrongly sending a password to a wrong server. "
        },
        {
            "user": "mtrmac",
            "body": "That’s great idea. We can very confidently expect that the sets of servers underlying a transport(`atomic:` native API + Docker registry API, `docker:` native API + sigstore) must be under a shared control.\n\nYes to `--{src,dest}-{username,password}`, please."
        },
        {
            "user": "runcom",
            "body": "Alright, do note that I'll make these new flags available only to `copy`. `inspect` and the now deprecated `layers` command will continue to behave as usual (w/o any user/pass flag set). "
        },
        {
            "user": "runcom",
            "body": "Or wait, I can add username and password flag locally for inspect/layers commands. I'll do this way. "
        },
        {
            "user": "runcom",
            "body": "@mtrmac PTAL, before going ahead and add docs/tests/`containers/image` bits, I would like to have your ack on the direction with `copy`. I'll also fix `layers|inspect` once we agree on `copy`."
        },
        {
            "user": "rhatdan",
            "body": "I don't like options with dashes (\"-\") in them.  Could we look at other tools like ssh? Would\n```\n--src dwalsh/password@registry --dest mitr/foobar@remote\n```\nbe better?"
        },
        {
            "user": "runcom",
            "body": "@rhatdan fine, but `--src` isn't clear enough because it's not saying we're talking about credentials "
        },
        {
            "user": "runcom",
            "body": "We don't need to specify the registry also, that's implied by src and dest in the command and we don't need that (something like that is how the Docker auth config file works but that's another story, here in the cli we directly honor user/pass combination without checking the registry they'll be used to authenticate against) "
        },
        {
            "user": "runcom",
            "body": "@rhatdan @mtrmac BTW, I'm fine compacting \"username:password\" in the cli as it  doesn't make sense to have 2 flags for user/pass when you must always use both. I'll rework this PR once we agree on flags naming though. "
        },
        {
            "user": "rhatdan",
            "body": "@runcom It would help me if you gave a couple of examples of what you are proposing for a CLI."
        },
        {
            "user": "runcom",
            "body": "Like `--src-creds runcom:password` - modulo no dash in the flag "
        },
        {
            "user": "rhatdan",
            "body": "I like that better"
        },
        {
            "user": "mtrmac",
            "body": "> BTW, I'm fine compacting \"username:password\" in the cli as it doesn't make sense to have 2 flags for user/pass when you must always use both. I'll rework this PR once we agree on flags naming though.\n\nThat feels a bit off, as a general security habit I like to avoid formatting/parsing and in-band signalling. More importantly, it _may_ make sense to provide an username but not a password (e.g. when authenticating using a Kerberos ticket, or a smartcard)"
        },
        {
            "user": "runcom",
            "body": "> BTW, I'm fine compacting \"username:password\" in the cli as it doesn't make sense to have 2 flags for user/pass when you must always use both. I'll rework this PR once we agree on flags naming though.\n> That feels a bit off, as a general security habit I like to avoid formatting/parsing and in-band signalling. More importantly, it may make sense to provide an username but not a password (e.g. when authenticating using a Kerberos ticket, or a smartcard)\n\nalright, no parsing then, any idea regarding Dan's issue on naming?"
        },
        {
            "user": "rhatdan",
            "body": "Why wouldn't --cred dwalsh\nCause the passwd checks to happen to use kerberos like sshd does?\n\nI guess if you had a case where there was a password without a user then we have a problem."
        },
        {
            "user": "mtrmac",
            "body": "> Why wouldn't --cred dwalsh\nCause the passwd checks to happen to use kerberos like sshd does?\n\nGood point, that works fine."
        },
        {
            "user": "runcom",
            "body": "> Why wouldn't --cred dwalsh\n> Cause the passwd checks to happen to use kerberos like sshd does?\n> Good point, that works fine.\n\nI probably missed something, are we ok with `--creds username:password`? I didn't understand Dan's example :confused: "
        },
        {
            "user": "mtrmac",
            "body": "> I don't like options with dashes (\"-\") in them. \n\nThis could also be handled by providing aliases:\n```go\ncli.StringFlag {\n    Name: \"src-username, suser\"\n    // …\n}\n```\n\nand then\n```\nskopeo copy --suser mitr --spass sourcepass $source --duser Miloslav --dpass destpass $dest\n```\n(or even `--su`/`--sp`/`--du`/`--dp` but that’s probably too unreadable).\n\nOTOH it _is_ more typing than\n```\nskopeo copy --screds mitr:sourcepass $source --dcreds Miloslav:destpass $dest\n```\n"
        },
        {
            "user": "rhatdan",
            "body": "runcom I am just giving a username not a password\n--creds dwalsh \nMeans Username = dwalsh No password\n--creds dwalsh:foobar\nMeans Username=dwalsh Password=foobar."
        },
        {
            "user": "mtrmac",
            "body": "Overall, I like the separation of username/password but it does mean quite a bit of extra typing.\n\nWRT the too-long-option names, we can provide shortcuts but it would still be nice to at least allow using long readable ones (`--dcreds` is also pretty cryptic).\n\n= overall I don’t have a strong opinion on the single/two-argument decision."
        },
        {
            "user": "mtrmac",
            "body": "> We can very confidently expect that the sets of servers underlying a transport(`atomic:` native API + Docker registry API, `docker:` native API + sigstore) must be under a shared control.\n\nSadly, we can’t, when uploading to `docker.io` for docker/distribution vs. company-specific sigstore.\n\nOuch.\n\n(This does not _immediately_ affect this PR because that is only setting a docker/distribution username:password. But any more such cases, and telling the user to run `docker login` so that we have a hostname-associated configuration stored and not a bazillion of command-line options will start to look pretty good.)"
        },
        {
            "user": "runcom",
            "body": "> runcom I am just giving a username not a password\n> --creds dwalsh\n> Means Username = dwalsh No password\n> --creds dwalsh:foobar\n> Means Username=dwalsh Password=foobar.\n\nYup, thanks @rhatdan - I was asking if @mtrmac is fine with that also, Miloslav are you?"
        },
        {
            "user": "mtrmac",
            "body": "I don’t have a strong opinion on `creds` vs. `username`+`password`; separating the source and destination still seems worthwhile to me."
        },
        {
            "user": "runcom",
            "body": "@mtrmac I was talking just about the \"user:pass\" format which should be parsed "
        },
        {
            "user": "mtrmac",
            "body": "> @mtrmac I was talking just about the \"user:pass\" format which should be parsed\n\nYeah, I don’t have a strong opinion. If you like that, go for it."
        },
        {
            "user": "runcom",
            "body": "@mtrmac @rhatdan PTAL again, before going ahead and add docs/tests/`containers/image` bits, I would like to have your ack about `copy`. I'll also fix `layers|inspect` once we agree on `copy`.\nThe patch right now allows:\n```bash\n$ skopeo copy --src-creds dwalsh:password --dcreds runcom docker://busybox docker://runcom/credentials\n```"
        },
        {
            "user": "rhatdan",
            "body": "Missing changes to man pages and bash completions."
        },
        {
            "user": "runcom",
            "body": "Yup, if @mtrmac ack on the changes done so far, I'll update man, docs and everything else (adding tests also) "
        },
        {
            "user": "runcom",
            "body": "This is ready to be reviewed fully. @mtrmac @rhatdan added man changes, README changes, tests, completions (it's auto-generated so...). What's missing is to create a PR for `containers/image` changes, I'll do that shortly after this has been reviewed."
        },
        {
            "user": "rhatdan",
            "body": "Still missing bash completion fixes."
        },
        {
            "user": "runcom",
            "body": "> Still missing bash completion fixes.\n\nwhere is bash completion? `hack/make/bash_autocomplete` auto generates the bash completion on `make install` - not sure what I should fix :confused: "
        },
        {
            "user": "rhatdan",
            "body": "Looks like it only generates one level of bash completions.  Lets ignore this for now and we can open an issue to have a much more complete bach completions."
        },
        {
            "user": "runcom",
            "body": "> Looks like it only generates one level of bash completions. Lets ignore this for now and we can open an issue to have a much more complete bach completions.\n\nack, there's already an issue upstream which tracks this https://github.com/urfave/cli/issues/188"
        },
        {
            "user": "runcom",
            "body": "@mtrmac @rhatdan updated"
        },
        {
            "user": "rhatdan",
            "body": "Bash Completions https://github.com/projectatomic/skopeo/pull/256"
        },
        {
            "user": "runcom",
            "body": "> Bash Completions #256\n\nthanks Dan, we'll merge this one and re-iterate on #256"
        },
        {
            "user": "runcom",
            "body": "my bad, I have to fix containers/image and revendor here. I'll do it asap, sorry"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 256
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 257
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 249
        }
    ]
}