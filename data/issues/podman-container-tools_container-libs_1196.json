{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 1196,
    "type": "pull_request",
    "title": "common/pkg/sysctl: accept slash-separated sysctl names",
    "state": "closed",
    "labels": [
        "common"
    ],
    "user": "ijajmulani",
    "body": "#### What this PR does / why we need it\nPodman rejects slash-separated sysctl keys with:\n`sysctl 'net/ipv6/conf/bond1.340/autoconf' is not allowed`\n\n```\n[root@mymachine ~]# docker run --rm -ti --name c1 --network bond1.340   --sysctl=net/ipv6/conf/bond1.340/autoconf=0   quay.io/libpod/busybox\nError: sysctl 'net/ipv6/conf/bond1.340/autoconf' is not allowed\n```\n\n1. `pkg/sysctl.Validate()` only accepts keys whose normalized form matches prefixes like `net.` or `fs.mqueue.`. Slash-first sysctl names are valid per `sysctl.d(5)` but fail the prefix check. \n2. This is a problem for network interfaces whose names contain dots (e.g. VLAN interfaces like `bond1.340`). For those, slash-first notation is required to set per-interface sysctls such as `net/ipv6/conf/bond1.340/autoconf`.\n\n#### Which issue(s) this PR fixes\nN/A (no existing issue filed)\n\n#### Special notes for reviewers\n- Normalization follows the same rules as\n  [opencontainers/runc#3257](https://github.com/opencontainers/runc/pull/3257).\n- Validation uses the normalized key; the original user-provided key is\n  preserved in the returned map passed to the runtime.\n- Runtime path handling is separate (e.g. crun); this PR only fixes\n  Podman's allowlist validation in `containers/common`.\n\n#### User-visible impact\nUsers can pass slash-separated sysctls to Podman, e.g.:\n```bash\npodman run --sysctl net/ipv6/conf/bond1.340/autoconf=0 \n```\n\nDot-first escaped form (net.ipv6.conf.bond1/340.autoconf=0) was already accepted by prefix check; slash-first form was not.\n\n### How this was tested\n- [x] go test ./pkg/sysctl/\n- [x] Unit tests for convertSysctlVariableToDotsSeparator() and slash-separated VLAN interface keys\n- [x] Manual: podman run --sysctl net/ipv6/conf/bond1.340/autoconf=0 with macvlan/ipvlan on a VLAN parent interface\n```\nroot@mymachine:~/workspace/podman# /usr/local/bin/podman-patched run --rm --network=none  -- sysctl=net/ipv6/conf/all/autoconf=0   alpine cat /proc/sys/net/ipv6/conf/all/autoconf\n\n0\n\nroot@mymachine:~/workspace/podman# /usr/local/bin/podman-patched run --rm --network=none  --sysctl=net/ipv6/conf/all/autoconf=1   alpine cat /proc/sys/net/ipv6/conf/all/autoconf\n1\n```",
    "comments": [
        {
            "user": "ijajmulani",
            "body": "@giuseppe and @Honny1 can you please review?"
        },
        {
            "user": "giuseppe",
            "body": "@kolyshkin PTAL"
        },
        {
            "user": "Honny1",
            "body": "@ijajmulani \n\nLinter:\n```\ntime=\"2026-09-22T16:28:39Z\" level=info msg=\"using commit range: 6dedb406c164c234f934decb3d929e4f176b4712..HEAD\"\n * e51ef3d579 \"common/pkg/sysctl: accept slash-separated sysctl names Normalize sysctl keys per sysctl.d(5) before validation so slash-first names like net/ipv6/conf/bond1.340/autoconf are accepted for VLAN interface names containing dots.\" ... FAIL\n  - FAIL - commit subject exceeds 90 characters\n\n```"
        },
        {
            "user": "ijajmulani",
            "body": "@Honny1 \nCan you please review again?"
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "crun",
            "number": 2265
        }
    ]
}