{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 26616,
    "type": "issue",
    "title": "test: \"podman exec - leak check\" is not working correctly",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR",
        "triaged",
        "CI"
    ],
    "user": "Luap99",
    "body": "Added in https://github.com/containers/podman/commit/774a36d1771b083825fcf4c89385e90499537cb1\n\n```\n@test \"podman exec - leak check\" {\n    skip_if_remote \"test is meaningless over remote\"\n\n    # Start a container in the background then run exec command\n    # three times and make sure no any exec pid hash file leak\n    run_podman run -td $IMAGE /bin/sh\n    cid=\"$output\"\n\n    is \"$(check_exec_pid)\" \"\" \"exec pid hash file indeed doesn't exist\"\n\n    for i in {1..3}; do\n        run_podman exec $cid /bin/true\n    done\n\n    is \"$(check_exec_pid)\" \"\" \"there isn't any exec pid hash file leak\"\n\n    run_podman rm -t 0 -f $cid\n}\n```\n\nThis test never checked what it tried to check, `check_exec_pid` is not defined anywhere so this errors and thus the output is always empty. The test defined the find_exec_pid_files function instead which likely was planned to be used there.\nHowever looking that that function I am not sure it work correctly either it matches `exec_pid_*` but our exec pidfile is just named `exec_pid` so I doubt it would match anything.\n\nAlso this test is marked as `ci:parallel` which cannot work either when we search the full runroot for files as another test might create some in parallel which would trigger a false positive \n\nGiven we had problems with leaking paths multiple times checking that still sounds useful so it would be good to update the test so it actually works. \n",
    "comments": [
        {
            "user": "Craig-Spencer-12",
            "body": "/assign"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 26619
        }
    ]
}