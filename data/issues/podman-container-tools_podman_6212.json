{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 6212,
    "title": "Use of constant pkg/network/config.go#CNIConfigDir",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "m8nmueller",
    "body": "/kind bug\n\n**Description**\n\nThe files pkg/network/files.go and pkg/api/handlers/libpod/networks.go still make use of the static constant _CNIConfigDir_ in pkg/network/config.go.\n\nThe resulting issue appears when the cni config dir is changed in libpod.conf. While `network create` and `network ls` use pkg/domain/infra/abi/network.go#getCNIConfDir, `inspect` and `remove` use pkg/network/files.go#GetCNIConfigPathByName (for `inspect` indirectly via _ReadRawCNIConfByName_).\n\nI will fix this by replacing the use of the constant in _GetCNIConfigPathByName_ by a call to _getCNIConfDir_.",
    "comments": []
}