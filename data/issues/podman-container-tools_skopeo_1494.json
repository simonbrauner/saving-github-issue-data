{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 1494,
    "type": "pull_request",
    "title": "Cirrus: Fix only_if error",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "cevich",
    "body": "The logic parser for `only_if` and `skip` is a little weird in that it\nwants a boolean, but doesn't support using them.  In this case, using `1\n== 0` results in an error from the parser:\n\n`Error while parsing tasks: INVALID_ARGUMENT: internal boolevator error:\nparsing error: 1 == 0 :1:1 - 1:2 unexpected Int while scanning\nextensions!`\n\nHowever, since the goal is to block this particular task from running\n(essentially) indefinitely, it's easier to just remove it.  We can rely\non git recording it's history in case it needs to be recovered in the\nfuture.\n\nSigned-off-by: Chris Evich <cevich@redhat.com>",
    "comments": [
        {
            "user": "cevich",
            "body": "> success_task depends_on cross; is that a problem?\n\nIt's not suppose to be be a problem (I confirmed this years ago with them).  The `depends_on` is suppose to properly handle the case of children being skipped for any reason.\n\nProperly as in: skipped == pass when evaluating \"dependency was met\".\n\nSo if this ever breaks, we need to report it to them ASAP (lots of our CI depends on this behavior)."
        },
        {
            "user": "cevich",
            "body": "> The “Build parsing results” step\n\nFWIW: I just ignore that thing, it complains about a lot of things we do."
        },
        {
            "user": "cevich",
            "body": "Uggg, wait, no, my bad.  This is a real problem.  Fixing...."
        },
        {
            "user": "mtrmac",
            "body": "Yeah, I didn’t see any test results in the GitHub box with the previous version. This LGTM, feel free to merge after tests pass."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12168
        }
    ]
}