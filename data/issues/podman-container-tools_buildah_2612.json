{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 2612,
    "type": "issue",
    "title": "Unshare (non-root): Buildah Script Example in MAN page does not work",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-issue",
        "locked - please file new issue/PR"
    ],
    "user": "weeping-somnambulist",
    "body": "`buildah unshare` man page script example does not work. I take the script example, I save it as a .sh file, I even make it +x, I get an error message when attempting to run it.\n\nYou might also want to note I'm using ZSH, which appears to affect not only how the here doc works, but also seems to make any variables created in a sub-shell not work as expected either.\n\nNot sure if I should create another bug here, as I don't understand sub-shell that buildah is doing with the unshare command, but the only usage of this command that has worked for me is `cnt=$(buildah from scratch); buildah unshare --mount FOO=$cnt`\n\nFRUSTRATED USER COMMENT:\n\nThis probably isn't the place for this, but the `unshare` command is exceptionally frustrating and really negates the value of `buildah` in lot of ways, as you can't easily mount your container's filesystem *in a script* without having to jump through the `unshare` hoop and deal with all the sub-shell madness. A great example is that this *DOES NOT* work with Make. \n\nIt's super, super disappointing and another example of how the podman/buildah suite of tools falls flat when faced with root/rootless issues. I just cannot understand why the development team can't account for the tool being run as non-root and why it continues to make users pull their hair out trying to tease the myriad of functional differences between the two. I have to say I've spent waaaay more time on these kinds of issues than I should have and it's frankly silly to even expect users to understand the differences. Just a quick peek at the buildah/podman issue tracker should tell you it's a problem.\n\nFor me it represents yet another day's worth of work wasted trying to make the switch to Podman/Buildah, as this Makefile just isn't going to work like this, and I'm not running it as root. I kind of can't believe that in 2020 root vs non-root is even still a thing to be quite frank. \n\n**Steps to reproduce the issue:**\n1. Follow the man page to create a script file\n2. `buildah unshare buildah-script.sh`\n3. cry\n\n\n**Describe the results you received:**\n```\n⌬ cat buildah-script.sh \n#!/bin/sh\nctr=$(buildah from scratch)\nmnt=$(buildah mount $ctr)\nzypper --non-interactive install --installroot=$mnt zsh\nbuildah config --entrypoint=\"/bin/zsh\"\nbuildah commit $mnt\nbuildah unmount\n\n⌬ buildah unshare buildah-script.sh\nERRO exec: \"buildah-script.sh\": executable file not found in $PATH \nERRO (unable to determine exit status)            \nERRO exit status 1 \n```\n\n**Describe the results you expected:**\nAn image created as documented in the MAN page.\n\n**Output of `rpm -q buildah` or `apt list buildah`:**\n\n```\n⌬ rpm -q buildah\nbuildah-1.15.1-1.2.x86_64\n```\n\n**Output of `buildah version`:**\n\n```\n⌬ buildah version\nVersion:         1.15.1\nGo Version:      go1.14.7\nImage Spec:      1.0.1-dev\nRuntime Spec:    1.0.2-dev\nCNI Spec:        0.4.0\nlibcni Version:  v0.7.2-0.20190904153231-83439463f784\nimage Version:   5.5.1\nGit Commit:      unknown\nBuilt:           Sun Aug  2 18:00:00 2020\nOS/Arch:         linux/amd6\n```\n\n**Output of `podman version` if reporting a `podman build` issue:**\n\n```\n(paste your output here)\n```\n\n**Output of `cat /etc/*release`:**\n\n```\n⌬ cat /etc/os-release \nNAME=\"openSUSE Tumbleweed\"\n# VERSION=\"20200909\"\nID=\"opensuse-tumbleweed\"\nID_LIKE=\"opensuse suse\"\nVERSION_ID=\"20200909\"\nPRETTY_NAME=\"openSUSE Tumbleweed\"\nANSI_COLOR=\"0;32\"\nCPE_NAME=\"cpe:/o:opensuse:tumbleweed:20200909\"\nBUG_REPORT_URL=\"https://bugs.opensuse.org\"\nHOME_URL=\"https://www.opensuse.org/\"\nLOGO=\"distributor-logo\"\n\n```\n\n**Output of `uname -a`:**\n\n```\n⌬ uname -a\nLinux rocinante 5.8.7-1-default #1 SMP Sun Sep 6 07:39:46 UTC 2020 (7fc52c0) x86_64 x86_64 x86_64 GNU/Linux\n```\n\n**Output of `cat /etc/containers/storage.conf`:**\n\n```\n⌬ cat ~/.config/containers/storage.conf \n# This file is is the configuration file for all tools\n# that use the containers/storage library.\n# See man 5 containers-storage.conf for more information\n# The \"container storage\" table contains all of the server options.\n[storage]\n\n# Default Storage Driver\ndriver = \"overlay\"\n\n# Temporary storage location\nrunroot = \"/var/run/user/1000/containers\"\n\n# Primary Read/Write location of container storage\ngraphroot = \"/home/sbrady/.local/share/containers/storage/\"\n\n[storage.options]\n\n# Storage options to be passed to underlying storage drivers\n\n# AdditionalImageStores is used to pass paths to additional Read/Only image stores\n# Must be comma separated list.\n# additionalimagestores = [\n# ]\n\n# Size is used to set a maximum size of the container image.  Only supported by\n# certain container storage drivers.\n# size = \"\"\n\n# Path to an helper program to use for mounting the file system instead of mounting it\n# directly.\nmount_program = \"/usr/bin/fuse-overlayfs\"\n\n# OverrideKernelCheck tells the driver to ignore kernel checks based on kernel version\n# override_kernel_check = \"false\"\n\n# mountopt specifies comma separated list of extra mount options\n# mountopt = \"user_subvol_rm_allowed\"\n\n# Remap-UIDs/GIDs is the mapping from UIDs/GIDs as they should appear inside of\n# a container, to UIDs/GIDs as they should appear outside of the container, and\n# the length of the range of UIDs/GIDs.  Additional mapped sets can be listed\n# and will be heeded by libraries, but there are limits to the number of\n# mappings which the kernel will allow when you later attempt to run a\n# container.\n#\n# remap-uids = 0:1668442479:65536\n# remap-gids = 0:1668442479:65536\n\n# Remap-User/Group is a name which can be used to look up one or more UID/GID\n# ranges in the /etc/subuid or /etc/subgid file.  Mappings are set up starting\n# with an in-container ID of 0 and the a host-level ID taken from the lowest\n# range that matches the specified name, and using the length of that range.\n# Additional ranges are then assigned, using the ranges which specify the\n# lowest host-level IDs first, to the lowest not-yet-mapped container-level ID,\n# until all of the entries have been used for maps.\n#\n# remap-user = \"storage\"\n# remap-group = \"storage\"\n\n# If specified, use OSTree to deduplicate files with the overlay backend\n# ostree_repo = \"\"\n\n# Set to skip a PRIVATE bind mount on the storage home directory.  Only supported by\n# certain container storage drivers\n# skip_mount_home = \"false\"\n\n# [storage.options.thinpool]\n# Storage Options for thinpool\n\n# autoextend_percent determines the amount by which pool needs to be\n# grown. This is specified in terms of % of pool size. So a value of 20 means\n# that when threshold is hit, pool will be grown by 20% of existing\n# pool size.\n# autoextend_percent = \"20\"\n\n# autoextend_threshold determines the pool extension threshold in terms\n# of percentage of pool size. For example, if threshold is 60, that means when\n# pool is 60% full, threshold has been hit.\n# autoextend_threshold = \"80\"\n\n# basesize specifies the size to use when creating the base device, which\n# limits the size of images and containers.\n# basesize = \"10G\"\n\n# blocksize specifies a custom blocksize to use for the thin pool.\n# blocksize=\"64k\"\n\n# directlvm_device specifies a custom block storage device to use for the\n# thin pool. Required if you setup devicemapper.\n# directlvm_device = \"\"\n\n# directlvm_device_force wipes device even if device already has a filesystem.\n# directlvm_device_force = \"True\"\n\n# fs specifies the filesystem type to use for the base device.\n# fs=\"xfs\"\n\n# log_level sets the log level of devicemapper.\n# 0: LogLevelSuppress 0 (Default)\n# 2: LogLevelFatal\n# 3: LogLevelErr\n# 4: LogLevelWarn\n# 5: LogLevelNotice\n# 6: LogLevelInfo\n# 7: LogLevelDebug\n# log_level = \"7\"\n\n# min_free_space specifies the min free space percent in a thin pool require for\n# new device creation to succeed. Valid values are from 0% - 99%.\n# Value 0% disables\n# min_free_space = \"10%\"\n\n# mkfsarg specifies extra mkfs arguments to be used when creating the base.\n# device.\n# mkfsarg = \"\"\n\n# use_deferred_removal marks devicemapper block device for deferred removal.\n# If the thinpool is in use when the driver attempts to remove it, the driver\n# tells the kernel to remove it as soon as possible. Note this does not free\n# up the disk space, use deferred deletion to fully remove the thinpool.\n# use_deferred_removal = \"True\"\n\n# use_deferred_deletion marks thinpool device for deferred deletion.\n# If the device is busy when the driver attempts to delete it, the driver\n# will attempt to delete device every 30 seconds until successful.\n# If the program using the driver exits, the driver will continue attempting\n# to cleanup the next time the driver is used. Deferred deletion permanently\n# deletes the device and all data stored in device will be lost.\n# use_deferred_deletion = \"True\"\n\n# xfs_nospace_max_retries specifies the maximum number of retries XFS should\n# attempt to complete IO when ENOSPC (no space) error is returned by\n# underlying storage device.\n# xfs_nospace_max_retries = \"0\"\n```\n",
    "comments": [
        {
            "user": "smac89",
            "body": "Try `buildah unshare ./buildah-script.sh`"
        },
        {
            "user": "rhatdan",
            "body": "@SwitchedToGitlab Sorry about your fustration with Rootless Buildah and to a smaller degree Podman. With rootless mode we are pushing the boundaries of what a non privileged user can do.  One of the requirements is to use the user namespace. A normal user is not allowed to mount file system, unless they are in a user namespace along with their own mount namespace.  Then they are allowed to mount different types of file systems.  When the exit the user namespace then they go back to the host mount namespace and the mount point no does not exists in this mount namespace.\n\nSo would could make \nbuildah mount $ctr \n\nEnter the usernamespace and mount namespace and mount the file system fine.  BUT when the command ends the user is back in the host mount namespace and does not see the mount. so they can not copy to it directly,without reentering the user namespace/mount namespace.  This is why running the script as rootless does not give you what you would expect.\n\nIf you do `buildah unshare` first and then run the script, everything will happen inside of the user namespace and you would get what you want.\n\nIf you build the script and execute it like @smac89 suggests, then everything would work.\n\nSadly we can not get everything fixed within the Kernel to give you the experience you would like, mainly because of security concerns.  But we can get you pretty close.  Whether that is bad enough to force you back to always using root for your containers, which is what happens when you run Docker containers, I wish we could do better."
        },
        {
            "user": "rhatdan",
            "body": "@TomSweeneyRedHat Want to make this into a Blog?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@rhatdan sure!"
        },
        {
            "user": "rhatdan",
            "body": "@TomSweeneyRedHat Did we ever blog this?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "No blog post yet."
        },
        {
            "user": "rhatdan",
            "body": "Ok I will throw one together."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "Sadly I wrote the blog a month ago and then forgot to get it published.  Working on this now."
        },
        {
            "user": "rhatdan",
            "body": "@edsantiago Could you check what is going on with zsh here? I think we missed the original problem."
        },
        {
            "user": "edsantiago",
            "body": "I'm sorry, what am I supposed to look at? The only problem I see in the original post is that OP forgot the `./`, so of course it will fail. @smac89 helpfully pointed this out, and I see no response from OP. Am I missing a real problem somewhere?"
        },
        {
            "user": "rhatdan",
            "body": "Ok , if that is all there is, we can close this issue once we publish the blog."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "Blog was published.\n\nhttps://www.redhat.com/sysadmin/buildah-unshare-command"
        }
    ],
    "cross_references": []
}