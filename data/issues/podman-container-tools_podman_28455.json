{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28455,
    "type": "pull_request",
    "title": "Fix filtering with negated labels",
    "state": "closed",
    "labels": [
        "6.0"
    ],
    "user": "simonbrauner",
    "body": "<!--\nThanks for sending a pull request!\n\nFor more detailed information, please review our contributing guidelines:\nhttps://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests\n-->\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\n`label!` filters are combined with AND, so that they are consistent with `label` filters.\n```\n\nWhile working on https://github.com/containers/podman/pull/28413, I found out that when filtering based on labels, the `label` filters are combined with AND, while `label!` filters are combined with OR. I thought it would make more sense to handle both labels with the same operation, as it would be unexpected otherwise, and the way it looks in the code `return !filters.MatchLabelFilters(filterValues, v.Labels())` makes it seem like a bug.\n\nCurrently, for podman this works correctly in `images`, and incorrectly for `containers`, `volumes`, and `pods`.\n\nThis is a breaking change and it may make podman more inconsistent with docker.\n\nIt looks like for docker, `label!` has the same problem in `volume prune`, so I reported it https://github.com/docker/cli/issues/6918.",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "@simonbrauner all green here, time to remove the draft label?"
        },
        {
            "user": "simonbrauner",
            "body": "The code should be ready, but I am waiting for decision of @mheon whether this is something that should be merged, because of the inconsistency with Docker."
        },
        {
            "user": "simonbrauner",
            "body": "I added note to the documentation, but the Docker incompatibility is nothing new in our code base as far as this functionality goes. We are using MatchNegatedLabelFilters on networks already https://github.com/containers/common/pull/1677/changes."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Code LGTM, just a couple of nitty nits on the docs."
        },
        {
            "user": "baude",
            "body": "looks like a rebase is needed @simonbrauner "
        },
        {
            "user": "simonbrauner",
            "body": "@baude yes, I added a test to the same place in two PRs. It should be fixed now."
        },
        {
            "user": "Honny1",
            "body": "PTAL @containers/podman-maintainers review and merge please."
        },
        {
            "user": "mheon",
            "body": "LGTM"
        }
    ],
    "cross_references": []
}