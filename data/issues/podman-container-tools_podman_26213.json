{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 26213,
    "type": "pull_request",
    "title": "compat: remove deprecated VirtualSize",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "stale-pr",
        "release-note",
        "kind/api-change",
        "locked - please file new issue/PR",
        "5.6"
    ],
    "user": "inknos",
    "body": "Since compat version 1.43 the VirtualSize field in the GET /images/{name}/json, GET /images/json, and\nGET /system/df responses is deprecated and will no longer be included in API v1.44. Use the Size field instead, which contains the same information.\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\ncompat: deprecate VirtualSize in version 1.43 and remove it in 1.44\n```\n",
    "comments": [
        {
            "user": "inknos",
            "body": "@mheon @Luap99 this is probably the most trivial PR for all docker api changes, and was not tested. should we add tests for this endpoint or mark it to be skipped?"
        },
        {
            "user": "Luap99",
            "body": "What happens if a client talks in an older API version? Should there be some version check around this instead?"
        },
        {
            "user": "inknos",
            "body": "I think the answer depends on our policy for deprecations. What is it? And is the libpod deprecation policy the same as for compat?\n"
        },
        {
            "user": "Luap99",
            "body": "I think the policy for compat is match whatever docker is doing. If you can run the latest docker and use API v1.40 (or anything below the removal) and it still returns that field then we need to do as well.\n\nFor libpod we have a strict semver commitment so any breaking changes such as remove response field should wait for the next major version."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        },
        {
            "user": "inknos",
            "body": "@Luap99 I implemented the api versioned deprecation, and added tests too. but now my eyes are crossing because tests are failing inconsistently.\n\n- tests for /images/json fail for 1.44\n- tests for /images/id/json pass\n- tests for /system/df fail for 1.43\n\nbut the change should be the same, I am removing the same field conditionally for all the endpoints. am I missing something obvious?"
        },
        {
            "user": "Luap99",
            "body": "> @Luap99 I implemented the api versioned deprecation, and added tests too. but now my eyes are crossing because tests are failing inconsistently.\n> \n>     * tests for /images/json fail for 1.44\n\nLooking at the code this is a different endpoint which uses GetImage**s**() and that does not call imageDataToImageInspect at all. It seems like we are just returning the podman/libpod output .\n\nYou would need to modify it in the same way as https://github.com/containers/podman/commit/3ef33653ffdfa5a2cd22a3e7e3302fd8c73fd7bd there\n\n> \n>     * tests for /images/id/json pass\n> \n>     * tests for /system/df fail for 1.43\n\nAFAIK the test only has a single image and as such Size == UniqueSize so Size - UniqueSize = 0 and as such because the type has omitempty it is not added to the json thus it always fails even on an older version.\n\nIt seems list/inspect on the other hand sets VirtualSize == Size so maybe df should have the same. I really have no idea what VirtualSize is meant represent exactly but I think df calculates it wrong then\n\n> \n> \n> but the change should be the same, I am removing the same field conditionally for all the endpoints. am I missing something obvious?\n\n"
        },
        {
            "user": "Luap99",
            "body": "also you will need to add something like `//nolint:staticcheck // Deprecated field, but we must keep setting it for older API users` to the places VirtualSize is set as the linter complains otherswise"
        },
        {
            "user": "inknos",
            "body": "> Looking at the code this is a different endpoint which uses GetImages() and that does not call imageDataToImageInspect at all. It seems like we are just returning the podman/libpod output .\n\nright, I see now\n\n> It seems list/inspect on the other hand sets VirtualSize == Size so maybe df should have the same. I really have no idea what VirtualSize is meant represent exactly but I think df calculates it wrong then\n\nThat's also confusing to me. I'll address your comment and repush\n\n> also you will need to add something like //nolint:staticcheck // Deprecated field, but we must keep setting it for older API users to the places VirtualSize is set as the linter complains otherswise\n\nyup that was a followup since my linter was shouting about it"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "inknos",
            "body": "let's see, validatepr failed after the linter, so I am relying on this CI and will fix what the bot says it's wrong"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/26213#\" title=\"Author self-approved\">inknos</a>*, *<a href=\"https://github.com/containers/podman/pull/26213#pullrequestreview-3078650315\" title=\"Approved\">Luap99</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [Luap99]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}