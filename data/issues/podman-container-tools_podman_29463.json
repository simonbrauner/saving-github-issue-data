{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29463,
    "type": "pull_request",
    "title": "libpod/events: support secret filter for events",
    "state": "open",
    "labels": [],
    "user": "HindzStark",
    "body": "### Description\nThis PR adds support for filtering events by `secret` in `podman events --filter secret=...`.\n\n`events.Secret` is a core event type defined in `libpod/events/config.go`, and secret operations record events with `e.Type = events.Secret`. However, `generateEventFilter` previously lacked a `case \"SECRET\":` block, causing `podman events --filter secret=<name>` to fail with `Error: SECRET is an invalid filter`.\n\n**Changes included:**\n1. Added `case \"SECRET\":` to `generateEventFilter` (`libpod/events/filters.go`) to match secret events by name or ID prefix.\n2. Added `\"secret=\"` key to `keyValueCompletion` in `AutocompleteEventFilter` (`cmd/podman/common/completion.go`) to support shell tab-autocompletion.\n3. Added unit tests in `libpod/events/filters_test.go` covering secret event filter matching.\n\nFixes: #29462\n\n### Checklist\n- [x] Commits are signed off with Developer Certificate of Origin (DCO)\n- [x] Unit tests have been added and verified\n\n### How this was tested\nVerified unit tests locally inside a WSL2 Ubuntu development environment:\n```bash\n$ go test -v ./libpod/events/...\n=== RUN   TestGenerateEventFilterSecret\n--- PASS: TestGenerateEventFilterSecret (0.00s)\nPASS\nok  \tgo.podman.io/podman/v6/libpod/events\t0.013s\n",
    "comments": [
        {
            "user": "mheon",
            "body": "Needs a commit message and adjustments to the manpages"
        },
        {
            "user": "mheon",
            "body": "Also, please add an E2E test for the new filter in `test/e2e/events_test.go`"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "can you add a `%s ` and `e.name` to: https://github.com/podman-container-tools/podman/blob/86bf912fc5c719f368a07f080e83c7f046a994fe/libpod/events/events.go#L96\n\nala:\n\t\thumanFormat = fmt.Sprintf(\"%s %s %s %s %s\", e.Time, e.Type, e.Status, id, e.Name)\n"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "also, in events.go, should you be adding e.name to this function:\n\nhttps://github.com/podman-container-tools/podman/blob/86bf912fc5c719f368a07f080e83c7f046a994fe/libpod/events.go#L198"
        },
        {
            "user": "jankaluza",
            "body": "I've been playing with that locally and tried to learn more about the events in general and noticed one thing. The ID and Name are not logged to journald backend at all, so following does not work:\n\n```\n$ echo secret | podman --events-backend=journald secret create testsecret -\n$ podman --events-backend=journald events --since 1m --stream=false --filter type=secret --format 'ID={{.ID}} Name={{.Name}}'\n$ podman --events-backend=journald events --since 1m --stream=false --filter secret=testsecret\n```\n\nOr even without `--events-backend=journald` on my Fedora where the journald is a default backend. It however works well with \"file\" events-backend.\n\nMaybe it would make sense to also fix the journald backend to log PODMAN_ID and PODMAN_NAME (I hope those would be the good names) so this new feature works with journald.\n"
        },
        {
            "user": "jankaluza",
            "body": "I think it might be as easy as adding new case for Secret with PODMAN_ID and PODMAN_NAME here:\n\nhttps://github.com/podman-container-tools/podman/blob/main/libpod/events/journal_linux.go#L39\n\nand here:\n\nhttps://github.com/podman-container-tools/podman/blob/main/libpod/events/journal_linux.go#L246"
        }
    ],
    "cross_references": []
}