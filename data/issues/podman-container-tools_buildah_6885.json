{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6885,
    "type": "pull_request",
    "title": "Disallow Dockerfile symlinks escaping build context",
    "state": "open",
    "labels": [],
    "user": "mahendrarathore1742",
    "body": "## Summary\n\nThis change aligns Buildah with Docker/BuildKit behavior by rejecting Dockerfile paths\ninside the build context that resolve (via symlink) to a target outside the context.\n\n## What Changed\n\n- Validate Dockerfile paths in `imagebuildah.BuildDockerfiles` to ensure any symlink\n  resolves within `ContextDirectory`.\n- Add a regression test that reproduces the symlink-escape scenario.\n\n## Why\n\nBuilds should not be able to read files outside the declared build context.\nDocker/BuildKit fails in this case; Buildah previously allowed it. This fixes the\ninconsistency and closes the security gap.\n\n## Testing\n\n```bash\ngo test ./imagebuildah \\\n  -run TestDockerfileSymlinkOutsideContext \\\n  -tags \"containers_image_openpgp exclude_graphdriver_btrfs\"\n```\n\n> **Note:** Full `go test ./...` fails in this container due to conformance tests\n> requiring privileged operations (overlay mounts and chown).\n\n## Closes\n\nCloses #6861",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Some unhappy tests which I restarted."
        },
        {
            "user": "nalind",
            "body": "You're going to want to rebase to pick up missing packages that are causing those \"pivot_root: command not found\" test failures, added to the VM images that we updated to in #6911."
        },
        {
            "user": "simonbrauner",
            "body": "There seems to be an overlap with https://github.com/podman-container-tools/buildah/pull/6886 which, as far as I can tell, deals with the same problem but at the level of `DiscoverContainerfile` instead of `BuildDockerfiles`.\n\nWhen I tested this PR, this corner case https://github.com/podman-container-tools/buildah/pull/6886#issuecomment-4958005777 differs from Docker behavior the same way as the other PR used to."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "buildah",
            "number": 6886
        }
    ]
}