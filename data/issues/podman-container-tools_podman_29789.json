{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29789,
    "type": "pull_request",
    "title": "Bump buildah to main",
    "state": "open",
    "labels": [
        "bloat_approved",
        "kind/api-change"
    ],
    "user": "nalind",
    "body": "#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] I have read and understood our [contributing guidelines](https://github.com/podman-container-tools/podman/blob/main/CONTRIBUTING.md) and will not have more than two open PRs as a new contributor.\n- [x] PR description, commit message, and GitHub comments are human-written, per [LLM Policy](https://github.com/podman-container-tools/community/blob/main/LLM_POLICY.md)\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/podman-container-tools/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/podman-container-tools/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/podman-container-tools/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```",
    "comments": [
        {
            "user": "nalind",
            "body": "Added bloat-approved for 67k of growth flagged at https://github.com/podman-container-tools/podman/actions/runs/35235831284/job/105264686606#step:15:397"
        },
        {
            "user": "Luap99",
            "body": "podman system prune --build clean up after terminated build\n\nis now failing with remote because the server gets to cleanup the container correctly as the test only kill the client. So that means the context works, great but we need to find something else for the test.\nEither we kill the server pid and then restart it or maybe better in all cases could we not just do a buildah from + buildah run to have a leaked buildah container to prune?"
        },
        {
            "user": "nalind",
            "body": "> podman system prune --build clean up after terminated build\n> \n> is now failing with remote because the server gets to cleanup the container correctly as the test only kill the client. So that means the context works, great but we need to find something else for the test. Either we kill the server pid and then restart it or maybe better in all cases could we not just do a buildah from + buildah run to have a leaked buildah container to prune?\n\nWe can create one intentionally, which might work better for remote setups.  Trying that."
        }
    ],
    "cross_references": []
}