{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 1111,
    "type": "pull_request",
    "title": "OCPNODE-2018: Add OCI Referrers API support for signature read and write",
    "state": "open",
    "labels": [
        "storage",
        "image"
    ],
    "user": "saschagrunert",
    "body": "Ref #221\n\nAdd support for the OCI Distribution Spec 1.1 Referrers API for both reading and writing sigstore signatures.\n\n**Reading:** Query `GET /v2/<name>/referrers/<digest>` with `Link` pagination, fall back to the referrers tag schema for registries without native API support, and only use referrers with cosign's signature artifactType (`application/vnd.dev.cosign.artifact.sig.v1+json`) and simple signing layers. Referrer digests are validated and verified. Signatures found via both referrers and the cosign tag are fetched once and deduplicated. The lookup is best-effort so a failing endpoint never hides cosign tag signatures, and a registry that does not serve the endpoint (404, 405, 501, 401 or 403) is remembered on the client so that later lookups skip straight to the tag schema.\n\n**Writing:** The destination is selected by the new `sigstore-attachments-write` option in `registries.d`, one of `cosign-tag` (the default), `referrers` or `both`. Writing referrers pushes each signature as an individual OCI artifact manifest with a complete `subject` descriptor and cosign's signature artifactType, and maintains the referrers tag schema index only for registries without native API support. Existing signatures are recognized by their payload rather than by the shape of the manifest carrying them, so a signature another tool already attached (for example by `cosign sign --registry-referrers-mode oci-1-1`) is not duplicated.\n\nThe default leaves the on-registry layout unchanged: writing referrers is opt-in, reading always consults both mechanisms.\n\nBoth paths are gated on the existing `use-sigstore-attachments` flag in `registries.d` configuration.\n\n**Consumer PRs for CI testing:**\n- CRI-O: https://github.com/cri-o/cri-o/pull/10269\n- Skopeo: https://github.com/podman-container-tools/skopeo/pull/2971\n\nJIRA: https://redhat.atlassian.net/browse/OCPNODE-2018\n",
    "comments": [
        {
            "user": "saschagrunert",
            "body": "@mtrmac PTAL"
        },
        {
            "user": "mtrmac",
            "body": "@saschagrunert I’m very interested in the feature but it can’t be an immediate priority."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Packit jobs failed. @podman-container-tools/packit-jobs please check."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "Packit jobs failed. @podman-container-tools/packit-jobs please check."
        },
        {
            "user": "joelanford",
            "body": "I may be missing it in the diff, but I didn't see any change to the public API that would enable callers to make requests to the referrers API directly.\n\nIs that intentionally out-of-scope, deferred to later, or not planned? I ask because this library is defacto required for any client that needs to honor the `containers/image`-style configuration. But not having public APIs that cover the latest OCI spec changes means clients cannot access everything that image registries actually support.\n\nI'm particularly interested in the context of OCP's Operator Lifecycle Manager, for which I've explored using custom OCI artifacts and the referrers API for OLM packaging primitives, but so far those explorations have had to use other libraries (e.g. https://github.com/oras-project/oras-go), which we can't easily ship in OCP due to requirements to integrate with the various image registry configuration APIs."
        },
        {
            "user": "joelanford",
            "body": "Another use case is SBOMs and/or a custom release note artifact that are published as referrers to existing bundle and related workload images. See https://redhat.atlassian.net/browse/OCPSTRAT-3762"
        },
        {
            "user": "mtrmac",
            "body": "The approach here is that signatures are “an integral part of the image” and always copied with the image. So API callers should not need to be opting into / out of referrer use for signatures _in code_ (other than the system’s per-repo configuration, or API callers’ ability to ignore the system’s configuration and work with an entirely caller-specific configuration).\n\nYes, that’s not an ideal fit for some use cases, e.g. publishing source code archives and linking them using referrers to the production containers: users who want to run the container don’t always want a copy of the container. We will need some kind of API, or more detailed configuration, for that, but that’s also a fairly different feature (notably “copy an image along with its 100 MBs of referrers” would require a significant restructuring of the main copy pipeline; we hold the signatures in memory, that would not work for large companion artifacts)."
        },
        {
            "user": "joelanford",
            "body": "@mtrmac yes, definitely understood that the main copy pipeline would need fairly significant restructuring. But do you think it would be possible to put native referrers read support directly into the docker image source in a way that keeps the existing copy pipeline unchanged, while giving lower-level clients a way to pull/inspect/recurse arbitrary referrers?"
        },
        {
            "user": "mtrmac",
            "body": "Sure, in principle, but _this PR_ is already big enough (and starved of review attention enough) that I’d prefer not to include it here."
        }
    ],
    "cross_references": [
        {
            "organization": "cri-o",
            "repository": "cri-o",
            "number": 10269
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2971
        }
    ]
}