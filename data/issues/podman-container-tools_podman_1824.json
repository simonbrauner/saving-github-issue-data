{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 1824,
    "type": "pull_request",
    "title": "pypodman: Don't use $HOST and $USER variables for remote",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "afbjorklund",
    "body": "Also, don't use $PORT. These are too generic.\n\nMake sure to read $LOGNAME after the config.\n\n----\n\nMakes it hard to configure \"root\" in pypodman.conf\n\nIt will always use $USER, for the ssh login name ?",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "/ok-to-test"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "The change LGTM in what you're attempting to do.  I'll let @mheon and @jwhonce weigh in to see if they've issues."
        },
        {
            "user": "afbjorklund",
            "body": "It would be OK to use something specific, like $PYPODMAN_USER and $PYPODMAN_HOST/PORT\n\nI just hid all that complexity in the `pypodman.conf` file, although _that_ could need a variable instead...\n\nCurrently you have to override $XDG_CONFIG_HOME, which also hard (a bad idea) to do globally:\n\n``` python\n        \"\"\"Find and fill in any arguments not passed on command line.\"\"\"\n        args.xdg_runtime_dir = os.environ.get('XDG_RUNTIME_DIR', '/tmp')\n        args.xdg_config_home = os.environ.get('XDG_CONFIG_HOME',\n                                              os.path.expanduser('~/.config'))\n        args.xdg_config_dirs = os.environ.get('XDG_CONFIG_DIRS', '/etc/xdg')\n```\n\nWould be better to set $PYPODMAN_CONFIG_HOME, when you want to use a temporary config ?\n\n---\n\nAnyway, this change allows me to **not** have to use `pypodman --username=root` all the time...\n\nI will do a separate patch for the variable. Already contributed a patch for `$VARLINK_BRIDGE`."
        },
        {
            "user": "rhatdan",
            "body": "I believe the variables should be prefixed with PODMAN_ not PYPODMAN.  That would give us the flexibility to name the tool `podman` or `podman_remote` when installed on a non linux box."
        },
        {
            "user": "afbjorklund",
            "body": "It would also give you the opportunity to use a different language :-)\n\nMaybe the same approach should be taken for the pypodman.conf ?"
        },
        {
            "user": "afbjorklund",
            "body": "@jwhonce : added it to all environment variables, for consistency."
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        },
        {
            "user": "jwhonce",
            "body": "LGTM\n"
        },
        {
            "user": "rhatdan",
            "body": "/approve"
        },
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/1824#issuecomment-439955816\" title=\"Approved\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 4156
        }
    ]
}