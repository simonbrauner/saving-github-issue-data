{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 27965,
    "type": "pull_request",
    "title": "Add cache volumes for validatepr builds",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "afbjorklund",
    "body": "This speeds up the build a lot (10x), the second time.\n\nAlso add `--init`, so that it is possible to cancel it.\n\n---\n\nSince it is building for 3 platforms and linting 3 targets, there is a _lot_ of cache data:\n\n```\n2,3G\t/home/anders/.local/share/containers/storage/volumes/validatepr-gocache\n499M\t/home/anders/.local/share/containers/storage/volumes/validatepr-gomodcache\n134M\t/home/anders/.local/share/containers/storage/volumes/validatepr-lintcache\n31M\t/home/anders/.local/share/containers/storage/volumes/validatepr-precommitcache\n```\n\nThe image is also quite big, so that also takes a while to download the first time:\n\n```\nREPOSITORY                 TAG         IMAGE ID      CREATED      SIZE\nquay.io/libpod/validatepr  latest      6eaeda9bed07  7 weeks ago  892 MB\n```\n\nThe description of the job is a bit misleading, since it does much more than just format/lint.\n\nOn my machine this speeds up the `make validatepr` time, from 8-10 minutes to 45-60 seconds.\n\nIronically, `make validatepr` then fails on the manpage checks... But the build and lint was fast!\n\n```Makefile\n.PHONY: validate\nvalidate: validate-source validate-binaries\n```\n\n```console\n$ make validate-binaries\n...\nCan't use string (\"1\") as a HASH ref while \"strict refs\" in use at hack/xref-helpmsgs-manpages line 309.\nmake: *** [Makefile:619: xref-helpmsgs-manpages] Fel 255\n```\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "afbjorklund",
            "body": "You can \"port\" it to windows separately, if you don't want to run the standard `make` there\n\nBut I don't think there was anything specific to Linux, even if the src mount will be remote"
        },
        {
            "user": "l0rd",
            "body": "@afbjorklund @Luap99  yeah, I can port it to Windows in a separate PR if you can't do/test that."
        },
        {
            "user": "l0rd",
            "body": "> You can \"port\" it to windows separately, if you don't want to run the standard `make` there\n\nFor better or worse, [we have gone a different path](https://github.com/containers/podman/blob/main/build_windows.md#winmake-validatepr): on Windows, we have a distinct makefile (`winmake.ps1`). Using the Linux `Makefile` on Windows is untested, and some Windows-specific tasks exist in the Windows version only."
        },
        {
            "user": "afbjorklund",
            "body": "I use WSL :man_shrugging: (where needed)"
        },
        {
            "user": "afbjorklund",
            "body": "I don't understand why nobody else find the \"make validatepr\" requirement overwhelming?\n\nOr if everyone is already running ARM, or why the issue with darwin amd64 failing didn't block"
        },
        {
            "user": "afbjorklund",
            "body": "The container probably needs `golangci-lint` and `pre-commit` added to it, as well.\n\nBut then it needs to be versioned accordingly, when the version of those change...\n\n`VERSION=$(GOLANGCI_LINT_VERSION) ./hack/install_golangci.sh`\n\nCurrently those tools are installed every time, which is probably not necessary?\n\nEDIT: Never mind, it is installed in the PWD, which is then mounted during build:\n\n`Using existing ./bin/golangci-lint`\n\nThe pre-commit drives python and pip, but after that it is not a very big install.\n\n```\n2,2M\t/root/.cache/pip\n5,7M\t/root/.local/share/virtualenv\n```\n"
        },
        {
            "user": "mheon",
            "body": "LGTM on my side. I mostly just use `make validate` instead, but the speedup for those using the more correct endpoint should be very noticeable."
        },
        {
            "user": "Luap99",
            "body": "> I don't understand why nobody else find the \"make validatepr\" requirement overwhelming?\n\nYeah I don't use it, I think the motivation was to have somethign on windows/macos systems that \"just works\" there. "
        },
        {
            "user": "afbjorklund",
            "body": "> I mostly just use `make validate` instead,\n\nMaybe that target should be in the checklist?\n\nInstead of one that isn't used by maintainers\n\nThen you could just run that from a toolbox container, instead of having to fix the ephemeral container...\n\nIf there already was a user account and a persistent home, then it wouldn't have to mess with /root/.cache"
        },
        {
            "user": "Luap99",
            "body": "> Then you could just run that from a toolbox container, instead of having to fix the ephemeral container...\n\nwhich well doesn't run on windows/macos AFAIK, anyhow you don't have to convince me. I didn't like this target from the start, but there is the fact that some like to try to make it as simple as possible for someone to run the basic validation at least.\n\nOf course there there is also the fact that validatepr does actually some cross arch build checks which normal validate doesn't do either. So yeah the situation is a bit of a mess."
        },
        {
            "user": "afbjorklund",
            "body": "Now it runs in 30 seconds (assuming cache and no major changes), and what better is - it passes (after rebase)\n\nEDIT: My local `make validatepr`, that is. ~~The CI looks broken, due to the FreeBSD node not having `git` installed~~"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 27925
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28324
        }
    ]
}