{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 16447,
    "title": "Mount by default other directories than the HOME folder when creating machines on macOS",
    "state": "closed",
    "labels": [
        "stale-issue",
        "podman-desktop",
        "locked - please file new issue/PR"
    ],
    "user": "benoitf",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nAlso, there is a running list of known issues in the [Podman Troubleshooting Guide](https://github.com/containers/podman/blob/main/troubleshooting.md),\nplease reference that page before opening a new issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind feature\n\n**Description**\n\nBy default, `podman machine init` will mount the `$HOME` folder\n\nI think default, should be the same than on Docker side\nhttps://docs.docker.com/desktop/settings/mac/#file-sharing\n\n>By default the /Users, /Volume, /private, /tmp and /var/folders directory are shared. If your project is outside this directory then it must be added to the list, otherwise you may get Mounts denied or cannot start service errors at runtime.\n\nSo for example /tmp should be added as sometimes you experiment to mount something from the tmp folder.\n\n\n**Steps to reproduce the issue:**\n\n1. podman machine init\n\n2. podman machine -v volume:volume\n\n3.\n\n**Describe the results you received:**\n\n/tmp is not available\n\n**Describe the results you expected:**\n/tmp is mounted\n\n\n**Additional information you deem important (e.g. issue happens only occasionally):**\n\n**Output of `podman version`:**\n\n```\n(paste your output here)\n```\n\n**Output of `podman info`:**\n\n```\n(paste your output here)\n```\n\n**Package info (e.g. output of `rpm -q podman` or `apt list podman` or `brew info podman`):**\n\n```\n(paste your output here)\n```\n\n**Have you tested with the latest version of Podman and have you checked the Podman Troubleshooting Guide? (https://github.com/containers/podman/blob/main/troubleshooting.md)**\n\n\nYes/No\n\n**Additional environment details (AWS, VirtualBox, physical, etc.):**\n",
    "comments": [
        {
            "user": "afbjorklund",
            "body": "Mounting `/tmp` with a remote filesystem is somewhat controversial, many expect that to be local (or even tmpfs/memory)\n\nYou could of course mount it somewhere else, but then the container volume mount need to be adjusted accordingly too..."
        },
        {
            "user": "ashley-cui",
            "body": "Okay, I can definitely mount `/Users`, `/private`, and `/var/folders`. \n`/Volumes` fails with a `Too many levels of symbolic links`, as Volumes on Mac is just a symlink to `/` which seems like a bad idea to mount anyway.\n`/tmp` fails because the Podman machine uses the `tmp` directory inside the machine and writes content to it on boot, causing the mount to fail. However, on Mac, `/tmp` is symlinked to `/private/tmp` anyway.\n\nLet me know if it's sufficient to just support `/Users`, `/private`, and `/var/folders`. "
        },
        {
            "user": "benoitf",
            "body": "would be a nice enhancement as I think for example on macOS people start containers mounting stuff mainly from their home folder or /private/tmp"
        },
        {
            "user": "ashley-cui",
            "body": "Opened a PR that mounts `/Users`, `/private`, and `/var/folders`!"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "ashley-cui",
            "body": "Closed via https://github.com/containers/common/pull/1265 "
        }
    ]
}