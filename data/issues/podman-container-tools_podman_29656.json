{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29656,
    "type": "pull_request",
    "title": "api: reflect container stats errors in HTTP status codes",
    "state": "closed",
    "labels": [
        "kind/api-change"
    ],
    "user": "hkwi",
    "body": "Existing `KillContainer` handling uses HTTP status code 409 when the request cannot be performed because of the current container state. HTTP status code 404 is also used when the target container does not exist.\n\nIn contrast, errors from the stats handler are not reflected in the HTTP status code. The HTTP status code is always 200, and the errors are recorded only as generic errors in the server log.\n\nTo maintain compatibility with both streaming enabled and disabled, this change treats obtaining at least one complete unit of response content as the response contract. It then keeps the response content consistent with the HTTP status code.\n\n#### Checklist\n\n- [x] I have read and understood our [contributing guidelines](https://github.com/podman-container-tools/podman/blob/main/CONTRIBUTING.md) and will not have more than two open PRs as a new contributor.\n- [x] PR description, commit message, and GitHub comments are human-written, per [LLM Policy](https://github.com/podman-container-tools/community/blob/main/LLM_POLICY.md)\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits. (`git commit -s`).\n- [x] Referenced issues using `Fixes: #00000` in commit message (not applicable)\n- [x] Tests have been added/updated.\n- [x] Documentation has been updated (no documentation changes are needed).\n- [x] All commits pass `make validatepr` (format/lint checks).\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nThe container stats API now returns an appropriate HTTP error status when stats cannot be collected before the response starts.\n```\n\nTranslation note: The original Japanese text for this pull request description and commit message was written by the contributor. An LLM was used only to translate that text into English.\n",
    "comments": [
        {
            "user": "Luap99",
            "body": "also windows unit tests failt o compile because the package only works for linux/freebsd\n\nyou will have to add the package to the skippackages in winmake.ps1"
        },
        {
            "user": "hkwi",
            "body": "Added the exception to `winmake.ps1` and squashed the commits.\n\nTranslation note: The original Japanese comment was written by the contributor and translated into English using an LLM.\n"
        }
    ],
    "cross_references": [
        {
            "organization": "hashicorp",
            "repository": "nomad-driver-podman",
            "number": 530
        }
    ]
}