{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 12441,
    "title": "Bindings: remove(nonexistent) does not return response code",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "Discovered via lint. The `images_test.go:It(\"remove image\")` test was a NOP. Fixing it resulted in:\n```go\n\t\t// Remove invalid image should be a 404\n\t\tresponse, errs := images.Remove(bt.conn, []string{\"foobar5000\"}, nil)\n\t\tExpect(len(errs)).To(BeNumerically(\">\", 0))\n\t\tcode, _ := bindings.CheckResponseCode(errs[0])\n\t\t// FIXME FIXME FIXME: THIS IS BROKEN\n\t\t// FIXME FIXME FIXME: we get msg: \"foobar5000: image not known\"\n\t\t// FIXME FIXME FIXME: ...with no ResponseCode\n\t\tExpect(code).To(BeNumerically(\"==\", -1))    <--- should be 404 maybe? I have no idea\n```\nOn a similar vein, in the same test:\n```go\n\t\t// try to remove the image \"alpine\". This should fail since we are not force\n\t\t// deleting hence image cannot be deleted until the container is deleted.\n\t\tresponse, errs = images.Remove(bt.conn, []string{alpine.shortName}, nil)\n\t\tcode, _ = bindings.CheckResponseCode(errs[0])\n\t\t// FIXME FIXME FIXME: another invalid error\n\t\t// FIXME FIXME FIXME: this time msg=\"Image used by SHA: ...\"\n\t\tExpect(code).To(BeNumerically(\"==\", -1))\n```",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@baude PTAL"
        },
        {
            "user": "vrothberg",
            "body": "I'll tackle this one.  Everything's in order but I will open a PR explaining the behavior."
        }
    ]
}