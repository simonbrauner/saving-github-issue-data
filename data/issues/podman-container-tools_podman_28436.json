{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28436,
    "type": "issue",
    "title": "CDI GPU devices not passed through when using `podman compose`",
    "state": "closed",
    "labels": [
        "kind/bug",
        "jira",
        "needs-info",
        "triaged"
    ],
    "user": "matteogrechi",
    "body": "### Issue Description\n\nWhen using `podman compose` (backed by the `docker-compose` plugin), CDI GPU devices specified in the `devices:` field of a compose file are silently ignored. The container starts without GPU access and falls back to CPU.\n\nThe same configuration works correctly with:\n- `podman run --device nvidia.com/gpu=all`\n- `podman-compose` (pip package)\n\n### Steps to reproduce the issue\n\n1. Generate CDI spec:\n```bash\nsudo nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml\n```\n\n2. Create `compose.yaml`:\n```yaml\nservices:\n  test:\n    image: nvcr.io/nvidia/cuda:12.2.0-base-ubuntu22.04\n    command: nvidia-smi\n    devices:\n      - nvidia.com/gpu=all\n    security_opt:\n      - label=disable\n```\n\n3. Run:\n```bash\npodman compose up\n```\n\n### Describe the results you received\n\nCDI device is ignored. Container starts without GPU. No error or warning is shown.\n\n### Describe the results you expected\n\nContainer starts with GPU access. `nvidia-smi` runs successfully inside the container.\n\n### podman info output\n\n```yaml\n- OS: Ubuntu 24.04\n- Podman version: 4.9.3\n- Compose backend: `/usr/libexec/docker/cli-plugins/docker-compose` v2.37.1\n- nvidia-container-toolkit: 1.19.0\n- GPU: NVIDIA Tesla T4\n- CDI spec: generated at `/etc/cdi/nvidia.yaml`\n```\n\n### Podman in a container\n\nNo\n\n### Privileged Or Rootless\n\nRootless\n\n### Upstream Latest Release\n\nNo\n\n### Additional environment details\n\n_No response_\n\n### Additional information\n\n### Workaround\n\nUsing `podman run` directly with `--device nvidia.com/gpu=all` works correctly.\nUsing `podman-compose` (pip package) also works correctly.\n\n### Additional Notes\n\nThe `deploy.resources.reservations.devices` block with `driver: cdi` is also ignored by the docker-compose backend.\n\nThis appears to be because `podman compose` delegates to the `docker-compose` plugin which does not honor CDI device syntax, bypassing podman's native CDI handling entirely.",
    "comments": [
        {
            "user": "matteogrechi",
            "body": "May be related to --> https://github.com/containers/podman/issues/28309"
        },
        {
            "user": "Honny1",
            "body": "Docker Compose hits Podman’s Docker-compat create path, which always encodes `HostConfig.Devices` as `host:container:perms`, so CDI ids like `nvidia.com/gpu=all` get extra colons, stop parsing as CDI, and fail or silently drop GPU while podman run `--device` still works because it passes the id unchanged."
        },
        {
            "user": "Honny1",
            "body": "@matteogrechi, Hi, can you check your setup with the latest podman?\n\nPodman 4.9.3 - does not have the `DeviceRequests` handling\n\nI think it was fixed by https://github.com/containers/podman/pull/25171"
        },
        {
            "user": "github-actions[bot]",
            "body": "A reviewer has determined we need more information to understand the reported issue.  A comment on what is missing should be provided. Be certain you:\n\n* provide an exact reproducer where possible\n* verify you have provided all relevant information - minimum is `podman info`\n* answer any follow up questions\n\nIf no response to the `needs-info` is provided in 30 days, this issue may be closed by our stale bot.\n\nFor more information on reporting issues on this repository, consult our [issue guide](ISSUE.md).\n"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28309
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28495
        }
    ]
}