{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 6629,
    "title": "installing oci-umount causes podman to fail",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "edsantiago",
    "body": "With `oci-umount-2:2.5-4.gitc3cda1f.fc32` installed, `podman run` fails:\n```console\n# podman run alpine date\nError: error setting up OCI Hooks: parsing hook \"/usr/share/containers/oci/hooks.d/oci-umount.json\": unrecognized hook version: \"\"\n```\nThis is `podman-2:2.0.0-0.2.rc6.fc32`. It is a regression: `1.9.3-1.fc32` works fine.\n",
    "comments": [
        {
            "user": "heatmiser",
            "body": "Seeing the same error...Fedora 31\n\n`# sudo podman run -i -t -P --name devcntr-$(date +%Y-%m-%d.%H%M) --hostname devcntr-$(date +%Y-%m-%d.%H%M) -v /etc/localtime:/etc/localtime:ro -v ~/cntrutil01:/cntrutil01:Z localhost/fed-cloudcntr:latest /bin/bash\nError: error setting up OCI Hooks: parsing hook \"/usr/share/containers/oci/hooks.d/oci-umount.json\": unrecognized hook version: \"\": parsing hook \"/usr/share/containers/oci/hooks.d/oci-systemd-hook.json\": unrecognized hook version: \"\"`\n\n`# podman -v`\n`podman version 2.0.0-dev`\n`# rpm -q podman`\n`podman-2.0.0-1592321502.git908bc3fb.fc31.x86_64`\n...from Copr repo for Upstream_CRIO_Family owned by baude\nRecently executed package updates, not sure what previous version of podman was, but it worked"
        }
    ]
}