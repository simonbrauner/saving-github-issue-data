{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 11212,
    "type": "pull_request",
    "title": "cgroup-manager-systemd: Warn early if user is rootless and no relevent user session is present.",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "flouthoc",
    "body": "If podman is invoked with cgroup manager as `systemd` and user is `rootless` , podman should fail early if no valid session is present.\n\n**Problem**:\nCurrently following scenario is handled by runtimes. Idea is to make experience more seamless for users with appropriate failures at manager level.\n\nPS: We can also just spit a warning instead of strict failure.\n\nCloses: https://github.com/containers/podman/issues/11197\n\n",
    "comments": [
        {
            "user": "flouthoc",
            "body": "@mheon Could you please take a look ? We can also spit a warning instead of explicit failure."
        },
        {
            "user": "mheon",
            "body": "I'm unsure as to whether this is a good idea. We have a lot of people running rootless Podman in cron jobs, systemd units, and other environments that don't have a login session, generally with success. Warning line is a lot better, and we might even want to drop it down to Info level so it's not printed by default, but people will still see it with `--log-level=debug`.\n\nAlso, this shouldn't be for all rootless systems - I think this is Rootless CGv2 + Systemd cgroups driver only."
        },
        {
            "user": "flouthoc",
            "body": "@mheon Agreed we can suppress this to debug log level as well."
        },
        {
            "user": "flouthoc",
            "body": "@TomSweeneyRedHat @rhatdan PTAL "
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/11212#\" title=\"Author self-approved\">flouthoc</a>*, *<a href=\"https://github.com/containers/podman/pull/11212#pullrequestreview-730440869\" title=\"Approved\">giuseppe</a>*, *<a href=\"https://github.com/containers/podman/pull/11212#pullrequestreview-731022103\" title=\"Approved\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [giuseppe,rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "flouthoc",
            "body": "@rhatdan added `logrus.Debugf` for internal errors instead of `logrus.Warnf` as few of tests are invoking top level commands like `info` with cgroup manager=systemd such tests will get warning for \"invalid systemd sessions\" but they have no intentions of running a container so I think we can use `logrus.Debugf` instead of a warning otherwise we need to fix downstream tests so that they expect a warning."
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}