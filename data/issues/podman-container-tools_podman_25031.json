{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 25031,
    "title": "More \"network update\" options",
    "state": "closed",
    "labels": [
        "kind/feature",
        "network",
        "locked - please file new issue/PR"
    ],
    "user": "famfo",
    "body": "### Feature request description\n\nI would like to have more options to update an existing network than just adding and removing DNS entries. For example:\n- adding a new subnet / gateway\n- removing a subnet / gateway\n- enable IPv6 networking\n\n### Suggest potential solution\n\nTo have the `podman network update` command include all / most of the options `podman network create` to update existing networks.\n\n### Have you considered any alternatives?\n\nA command to edit the network definition file directly with `$EDITOR` with some simple docs in a comment.\n\n### Additional context\n\nThe default network doesn't have IPv6 support so I need to edit it on all my setups.",
    "comments": [
        {
            "user": "Luap99",
            "body": "I am going to close this as not planned.\nActually updating critical setting such as ip addresses cannot be supported easily and would cause a huge maintenance overhead that is simply not worth the trouble.\nUpdating any setting while a container is running would mean updating interfaces and firewall rules on the fly which is just to much work to do it correctly.\n\nWhen no container is running we could just rewrite the config file but there isn't even proper tracking on if a container is running on a network at any given time in the network backend so this operation is not race free and IMO thus impossible.\n\nIf you want to edit files by hand you can do this but that is totally unsupported. "
        }
    ]
}