{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 15505,
    "type": "issue",
    "title": "completion bash does not suggest `-s` after `kube generate`",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "dilyanpalauzov",
    "body": "After calling `. <(podman completion bash)` I type\n```console\n$ podman kube generate <TAB>\n```\nI expect that one of the suggestions is `-s`, but there is no such suggestion.\n\n* `podman completion bash` shall generate completions `-f` and `-s` after `podman kube generate <TAB>`\n* Likewise for `podman generate kube <TAB>`\n\n```console\n$ podman version\nClient:       Podman Engine\nVersion:      4.3.0-dev\nAPI Version:  4.3.0-dev\nGo Version:   go1.18.5\nGit Commit:   ff96bc47e39c4282baffa948d5169edd8bdad44f\nBuilt:        Fri Aug 26 22:39:03 2022\nOS/Arch:      linux/amd64\n```",
    "comments": [
        {
            "user": "dilyanpalauzov",
            "body": "In fact after `podman generate systemd <TAB>`none of the `--options` are suggested:\n```\n      --after stringArray         Add dependencies order to the generated unit file\n      --container-prefix string   Systemd unit name prefix for containers (default \"container\")\n  -f, --files                     Generate .service files instead of printing to stdout\n      --format string             Print the created units in specified format (json)\n  -n, --name                      Use container/pod names instead of IDs\n      --new                       Create a new container or pod instead of starting an existing one\n      --no-header                 Skip header generation\n      --pod-prefix string         Systemd unit name prefix for pods (default \"pod\")\n      --requires stringArray      Similar to wants, but declares stronger requirement dependencies\n      --restart-policy string     Systemd restart-policy (default \"on-failure\")\n      --restart-sec uint          Systemd restart-sec\n      --separator string          Systemd unit name separator between name/id and prefix (default \"-\")\n      --start-timeout uint        Start timeout override\n      --stop-timeout uint         Stop timeout override (default 10)\n      --template                  Make it a template file and use %i and %I specifiers. Working only for containers\n      --wants stringArray         Add (weak) requirement dependencies to the generated unit file\n```\n\nLikewise after `podman generate spec <TAB>` these options are not completed:\n```\n  -c, --compact           Print the json in a compact format for consumption\n  -f, --filename string   Write output to the specified path\n  -n, --name              Specify a new name for the generated spec (default true)\n```"
        },
        {
            "user": "Luap99",
            "body": "This is how it is supposed to work.\nBy default you will only get arguments completed. To get options you have to type `-` and then press tab."
        }
    ],
    "cross_references": []
}