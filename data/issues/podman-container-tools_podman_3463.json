{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3463,
    "title": "Running `sudo -E podman` after rootless `podman` has side effects",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "marcov",
    "body": "**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind bug\n\n\n**Description**\n\nRunning `sudo -E podman <anycommand>` after a rootless podman command (that creates the pause process) causes podman to misbehave.\n\n**Steps to reproduce the issue:**\n```\n$ ./bin/podman ps\nCONTAINER ID  IMAGE  COMMAND  CREATED  STATUS  PORTS  NAMES\n\n$ /usr/bin/sudo ./bin/podman ps\nCONTAINER ID  IMAGE  COMMAND  CREATED  STATUS  PORTS  NAMES\n\n$ /usr/bin/sudo -E ./bin/podman ps\nError: error creating libpod runtime: error creating runtime static files directory\n/var/lib/containers/storage/libpod: mkdir /var/lib/containers/storage: permission denied\n\n$ rm /run/user/1000/libpod/pause.pid; pkill -9 podman\n\n$ /usr/bin/sudo -E ./bin/podman ps\nCONTAINER ID  IMAGE  COMMAND  CREATED  STATUS  PORTS  NAMES\n```\n**Output of `podman version`:**\n\n```\nVersion:            1.4.4-dev\nRemoteAPI Version:  1\nGo Version:         go1.12.5\nOS/Arch:            linux/amd64\n```",
    "comments": [
        {
            "user": "mheon",
            "body": "@giuseppe PTAL"
        },
        {
            "user": "marcov",
            "body": "Adding some debug Printf right before trying to create `/var/lib/containers/storage`, I can see that the user namespace of the `sudo -E podman` is the same of the podman pause process showed in `lsns -t user`\n"
        },
        {
            "user": "giuseppe",
            "body": "Isn't XDG_RUNTIME_DIR pointing to the wrong directory with sudo -E? "
        },
        {
            "user": "marcov",
            "body": "Yea, it is still pointing to `/run/user/1000`. I guess that's not OK then..."
        },
        {
            "user": "giuseppe",
            "body": "I've opened a PR here: https://github.com/containers/libpod/pull/3468"
        }
    ]
}