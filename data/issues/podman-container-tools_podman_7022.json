{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7022,
    "title": "APIv2: No events on image build",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "marusak",
    "body": "**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind bug\n\n**Description**\n`podman build` does not produce any event in the REST API.\n\n**Steps to reproduce the issue:**\n\n1. `sudo curl --unix-socket /run/podman/podman.sock http://d/v1.24/events` - start listening on events\n\n2. ` sudo podman build .` (some simple Dockerfile)\n\n3. `sudo podman images -a` shows a new image, but there is no events in the step 1. (`sudo podman pull` produces event)\n\n**Describe the results you received:**\n\n\n**Describe the results you expected:**\nSome events that the image list has changed.\n\n**Output of `podman version`:**\n\n```\nVersion:      2.0.1\nAPI Version:  1\nGo Version:   go1.14.3\nBuilt:        Thu Jan  1 01:00:00 1970\nOS/Arch:      linux/amd64\n```",
    "comments": [
        {
            "user": "vrothberg",
            "body": "Thanks for opening an issue, @marusak. I can reproduce: both `podman build` and `podman-remote build` do not yield an event."
        },
        {
            "user": "vrothberg",
            "body": "I found some time to have a closer look. It seems there is no `build` event at all. @mheon did we have some in previous versions?"
        },
        {
            "user": "mheon",
            "body": "I don't see how we would, given that the events code lives in Libpod and\nimage build is entirely in Buildah land\n\nOn Mon, Jul 20, 2020, 06:29 Valentin Rothberg <notifications@github.com>\nwrote:\n\n> I found some time to have a closer look. It seems there is no build event\n> at all. @mheon <https://github.com/mheon> did we have some in previous\n> versions?\n>\n> —\n> You are receiving this because you were mentioned.\n> Reply to this email directly, view it on GitHub\n> <https://github.com/containers/podman/issues/7022#issuecomment-660944919>,\n> or unsubscribe\n> <https://github.com/notifications/unsubscribe-auth/AB3AOCDO5ITCTHH2BVPR2RDR4QMATANCNFSM4PB7LGXA>\n> .\n>\n"
        },
        {
            "user": "vrothberg",
            "body": "> I don't see how we would, given that the events code lives in Libpod and image build is entirely in Buildah land\n\nI think it's pretty similar to pulling, which happens in c/image. We could track down all buildah API calls in the code and record events there. Wouldn't that work?"
        },
        {
            "user": "mheon",
            "body": "The problem specifically with build is that we immediately hand off from our CLI to the Buildah CLI - the other calls are usually a lot more integrated with the rest of our code (`commit` on images, for example - uses Buildah code, but just library calls, not handing off the entire responsibility, so we can embed the events calls).\n\nWe could potentially generate an event before we ever call Buildah, but I don't think we know some important things at that point (ID of the final image being the big one)"
        },
        {
            "user": "rhatdan",
            "body": "Let's integrate something intoBuildah, that it can call functions on events.  Would seem to be the best way."
        },
        {
            "user": "mheon",
            "body": "We should move the events code into c/common to allow Buildah to also call into it."
        },
        {
            "user": "mheon",
            "body": "We will have issues if they want to do container events, though, because we can't see those in `podman ps` or the list API, so Cockpit will get very confused."
        },
        {
            "user": "vrothberg",
            "body": "> We will have issues if they want to do container events, though, because we can't see those in `podman ps` or the list API, so Cockpit will get very confused.\n\nVery good point. I think `Buildah` as a tool shouldn't do events, but `buildah` as a library should provide a way to opt-in to events. This way, Podman could call the buildah API with events enabled where we'd get events on image pulls, commits, builds etc."
        },
        {
            "user": "vrothberg",
            "body": "I suggest that we can tackle that _after_ we split libpod from Podman."
        }
    ]
}