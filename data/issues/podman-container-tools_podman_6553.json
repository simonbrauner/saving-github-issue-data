{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 6553,
    "type": "pull_request",
    "title": "--replace for containers and pods",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "vrothberg",
    "body": "Add a `--replace` flag to `podman {create,run}` and `podman pod create` to allow for replacing and removing existing named containers and pods.\n\nUse that flag in `generate systemd` to make the generated units more robust and remove named containers or pods even after a system crash.\n\nFixes: #5485",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/6553#\" title=\"Author self-approved\">vrothberg</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [vrothberg]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "vrothberg",
            "body": "@mheon @rhatdan PTAL"
        },
        {
            "user": "mheon",
            "body": "Oh, can we force this to only be used when a name is explicitly specified? So error if `podman run` with `--replace` but no `--name` is given?"
        },
        {
            "user": "vrothberg",
            "body": "> Oh, can we force this to only be used when a name is explicitly specified? So error if `podman run` with `--replace` but no `--name` is given?\n\nI prefer to avoid a conflict as it will make the systemd generation easier (we don't have to look for a --name). If you feel strongly, I can make it work (approximate)."
        },
        {
            "user": "vrothberg",
            "body": "@mheon, I wired the flag into `pod create` and into the systemd units as well. The big --new pod PR was merged, so I could complete --replace."
        },
        {
            "user": "vrothberg",
            "body": "@giuseppe @rhatdan @TomSweeneyRedHat PTAL"
        },
        {
            "user": "rhatdan",
            "body": "LGTM"
        },
        {
            "user": "mheon",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 5485
        }
    ]
}