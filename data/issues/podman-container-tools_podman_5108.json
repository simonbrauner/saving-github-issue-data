{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 5108,
    "title": "suggestion: rename `podman service` to avoid conflict with `docker service`",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "AkihiroSuda",
    "body": "`podman service`  looks as if it was Podman's implementation of `docker service`.\n\nI suggest renaming `podman service` to `podman daemon`, because it seems actually reimplementation of `docker daemon` that had existed in Docker 1.10 and prior versions.\n",
    "comments": [
        {
            "user": "mheon",
            "body": "We're trying to avoid running this as a daemon (using systemd socket-activation, and having a timeout on the command so it's not always on), so `podman daemon` is probably a bad idea, but I'm not opposed to renaming to ensure we don't conflict."
        },
        {
            "user": "mheon",
            "body": "If so, we should do this quick, before we cut a 1.8.0 final and formalize the name in an official release"
        },
        {
            "user": "mheon",
            "body": "@baude @rhatdan @vrothberg PTAL"
        },
        {
            "user": "AkihiroSuda",
            "body": "Or maybe `podman system service`\n"
        },
        {
            "user": "vrothberg",
            "body": "`podman listen`?\n\nThanks for pointing that out, @AkihiroSuda !"
        },
        {
            "user": "rhatdan",
            "body": "Why would podman service conflict with docker.service?"
        },
        {
            "user": "rhatdan",
            "body": "Podman is not listening by default at docker.sock, If you install podman-docker, it will create a linkfile between /run/docker.sock and /run/podman.sock.  But podman-docker conflicts with docker-* packages."
        },
        {
            "user": "vrothberg",
            "body": "There's a `docker service` command -> https://docs.docker.com/engine/reference/commandline/service/"
        },
        {
            "user": "rhatdan",
            "body": "So this is their replacement for dockerd?  If it is, then podman service makes sense.  Except for not listening on docker.sock?"
        },
        {
            "user": "mheon",
            "body": "No, it's a swarm command, AFAIK - it launches a service via swarm"
        },
        {
            "user": "rhatdan",
            "body": "Well we have said we will not support Docker swarm, does it matter we have the same name?"
        },
        {
            "user": "mheon",
            "body": "Potential confusion? I'd prefer not to knowingly conflict with Docker commands if we can afford not to"
        },
        {
            "user": "rhatdan",
            "body": "Fine, except that if we come up with a strange name for this service.\npodman restapi\npodman server"
        },
        {
            "user": "mheon",
            "body": "`podman server` sounds good to me"
        },
        {
            "user": "mheon",
            "body": "`podman system service` also works for me"
        },
        {
            "user": "rhatdan",
            "body": "Yes I like keeping it under system."
        }
    ]
}