{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 12287,
    "type": "issue",
    "title": "[RFE] respect `target` option in `mount` type secret",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "mskarbek",
    "body": "**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind feature\n\n**Description**\n\n`podman run -it --secret token,type=env,target=TOKEN ubi8/ubi:latest bash` is currently a valid command.\n`podman run -it --secret token,type=mount,target=TOKEN ubi8/ubi:latest bash` ends with error:\n```\nError: target option is invalid for mounted secrets: error parsing secret\n```\nI would like to be able to specify `target` option in `mount` type as file name. The above command should produce file `/run/secrets/TOKEN` in that instance.\n\nUse case:\nMultiple containers of the same image, each configured individually with their secrets but with a common internal script/unit file. I have a unit file that always looks in the same place, but each container gets their individual secret.\n\nEdit:\nThis should also include `mount-env` (#12284) when/if implemented.\n",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@ashley-cui PTAL"
        },
        {
            "user": "rhatdan",
            "body": "@flouthoc PTAL"
        },
        {
            "user": "rhatdan",
            "body": "@mskarbek interested in opening a PR to allow this?"
        },
        {
            "user": "mskarbek",
            "body": "@rhatdan is there a documentation describing code structure, or maybe recording of a codding session that shows of code structure during implementation of some feature? I have written little to no Go code, but I'll gladly learn something new."
        },
        {
            "user": "flouthoc",
            "body": "@mskarbek I think this is a good RFE and needs a small patch. @rhatdan @ashley-cui  PTAL"
        },
        {
            "user": "mskarbek",
            "body": "Can this be backported to 3.4.2-rhel branch/included in 3.4 release in RHEL?"
        },
        {
            "user": "mheon",
            "body": "I recommend you open a BZ if you want a backport."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12294
        }
    ]
}