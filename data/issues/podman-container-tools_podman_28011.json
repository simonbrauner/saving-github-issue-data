{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28011,
    "type": "pull_request",
    "title": "rootless_tutorial: mention LDAP support for subid ranges",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "alexey-tikhonov",
    "body": "#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [ ] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone.\n```\n",
    "comments": [
        {
            "user": "alexey-tikhonov",
            "body": "> [ ] All commits pass `make validatepr` (format/lint checks)\n\nI tried this but it ended with\n```\nLinting for GOOS=linux\n+ ./bin/golangci-lint run --build-tags=apparmor,seccomp,selinux\nhack/golangci-lint.sh: line 29: 25089 Killed                     ( set -x; ./bin/golangci-lint run --build-tags=\"${TAGS}${EXTRA}\" \"$@\" )\n```\n\nIs this required/helpful for a .md doc update?"
        },
        {
            "user": "mheon",
            "body": "It does check for more than just code lint (extra whitespace at EOL, among other things), and we run it against every PR, but the amount of time it takes to run is quite notable. Unusual that it failed, though?"
        }
    ],
    "cross_references": []
}