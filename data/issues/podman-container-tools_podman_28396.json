{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28396,
    "type": "pull_request",
    "title": "compat: populate IPRange in IPAM config when listing networks",
    "state": "closed",
    "labels": [
        "kind/api-change"
    ],
    "user": "crawfordxx",
    "body": "## Summary\n\nWhen converting a libpod network to a Docker-compatible network in\n`convertLibpodNetworktoDockerNetwork`, the `IPAMConfig.IPRange` field\nwas left empty with a `// TODO add range` comment.\n\nThis PR resolves the TODO by reconstructing the CIDR prefix from the\n`LeaseRange` stored on each subnet. The forward conversion (Docker\n`IPRange` CIDR → `LeaseRange{StartIP, EndIP}`) already exists in the\n`createNetwork` path. This adds the reverse direction:\n- Given `StartIP` and `EndIP` from `LeaseRange`, check whether they\n  form a valid aligned IPv4 CIDR block (power-of-two size, aligned\n  start address).\n- If so, return the corresponding `netip.Prefix` as `IPAMConfig.IPRange`.\n- Otherwise (non-CIDR-aligned ranges, IPv6) leave `IPRange` empty.\n\nThis means `docker network inspect` now shows the `IPRange` for\nnetworks created with `--ip-range` (or the Docker compat API equivalent),\nrestoring round-trip fidelity for the IPAM configuration.\n\nFixes #28378",
    "comments": [
        {
            "user": "crawfordxx",
            "body": "The MacOS arm64 build failure appears to be a transient Cirrus CI issue — the change only touches `pkg/api/handlers/compat/networks.go` which has `//go:build !remote && (linux || freebsd)` at the top, so it is excluded from macOS compilation entirely. All Linux/cross-compilation builds passed."
        },
        {
            "user": "baude",
            "body": "@crawfordxx i was able to rerun the test in question. it passed.  Ive now asked for all failed jobs to retry. thanks for the PR"
        },
        {
            "user": "baude",
            "body": "ok, api test failures here now have been revealed."
        },
        {
            "user": "crawfordxx",
            "body": "Fixed. The bug was in `leaseRangeToIPRangePrefix`: `FirstIPInSubnet` returns `network+1` (first usable host), not the network address itself. So for `10.10.61.128/25` the stored `StartIP` is `10.10.61.129` and `EndIP` is `10.10.61.255`. The old code computed `count = 127`, which is not a power of two, and returned `false`.\n\nThe fix recovers the network address as `StartIP - 1`, then computes `size = EndIP - networkAddr + 1 = 128`, which passes all checks and returns the correct `10.10.61.128/25` prefix."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "crawfordxx",
            "body": "Thanks for the review, @Honny1!\n\nRefactored in the latest commit:\n- Replaced the manual bit-loop with `bits.OnesCount32` (power-of-two check) and `bits.TrailingZeros32` (prefix length).\n- Extracted a small `toUint32` helper to avoid repeating the byte-shift expression.\n- Added `networks_test.go` covering nil inputs, /24, /25, /16, /32, misaligned range, non-power-of-two size, reversed endpoints, and IPv6.\n\nRe: moving to `libnetwork`/container-libs — happy to do that as a follow-up, but wanted to land the functional fix first. Let me know if you'd prefer it in the same PR."
        },
        {
            "user": "crawfordxx",
            "body": "Thanks for the detailed feedback, @Honny1 and @Luap99!\n\nUpdated in the latest commit:\n- Replaced the manual `toUint32` helper with `binary.BigEndian.Uint32` / `binary.BigEndian.PutUint32` throughout\n- Replaced the combined bitwise alignment check (`netU&(size-1) != 0`) with `proposedPrefix.Masked() != proposedPrefix` as in step 5 of the pseudocode\n- Renamed `netU` / `size` to `networkU` / `totalIPs` to better match the pseudocode naming\n\nThe `networkU = startU - 1` step is still needed because `FirstIPInSubnet` stores `network+1` in `StartIP`, so we recover the actual network address before constructing the prefix. After that the logic follows the pseudocode directly."
        },
        {
            "user": "Honny1",
            "body": "@crawfordxx, can you please rebase on main? Thanks"
        }
    ],
    "cross_references": []
}