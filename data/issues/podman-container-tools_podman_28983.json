{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28983,
    "type": "pull_request",
    "title": "Add --pidfile flag to podman start",
    "state": "open",
    "labels": [
        "stale-pr"
    ],
    "user": "nomarek",
    "body": "`podman create` and `podman run` already have `--pidfile`, but the path is fixed at create time and reused on every start. Toolbx wants a fresh path per start so it can read the PID of the container's main process and wait on it before running `podman exec`. This adds the same `--pidfile` flag to `podman start`.\n\nAfter the container starts, its PID is read with libpod's `Container.PID()` and written to the given path. As on `create`/`run`, the flag only works for a single container, is rejected with `--attach`, and is hidden on the remote client.\n\nTests: new e2e specs check that the PID written to the file matches `.State.Pid`, both for a freshly started and for an already-running container, plus error cases for starting more than one container (by argument and by `--filter`) and for `--attach`.\n\n#### Checklist\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits. (`git commit -s`).\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] Tests have been added/updated (or no tests are needed)\n- [x] Documentation has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] Release note entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nAdded a `--pidfile` option to `podman start` that writes the started container's PID to the given path (not supported with the remote client).\n```\n",
    "comments": [
        {
            "user": "ashley-cui",
            "body": "Please squash your commits once you are ready for merge. "
        },
        {
            "user": "nomarek",
            "body": "Squashed it down to a single commit."
        },
        {
            "user": "mheon",
            "body": "@Luap99 I seem to remember you being opposed to this approach at some point in the past - am I misremembering?"
        },
        {
            "user": "Luap99",
            "body": "see the linked issue https://github.com/podman-container-tools/podman/issues/25849\n\nI am not exactly a fan of this, if others see a strong need for this sure but looking at the code this is super racy which I would oppose."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": []
}