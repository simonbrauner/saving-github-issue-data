{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6933,
    "type": "pull_request",
    "title": "Add a workaround for using `mount` without privileges, re-enable rootless tests in CI",
    "state": "closed",
    "labels": [],
    "user": "nalind",
    "body": "#### What type of PR is this?\n\n/kind feature\n\n#### What this PR does / why we need it:\n\n* Add a `buildah mount-sshfs` workaround for not being able to `buildah mount` when not root.\n* Rework integration tests to use `buildah mount-sshfs` when run by a UID other than 0, and working around the workaround's limitations (chiefly: avoid `buildah run/add/copy` while the filesystem is mounted).\n* Re-enable rootless tests in CI.\n\n#### How to verify it\n\nNew and updated integration tests!\n\n#### Which issue(s) this PR fixes:\n\nThis is mainly about re-enabling tests in CI.  We weren't really testing with overlay in rootless scenarios before.\n\n#### Special notes for your reviewer:\n\nThe new commands are marked as hidden.\nThis doesn't add new library APIs.\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nAdds undocumented `buildah serve-sftp` and `buildah sshfs-mount` commands.  Don't tell anyone.\n```",
    "comments": [
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\n@ddarrah WDYT?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "Pinging Dave mostly  to make sure this is tracked for RHEL if we move forward with this."
        },
        {
            "user": "nalind",
            "body": "I keep meaning to bring up the sshfs/sftp part of it at a meeting to get a better feel for whether or not we want to include that part of this, since it has limitations if you try to use that outside of test cases."
        },
        {
            "user": "nalind",
            "body": "Been kicking the idea around since https://redhat.atlassian.net/browse/RUN-1457, finally had a reason to look into it."
        },
        {
            "user": "Luap99",
            "body": "I think it is certainly a nice use case for the tests. Maybe hiding the commands and label it unsupported on RHEL would help to reduce or maintenance load."
        },
        {
            "user": "ddarrah",
            "body": "It's a nice approach.  Do I understand correctly that the pipeloop is self contained in the test directory, and that sshfs is already a part of RHEL?  If not, I see no issue with installing it simply for the tests from EPEL.  We pull BATS in to run tests.  I just don't want any part of a new RHEL package, especially just to run tests."
        },
        {
            "user": "nalind",
            "body": "The pipeloop helper is standalone, but AFAIK sshfs is not a part of RHEL or EPEL."
        },
        {
            "user": "nalind",
            "body": "> The pipeloop helper is standalone, but AFAIK sshfs is not a part of RHEL or EPEL.\n\nMy mistake: I was looking for a package named \"sshfs\", not \"fuse-sshfs\".  It's been noted elsewhere that fuse-sshfs is in EPEL 9 and 10.\n\nChanged the new commands to be undocumented."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\nAlthough it would be nice if SFTP dropped the use of kr/fs and moved to the newer Go Filepath.* routines.  Way outside the purview of this review, however."
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}