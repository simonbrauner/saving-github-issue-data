{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29425,
    "type": "issue",
    "title": "hack/ci/ci.sh has an environment dependency (in all repos)",
    "state": "open",
    "labels": [
        "kind/bug"
    ],
    "user": "mtrmac",
    "body": "### Issue Description\n\nThe\n```\nlimactl copy \"$REPO_DIR\" $LIMA_VM_NAME:/var/tmp/…\n```\n\nline in `hack/ci/ci.sh` should include `-r`. This does work in CI, but breaks in (some?) local VMs. Guessing, perhaps depending on whether `scp` or `rsync` is used.\n\nApples also to Buildah and Skopeo.\n\n@timcoding1988 PTAL at least to confirm (low priority, perhaps delegate).\n\n```\n/usr/bin/scp: local \"$CHECKOUT/hack/ci/../..\" is not a regular file\n/usr/bin/scp: failed to upload file $CHECKOUT/hack/ci/../.. to /var/tmp/buildah\n```",
    "comments": [
        {
            "user": "mwemmz",
            "body": "Hi @mtrmac, I'd like to work on this one. Looking at the limactl copy \"$REPO_DIR\" $LIMA_VM_NAME:/var/tmp/… line in hack/ci/ci.sh, I'll add the -r flag so it recurses correctly when copying the repo directory — that should resolve the scp: local ... is not a regular file error you're seeing locally.\n\nI'll open a PR shortly. Happy to also flag the same fix for Buildah and Skopeo if that's useful, since you mentioned it applies there too."
        }
    ],
    "cross_references": []
}