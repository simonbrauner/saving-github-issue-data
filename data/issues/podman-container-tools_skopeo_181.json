{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 181,
    "type": "issue",
    "title": "avoid policy.json check?",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "runcom",
    "body": "On a fresh machine install I just needed skopeo to pull some oci images needed for testing and I realized the tool is failing with:\n\n```\n$ skopeo copy docker://busybox oci:busybox\nFATA[0000] Error loading verification policy: open /etc/containers/policy.json: no such file or directory\n```\n\n@mtrmac do you think we can relax this somehow? I know I can use `default-policy.json` tbh but.. wdyt?\n",
    "comments": [
        {
            "user": "runcom",
            "body": "(I'm not totally sure about this proposal because if you happen to compile from source then you probably know you have to `cp default-policy.json /etc/containers/policy.json` and if you install from distros, probably the distro is taking care of installing `/etc/containers/policy.json` for you)\n"
        },
        {
            "user": "mtrmac",
            "body": "Yeah, this was highlighted in #157 .\n\nThe default absolutely must be to use the config file (instead of security being opt-in), and then it IMHO only makes sense to fail if that file unexpectedly disappears.\n\nI wouldn’t be philosophically opposed to something like `--policy-insecure-allow-anything` instead of requiring the users to keep a permissive policy file around, but I also won’t work on it this month, and I’m not sure that it is all that more convenient than `--policy $insecure-policy.json`.\n\nFor development, yeah, run `make install`. Or there could be a `make debug-build` which uses `-ldflags \"-X github.com/containers/image/signature.systemDefaultPolicyPath=$(pwd)/default-policy.json\"`, but then I would be rather worried about such a build getting to a released package somewhere.\n"
        },
        {
            "user": "runcom",
            "body": "> For development, yeah, run make install. Or there could be a make debug-build which uses -ldflags \"-X github.com/containers/image/signature.systemDefaultPolicyPath=$(pwd)/default-policy.json\", but then I would be rather worried about such a build getting to a released package somewhere.\n\nright, it will be hard to enforce this, though `debug-build` or `dev-build` makes a lot of sense to me\n"
        },
        {
            "user": "kushaldas",
            "body": "I would like to implement that `--policy-insecure-allow-anything` option if that is okay with you all. \n"
        },
        {
            "user": "kushaldas",
            "body": "I have a small patch with `skopeo --policy-insecure-allow-anything copy docker://busybox oci:busybox` working. But as this is my first day here, I would love to know if there is a better way (read any API call) to create an insecurepolicy than using a space-stripped JSON string as a variable?\n"
        },
        {
            "user": "rhatdan",
            "body": "Can we just call it --insecure?\n"
        },
        {
            "user": "runcom",
            "body": "> Can we just call it --insecure?\n\nsame for me\n"
        },
        {
            "user": "kushaldas",
            "body": "Will modify the flag, and update the PR.\n"
        },
        {
            "user": "mtrmac",
            "body": "> I would love to know if there is a better way (read any API call) to create an insecurepolicy than using a space-stripped JSON string as a variable?\n\nhttps://github.com/projectatomic/skopeo/pull/203/files#r78563547 this is hopefully a stable API.\n"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 200
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 203
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 319
        },
        {
            "organization": "virt-manager",
            "repository": "virt-bootstrap",
            "number": 2
        },
        {
            "organization": "hpc",
            "repository": "charliecloud",
            "number": 467
        }
    ]
}