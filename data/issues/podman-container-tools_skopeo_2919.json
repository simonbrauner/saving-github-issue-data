{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 2919,
    "type": "issue",
    "title": "Epic: GHA migration follow-up from skopeo-sandbox review",
    "state": "open",
    "labels": [],
    "user": "timcoding1988",
    "body": "## Summary\n\nTracking epic for unresolved CI review feedback from the `podman-container-tools/skopeo-sandbox` Cirrus → GHA migration.\n\nThe sandbox work landed upstream in PR #2883. These items are follow-up from sandbox PR https://github.com/podman-container-tools/skopeo-sandbox/pull/1 / PR https://github.com/podman-container-tools/skopeo-sandbox/pull/9 review threads and the upstream audit before deprecating the sandbox repo.\n\n## Sub-issues\n\n- [ ] #2912 — migrate `renovate.json5` to `podman-container-tools/automation`\n- [ ] #2913 — add zizmor workflow for GitHub Actions security scanning\n- [ ] #2914 — remove leftover Cirrus config after GHA migration\n- [ ] #2915 — remove `git safe.directory` workaround from container jobs\n- [ ] #2916 — golangci-lint installed via `make tools` instead of `golangci-lint-action`\n- [ ] #2917 — `ostree_rs_ext` job uses archived `ostree-rs-ext` repository\n- [ ] #2918 — `cancel-in-progress` cancels overlapping CI runs on `main` pushes\n- [ ] #2922 — `doccheck` does build + install before `validate-docs`\n- [ ] #2923 — redundant `make vendor` in `test_skopeo` job\n- [ ] #2924 — `ostree_rs_ext` reinstalls Rust and rebuilds dependencies every run\n\n## Related (not sub-issues)\n\n- PR #2911 — Renovate-managed `AUTOMATION_RELEASE` in `ci.yml` (companion to #2912)\n- Draft PR #2885 — started `safe.directory` / Makefile `GOBIN` cleanup (companion to #2915)\n- #2633 — broader CI Switch: Cirrus → TMT umbrella\n- Draft PR #2640 — TMT work including `ostree-rs-ext` (related to #2917)\n\n## Background\n\n- Sandbox: https://github.com/podman-container-tools/skopeo-sandbox\n- Upstream migration: PR #2883",
    "comments": [
        {
            "user": "timcoding1988",
            "body": "@mtrmac please let me know if this is good. I opened the issues (even some of them has been worked on), as long as it is not completed. we can use this epic for track and discussion.\n\n@Luap99  we can private or delete the sandbox repo.  "
        },
        {
            "user": "mtrmac",
            "body": "@timcoding1988 Thanks for filing those, and especially for linking all the later context.\n\nA few more things worth recording I think (all optimizations that shouldn’t materially change the test contents, so pretty low-priority):\n- https://github.com/podman-container-tools/skopeo-sandbox/pull/1#discussion_r3181672805 : `doccheck` should be possible to do with just `make validate-docs` without `make install` (and without removing `/usr/bin/skopeo`)\n- https://github.com/podman-container-tools/skopeo-sandbox/pull/1#discussion_r3182590794 : `make vendor` in the `test_skopeo` task is redundant (and `go mod verify` is fairly costly): that task depends on `validate` which already checked this\n- https://github.com/podman-container-tools/skopeo-sandbox/pull/1#discussion_r3182673020 : (If we preserve the `ostree_rs_ext` tests,) perhaps we can avoid installing Rust + dependencies on every run, by building into CI images or caching"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2922
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2923
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 2924
        }
    ]
}