{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28897,
    "type": "pull_request",
    "title": "image scp: preserve usernames containing an \"@\"",
    "state": "closed",
    "labels": [],
    "user": "ROKUMATE",
    "body": "## Problem\n`podman image scp` parses the `user@localhost::image` argument by cutting on the\nfirst `@`. When the username itself contains an `@` — e.g. an Active Directory\naccount like `user@domain` — it gets truncated to just `user`:\n\n    $ sudo podman image scp aduser@domain@localhost::example.com/foo/bar:latest\n    Error: user: unknown user aduser\n\n## Impact\nUsers on AD-joined (or any `user@domain`) machines can't use `podman image scp`\nto copy images between users — the local user lookup fails before the transfer\neven starts.\n\n## Fix\nIn `ParseImageSCPArg`, the `@localhost::` case only ever matches that exact\nseparator, so split the argument on `@localhost::` instead of the bare `@`. This\nkeeps the full username (including any `@domain`) and leaves the remote\n`user@host::` SSH path unchanged.\n\n    user@localhost::img          -> user        (unchanged)\n    user@domain@localhost::img   -> user@domain  (fixed)\n\n## Test\nAdded `TestParseImageSCPArg` in `pkg/domain/utils/scp_test.go` covering both the\nplain `user` and the `user@domain` cases. `go test ./pkg/domain/utils/` passes.\n\nFixes: #27655\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "ROKUMATE",
            "body": "check now \n"
        },
        {
            "user": "ROKUMATE",
            "body": "Added the integration test as well 👍"
        },
        {
            "user": "runiq",
            "body": "OP from the original issue here, I'd just like to say thanks a lot for implementing this :heart: "
        },
        {
            "user": "ROKUMATE",
            "body": "@runiq thanks to you to for helping me find my first issue for contributation 🙇🏼 "
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "LGTM\none of the tests is wonky, I've restarted in hopes that it is a flake."
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "tmt tests failed for commit b9003828db2580f465604a1446c540cee420ca5e. @lsm5, @psss, @thrix please check."
        },
        {
            "user": "Honny1",
            "body": "/packit retest-failed"
        },
        {
            "user": "ROKUMATE",
            "body": "i dont think packit ci failing was cause of mine commit ? ... if yes do tell i will just check myself as \nsame packit failed and passed my mine other prs not sure what eactly is failling by that "
        },
        {
            "user": "Honny1",
            "body": "Packit is flaky, and also sometimes our CI is flaky too. So I rerun tests. You can check the RAW logs and check failure if it is not related to your change, you can ask in the comments for a rerun. "
        },
        {
            "user": "ROKUMATE",
            "body": "will keep in mind next times \n"
        }
    ],
    "cross_references": []
}