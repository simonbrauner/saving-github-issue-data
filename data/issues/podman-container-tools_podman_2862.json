{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2862,
    "type": "pull_request",
    "title": "add image rmi event",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "baude",
    "body": "when deleting a commited image, the path for deletion has an early exit\nand the image remove event was not being triggered.\n\nSigned-off-by: baude <bbaude@redhat.com>",
    "comments": [
        {
            "user": "openshift-ci-robot",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/libpod/pull/2862#\" title=\"Author self-approved\">baude</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Flibpod).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/libpod/blob/master/OWNERS)~~ [baude]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "I would think it would be better if you changed the return nil to a break, That way you are sure that an image was removed.  And take off the defer.\n\n"
        },
        {
            "user": "rhatdan",
            "body": "How about\n```\ndiff --git a/libpod/image/image.go b/libpod/image/image.go\nindex cc056b81..b51135b9 100644\n--- a/libpod/image/image.go\n+++ b/libpod/image/image.go\n@@ -361,6 +361,12 @@ func (i *Image) Remove(force bool) error {\n        if _, err := i.imageruntime.store.DeleteImage(i.ID(), true); err != nil {\n                return err\n        }\n+       removed := 0\n+       defer func() {\n+               if removed > 0 {\n+                       i.newImageEvent(events.Remove)\n+               }\n+       }()\n        for parent != nil {\n                nextParent, err := parent.GetParent()\n                if err != nil {\n@@ -379,11 +385,11 @@ func (i *Image) Remove(force bool) error {\n                if _, err := i.imageruntime.store.DeleteImage(id, true); err != nil {\n                        logrus.Debugf(\"unable to remove intermediate image %q: %v\", id, err)\n                } else {\n+                       removed++\n                        fmt.Println(id)\n                }\n                parent = nextParent\n        }\n-       defer i.newImageEvent(events.Remove)\n        return nil\n }\n```"
        },
        {
            "user": "baude",
            "body": "@rhatdan i think the question is if we really want to expose that an intermediate image was deleted."
        },
        {
            "user": "rhatdan",
            "body": "Ok you are just showing that the DeleteImage was successful.  Which looks good.  Ignore my suggestion.\nLGTM"
        },
        {
            "user": "rhatdan",
            "body": "@mheon @vrothberg @giuseppe @TomSweeneyRedHat PTAL"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "tests aren't hip @baude, IDK if that's expected or not."
        },
        {
            "user": "mheon",
            "body": "LGTM. I'll hold off on a /lgtm until tests go green."
        },
        {
            "user": "mheon",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}