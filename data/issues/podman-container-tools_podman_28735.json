{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28735,
    "type": "pull_request",
    "title": "docs(api): drop trailing slash on podman.io/community contact URL",
    "state": "closed",
    "labels": [
        "kind/api-change",
        "No New Tests"
    ],
    "user": "mvanhorn",
    "body": "Drops the trailing slash on the `https://podman.io/community/` contact URL in `pkg/api/server/doc.go`. The generated swagger spec carries that URL into the ReDoc-rendered API reference at the top of the page, where issue #28298 reports it as a 404.\n\n`https://podman.io/community/` returns 404 today; `https://podman.io/community` (no trailing slash) returns 200 and is the form the live site uses in its own navigation, so dropping the slash restores a working link without changing the destination.\n\nThis addresses only the 404 portion of #28298. The deep-link regression (URLs like `?version=latest#tag/.../operation/...` clearing on load) is a separate ReDoc / single-page-app behavior change and is out of scope for this PR.\n\n#### Does this PR introduce a user-facing change?\n```release-note\nFixed broken contact URL at the top of the rendered API reference (`https://podman.io/community/` 404 → `https://podman.io/community`).\n```",
    "comments": [
        {
            "user": "danishprakash",
            "body": "> https://podman.io/community/ returns 404 today;\n\nIt doesn't for me, and looking at the comments on #28298, this issue has been fixed."
        },
        {
            "user": "Luap99",
            "body": "> > https://podman.io/community/ returns 404 today;\n> \n> It doesn't for me, and looking at the comments on #28298, this issue has been fixed.\n\nThe site is still broken though, all logos are missing which is not the case if the trailing slash is removed so I think this is easy and harmless to merge right now and figure out what the heel the website does later."
        },
        {
            "user": "danishprakash",
            "body": "> all logos are missing which is not the case if the trailing slash is removed\n\nI didn't notice that earlier, thanks for pointing it out."
        },
        {
            "user": "mvanhorn",
            "body": "Thanks @Luap99 for landing the trailing-slash fix on the community URL. Nice to get that cleaned up in the ReDoc reference."
        }
    ],
    "cross_references": []
}