{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 12414,
    "type": "pull_request",
    "title": "tunnel: allow `remote` and `API` to accept `--secrets`",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "flouthoc",
    "body": "Following commit makes sure that `build` api can accept external\nsecret and allows currently `NOOP` `podman-remote build -t tag\n--secret id=mysecret,src=/path/on/host` to become functional.\n\n#### TLDR:\nAdds support for \n`podman-remote build -t tag\n--secret id=mysecret,src=/path/on/host .`\n\n---\n\nJust like `docker` following api is a hidden field and only exposed to\n`podman-remote` but could document it if it needs exposed on `swagger`.\n\nCloses: https://github.com/containers/podman/issues/12415",
    "comments": [
        {
            "user": "flouthoc",
            "body": "Solves: https://github.com/containers/podman/issues/12415#issuecomment-975802180"
        },
        {
            "user": "flouthoc",
            "body": "@vrothberg Sure I am just waiting for discussion to close on something will add tests as soon as this is ready to be merged. Marking this as `draft` for now. Since this still does not solves the actual problem."
        },
        {
            "user": "flouthoc",
            "body": "@vrothberg We could copy secrets over to `remote VM` but not sure if its the right thing to do. I think we should do a bind mount hence this would still not solve cases like `podman-remote build -t tag --secret id=mysecret,src=/path/on/host .`"
        },
        {
            "user": "rhatdan",
            "body": "It is the right thing to do, copy the secret to the remote machine, but destroy it when the container is removed.  Secrets are secret to the container, IE they do not get committed to the image.  They are not secrets between the client and server.  You might want to clarify this in the man pages, but they need to be copied.  Mounting will not work if you are actually using a remote system as opposed to a VM."
        },
        {
            "user": "flouthoc",
            "body": "@rhatdan Sure makes sense. I was also worried about this difference between `mac` and `podman-remote server`. Copy should work for both of these cases. I'll amend my PR."
        },
        {
            "user": "vrothberg",
            "body": "@ashley-cui PTAL"
        },
        {
            "user": "flouthoc",
            "body": "@vrothberg thanks resolved in latest commit."
        },
        {
            "user": "mheon",
            "body": "Should we consider any additional security here, given this is secret data? I'm a little concerned that they might be included in built images unintentionally if an image does something like `COPY *`"
        },
        {
            "user": "rhatdan",
            "body": "/approve\n"
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/12414#\" title=\"Author self-approved\">flouthoc</a>*, *<a href=\"https://github.com/containers/podman/pull/12414#issuecomment-981701106\" title=\"Approved\">rhatdan</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "rhatdan",
            "body": "@flouthoc please test out @mheon possiblity.  Basically try a Containerfile with\n\nfrom fedor\ncopy  * /test/\n\nAnd then run a test like\n\npodman --remote -f /LOCATIONOF/Containerfile --secret ... /NOTLOCATIONOFContainerfile\n\nThen make sure the secret and Containerfile do not end up i the image."
        },
        {
            "user": "flouthoc",
            "body": "@mheon @rhatdan I tested this with `docker` and `buildkit` : `COPY * /` is a user driven command even docker copies everything to working directory from current context. If build secret file is in current context and user wants to run `COPY * /` all files will be copied over since that is what user wants.\n\nI guess we could workaround that by adding extra `.dockerignore` if we want but that would a different behavior though. \n\nPS: `Docker copies` everything including `Dockerfile` and so do we."
        },
        {
            "user": "mheon",
            "body": "But does Buildkit move all secrets into the build context, if they weren't already? Because it seems like that's what this code is doing."
        },
        {
            "user": "flouthoc",
            "body": "@mheon I haven't tried it on `mac` but i think it should not add secrets to context if they not part of it by default cause they must be `binding` files instead of copying it to context.\n\nI guess we could have a similar thing by checking if secret was not part of current context then add it to `.dockerignore` that should be able to achieve us a similar functionality. "
        },
        {
            "user": "flouthoc",
            "body": "> @flouthoc please test out @mheon possiblity. Basically try a Containerfile with\n> \n> from fedor copy * /test/\n> \n> And then run a test like\n> \n> podman --remote -f /LOCATIONOF/Containerfile --secret ... /NOTLOCATIONOFContainerfile\n> \n> Then make sure the secret and Containerfile do not end up i the image.\n\n@mheon @rhatdan Following case of leaking secrets into image is handled in a new commit also added a new test to verify the same."
        },
        {
            "user": "flouthoc",
            "body": "@rhatdan @mheon PTAL. New test verifies the concern."
        },
        {
            "user": "mheon",
            "body": "LGTM"
        },
        {
            "user": "flouthoc",
            "body": "@rhatdan PTAL"
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12415
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12167
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 12985
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 14615
        }
    ]
}