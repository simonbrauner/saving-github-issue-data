{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29724,
    "type": "pull_request",
    "title": "Fix build --secret leaving plaintext next to the context",
    "state": "open",
    "labels": [
        "kind/api-change"
    ],
    "user": "BetterAndBetterII",
    "body": "## Summary\n\n`processSecrets` renamed each build secret into the **parent of the build context** and never deleted it. For local-API / `podman machine` builds that parent is a user directory, so a successful `podman build --secret` left `podman-build-secret-*` next to the context (mode 0600 plaintext).\n\nSecrets are still moved out of the context so `COPY` cannot pick them up, but they now go under `parse.GetTempDir()` and are removed by the existing `temporaryFiles` cleanup when the build finishes.\n\n## Test plan\n\n- [x] `go test -tags 'exclude_graphdriver_btrfs containers_image_openpgp' ./pkg/api/handlers/compat/ -count=1 -run TestProcessSecretsDoesNotLeavePlaintextInContextParent`\n- [ ] Reproduce issue steps: build from a third directory with `--secret`, confirm no `podman-build-secret-*` remains in the context parent\n\nFixes #29687",
    "comments": [
        {
            "user": "Luap99",
            "body": "> Local API should keep the context as localcontextdir and send only the secrets as a small tar/body, unpacked under the builder scratch dir. We should not write podman-build-secret-* into the user's context. Tarring the whole context would defeat the local API.\n\nAh I was overlooking the localapi thing when I looked at the issue. I wonder if it is worthwhile to allow passing in secrets to the buildah API in memory. Then we just need to send them via the api included the file content, since the secret files are supposed to be small that should not be a problem to keep in memory."
        }
    ],
    "cross_references": []
}