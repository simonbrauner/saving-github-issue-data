{
    "organization": "podman-container-tools",
    "repository": "container-libs",
    "number": 822,
    "type": "pull_request",
    "title": "common: move machine marker file",
    "state": "closed",
    "labels": [
        "common"
    ],
    "user": "Luap99",
    "body": "Move the marker file location to /etc/podman-machine. Because we want to overmount /etc/containers it means the /etc/containers/podman-machine file created by ignition gets shadowed otherwise.\n\nHowever because ignition runs only once we should still support machines created on 5.X so keep a fallback that checks the older file as well if the first one is not found.\n\nAlso rework the logic to use sync.OnceValue and make it better unit testable.\n\n\n",
    "comments": [
        {
            "user": "Luap99",
            "body": "Split out from https://github.com/containers/container-libs/pull/779 because I will need to have this change inside the machine images first before I can do the overmount of /etc/containers as otherwise CI cannot pass in podman as the inside podman would still look at the old path only https://github.com/containers/podman/pull/28573\n\ncc @baude @l0rd @ashley-cui "
        }
    ],
    "cross_references": []
}