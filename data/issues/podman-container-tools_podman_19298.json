{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 19298,
    "type": "pull_request",
    "title": "Ensure that we appropriately warn that TCP is insecure",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "locked - please file new issue/PR"
    ],
    "user": "mheon",
    "body": "`podman system service` + TCP is not a configuration we should be recommending. There was already language about this in the manpages, but it was not sufficient in explaining how bad of an idea this is. Expand the manpage warnings, add a dedicated heading so people notice, and add a warning every time the service starts with a TCP URL that directs people to the manpage to see that explanation.\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\". That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\nAdd a warning when `podman system service` binds to a TCP socket. This is not a secure configuration and the Podman team recommends against using it.\n```\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/19298#\" title=\"Author self-approved\">mheon</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [mheon]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "@ashley-cui We want this in 4.6 final, sorry for leaving it until the last minute"
        },
        {
            "user": "mheon",
            "body": "@vrothberg @Luap99 @rhatdan PTAL at the manpage wording"
        },
        {
            "user": "mheon",
            "body": "@edsantiago The test here is horribly hacky, but I didn't really want to do a delay loop waiting for a single warning message to appear."
        },
        {
            "user": "edsantiago",
            "body": "You can piggyback on an existing test:\n```diff\ndiff --git a/test/system/271-tcp-cors-server.bats b/test/system/271-tcp-cors-server.bats\nindex 7282050a5..982a33cb8 100644\n--- a/test/system/271-tcp-cors-server.bats\n+++ b/test/system/271-tcp-cors-server.bats\n@@ -16,13 +16,18 @@ SOCKET_FILE=\"$UNIT_DIR/$SERVICE_NAME.socket\"\n @test \"podman system service - tcp CORS\" {\n     skip_if_remote \"system service tests are meaningless over remote\"\n     PORT=$(random_free_port 63000-64999)\n-    $PODMAN system service --cors=\"*\" tcp:$SERVICE_TCP_HOST:$PORT -t 20 &\n+    log=${PODMAN_TMPDIR}/system-service.log\n+    $PODMAN system service --cors=\"*\" tcp:$SERVICE_TCP_HOST:$PORT -t 20 &> $log &\n     podman_pid=\"$!\"\n     sleep 5s\n     run curl -s --max-time 10 -vvv $SERVICE_TCP_HOST:$PORT/_ping 2>&1\n     is \"$output\" \".*< Access-Control-Allow-Origin: \\*.*\" \"access-control-allow-origin verifies CORS is set\"\n     kill $podman_pid\n     wait $podman_pid || true\n+\n+    # Running server over TCP is a bad idea. We should see a warning\n+    assert \"$(< $log)\" =~ \"Using the Podman API service with TCP sockets\" \\\n+           \"podman warns about server on TCP\"\n }\n \n @test \"podman system service - tcp without CORS\" {\n```\n(diff applies to original push, not the push from a few seconds ago)"
        },
        {
            "user": "mheon",
            "body": "@edsantiago I just dropped the other test, that file is otherwise... completely skipped? So I was a bit iffy on using it. I think the one system test should be fine, no?"
        },
        {
            "user": "mheon",
            "body": "Failures in RPM build look like gvproxy"
        },
        {
            "user": "edsantiago",
            "body": "Totally fine with me to go with system tests - that way we get extra testing in Fedora and RHEL gating.\n\nAnd, yeah, rpm-build has been broken since yesterday. It doesn't gate anything."
        },
        {
            "user": "ashley-cui",
            "body": "Unhappy system test :( "
        },
        {
            "user": "mheon",
            "body": "Looking"
        },
        {
            "user": "mheon",
            "body": "Apparently `tcp:localhost:8000` (or similar) breaks Golang's URI parser (it doesn't know the scheme is TCP) but not our socket-bind code? The test functions as expected but the URL is not detected as using a TCP schema. Investigating."
        },
        {
            "user": "ashley-cui",
            "body": "LGTM, @containers/podman-maintainers PTAL looks like its going to go green"
        },
        {
            "user": "edsantiago",
            "body": "tentative LGTM. I, too, dislike warnings in the server (they will never be seen by any human). I just can't think of a better way to do the discouraging. Man page looks good."
        },
        {
            "user": "ashley-cui",
            "body": "/lgtm"
        },
        {
            "user": "edsantiago",
            "body": "@ashley-cui do you have cherrypick privs on this repo? If so could you please ask the bot?"
        },
        {
            "user": "ashley-cui",
            "body": "Folded the cherry-pick into the release bump PR: https://github.com/containers/podman/pull/19300"
        }
    ],
    "cross_references": []
}