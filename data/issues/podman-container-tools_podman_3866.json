{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 3866,
    "title": "Document using rootless container with systemd",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "MatMaul",
    "body": "Rootless podman works great with systemd to manage services ran as a user.\nHowever it would be nice to add some docs for that somewhere, or tweak `generate systemd` for the rootless use case.\n\nThe 2 difficult points I ran into was:\n- the service must depends on `user@uid.service` so the user dbus session is initialized and the /run user folder exists\n- KillMode=none as explained in #3657\n\nHere is a working unit:\n```\n[Unit]\nDescription=nginx\nRequires=user@1003.service\nAfter=user@1003.service\n\n[Service]\nType=simple\nKillMode=none\nExecStartPre=-/usr/bin/podman rm -f nginx\nExecStartPre=/usr/bin/podman pull nginx\nExecStart=/usr/bin/podman run --name=nginx -p 8080:80 nginx\nExecStop=/usr/bin/podman stop nginx\nRestart=always\nUser=nginx\nGroup=nginx\n\n[Install]\nWantedBy=multi-user.target\n```",
    "comments": [
        {
            "user": "rhatdan",
            "body": "PRs and Blogs welcomed.  :^)"
        },
        {
            "user": "MatMaul",
            "body": "Where do you think I could contribute such a doc ?"
        },
        {
            "user": "MatMaul",
            "body": "rootless_tutorial.md ?"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "I'd drop it in rootless_tutorial at least to start.  I'd make a new section for it in that md file."
        },
        {
            "user": "mheon",
            "body": "I think both of these could be pretty easily dropped into `generate systemd` as defaults based on a rootless.IsRootless() check"
        }
    ]
}