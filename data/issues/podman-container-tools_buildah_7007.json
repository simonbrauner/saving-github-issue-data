{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 7007,
    "type": "pull_request",
    "title": "Add context-aware versions of several functions and some methods",
    "state": "open",
    "labels": [],
    "user": "nalind",
    "body": "#### What type of PR is this?\n\n/kind other\n\n#### What this PR does / why we need it:\n\nIntroduce versions of a number of functions and some methods which take a context.Context, to least try to provide proper cancellation support.\n\nWhen copier.Get() returns an error, also write a non-NUL string after whatever it's already written, so that a tar.Reader that reads what it output will also flag the malformed header with an unexpected error.\n\nIn the CLI, pass our default global context down in places where we didn't before, connect it to an experimental timeout flag, and have it cancel on `os.Interrupt` signals, too.\n\n#### How to verify it\n\nNew/updated unit tests!\n\n#### Which issue(s) this PR fixes:\n\nNone\n\n#### Special notes for your reviewer:\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nNone\n```",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "Ephemeral COPR build failed. @containers/packit-build please check."
        },
        {
            "user": "vtushar06",
            "body": "@Luap99 once this lands and gets vendored I can check whether the prune --build flake in podman#28868 stops showing.."
        },
        {
            "user": "nalind",
            "body": "Rebased and fixed some warnings that the \"usestdlibvars\" linter would warn about."
        },
        {
            "user": "nalind",
            "body": "Reordered a couple of the commits to trigger CI."
        },
        {
            "user": "nalind",
            "body": "Rewrote godoc to make it clearer that the non-Context() versions of functions and methods that now have a Context() counterpart are wrappers, have them use context.TODO() instead of context.Background(), and tell \"go fix\" to inline them to make it easier to spot where code that vendors us could benefit from switching to the newer variations.  That last part is flagged by the \"gocheckcompilerdirectives\" linter, so I've disabled it and left a comment next to the version in the Makefile so that, hopefully, we'll be able to re-enable that when we update to a newer version of the linter.\nUpdated a couple of call sites that I'd missed without its help."
        },
        {
            "user": "nalind",
            "body": "Rebasing, updating the note about re-enabling \"gocheckcompilerdirectives\", because it looks like golangci-lint 2.13 accepts the \"//go:fix\" directives being introduced here.  Also added smaller patches to correct things that v2.13.1 flags."
        },
        {
            "user": "nalind",
            "body": "Today's release of Go 1.27 caused our validate workflow to switch to 1.27, and that required manually bumping the version of the linter to avoid making it panic, so we don't need to disable the \"gocheckcompilerdirectives\" linter any more."
        },
        {
            "user": "nalind",
            "body": "Broke the linter updates out into #7058, if we want to handle them on their own."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29279
        }
    ]
}