{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 29460,
    "type": "pull_request",
    "title": "hyperv: add timeout to vsock ready wait",
    "state": "open",
    "labels": [
        "machine",
        "No New Tests"
    ],
    "user": "RudraDudhat2509",
    "body": "Fixes #29455, full description, root cause, and testing details are there.\n\n#### Checklist\n\n- [x] I have read and understood our [contributing guidelines](https://github.com/podman-container-tools/podman/blob/main/CONTRIBUTING.md) and will not have more than two open PRs as a new contributor.\n- [x] PR description, commit message, and GitHub comments are human-written, per [LLM Policy](https://github.com/podman-container-tools/community/blob/main/LLM_POLICY.md)\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all commits. (`git commit -s`).\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/podman-container-tools/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/podman-container-tools/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] Format and lint verified directly: `golangci-lint` v2.12.2 (the exact version Podman CI uses) reports 0 issues, `gofumpt` clean, `go vet` clean. Full containerized `make validatepr` not run (no Docker Desktop engine available in my environment), but the checks it gates on for a Go source change, format and lint, are verified for real above.\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below\n\n#### Does this PR introduce a user-facing change?\n\n```release-note\nFixed `podman machine init`/`start` hanging indefinitely on the HyperV backend if the guest VM never signals that it's ready. It now fails with a clear timeout error after 5 minutes instead of hanging forever.\n```\n",
    "comments": [
        {
            "user": "mheon",
            "body": "I don't know if there's much point to a unit test here"
        },
        {
            "user": "RudraDudhat2509",
            "body": "@mheon Removed the unit test as well as reduced the timeout to 30s (a0d2234), lmk if anything else is required from my end and thx for a quick review !"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @podman-container-tools/packit-jobs please check. Everyone else, feel free to ignore."
        },
        {
            "user": "mheon",
            "body": "LGTM"
        },
        {
            "user": "RudraDudhat2509",
            "body": "Unrelated to this PR, but I noticed something while its CI was running (run 31581038263, job `macos machine applehv`).\n\nIt looks like the same bug is happening on the AppleHV side too. this time it happened in `pkg/machine/apple/apple.go`. `podman machine start` ended up hanging for 10m8s until the external test harness killed it after 600s (`config_test.go:59`). \n\nI actually ran into this bug on AppleHV first and wrote a fix for it before finding the same issue on Hyper-V. I tested the fix as much as I could, but never opened a PR because I don't have access to real Mac hardware and didn't want to submit something I couldn't properly verify.\n\nHappy to open a pr if a maintainer can verify it\n"
        }
    ],
    "cross_references": []
}