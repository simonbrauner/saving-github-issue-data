{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 13387,
    "title": "What's the podman equivalent of Docker's `volume-opt` parameter in mount strings?",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "rhatdan",
    "body": "### Discussed in https://github.com/containers/podman/discussions/13378\n\n<div type='discussions-op-text'>\n\n<sup>Originally posted by **dstaley** February 28, 2022</sup>\nI'm trying to translate the following `--mount` string from Docker to podman:\n\n```\n\"src=services,dst=/services,volume-opt=device=:/volume3/Services,\\\"volume-opt=o=addr=ganymede.localdomain,vers=3,soft\\\",type=volume,volume-driver=local,volume-opt=type=nfs\"\n```\n\nWhen running a container with this mount string, I get the error:\n\n```\nError: volume-opt: invalid mount option\n```\n\nI understand that the options passed to `--mount` are basically parsed out and passed to the OS's underlying `mount` command, so I assume that `volume-opt` isn't getting translated to whatever it needs to be for `mount` to understand it.\n\nI've seen a few sample commands that create volumes using `--opt`, however I don't see anything like that in the [docs](https://docs.podman.io/en/latest/markdown/podman-run.1.html#mount-type-type-type-specific-option) for the `--mount` argument.</div>",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@flouthoc PTAL"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "@flouthoc have you had a chance to look at this?"
        },
        {
            "user": "flouthoc",
            "body": "@rhatdan Above PR should close this. I am wondering why is this not documented by docker."
        }
    ]
}