{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28495,
    "type": "pull_request",
    "title": "Fix Docker API DeviceMapping for CDI devices",
    "state": "closed",
    "labels": [
        "kind/api-change"
    ],
    "user": "Honny1",
    "body": "<!--\nThanks for sending a pull request!\n\nFor more detailed information, please review our contributing guidelines:\nhttps://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests\n-->\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [ ] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [ ] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [ ] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [ ] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [ ] All commits pass `make validatepr` (format/lint checks)\n- [ ] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nCompat API Container create no longer turns CDI-qualified HostConfig.Devices entries into invalid `host:container:permissions` strings when optional fields are empty, so compose-style clients can pass CDI selectors reliably. DeviceRequests with driver cdi are now matched case-insensitively.\n```\n",
    "comments": [
        {
            "user": "Honny1",
            "body": "> an e2e compose test like test/compose/cdi_device/docker-compose.yml would make sense, I guess just add a second device to the config and then pass it as normal device in compose?\n\nI'm not sure about this. Newer versions (since https://github.com/docker/compose/pull/12184) of Compose send CDI devices through `cc.HostConfig.Resources.DeviceRequests`, so the new test will always pass.\n\n"
        },
        {
            "user": "Honny1",
            "body": "I am not sure if this change fixes the reported issues, as I cannot directly verify them. The main goal of this change is to make defensive improvements for edge cases that might occur with older versions of Compose or other clients."
        },
        {
            "user": "mheon",
            "body": "It seems like a reasonable change regardless. I wonder if the people seeing this are all on Compose v1? We dropped all our tests for that..."
        },
        {
            "user": "mheon",
            "body": "Sure, LGTM"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 28309
        }
    ]
}