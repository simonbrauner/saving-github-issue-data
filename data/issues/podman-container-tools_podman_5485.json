{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 5485,
    "type": "issue",
    "title": "Problems with podman generate systemd",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "larsks",
    "body": "<!--\n---------------------------------------------------\nBUG REPORT INFORMATION\n---------------------------------------------------\nUse the commands below to provide key information from your environment:\nYou do NOT have to include this information if this is a FEATURE REQUEST\n\n**NOTE** A large number of issues reported against Podman are often found to already be fixed\nin more current versions of the project.  Before reporting an issue, please verify the\nversion you are running with `podman version` and compare it to the latest release\ndocumented on the top of Podman's [README.md](../README.md).  If they differ, please\nupdate your version of Podman to the latest possible and retry your command before creating\nan issue.\n\nIf you are filing a bug against `podman build`, please instead file a bug\nagainst Buildah (https://github.com/containers/buildah/issues). Podman build\nexecutes Buildah to perform container builds, and as such the Buildah\nmaintainers are best equipped to handle these bugs.\n-->\n\n**Is this a BUG REPORT or FEATURE REQUEST? (leave only one on its own line)**\n\n/kind bug\n\n\n**Description**\n\nThe unit files produced by `podman generate systemd --new ...` are problematic because they will fail to start the service if the system crashes.  This happens because the container is only removed as part of `ExecStopPost`, and during a crash (/power failure/etc) that never runs.  The result is that a subsequent attempt to start the service will fail with `Error: error creating container storage: the container name \"your_container_name\" is already in use by ...`.\n\nYou can't simply drop the `--new`, because then the resulting unit file won't actually create the container if it doesn't exist.\n\nWhat you really need to make this robust is one of two things:\n\n- Tie the lifetime of the container to the lifetime of the `podman` command. When the `podman` exits, the container is gone. This would make a containerized service behave like a normal un-containerized service, which would work well with a process manager like `systemd`.\n\n- Allow replacing an existing container with a new container.  E.g., introduce a `--replace` flag to `podman run` so that `podman run --replace --name foo ...` would permit create a new container named `foo` *even if one already exists*.\n",
    "comments": [
        {
            "user": "mheon",
            "body": "@vrothberg PTAL"
        },
        {
            "user": "vrothberg",
            "body": "Thanks for reaching out, @larsks !\n\nI like the idea of a `--replace` flag as it allows to generalize this problem beyond systemd.\n\n@mheon @giuseppe @rhatdan WDYT?"
        },
        {
            "user": "giuseppe",
            "body": "could we add a `ExecStartPre` that does a `rm -f`?"
        },
        {
            "user": "vrothberg",
            "body": "> could we add a `ExecStartPre` that does a `rm -f`?\n\nThat will only work for containers that have been named but won't work for unnamed containers."
        },
        {
            "user": "mheon",
            "body": "No issues here, sounds reasonable to do\n\nOn Fri, Mar 13, 2020, 06:21 Valentin Rothberg <notifications@github.com>\nwrote:\n\n> could we add a ExecStartPre that does a rm -f?\n>\n> That will only work for containers that have been named but won't work for\n> unnamed containers.\n>\n> —\n> You are receiving this because you were mentioned.\n> Reply to this email directly, view it on GitHub\n> <https://github.com/containers/libpod/issues/5485#issuecomment-598651328>,\n> or unsubscribe\n> <https://github.com/notifications/unsubscribe-auth/AB3AOCHKCAX5D5UN6NJ4QZLRHICJHANCNFSM4LGVPUBA>\n> .\n>\n"
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        },
        {
            "user": "rhatdan",
            "body": "@larsks @vrothberg Does this issue still exist?"
        },
        {
            "user": "vrothberg",
            "body": "Yes. The idea is to add `--replace` to run/create which will remove an existing container in case of a name conflict. There are a couple of issues I want to tackle once the v2 work is closer to the finish line."
        },
        {
            "user": "rhatdan",
            "body": "@vrothberg Any progress on this?"
        },
        {
            "user": "vrothberg",
            "body": "Thanks for the ping. No progress yet but I'll tackle it now."
        },
        {
            "user": "vrothberg",
            "body": "#6553 will fix it :+1: "
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 6553
        }
    ]
}