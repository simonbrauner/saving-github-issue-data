{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 22388,
    "type": "pull_request",
    "title": "Makefile: do not hardcode `GOOS` in `podman-remote-static` target",
    "state": "closed",
    "labels": [
        "lgtm",
        "approved",
        "release-note-none",
        "locked - please file new issue/PR"
    ],
    "user": "xduugu",
    "body": "Follow-up of https://github.com/containers/podman/pull/22060#issuecomment-2009960603\n\n<!--\nThanks for sending a pull request!\n\nPlease make sure you've read our contributing guidelines and how to submit a pull request (https://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests).\n\nIn case you're only changing docs, make sure to prefix the pull-request title with \"[CI:DOCS]\". That will prevent functional tests from running and save time and energy.\n\nFinally, be sure to sign commits with your real name. Since by opening\na PR you already have commits, you can add signatures if needed with\nsomething like `git commit -s --amend`.\n-->\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nIf no, just write `None` in the release-note block below. If yes, a release note\nis required: Enter your extended release note in the block below. If the PR\nrequires additional action from users switching to the new release, include the\nstring \"action required\".\n\nFor more information on release notes, please follow the Kubernetes model:\nhttps://git.k8s.io/community/contributors/guide/release-notes.md\n-->\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "rhatdan",
            "body": "/approve\nLGTM\n@containers/podman-maintainers PTAL\nThanks @xduugu "
        },
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/22388#issuecomment-2058851082\" title=\"Approved\">rhatdan</a>*, *<a href=\"https://github.com/containers/podman/pull/22388#\" title=\"Author self-approved\">xduugu</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [rhatdan]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "mheon",
            "body": "@cevich PTAL"
        },
        {
            "user": "baude",
            "body": "please use an actual commit message on why we need to do this."
        },
        {
            "user": "cevich",
            "body": "Yes, please write up a complete commit message and PR description.  Though I'm glad to see CI passing with this, I don't recall off-hand if any CI builds exercise any of the affected targets.  I can double-check once the commit message is ready."
        },
        {
            "user": "edsantiago",
            "body": "Careful - this was added in #14444  to fix #14201, which is a Windows thing, which means it's probably not tested or exercised in CI."
        },
        {
            "user": "cevich",
            "body": "Thanks for the archaeology and confirming Ed.  Though IIRC, Windows builds are in a weird CI situation, where we test the installer using a cross-compiled binary but the official release builds come from the `winmake` powershell script.  The later I don't believe runs a build anywhere in CI."
        },
        {
            "user": "xduugu",
            "body": "@baude What do you have in mind? It actually never was required to restrict the OS in `podman-remote-static`. I just did not remove it when I added the target in #22060, so should I just write this into the commit message?\n\n@edsantiago The `podman-remote-static` target in #14444 is not the same as the one that I added in #22060. The CI most likely uses the `release-artifacts` target which again calls `make podman-remote-static-linux_amd64` and `make podman-remote-static-linux_arm64` to generate the binaries.\n\nThe history is:\n\n- #14444 sets `GOOS` to `linux`\n- #16667 renames `podman-remote-static` target to `podman-remote-static-linux_amd64` and adds `podman-remote-static-linux_arm64`\n- #18238 adds target `release-artifacts` which uses `podman-remote-static-linux_amd64` and `podman-remote-static-linux_arm64`\n- #22060 adds a new target `podman-remote-static` which is also used by `podman-remote-static-linux_amd64` and `podman-remote-static-linux_arm64`"
        },
        {
            "user": "cevich",
            "body": "> The CI most likely uses the\n\nFrom various points CI runs:\n* `make podman-release`\n* `make podman-remote-release-windows_amd64.zip` (Linux cross-compile)\n* `make podman-release-${arch}.tar.gz GOARCH=$arch` for a large set of `$arch` values\n* `make podman-remote` (OSX)\n* `make podman-remote-release-darwin_amd64.zip` and `make podman-remote-release-darwin_arm64.zip`\n\nI did not track through the `Makefile` to see if any of those land down `$(SRCBINDIR)/podman-remote-static-linux_%` (with some value for `%`)"
        },
        {
            "user": "TomSweeneyRedHat",
            "body": "@cevich @edsantiago @baude thoughts on what to do with this one?"
        },
        {
            "user": "cevich",
            "body": "Oh sorry I completely forgot about this.  IIRC this gives us the option to produce static podman-remote binaries, have I got that right?  I *think* this change is syntactically correct, if somebody else could confirm we're actually touching the `$(SRCBINDIR)/podman-remote-static-linux_%` target in CI somewhere.  If so then this LGTM."
        },
        {
            "user": "rhatdan",
            "body": "/lgtm"
        }
    ],
    "cross_references": []
}