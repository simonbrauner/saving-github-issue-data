{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 1620,
    "type": "pull_request",
    "title": "fea(#1619): skopeo sync support use src or dest use oci format",
    "state": "closed",
    "labels": [
        "kind/feature",
        "stale-pr",
        "locked - please file new issue/PR"
    ],
    "user": "tanguofu",
    "body": "",
    "comments": [
        {
            "user": "tanguofu",
            "body": "> 3. use `library/mysql` `library/registry`  as dir name,  then it will put the offline  registry `127.0.0.1:5000` with same docker image namespace. keep the images name not change also can avoid same  image;tag name conflict.\n\nI’m not sure what conflict you mean. Note that the OCI image reference annotations are not limited to Docker-like tags; `library/mysql:42.43.47` is a valid OCI image annotation, so `oci:directory-path:library/mysql:42.43.47` is a valid way to refer to an image using the `oci:` transport. In particular, `oci:directory:library/mysql:latest` and `oci:directory:library/registry:latest` don’t conflict.\n\n> as your mention that `OCI can’t represent the original format of all images`, could u explain or give some case that oci \n incompatible with docker image format. \n\nDocker schema1/schema2 images (i. e. any images not already in the OCI format — most images in the wild, AFAIK) will be converted to OCI. That can preserve all _features_ fine (except for health checks, I think) — but it inevitably changes the _binary representation_, and the image’s manifest digest. So a Docker schema2 image `library/mysql@sha256:$digestvalue` would, after transferring using the OCI format, have a different digest at the destination."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this PR had no activity for 30 days."
        }
    ],
    "cross_references": [
        {
            "organization": "containers",
            "repository": "image",
            "number": 1522
        },
        {
            "organization": "podman-container-tools",
            "repository": "skopeo",
            "number": 1619
        }
    ]
}