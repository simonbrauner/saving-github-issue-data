{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 11929,
    "title": "generate kube -s: do not insert annotations for the bind in the service yaml",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "dilyanpalauzov",
    "body": "I run a podman container as described at https://mail.aegee.org/cgit/aegee-ldap/tree/readme.md#n13 .  Then call `podman generate kube -s busy_mclaren`.  The output is:\n\n```\n# Save the output of this file and use kubectl create -f to import\n# it into Kubernetes.\n#\n# Created with podman-3.4.0\napiVersion: v1\nkind: Service\nmetadata:\n  annotations:\n    bind-mount-options:/home/d/data: Z\n  creationTimestamp: \"2021-10-12T05:51:48Z\"\n  labels:\n    app: busymclaren\n  name: busymclaren\nspec:\n  ports:\n  - name: \"3890\"\n    nodePort: 31091\n    port: 3890\n    protocol: TCP\n    targetPort: 0\n  selector:\n    app: busymclaren\n  type: NodePort\nstatus:\n  loadBalancer: {}\n```\n\nApart from the fact, that the annotation is syntactically not accepted by `kubelet apply -f`, such annotations have nothing to do with the Service and therefore shall not be emitted.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "@baude PTAL"
        },
        {
            "user": "rhatdan",
            "body": "Seems this is a duplicate of https://github.com/containers/podman/issues/11930"
        },
        {
            "user": "dilyanpalauzov",
            "body": "This ticket is about the useless anotation, the other ticket is about targetPort:0."
        },
        {
            "user": "mheon",
            "body": "It's useless for Kube, but it's necessary for preserving this information for `podman play kube` - one of our goals is that `podman play kube` on YAML from `podman generate kube` should produce close-to-identical containers. Podman containers and Kube YAML are not perfectly matched, hence the need for such annotations."
        },
        {
            "user": "dilyanpalauzov",
            "body": "The annotation is included in the Pod and in the Service object.  I mean, that it is sufficient to include the annotation only in the Pod object."
        },
        {
            "user": "mheon",
            "body": "Ahh, fair."
        },
        {
            "user": "rhatdan",
            "body": "Interested in opening a PR to fix it?"
        },
        {
            "user": "dilyanpalauzov",
            "body": "I am not interested."
        },
        {
            "user": "baude",
            "body": "would changing where it is in the stanza be a  breaking change though?\n"
        },
        {
            "user": "dilyanpalauzov",
            "body": "This ticket is not about the syntactically invalid annotation.\n\nThis ticket is about the unnecessary annotation in the Service yaml output."
        },
        {
            "user": "rhatdan",
            "body": "Not if nothing uses it."
        },
        {
            "user": "dilyanpalauzov",
            "body": "I use the latest podman - commit 171f7b8975194ff - and I see no difference in the output of `podman generate kube -s` - the generated annotation is syntactically invalid.  The bind annotation is included both in the Pod.metadata and Service.metadata .  including the bind annotation in Service.metadata seems unnecessary."
        }
    ]
}