{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 7462,
    "type": "issue",
    "title": "Compatibility: Add filters for listing networks",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "minioin",
    "body": "I tried to run k3d with podman system service but k3d returned error with ``filters for listing networks is not implemented ``. As I looked at docker [api](https://docs.docker.com/engine/api/v1.40/#operation/NetworkList), it lists filters as an option.\n\nThis features is one of the first thing I encountered while running [k3d under podman ](https://github.com/rancher/k3d/issues/84).\n\n",
    "comments": [
        {
            "user": "Luap99",
            "body": "The problem is that podman handles networking differently from docker. The podman backend only supports `name` and `plugin` as valid filter keys for networks. I guess that's the reason why the api don't allow filters. "
        },
        {
            "user": "minioin",
            "body": "Considering that we already need a system service for docker compatibility, how is it difficult to maintain a list of networks to implement this feature? What is blocking this api exactly?"
        },
        {
            "user": "Luap99",
            "body": "@mheon stated that he is looking to improve this in the future https://github.com/containers/podman/issues/7080#issuecomment-664021523\n\n@minioin Do you know what filter k3d is using? I think we can at least implement the name filter for now."
        },
        {
            "user": "rhatdan",
            "body": "@minioin Time is the only thing blocking us.  Getting community to help build it is always helpful."
        },
        {
            "user": "iwilltry42",
            "body": "@Luap99 , in k3d we're only filtering by name: https://github.com/rancher/k3d/blob/main/pkg/runtimes/docker/network.go#L47-L54\n```Go\n        // (1) configure list filters\n\targs := filters.NewArgs()\n\targs.Add(\"name\", name)\n\n\t// (2) get filtered list of networks\n\tnetworkList, err := docker.NetworkList(ctx, types.NetworkListOptions{\n\t\tFilters: args,\n\t})\n```"
        },
        {
            "user": "rhatdan",
            "body": "@iwilltry42 @minioin  @Luap99 Can we close this issue now since your PR has been merged?"
        },
        {
            "user": "mheon",
            "body": "I'm going to go ahead and close this. We're not 100% there yet, but we can open separate, more specific issues for any remaining missing filters."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 7536
        },
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 9803
        }
    ]
}