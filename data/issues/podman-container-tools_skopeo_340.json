{
    "organization": "podman-container-tools",
    "repository": "skopeo",
    "number": 340,
    "type": "pull_request",
    "title": "Simplify infrastructure setup, and make registry timeouts less strict",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR"
    ],
    "user": "mtrmac",
    "body": "This moves various setup steps from `SetUpTest` to `SetUpSuite` so that we don’t start up processes over and over; that is _slightly_ less reliable, but more efficient.\n\nAlso increases the registry startup timeout from 0.5 to 5 seconds, hopefully avoiding some CI flakes.\n\nFinally, completely stops starting some of the registries which we don’t use, with an `if false` to keep the existing code structure. (Alternatively we could drop that, and stop building the docker/registry code altogether.",
    "comments": [
        {
            "user": "mtrmac",
            "body": "(This is not intended to go into today's release.)"
        },
        {
            "user": "runcom",
            "body": "> This moves various setup steps from SetUpTest to SetUpSuite so that we don’t start up processes over and over; that is slightly less reliable, but more efficient.\n\n:cry: can we compare how efficient is this? I don't really like leaving registries states behind when jumping to a new test (the docker test suite, far bigger than this, still setup registries in `SetUpTest` afaict)\n\n> \n> Also increases the registry startup timeout from 0.5 to 5 seconds, hopefully avoiding some CI flakes.\n\n:+1: \n\n> \n> Finally, completely stops starting some of the registries which we don’t use, with an if false to keep the existing code structure. (Alternatively we could drop that, and stop building the docker/registry code altogether.\n\nlet's drop for sure the V1 registries stuff as skopeo isn't intended to support that at all"
        },
        {
            "user": "mtrmac",
            "body": "> > This moves various setup steps from SetUpTest to SetUpSuite so that we don’t start up processes over and over; that is slightly less reliable, but more efficient.\n\n> :cry: can we compare how efficient is this? I don't really like leaving registries states behind when jumping to a new test (the docker test suite, far bigger than this, still setup registries in `SetUpTest` afaict)\n\nHonestly it was less than the variation between test runs. I’ll drop that part. (FWIW `copy_tests.go`, where most of the tests actually are, does use `SetUpSuite` already. But then starting an OpenShift cluster takes much longer.)"
        },
        {
            "user": "mtrmac",
            "body": "Updated:\n* `SkopeoSuite` keeps setting up new registry servers for each test. (`CopySuite` keeps usingper-suite registries.)\n* The unused registry servers are completely removed instead of `if false`.\n* The `docker/registry` server is no longer being built in `Dockerfile`."
        },
        {
            "user": "runcom",
            "body": "Awesome, LGTM pending tests"
        }
    ],
    "cross_references": []
}