{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 11914,
    "type": "issue",
    "title": "podman generate kube emits reduntant runAsGroup/runAsUser entries",
    "state": "closed",
    "labels": [
        "kind/bug",
        "In Progress",
        "locked - please file new issue/PR"
    ],
    "user": "dilyanpalauzov",
    "body": "I build my image with `USER 2000:2000` and this information is contained in the oci-image.  `podman generate kube CONTAINERNAME` prints:\n```\napiVersion: v1\nkind: Pod\nspec:\n  containers:\n    securityContext:\n      …\n      runAsGroup: 2000\n      runAsUser: 2000\n```\nSInce the group and user UID are inherited from the container description, `podman generate kube` shall not print them, as long as the caller of podman has not passed `--ueser` parameter.  Kubernetes will anyway apply 2000:2000 when executing the container.\n```\n$ podman version\nVersion:      3.4.0\nAPI Version:  3.4.0\nGo Version:   go1.16.8\nBuilt:        Thu Sep 30 21:40:21 2021\nOS/Arch:      linux/amd64\n```",
    "comments": [
        {
            "user": "cdoern",
            "body": "@baude this seems pretty easy to change. I found the point in the code where the switch would be made and have figured out a way to see whether or not the container has `--user` enabled. Does this look like something we should switch?"
        },
        {
            "user": "umohnani8",
            "body": "@cdoern yes, let's switch it. If the information is in the image, then we don't need to explicitly add it to the kube yaml."
        },
        {
            "user": "rhatdan",
            "body": "Also if the image changes, we would need to change yaml file."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 11944
        }
    ]
}