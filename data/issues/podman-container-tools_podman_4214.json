{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 4214,
    "type": "issue",
    "title": "Don't write configuration in ~/.config/containers; only read them",
    "state": "closed",
    "labels": [
        "kind/bug",
        "locked - please file new issue/PR"
    ],
    "user": "debarshiray",
    "body": "/kind bug\n\n**Description**\n\nOne of the bigger pain points of the Cgroups v2 migration was the presence of `runtime=\"runc\"` in `~/.config/containers/libpod.conf`. In the past we had a [bug](https://github.com/containers/libpod/issues/2158) where `~/.config/containers/storage.conf` could get corrupted, creating all sorts of confusion.\n\nDumping default values into configuration files in `$HOME` locks the user into a specific software version. There's no clean way to update the defaults along with the software because operating system updates are not meant to touch `$HOME`.\n\nIf we do want to create these files as guidelines for users, then everything should be commented out, as if the file doesn't exist; but then we have to maintain code that writes the files, as opposed to just reading them. It seems easier to just let humans do the writing, and document it in a manual.\n\nIdeally all the defaults should be embedded in code, and it should be able to work without any configuration at all on a sanely set up environment. If we ship any configuration, to aid the code because it cannot auto-detect something or as vendor configuration, then it should only be in one of the OS-controlled locations. Ideally, this should be in [/usr](http://0pointer.net/blog/projects/stateless.html) (eg., `/usr/share/zoneinfo`, `/usr/lib/sysctl.d`, etc.) but the current `/etc/containers` is fine for the purpose of this issue.",
    "comments": [
        {
            "user": "mheon",
            "body": "@vrothberg is looking into rewriting our configuration-handling logic to allow this.\n\nCurrent thinking is that we will still write a config file, but it will only contain paths for c/storage and Libpod; everything else will be sourced from the root defaults."
        },
        {
            "user": "QuLogic",
            "body": "This looks a bit like #3658."
        },
        {
            "user": "mheon",
            "body": "@vrothberg I think your latest PR fixes this?"
        },
        {
            "user": "vrothberg",
            "body": "Yes, Podman is not auto-generating it anymore."
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 4302
        }
    ]
}