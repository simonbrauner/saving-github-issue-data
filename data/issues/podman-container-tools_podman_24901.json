{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 24901,
    "type": "issue",
    "title": "Quadlet: Handle multiple Exec= entries",
    "state": "closed",
    "labels": [
        "kind/feature"
    ],
    "user": "wnhrt",
    "body": "### Feature request description\n\nAllowing multiple `Exec=` entries, like with `Label=`, would provide better readability and maintainability.\n\nA prime example is [the example docker-compose.yml of treafik](https://doc.traefik.io/traefik/user-guides/docker-compose/basic-example/) with four long entries.\nMy own configuration is 18 arguments long and at exactly 1000 characters not easily manageable in a single line.\n\n\n\n### Suggest potential solution\n\nAllowing multiple `Exec=` entries, like with `Label=`.\n\n### Have you considered any alternatives?\n\nNo. I am in the progress of migrating from docker compose and have not much experience with quadlets.\nIf there is an alternative approach, which I overlooked, please let me know.\n\n### Additional context\n\nRelated issue: https://github.com/containers/podman/issues/22027\n\nI was able to start a container with multiple `Exec=` entries but `podman ps` shows only the arguments from the last entry.\nThis case, silently ignoring present configuration, seems more problematic than failing with an error.\n\n```\n[Container]\nImage=docker.io/library/traefik\nExec=\"--api.insecure=true\"\nExec=\"--providers.docker=true\"\nExec=\"--providers.docker.exposedbydefault=false\"\nExec=\"--entryPoints.web.address=:80\"\nNotify=true\nVolume=%t/podman/podman.sock:/var/run/docker.sock\nSecurityLabelDisable=true\n```\n\n```\nCONTAINER ID  IMAGE                             COMMAND               CREATED        STATUS        PORTS       NAMES\n1ab2d3e4f5g6  docker.io/library/traefik:latest  --entryPoints.web...  7 minutes ago  Up 7 minutes  80/tcp      systemd-traefik\n```\n",
    "comments": [
        {
            "user": "ygalblum",
            "body": "When deciding whether to all the occurrences of a key or only the last, Quadlet follows what is supported by podman. For this reason, keys like `Label` may be set multiple times while for others that may be set only once, such as `Entry`, only the last occurrence is used. Furthermore, using only the last occurrence allows overriding the value in drop-in unit files.\n\nAs for you case, have you tried using the line continuation feature? For the example above it will look like:\n```\n[Container]\nImage=docker.io/library/traefik\nExec=--api.insecure=true \\\n--providers.docker=true \\\n--providers.docker.exposedbydefault=false \\\n--entryPoints.web.address=:80\nNotify=true\nVolume=%t/podman/podman.sock:/var/run/docker.sock\nSecurityLabelDisable=true\n```"
        }
    ],
    "cross_references": []
}