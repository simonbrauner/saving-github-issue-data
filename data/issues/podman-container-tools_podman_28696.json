{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 28696,
    "type": "pull_request",
    "title": "quadlet API: fix tar install when non-quadlet file sorts first",
    "state": "closed",
    "labels": [
        "kind/api-change",
        "6.0"
    ],
    "user": "inknos",
    "body": "QuadletInstall expects the quadlet file to be first in the file list, The API handler passes files in filepath.Walk order (lexicographic). Install would fail if a file like \"Containerfile\" comes before the quadlet file.\n\n<!--\nThanks for sending a pull request!\n\nFor more detailed information, please review our contributing guidelines:\nhttps://github.com/containers/podman/blob/main/CONTRIBUTING.md#submitting-pull-requests\n-->\n\n#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [ ] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [ ] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [ ] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [ ] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [ ] All commits pass `make validatepr` (format/lint checks)\n- [ ] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "inknos",
            "body": "@simonbrauner , if you want to take a look at this after what we discussed today, I decided to touch the api function instead of the QuadletInstall function\n"
        },
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "simonbrauner",
            "body": "I'm not sure whether it makes more sense to fix this in `libpod` instead of in `QuadletInstall`. Wouldn't there be a chance for this bug to reappear if there was some other caller of `QuadletInstall` which does not keep this in mind?\n\n`QuadletInstall expects the quadlet file to be first in the list` or I'm thinking, should something similar be written in the documentation of `QuadletInstall`? So that it is immediately clear that the ordering matters.\n\nOtherwise LGTM"
        },
        {
            "user": "Honny1",
            "body": "@inknos, can you please rebase on main and resolve conflicts? Thanks."
        },
        {
            "user": "inknos",
            "body": "a bunch of changes might be needed after the application PR merged. maybe this is not even required anymore. checking"
        },
        {
            "user": "inknos",
            "body": "@Honny1 should be fine for another round of review now"
        },
        {
            "user": "mheon",
            "body": "LGTM"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 29054
        }
    ]
}