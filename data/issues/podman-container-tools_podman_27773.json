{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 27773,
    "type": "pull_request",
    "title": "Add /usr/libexec/podman/qemu-system-arch",
    "state": "closed",
    "labels": [
        "locked - please file new issue/PR",
        "No New Tests"
    ],
    "user": "jnovy",
    "body": "#### Checklist\n\nEnsure you have completed the following checklist for your pull request to be reviewed:\n<!-- Use [x] to mark as done, or click the checkbox after opening PR -->\n\n- [x] Certify you wrote the patch or otherwise have the right to pass it on as an open-source patch by signing all\ncommits. (`git commit -s`). (If needed, use `git commit -s --amend`).  The author email must match\nthe sign-off email address. See [CONTRIBUTING.md](https://github.com/containers/podman/blob/main/CONTRIBUTING.md#sign-your-prs)\nfor more information.\n- [x] Referenced issues using `Fixes: #00000` in commit message (if applicable)\n- [x] [Tests](https://github.com/containers/podman/tree/main/test#readme) have been added/updated (or no tests are needed)\n- [x] [Documentation](https://github.com/containers/podman/blob/main/docs/README.md) has been updated (or no documentation changes are needed)\n- [x] All commits pass `make validatepr` (format/lint checks)\n- [x] [Release note](https://github.com/kubernetes/community/blob/master/contributors/guide/release-notes.md) entered in the section below (or `None` if no user-facing changes)\n\n#### Does this PR introduce a user-facing change?\n\n<!--\nWrite `None` if there are no user-facing changes, otherwise enter your release note below.\nInclude \"action required\" if users need to take action when upgrading.\n-->\n\n```release-note\nNone\n```\n",
    "comments": [
        {
            "user": "packit-as-a-service[bot]",
            "body": "[NON-BLOCKING] Packit jobs failed. @containers/packit-build please check. Everyone else, feel free to ignore."
        },
        {
            "user": "mheon",
            "body": "Why are we doing this? I assume something related to machine packaging, but what does this solve?"
        },
        {
            "user": "jakecorrenti",
            "body": "> Why are we doing this? I assume something related to machine packaging, but what does this solve?\n\nIf the user is on a RHEL host, podman machine currently only searches for the `qemu-system-*` binary, which wouldn't exist (just `qemu-kvm`). It's related to RUN-3579"
        },
        {
            "user": "baude",
            "body": "LGTM @Luap99 you were the one that suggested this ... care to review ?"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 27719
        }
    ]
}