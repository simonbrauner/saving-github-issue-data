{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 2372,
    "title": "allow file-descriptor passing from podman-exec",
    "state": "closed",
    "labels": [
        "kind/feature",
        "locked - please file new issue/PR"
    ],
    "user": "chergert",
    "body": "/kind feature\n\n**Description**\n\nAdd support for passing file-descriptors across `podman exec` execution boundary.\n\nI've started integrating some basic podman support into [GNOME Builder](https://wiki.gnome.org/Apps/Builder) using our native container abstractions (like we do for flatpak). One thing that makes this possible with flatpak is that we can pass file descriptors across the execution boundary. That way, I can feed a PTY for the debugger inferior to tooling like gdb while using the controlling PTY to control gdb itself.\n\nI've looked at calling runc directly using --preserve-fds, but that would mean duplicating a lot of what podman already does for us.",
    "comments": [
        {
            "user": "rhatdan",
            "body": "So you want to add a --preserve-fds flag to podman run/create?"
        },
        {
            "user": "debarshiray",
            "body": "> So you want to add a --preserve-fds flag to podman run/create?\n\nYes, but to podman run/exec, if I understood @chergert properly."
        },
        {
            "user": "rhatdan",
            "body": "@mheon @giuseppe WDYT?"
        },
        {
            "user": "giuseppe",
            "body": "It is an useful idea.\n\nThe only issue is that we are already passing down a bunch of fds, so we would need to properly reassign them.\n"
        },
        {
            "user": "mheon",
            "body": "The Flatpak way of doing this is `--forward-fd=FD`, which seems like a sane enough interface... I don't see many people using this, but the exec command isn't very cluttered right now, so I'm not opposed to adding it."
        },
        {
            "user": "rhatdan",
            "body": "@giuseppe Do you have time to work on it?"
        },
        {
            "user": "rhatdan",
            "body": "There are probably going to be SELinux issues with this.  SELinux should block the use of these fd's being passed in."
        },
        {
            "user": "rhatdan",
            "body": "Unless they are labeled with something like\nsystem_u:system_r:container_t:s0\n"
        },
        {
            "user": "rhatdan",
            "body": "@giuseppe Could you take a look at this?"
        },
        {
            "user": "giuseppe",
            "body": "PR for runc here: https://github.com/opencontainers/runc/pull/1995\n\nPR for libpod here: https://github.com/containers/libpod/pull/2426"
        }
    ]
}