{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 26215,
    "title": "OCI artifact pushes can trigger HTTP GET requests to remote locations",
    "state": "open",
    "labels": [
        "stale-issue"
    ],
    "user": "mtrmac",
    "body": "From https://github.com/containers/podman/pull/26111#discussion_r2098515201:\n\n> But, curiously enough, `oci/layout.GetBlob` can trigger a HTTP request if the manifest contains alternative URLs. …\n> \n> I guess c/image should provide an opt-out of `oci/layout` causing network traffic? We could also switch the order to check local files first; that would, pedantically, be an API break.",
    "comments": [
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ]
}