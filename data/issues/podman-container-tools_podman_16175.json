{
    "organization": "podman-container-tools",
    "repository": "podman",
    "number": 16175,
    "type": "pull_request",
    "title": "[wip] libpod: propogate custom DNS server to `/etc/resolv.conf` only when `dns_enabled` for network",
    "state": "closed",
    "labels": [
        "approved",
        "do-not-merge/work-in-progress",
        "release-note",
        "locked - please file new issue/PR"
    ],
    "user": "flouthoc",
    "body": "Podman populates container's `/etc/resolv.conf` with custom DNS servers ( specified via `--dns` or `dns_server` in containers.conf ) even when container is connected to a network where `dns_enabled` is `true`.\n\nCurrent behavior does not matches with docker, hence following commit ensures that podman only populates custom DNS server when container is not connected to any network where DNS is enabled and for the cases where `dns_enabled` is `true`\nthe resolution for custom DNS server will happen via ( `aardvark-dns` or `dnsname` ).\n\nReference: https://docs.docker.com/config/containers/container-networking/#dns-services\nCloses: https://github.com/containers/podman/issues/16172\n\n```release-note\ncontainer: `--dns` and `dns_server` behavior for containers connected to network matches with docker now \n```\n",
    "comments": [
        {
            "user": "openshift-ci[bot]",
            "body": "[APPROVALNOTIFIER] This PR is **APPROVED**\n\nThis pull-request has been approved by: *<a href=\"https://github.com/containers/podman/pull/16175#\" title=\"Author self-approved\">flouthoc</a>*\n\nThe full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=containers%2Fpodman).\n\nThe pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)\n\n<details >\nNeeds approval from an approver in each of these files:\n\n- ~~[OWNERS](https://github.com/containers/podman/blob/main/OWNERS)~~ [flouthoc]\n\nApprovers can indicate their approval by writing `/approve` in a comment\nApprovers can cancel approval by writing `/approve cancel` in a comment\n</details>\n<!-- META={\"approvers\":[]} -->"
        },
        {
            "user": "Luap99",
            "body": "Since aardvark has no support for custom dns right now we should not change this. Only when this feature is implemented we can switch this"
        },
        {
            "user": "flouthoc",
            "body": "> Since aardvark has no support for custom dns right now we should not change this. Only when this feature is implemented we can switch this\n\nThat's fair we can get this in at last when entire feature is implemented."
        },
        {
            "user": "rhatdan",
            "body": "Moving to WIP until the aardvark fix is in place."
        },
        {
            "user": "flouthoc",
            "body": "Can be merged if all of the dependencies for https://github.com/containers/podman/pull/16197 is merged and https://github.com/containers/podman/pull/16197 itself is merged."
        },
        {
            "user": "flouthoc",
            "body": "Closing in favor of: https://github.com/containers/podman/pull/16297"
        }
    ],
    "cross_references": [
        {
            "organization": "podman-container-tools",
            "repository": "podman",
            "number": 16172
        },
        {
            "organization": "containers",
            "repository": "netavark",
            "number": 452
        }
    ]
}