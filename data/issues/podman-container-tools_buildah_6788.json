{
    "organization": "podman-container-tools",
    "repository": "buildah",
    "number": 6788,
    "type": "issue",
    "title": "More efficient multiarch builds (reuse stages)",
    "state": "open",
    "labels": [
        "kind/feature",
        "stale-issue"
    ],
    "user": "matejvasek",
    "body": "When building multiarch images some stages are run unnecessarily multiple times (for each platform) even when the stage does not depend on the **target** platform. This is wasting resources and making build longer than it could be.\n\n### Example:\nConsider following Dockerfile:\n```dockerfile\nFROM --platform=$BUILDPLATFORM registry.access.redhat.com/ubi9/nodejs-22:latest AS build\nUSER root\nENV COREPACK_ENABLE_DOWNLOAD_PROMPT=0\nRUN npm i -g corepack && corepack enable\n\nWORKDIR /usr/src/app\n\nCOPY package.json yarn.lock .yarnrc.yml ./\nCOPY .yarn/ .yarn/\nRUN yarn install --immutable\n\nCOPY --exclude=node_modules --exclude=backend . .\nRUN  yarn build\n\nFROM --platform=$BUILDPLATFORM registry.access.redhat.com/ubi9/go-toolset:1.24 AS go-build\nARG TARGETOS\nARG TARGETARCH\n\nWORKDIR /opt/app-root/src/backend\n\nCOPY --chown=1001:0 backend/go.mod backend/go.sum /opt/app-root/src/backend/\nRUN go mod download\n\nCOPY --chown=1001:0 --from=build /usr/src/app/dist /opt/app-root/src/backend/static\nCOPY --chown=1001:0 backend/ /opt/app-root/src/backend/\nRUN CGO_ENABLED=0 GOOS=$TARGETOS GOARCH=$TARGETARCH go build -ldflags=\"-s -w\" -o plugin-backend .\n\nFROM registry.access.redhat.com/ubi9-micro:latest\n\nCOPY --from=go-build /opt/app-root/src/backend/plugin-backend /usr/bin/plugin-backend\nUSER 1001\n\nENTRYPOINT [\"plugin-backend\"]\n```\n\nWhen I try to build it:\n```sh\nbuildah bud --platform=linux/ppc64le,linux/s390x,linux/amd64,linux/arm64\n```\nThe first stage is executed four times, while it would be sufficient to run it only once. The stage runs under host platform (`FROM --platform=$BUILDPLATFORM…`), and in no way depend on target platform. It produces files that are used by latter stages and are platform independent.\n\nNote that `docker buildx` does exactly that, it runs the first stage just once.",
    "comments": [
        {
            "user": "matejvasek",
            "body": "For comparison the above build takes  about `00:02:30.0` on `buildah` and `00:01:30.0` on `docker buildx` against `podman` service."
        },
        {
            "user": "github-actions[bot]",
            "body": "A friendly reminder that this issue had no activity for 30 days."
        }
    ],
    "cross_references": []
}